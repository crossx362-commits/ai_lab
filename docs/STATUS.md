# 재와 별 — 현재 위치 · 다음 할 일

최신 바퀴(2026-08-30 07:41): **월드맵 배경 16:9 원본 계약**(`4e656c92`) — 다음 포인터대로 호출처를 확인해 `WorldMapScreen.BackgroundArt`가 실제 `bg_worldmap`을 소비하지만 기존 검사는 512px 이상·Bilinear만 확인해 16:10 원본도 통과하는 차집합을 골랐다. 기존 ox-alpha 이미지는 재생성하지 않고 `TextureImporter.GetSourceTextureWidthAndHeight`로 원본 PNG의 1.75~1.80 가로 비율을 계약하고 1280×800 대표 16:10을 거부했다. 단독 `HollowBgSelfCheck` PASS·전수 기존 기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 1280×720 PNG를 원본 해상도로 직접 보아 궤도 중심과 네 행성 원반이 분리되고 좌상단 타이틀/HUD·하단 도크 여백이 열려 있으며 잘림·축 어긋남 없이 월드맵 오버레이를 방해하지 않아 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 배경 9종을 일괄 변경하지 말고, 실제 소비처가 명확한 `bg_title` 한 역할만 원본 크기·구도를 확인해 같은 16:9 계약의 다음 후보인지 판정한다.**

최신 바퀴(2026-08-30 07:38): **파티·전투 스타일 공용 배경 16:9 원본 계약**(`52b7d4da`) — 다음 포인터대로 호출처를 확인해 `PartyScreen`과 `StyleScreen` 두 화면이 모두 `bg_party`를 소비하지만 기존 검사는 512px 이상·Bilinear만 확인해 세로형 원본도 통과하는 차집합을 골랐다. 기존 이미지는 재생성하지 않고 `TextureImporter.GetSourceTextureWidthAndHeight`로 원본 PNG의 1.75~1.80 가로 비율을 계약하고 720×1280 대표 세로형을 거부했다. 단독 `HollowBgSelfCheck` PASS·전수 기존 기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 1280×720 PNG를 원본 해상도로 직접 보아 중앙의 긴 파티 벤치·좌우 의자·상단의 어두운 정보 여백이 분리되고 잘림·축 어긋남 없이 두 화면의 정보 오버레이를 방해하지 않아 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 배경 9종을 일괄 변경하지 말고, 실제 소비처가 명확한 `bg_worldmap` 한 역할만 원본 크기·구도를 확인해 같은 16:9 계약의 다음 후보인지 판정한다.**

최신 바퀴(2026-08-30 07:35): **캐릭터 화면 배경 16:9 원본 계약**(`8fe9af3b`) — UI·아트 소비 화면의 호출처부터 재스캔해 `CharacterScreen.BackgroundArt`가 실제 `bg_character`를 읽지만 기존 `HollowBgSelfCheck`는 512px 이상·Bilinear만 확인해 정사각형이나 세로 배경도 통과하는 차집합을 골랐다. 기존 이미지는 재생성하지 않고 `TextureImporter.GetSourceTextureWidthAndHeight`로 원본 PNG의 1.75~1.80 가로 비율을 계약하고 800×800 대표 정사각형을 거부했다. 첫 검사에서 Unity NPOT 임포트 뒤 텍스처 크기를 판정해 신규 1건이 FAIL했으나 원본 품질 목적에 맞게 소스 크기로 교정했고, 단독 PASS·전수 기존 기준선 `7/216 FAIL`(프로세스 exit 0)로 복귀했다. 화면 변경 전 검증 커밋 후 1280×720 PNG를 원본 해상도로 직접 보아 석벽 바닥·중앙 액자 자리·좌측 서랍·우측 무기 거치대가 구분되고 정보 패널 뒤를 방해하지 않는 가로 구도로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 화면 배경 9종을 한꺼번에 바꾸지 말고, 실제 소비처가 두 곳인 `bg_party`(PartyScreen·StyleScreen) 한 역할만 원본 크기·구도를 확인해 같은 16:9 계약의 다음 후보인지 판정한다.**

최신 바퀴(2026-08-30 07:31): **마을 집 0번 ox-alpha 공용 품질 계약 편입**(`4e410a88`) — `BuildVillage` 실제 집 목록은 `village_house_0~2`인데 공용 집 계약이 `_1/_2`만 순회하는 차집합을 확인했다. `_0`은 3.60유닛 고체 건물이자 영지 창고 폴백으로 실제 소비되며 별도 알파 계약은 없었으므로, 기존 이미지는 재생성하지 않고 같은 256×256·정상 알파·완전 불투명·정확히 1/6 희박 거부 계약에 한 역할만 추가했다. 신규 5단언 PASS(투명 44,319·불투명 20,342/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 직접 보아 대칭 박공지붕·목재 골조·석조 하단·중앙문·양쪽 창·굴뚝 연기가 구분되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 3.60유닛 마을 집 역할로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: `BuildVillage` 11종은 정상·불투명·희박 계약 차집합이 모두 닫혔으므로 같은 목록을 다시 훑지 말고, UI·아트 상시 폴리싱 큐에서 아직 화면 품질 계약이 없는 소비 화면 한 곳을 호출처부터 고른다.**

최신 바퀴(2026-08-30 07:28): **마을 우물 ox-alpha 불투명 배경 네거티브**(`8d565853`) — `BuildVillage` 실제 배치 목록과 역할별 정상·불투명·희박 계약의 차집합에서 `village_well_0`은 정상 PNG와 1/6 희박 경계는 있으나 완전 불투명 사각 배경 거부만 빠졌음을 확인했다. 1.60유닛 마을 중앙 장식이자 영묘 폴백인 기존 256×256 ox-alpha 이미지는 재생성하지 않고, 완전 불투명 대표 픽스처를 기존 `HasWellAlphaSilhouette`에 주입해 `QA_NO_WELL_ALPHA`가 거부하도록 계약했다. 신규 단언 PASS(실자산 투명 50,268·불투명 14,941/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 직접 보아 삼각 지붕·두 기둥·도르래·석조 원통·물 표면이 분리되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 1.60유닛 중앙 랜드마크 역할로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: `BuildVillage`가 실제 배치하지만 공용 마을 집 계약의 변형 목록에서 빠진 `village_house_0`을 먼저 소비 경로·표시 크기·별도 계약 존재 여부까지 확인하고, 정말 미계약이면 한 역할만 정상·불투명·희박 계약에 연결한다.**

최신 바퀴(2026-08-30 07:26): **마을 헛간 ox-alpha 불투명 배경 네거티브**(`349bc67d`) — `BuildVillage` 실제 배치 목록과 역할별 정상·불투명·희박 계약의 차집합에서 `village_barn_0`은 정상 PNG와 1/4 희박 경계는 있으나 완전 불투명 사각 배경 거부만 빠졌음을 확인했다. 4.00유닛 대형 랜드마크이자 광산·수비대 폴백인 기존 256×256 ox-alpha 이미지는 재생성하지 않고, 완전 불투명 대표 픽스처를 기존 `HasBarnAlphaSilhouette`에 주입해 `QA_NO_BARN_ALPHA`가 거부하도록 계약했다. 신규 단언 PASS(실자산 투명 43,022·불투명 21,331/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 직접 보아 대칭 박공지붕·중앙 쌍문·양쪽 창·석조 기단이 구분되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 4.00유닛 마을 랜드마크 역할로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 같은 차집합에서 완전 불투명 네거티브가 빠진 `village_well_0` 하나만 기존 실루엣 함수에 대표 사각 픽스처로 연결한다.**

최신 바퀴(2026-08-30 07:23): **마을 목책 2종 ox-alpha 불투명 배경 네거티브**(`496b5492`) — `BuildVillage` 실제 배치 목록과 역할별 정상·불투명·희박 계약을 교차해 `village_fence_0~1`은 정상 PNG와 1/5 희박 경계는 있으나 완전 불투명 사각 배경 거부만 빠졌음을 확인했다. 0.90/0.95유닛 부지 경계 역할의 기존 256×256 ox-alpha 이미지는 재생성하지 않고, 완전 불투명 대표 픽스처를 기존 `HasFenceAlphaSilhouette`에 주입해 두 변형 모두 `QA_NO_FENCE_ALPHA`가 거부하도록 계약했다. 신규 단언 PASS(0: 투명 45,943·불투명 18,399, 1: 투명 47,493·불투명 16,937/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 두 실제 PNG를 원본 해상도로 직접 보아 높이가 다른 말뚝·가로 보·금속 못·하단 빈틈이 구분되고, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 작은 마을 경계 프랍으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 같은 차집합에서 완전 불투명 네거티브가 빠진 다음 역할 `village_barn_0` 하나만 골라 기존 실루엣 함수에 대표 사각 픽스처를 연결한다.**

최신 바퀴(2026-08-30 07:20): **마을 가로등 ox-alpha 불투명 배경 네거티브**(`727bc22e`) — `BuildVillage` 실제 배치 11종과 기존 품질 계약을 교차해 `village_lamp_0`만 정상 PNG·1/10 희박 경계는 있으나 완전 불투명 사각 배경 거부가 없음을 찾았다. 2.60유닛 길가 조명 역할의 기존 256×256 ox-alpha 이미지는 재생성하지 않고, 완전 불투명 대표 픽스처를 기존 `HasLampAlphaSilhouette`에 주입해 `QA_NO_LAMP_ALPHA`가 거부하도록 계약했다. 신규 단언 PASS(실자산 투명 55,571·불투명 9,682/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 직접 보아 목재 기둥·가로 걸이·금속 고리·발광 창 4칸·석조 기단이 분리되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 2.60유닛 길가 조명 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: `BuildVillage` 실제 배치 목록에서 완전 불투명 네거티브가 빠진 다음 역할(우선 `village_fence_0~1`) 하나만 차집합으로 골라 기존 실루엣 함수에 대표 사각 픽스처를 연결한다.**

최신 바퀴(2026-08-30 07:17): **마을 수레 ox-alpha 희박 경계 네거티브**(`11afc4e9`) — 최신 포인터대로 `FieldDecor.GetPropNames`와 품질 계약의 차집합을 재스캔해 평원·잿벌·던전 자연 산포 역할은 이미 모두 계약됐음을 확인했고, 평원 바이옴의 실제 `BuildVillage` 프랍 중 `village_cart_0`만 정상 PNG·불투명 사각 검사는 있으나 희박 실루엣 경계가 없음을 찾았다. 1.30유닛 고체 생활 프랍의 기존 256×256 ox-alpha 이미지는 재생성하지 않고, 정확히 1/8만 불투명한 대표 픽스처를 기존 `HasCartAlphaSilhouette`에 주입해 `QA_NO_CART_SPARSE_ALPHA`가 거부하도록 계약했다. 신규 단언 PASS(실자산 투명 42,579·불투명 22,067/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 직접 보아 포대 적재함·손잡이·두 바퀴가 즉시 구분되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 1.30유닛 마을 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 자연 산포 역할은 전부 닫혔으므로 다시 훑지 말고, `BuildVillage` 실제 배치 프랍의 기존 계약에서 완전 불투명 또는 희박 경계 중 한쪽이 빠진 역할 하나만 차집합으로 고른다.**

최신 바퀴(2026-08-30 07:14): **던전 수정 3종 ox-alpha 실소비 계약**(`826c7db2`) — STATUS가 지목한 `dungeon_crystal_0~2`를 `FieldDecor` 던전 자연물 산포의 실제 고체 프랍으로 확인하고 `prop_scale.json` 표시 크기 0.90/1.40/1.00유닛을 교차 확인했다. 기존 생성본은 다시 만들지 않고 세 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/10 초과를 공용 계약으로 연결했으며, 완전 불투명 사각과 정확히 1/10인 대표 희박 픽스처를 `QA_NO_DUNGEON_CRYSTAL_ALPHA`·`QA_NO_DUNGEON_CRYSTAL_SPARSE_ALPHA`가 거부하게 했다. 단독 신규 13단언은 모두 PASS(0: 투명 58,194·불투명 6,814, 1: 52,351·12,265, 2: 55,918·8,898/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 세 실제 PNG를 원본 해상도로 직접 보아 단일 첨탑·3기 군집·쌍첨탑의 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 0.90~1.40유닛 던전 지형 장식 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: `FieldDecor`의 잿벌·던전 실제 산포 5개 역할은 모두 품질 계약이 생겼으므로 중복하지 말고, 런타임 산포 목록을 기준으로 아직 품질 계약이 없는 다른 바이옴 프랍 한 역할을 표시 크기와 함께 재스캔한다.**

최신 바퀴(2026-08-30 07:11): **던전 잔해 3종 ox-alpha 실소비 계약**(`75f1e31d`) — 아직 품질 계약이 없던 `dungeon_rubble_0~2`를 한 역할 세트로 추적해 `FieldDecor` 던전 자연물 산포에서 실제 소비되고 `prop_scale.json`에서 0.60/0.55/0.65유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 세 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/10 초과를 공용 계약으로 연결했으며, 완전 불투명 사각과 정확히 1/10인 대표 희박 픽스처를 `QA_NO_DUNGEON_RUBBLE_ALPHA`·`QA_NO_DUNGEON_RUBBLE_SPARSE_ALPHA`가 거부하게 했다. 단독 신규 13단언은 모두 PASS(0: 투명 56,884·불투명 7,801, 1: 55,723·9,237, 2: 52,649·11,979/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 세 실제 PNG를 원본 해상도로 직접 보아 파편 무더기·3단 벽돌·깨진 단일 블록의 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 0.55~0.65유닛 던전 바닥 장식 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: `FieldDecor` 던전 산포에 실제 포함되지 않는 `dungeon_wall_*`·`dungeon_cover_*`는 실소비로 오인하지 말고, 아직 품질 계약이 없는 실제 산포 세트 `dungeon_crystal_0~2`를 호출처·표시 크기부터 확인한다.**

최신 바퀴(2026-08-30 07:07): **던전 기둥 3종 ox-alpha 실소비 계약**(`705d6e3d`) — 아직 품질 계약이 없던 `dungeon_pillar_0~2`를 한 역할 세트로 추적해 `FieldDecor` 던전 산포의 고체 장애물로 실제 소비되고 `prop_scale.json`에서 2.40/2.40/2.20유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 세 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/10 초과를 공용 계약으로 연결했으며, 완전 불투명 사각과 정확히 1/10인 대표 희박 픽스처를 `QA_NO_DUNGEON_PILLAR_ALPHA`·`QA_NO_DUNGEON_PILLAR_SPARSE_ALPHA`가 거부하게 했다. 첫 단독 검사에서 1/8 하한이 `dungeon_pillar_2` 정상본(7,133/65,536)을 거부해 실픽셀·2.20유닛 근거로 1/10에 교정했고, 재검사 신규 13단언은 모두 PASS(0: 투명 56,126·불투명 9,020, 1: 56,891·8,399, 2: 58,001·7,133/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 세 실제 PNG를 원본 해상도로 직접 보아 직선 석주·고리형 석주·쌍첨탑 석주의 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 2.20~2.40유닛 던전 장애물 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 이번 계약의 1/10 경계 네거티브를 중복하지 말고, 아직 실소비 품질 계약이 없는 던전 프랍 역할 세트를 호출처·표시 크기부터 재스캔한다.**

최신 바퀴(2026-08-30 07:10): **잿벌 탄화목 3종 ox-alpha 실소비 계약**(`bdabca25`) — `ash_charred_0~2`를 한 역할 세트로 추적해 `FieldDecor` 잿벌 자연물 산포의 고체 장애물로 실제 소비되고 `prop_scale.json`에서 0.90/0.50/0.85유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 세 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/32 초과를 공용 계약으로 연결했으며, 완전 불투명 사각과 정확히 1/32인 대표 희박 픽스처를 `QA_NO_ASH_CHARRED_ALPHA`·`QA_NO_ASH_CHARRED_SPARSE_ALPHA`가 거부하게 했다. 단독 신규 13단언은 모두 PASS(0: 투명 55,507·불투명 9,300, 1: 50,877·13,865, 2: 48,407·16,259/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 세 실제 PNG를 원본 해상도로 직접 보아 쪼개진 탄화 그루터기·그을린 수레바퀴·탄 판재 더미의 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 0.50~0.90유닛 잿벌 장애물 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 이미 이번 계약에 포함된 1/32 경계 네거티브를 중복하지 말고, 아직 실소비 품질 계약이 없는 다음 잿벌·던전 프랍 역할을 호출처·표시 크기부터 재스캔한다.**

최신 바퀴(2026-08-30 06:59): **잿벌 뼈 2종 ox-alpha 실소비 계약**(`c5427943`) — 아직 품질 계약이 없던 `ash_bone_0~1`을 한 역할 세트로 추적해 `FieldDecor` 잿벌 자연물 산포에서 실제 소비되고 `prop_scale.json`에서 1.20/0.70유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 두 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/16 초과를 공용 계약으로 연결했으며, 완전 불투명 사각과 정확히 1/16인 대표 희박 픽스처를 `QA_NO_ASH_BONE_ALPHA`·`QA_NO_ASH_BONE_SPARSE_ALPHA`가 거부하게 했다. 단독 신규 9단언은 모두 PASS(0: 투명 57,751·불투명 7,057, 1: 52,745·12,159/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 두 실제 PNG를 원본 해상도로 직접 보아 긴 뼈·굽은 골편의 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 0.70~1.20유닛 잿벌 장식 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: UI·아트 트랙에서 아직 실소비 품질 계약이 없는 잿벌 탄화목 `ash_charred_0~2` 한 역할 세트를 호출처·표시 크기부터 재스캔한다.**

최신 바퀴(2026-08-30 06:56): **평원 생울타리 희박 알파 경계 네거티브**(`3820a941`) — 직전 공용 계약의 `solid > pixels.Length / 8`이 정상 PNG와 완전 불투명 배경만 검사하던 빈틈을, 65,536픽셀 중 정확히 1/8만 불투명한 대표 픽스처로 닫았다. 실자산 검사 앞에서 공용 `HasFieldShrubRowAlphaSilhouette`에 한 번만 주입했고 `QA_NO_FIELD_SHRUB_ROW_SPARSE_ALPHA`가 엄격한 경계값을 거부했다. 단독 신규 단언과 실자산 수치는 PASS(투명 57,262·불투명 8,274/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 런타임·아트 변경은 없고 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그로 대체했으며, 실제 256×256 PNG를 원본 해상도로 직접 보아 네 포기 구분·중심축·바닥선·투명 여백·잘림 없음이 유지됨을 확인했다. 2D 검증이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 기존 월드 프랍 한 역할을 호출처·표시 크기부터 재스캔한다.**

최신 바퀴(2026-08-30 06:53): **평원 생울타리 ox-alpha 실소비 계약**(`64e2b521`) — STATUS가 지목한 `field_shrub_row_0`을 재스캔해 아트 반입은 이미 `3f2fe30b`로 닫혔고, `FieldDecor` 평원 자연물 산포에서 1.10유닛으로 실제 소비되지만 품질 계약만 없음을 확인했다. 기존 생성본은 다시 만들지 않고 Resources 실 PNG의 256×256 캔버스·투명 과반·불투명 1/8 초과를 `HasFieldShrubRowAlphaSilhouette`로 연결하고, 완전 불투명 사각 픽스처를 `QA_NO_FIELD_SHRUB_ROW_ALPHA`가 거부하게 했다. 단독 신규 4단언은 모두 PASS(투명 57,262·불투명 8,274/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 직접 보아 네 포기의 가로 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 1.10유닛 평원 경계 장식 역할의 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 정확히 1/8만 불투명한 대표 픽스처 하나를 `HasFieldShrubRowAlphaSilhouette`에 주입하고 `QA_NO_FIELD_SHRUB_ROW_SPARSE_ALPHA`로 엄격한 경계 거부를 고정한다.**

최신 바퀴(2026-08-29 22:42): **평원 그루터기 희박 알파 경계 네거티브**(`2a500443`) — 직전 계약의 `solid > pixels.Length / 8`이 정상 PNG와 완전 불투명 배경만 검사하던 빈틈을, 65,536픽셀 중 정확히 1/8만 불투명한 대표 픽스처로 닫았다. 변형 루프 밖에서 공용 `HasFieldStumpAlphaSilhouette`에 한 번만 주입했고 `QA_NO_FIELD_STUMP_SPARSE_ALPHA`가 엄격한 경계값을 거부했다. 단독 신규 단언과 두 실자산 수치는 모두 PASS(0: 투명 54,153·불투명 10,755, 1: 55,505·9,475/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·기준선 `7/216 FAIL`(프로세스 exit 0)을 유지했다. 런타임·아트 변경은 없고 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그의 실 PNG 로드로 대체했으며 2D 검증이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 기존 평원 생울타리 `field_shrub_row_0` 한 역할을 호출처·표시 크기부터 재스캔한다.**

최신 바퀴(2026-08-29 22:39): **평원 그루터기 2종 ox-alpha 실소비 계약**(`ddd55f7e`) — 아직 품질 계약이 없던 `field_stump_0~1`을 한 역할 세트로 추적해, `FieldDecor` 평원 자연물 산포의 고체 장애물이며 `prop_scale.json`에서 0.80/0.75유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 두 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/8 초과를 공용 계약으로 연결하고, 완전 불투명 사각 픽스처를 `QA_NO_FIELD_STUMP_ALPHA`가 두 변형 모두 거부하게 했다. 단독 신규 8단언은 모두 PASS(0: 투명 54,153·불투명 10,755, 1: 55,505·9,475/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 두 실제 PNG를 중성 회색 배경에서 원본 해상도로 직접 보아 잘린 나이테형·꺾인 줄기형의 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 0.75~0.80유닛 평원 장애물 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 정확히 1/8만 불투명한 대표 픽스처 하나를 변형 루프 밖의 `HasFieldStumpAlphaSilhouette`에 주입하고 `QA_NO_FIELD_STUMP_SPARSE_ALPHA`로 엄격한 경계 거부를 고정한다.**

최신 바퀴(2026-08-29 22:35): **평원 덤불 희박 알파 경계 네거티브**(`73c5022d`) — 직전 공용 계약의 `solid > pixels.Length / 16`이 정상 PNG와 완전 불투명 배경만 검사하던 빈틈을, 65,536픽셀 중 정확히 1/16만 불투명한 대표 픽스처 하나로 닫았다. 변형 루프 밖에서 같은 `HasFieldBushAlphaSilhouette` 경로에 한 번만 주입했고 `QA_NO_FIELD_BUSH_SPARSE_ALPHA`가 엄격한 경계값을 거부했다. 단독 신규 단언과 세 실자산 수치는 모두 PASS(0: 투명 54,816·불투명 9,559, 1: 53,335·10,752, 2: 61,419·4,117/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 유지(프로세스 exit 0)다. 화면 변경 전 검증 커밋 후 세 실제 PNG를 원본 해상도로 직접 보아 둥근 겹수관·가로 쌍봉·작은 단일 덤불의 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 0.65~0.75유닛 평원 장식 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 기존 평원 그루터기 `field_stump_0~1` 한 역할 세트를 호출처·표시 크기부터 재스캔한다.**

최신 바퀴(2026-08-29 22:33): **평원 덤불 3종 ox-alpha 실소비 계약**(`4fcfe025`) — 아직 품질 계약이 없던 `field_bush_0~2`를 한 역할 세트로 추적해, `FieldDecor` 평원 자연물 산포의 통과 가능한 낮은 장식이며 `prop_scale.json`에서 0.75/0.75/0.65유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 세 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/16 초과를 공용 계약으로 연결하고, 완전 불투명 사각 픽스처를 `QA_NO_FIELD_BUSH_ALPHA`가 세 변형 모두 거부하게 했다. 단독 신규 12단언은 모두 PASS(0: 투명 54,816·불투명 9,559, 1: 53,335·10,752, 2: 61,419·4,117/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 세 실제 PNG를 원본 해상도로 직접 보아 둥근 겹수관·가로 쌍봉·작은 단일 덤불의 구분, 중심축·바닥선·투명 여백·잘림 없음이 유지돼 0.65~0.75유닛 평원 장식 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 정확히 1/16만 불투명한 대표 픽스처 하나를 공용 `HasFieldBushAlphaSilhouette`에 주입하고 `QA_NO_FIELD_BUSH_SPARSE_ALPHA`로 엄격한 경계 거부를 고정한다.**

최신 바퀴(2026-08-29 22:30): **평원 바위 희박 알파 경계 네거티브**(`adab096f`) — 직전 공용 계약의 `solid > pixels.Length / 64`가 정상 PNG와 완전 불투명 배경만 검사하던 빈틈을, 65,536픽셀 중 정확히 1/64만 불투명한 대표 픽스처 하나로 닫았다. 변형 루프 밖에서 같은 `HasFieldRockAlphaSilhouette` 경로에 한 번만 주입했고 `QA_NO_FIELD_ROCK_SPARSE_ALPHA`가 엄격한 경계값을 거부했다. 단독 신규 단언과 세 실자산 수치는 모두 PASS(0: 투명 56,833·불투명 7,686, 1: 59,143·6,393, 2: 64,299·1,237/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 세 실제 PNG를 원본 해상도로 직접 보아 단일 큰 바위·쌍석·잔돌 세 알의 구분, 중심축·바닥 그림자·투명 여백·잘림 없음이 유지돼 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 기존 평원 프랍(`field_bush_0~2` 또는 `field_stump_0~1`) 한 역할 세트를 호출처·표시 크기부터 재스캔한다.**

최신 바퀴(2026-08-29 22:26): **평원 바위 3종 ox-alpha 실소비 계약**(`b2a1978b`) — 아직 품질 계약이 없던 `field_rock_0~2`를 한 역할 세트로 추적해, `FieldDecor` 평원 자연물 산포의 고체 장애물이며 `prop_scale.json`에서 0.90/0.70/0.30유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 세 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/64 초과를 공용 계약으로 연결하고, 완전 불투명 사각 픽스처를 `QA_NO_FIELD_ROCK_ALPHA`가 세 변형 모두 거부하게 했다. 첫 단독 검사에서 1/12 하한이 의도적인 0.30유닛 잔돌을 거부해 실픽셀 근거로 교정했고, 재검사 신규 12단언은 모두 PASS(0: 투명 56,833·불투명 7,686, 1: 59,143·6,393, 2: 64,299·1,237/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 세 실제 PNG를 원본 해상도로 직접 보아 단일 큰 바위·쌍석·잔돌 세 알이 즉시 구분되고 중심축·바닥 그림자·투명 여백·잘림 없음이 유지돼 0.30~0.90유닛 평원 장애물 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 정확히 1/64만 불투명한 대표 픽스처 하나를 공용 `HasFieldRockAlphaSilhouette`에 주입하고 `QA_NO_FIELD_ROCK_SPARSE_ALPHA`로 엄격한 경계 거부를 고정한다.**

최신 바퀴(2026-08-29 22:22): **V4 계속 오버레이 dim 아틀라스 선반영 검증**(`37262c9f`) — 큐가 지목한 1px 임시 dim 교체는 이미 `V4ContinueOverlay.Draw`에서 기존 `UiAtlas`의 `panel`을 9-slice로 전체 화면에 소비하고, 로드 실패 때도 같은 아틀라스 조각 `DrawFit`으로 폴백하도록 커밋돼 있었다. 재구현 없이 전용 계약이 `GUI.DrawTexture(dim)`·`new Texture2D(1, 1)`을 금지하고 `DrawSliced(dim, "panel")`을 요구하는지 확인했다. 측정 사본 동기화 후 `V4ContinueOverlaySelfCheck` 단독 배치 PASS(exit 0), 전수 스윕은 현재 공유 작업 트리 기준선과 같은 `7/216 FAIL`(프로세스 exit 0)로 신규 회귀가 없었다. 오너 Unity가 꺼져 플레이 모드 눈확인은 규칙대로 배치 로그로 대체했으며, 이미 커밋된 화면 변경이라 새 런타임·아트 변경은 없다. **다음: UI·아트 트랙에서 이미 닫힌 항목을 제외하고, 아직 실소비 품질 계약이 없는 기존 프랍 한 종을 호출처·표시 크기부터 재스캔한다.**

최신 바퀴(2026-08-29 18:17): **평원 나무 희박 알파 경계 네거티브**(`a3b96251`+`4da5aac0`) — 직전 공용 계약의 `solid > pixels.Length / 8`이 정상 PNG와 완전 불투명 배경만 검사하던 빈틈을, 65,536픽셀 중 정확히 1/8만 불투명한 대표 픽스처로 닫았다. 같은 입력을 네 변형마다 중복하지 않고 공용 `HasFieldTreeAlphaSilhouette`에 한 번만 주입했으며 `QA_NO_FIELD_TREE_SPARSE_ALPHA`가 엄격한 경계값을 거부했다. 단독 신규 단언과 네 실자산 수치 모두 PASS(0: 투명 47,631·불투명 17,480, 1: 49,844·15,193, 2: 49,412·15,543, 3: 55,364·9,766/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 런타임·아트 변경이 없고 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그로 대체했으며 2D 검증이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 큐의 V4 오버레이 dim 1px→atlas 한 건만 소비처와 기존 아틀라스를 확인한 뒤 폴리싱한다(오버레이 로직 재작업 금지).**

최신 바퀴(2026-08-29 18:15): **평원 나무 4종 ox-alpha 실소비 계약**(`ec28a3a5`) — 품질 계약이 없던 `field_tree_0~3`를 한 역할 세트로 추적해, `FieldDecor` 평원 자연물 산포의 키 큰 요소이자 고체 장애물로 소비되고 `prop_scale.json`에서 5.20/6.00/4.40/2.40유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 네 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/8 초과를 공용 계약으로 연결하고, 완전 불투명 사각 픽스처를 `QA_NO_FIELD_TREE_ALPHA`가 네 변형 모두 거부하게 했다. 단독 신규 16단언은 모두 PASS(0: 투명 47,631·불투명 17,480, 1: 49,844·15,193, 2: 49,412·15,543, 3: 55,364·9,766/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 네 실제 PNG를 원본 해상도로 직접 보아 곧은 키큰 나무·기운 나무·쌍갈래·작은 단일 수관이 즉시 구분되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 2.4~6.0유닛 평원 산포 역할군 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 정확히 1/8만 불투명한 희박 입력을 공용 `HasFieldTreeAlphaSilhouette`에 한 번 주입하고 `QA_NO_FIELD_TREE_SPARSE_ALPHA`로 엄격한 경계 거부를 고정한다.**

최신 바퀴(2026-08-29 18:12): **마을 큰 집·작은 집 희박 알파 경계 네거티브**(`f2a91c6c`) — 직전 공통 계약의 `solid > pixels.Length / 6`이 정상 PNG와 완전 불투명 배경만 검사하던 빈틈을, 65,536픽셀 중 정확히 1/6만 불투명한 경계 픽스처로 닫았다. 공용 `SparseSilhouetteAtBoundary(..., 6)`를 두 변형 모두 실제 자산과 같은 `HasVillageHouseAlphaSilhouette` 경로에 주입했고 `QA_NO_VILLAGE_HOUSE_SPARSE_ALPHA` 2건이 엄격한 경계값을 거부했다. 단독 신규 단언은 모두 PASS(큰 집 투명 37,738·불투명 26,227, 작은 집 투명 53,018·불투명 12,094/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 두 실제 PNG를 원본 해상도로 직접 보아 큰 집의 2층 입면·쌍굴뚝과 작은 집의 단칸 박공지붕이 즉시 구분되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 각 4.60/2.80유닛 마을 건물 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 `field_tree_0~3` 중 호출처·표시 크기를 확인한 한 역할 세트만 기존 생성본으로 평가한다.**

최신 바퀴(2026-08-29 18:09): **마을 큰 집·작은 집 ox-alpha 실소비 계약**(`856581db`) — 품질 계약이 없던 `village_house_1/2`를 한 역할 세트로 추적해, 필드 마을의 고체 장애물·길 회피가 함께 소비하고 큰 집은 본성 QA_NO, 작은 집은 대장간 QA_NO 폴백이며 `prop_scale.json`에서 각각 4.60/2.80유닛으로 표시됨을 확인했다. 기존 생성본은 다시 만들지 않고 두 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/6 초과를 같은 계약으로 연결하고, 완전 불투명 사각 픽스처를 `QA_NO_VILLAGE_HOUSE_ALPHA`가 두 변형 모두 거부하게 했다. 신규 8단언은 모두 PASS(큰 집 투명 37,738·불투명 26,227, 작은 집 투명 53,018·불투명 12,094/65,536), 단독 전체는 기존 영지 티어 기준선 16건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 두 실제 PNG를 원본 해상도로 직접 보아 큰 집은 2층 석조·목조 입면과 굴뚝, 작은 집은 단칸 문·창·박공지붕이 즉시 구분되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 각 표시 크기의 마을 건물 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 집 계약이 정확히 1/6만 불투명한 희박 실루엣도 두 변형 모두 거부하는 경계 픽스처를 같은 공용 경로에 추가한다.**

최신 바퀴(2026-08-29 18:06): **마을 과수나무 희박 알파 경계 네거티브**(`685e94cf`) — 직전 계약의 `solid > pixels.Length / 5`가 정상 PNG와 완전 불투명 배경만 검사하던 빈틈을, 65,536픽셀 중 정확히 1/5만 불투명한 경계 픽스처로 닫았다. 공용 `SparseSilhouetteAtBoundary(..., 5)`를 실제 자산과 같은 `HasVillageTreeAlphaSilhouette` 경로에 주입했고 `QA_NO_VILLAGE_TREE_SPARSE_ALPHA`가 엄격한 경계값을 거부했다. 단독 신규 단언은 PASS(실자산 투명 48,136·불투명 16,915/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 실제 256×256 PNG를 원본 해상도로 직접 보아 둥근 3단 수관·열매 점·줄기·받침이 분리되고 중심축·바닥선·투명 여백·잘림 없음이 유지돼 3.4유닛 과수원 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 `village_house_1/2` 또는 `field_tree_0~3`를 호출처·표시 크기부터 재스캔하고 기존 생성본 한 종만 평가한다.**

최신 바퀴(2026-08-29 18:04): **마을 과수나무 ox-alpha 실소비 계약**(`ef28a2b0`) — 품질 계약이 없던 기존 월드 프랍을 호출처부터 재스캔해, `BuildVillage`가 3개 부지마다 과수원 자리에 세우고 길 회피·고체 장애물 판정이 함께 소비하며 `prop_scale.json`에서 3.40유닛으로 표시되는 `village_tree_0` 한 종을 골랐다. 자산은 다시 만들지 않고 실제 Resources PNG의 256×256 캔버스·투명 과반·불투명 1/5 초과를 계약으로 연결하고 완전 불투명 사각 픽스처를 `QA_NO_VILLAGE_TREE_ALPHA`가 거부하게 했다. 단독 신규 4단언은 모두 PASS(투명 48,136·불투명 16,915/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 공유 작업 트리 기준선 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 중성 회색 비파괴 합성본을 원본 해상도로 직접 보아 둥근 3단 수관·열매 점·줄기/받침 구분, 중심축·바닥선·투명 여백·잘림 없음이 양호해 3.4유닛 과수원 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 과수나무 알파 계약이 정확히 1/5만 불투명한 희박 실루엣도 거부하는 네거티브 픽스처 하나를 같은 공용 경로로 추가한다.**

최신 바퀴(2026-08-29 18:00): **마을 프랍 알파 계약 공용화 + 목책 희박 경계 네거티브**(`84afb7e1`) — 수레·건초더미·우물·헛간·가로등·목책 여섯 벌에 복제된 알파 픽셀 순회를 `HasAlphaSilhouette` 한 경로로 합치고, 4종에 흩어진 정확한 임계 경계 배열 생성도 `SparseSilhouetteAtBoundary`로 통합했다. 자산별 불투명 하한(1/8·1/10·1/6·1/4·1/10·1/5), 정상 픽셀 수치와 기존 `QA_NO_*` 문구는 유지했다. 목책 두 변형에는 정확히 1/5만 불투명한 입력을 같은 공용 경로로 주입해 `QA_NO_FENCE_SPARSE_ALPHA` 2건이 모두 PASS했다. 단독은 신규·기존 마을 계약 PASS와 기존 영지 티어 기준선 15건으로 exit 1, 전수는 현재 공유 작업 트리 기준선 `7/216 FAIL`·프로세스 exit 0이다. 런타임·아트 변경이 없고 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그로 대체했으며 2D 검증이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 기존 월드 프랍을 호출처·표시 크기부터 재스캔하고 한 종만 평가한다.**

최신 바퀴(2026-08-29 17:57): **마을 가로등 희박 알파 네거티브 검증**(`c64a1893`) — 정상 PNG만 PASS하던 `solid > pixels.Length / 10` 계약에 65,536픽셀 중 정확히 1/10만 불투명한 경계 픽스처를 같은 `HasLampAlphaSilhouette` 경로로 주입했다. 실제 가로등은 PASS(투명 55,571·불투명 9,682/65,536), `QA_NO_LAMP_SPARSE_ALPHA`는 엄격한 경계값을 거부했다. 단독은 신규 PASS·기존 영지 티어 기준선 15건으로 exit 1, 전수는 신규 PASS·현재 다른 작업 트리 포함 `7/216 FAIL`(exit 0)로 기준선을 유지했다. 화면 변경 전 검증 커밋 후 실제 256×256 PNG를 원본 해상도로 직접 보아 목재 기둥·가로 걸이·등롱·석조 기단의 구분, 중심축·투명 여백·잘림 없음이 유지돼 2.60유닛 도로 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 별도 검증 바퀴에서 여섯 벌의 알파 픽셀 순회와 경계 픽스처 생성을 공용 헬퍼로 합치되 기존 정상 수치·네거티브 문구를 유지하고, 목책 1/5 경계 거부도 같은 공용 경로로 닫는다.**

최신 바퀴(2026-08-29 18:12): **마을 목책 ox-alpha 실소비 계약**(`5717b364`) — 품질 계약이 없던 월드 프랍을 호출처부터 재스캔해, 필드 마을 부지 경계와 영지 성벽 `QA_NO`가 소비하고 `prop_scale.json`에서 0.90/0.95유닛으로 표시되는 기존 `village_fence_0/1` 한 세트를 골랐다. 자산은 다시 만들지 않고 두 Resources PNG 모두 256×256 캔버스·투명 과반·불투명 1/5 초과를 계약으로 연결했다. 단독 신규 6단언은 모두 PASS(0: 투명 45,943·불투명 18,399, 1: 투명 47,493·불투명 16,937/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1이었고 전수 스윕은 신규 PASS·현재 다른 작업 트리 포함 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 두 원본을 중성 회색에 비파괴 합성해 실제로 보았고, 탑 높이·개구부 리듬이 다른 두 변형 모두 중심축·바닥선·투명 여백·잘림 없음이 양호해 1유닛 미만 경계 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 가로등의 정확히 1/10 희박 실루엣 네거티브를 닫고, 다음 별도 검증 바퀴에서 누적 알파 판정·경계 픽스처 사본을 공용화한다.**

최신 바퀴(2026-08-29 17:51): **마을 가로등 ox-alpha 실소비 계약**(`03c2a3bc`) — 품질 계약이 없던 월드 프랍을 호출처부터 재스캔해, 필드 마을 큰길 가장자리와 화살탑·마법탑 `QA_NO` 폴백이 함께 소비하고 `prop_scale.json`에서 2.60유닛으로 표시되는 기존 `village_lamp_0` 한 종을 골랐다. 자산은 다시 만들지 않고 실제 Resources PNG의 256×256 캔버스·투명 과반·불투명 1/10 초과를 계약으로 연결했다. 단독 신규 3단언은 모두 PASS(투명 55,571·불투명 9,682/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1이었고 전수 스윕은 신규 PASS·현재 다른 작업 트리 포함 `7/216 FAIL`(프로세스 exit 0)이다. 화면 변경 전 검증 커밋 후 원본을 중성 회색에 비파괴 합성해 다시 보아 목재 기둥·가로 걸이·등롱·석조 기단의 구분, 중심축·여백·잘림 없음이 양호해 2.60유닛 도로 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 가로등 알파 계약이 정확히 1/10만 불투명한 희박 실루엣을 실제로 거부하는 네거티브 픽스처 하나를 추가하되, 누적된 알파 판정 사본의 공용화는 별도 바퀴로 분리한다.**

최신 바퀴(2026-08-29 18:00): **마을 헛간 희박 알파 네거티브 검증**(`1fdcd99c`) — `solid > pixels.Length / 4`가 정상 PNG만 PASS하던 빈틈을, 65,536픽셀 중 정확히 1/4만 불투명한 희박 픽스처로 닫았다. 실제 헛간은 PASS(투명 43,022·불투명 21,331/65,536), `QA_NO_BARN_SPARSE_ALPHA`는 엄격한 경계값을 거부했다. 단독은 신규 PASS·기존 영지 티어 기준선 15건을 재현했고, 전수는 신규 PASS·현재 다른 작업 트리 포함 `7/216 FAIL`(exit 0)이다. 커밋 후 실제 PNG를 중성 회색에 비파괴 합성해 원본 해상도로 보았고, 박공지붕·중앙 쌍문·좌우 창·석조 기단의 구분과 중심축·여백·잘림 없음이 양호해 4유닛 마을 랜드마크 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 실소비 품질 계약이 없는 월드 프랍을 호출처·표시 크기부터 재스캔하고 기존 생성본 한 종만 평가한다.**

최신 바퀴(2026-08-29 17:41): **마을 헛간 ox-alpha 실소비 계약**(`6b6909e8`) — 품질 계약이 없던 월드 프랍을 호출처부터 재스캔해, 필드 마을의 고체 건물과 영지 광산·수비대 `QA_NO` 폴백이 함께 소비하고 `prop_scale.json`에서 4.00유닛으로 표시되는 기존 `village_barn_0` 한 종을 골랐다. 자산은 다시 만들지 않고 실제 Resources PNG의 256×256 캔버스·투명 과반·불투명 1/4 초과를 계약으로 연결했다. 단독 신규 3단언은 모두 PASS(투명 43,022·불투명 21,331/65,536), SelfCheck 전체는 기존 영지 티어 기준선 15건으로 exit 1이었고 전수 스윕도 현재 다른 작업 트리 포함 `6/216 FAIL`(프로세스 exit 0)이며 신규 헛간 계약은 PASS다. 화면 변경 전 검증 커밋 후 실제 PNG를 중성 회색에 비파괴 합성해 원본 해상도로 다시 보아 좌우 균형, 박공지붕·큰 쌍문·창·석조 기단의 분리, 투명 여백과 잘림 없음이 양호해 4유닛 마을 랜드마크 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 캡처는 배치 로그+실리소스 육안으로 대체했고 2D 검증이라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 헛간 알파 계약이 불투명 사각 배경 또는 정확히 1/4인 희박 실루엣을 실제로 거부하는 네거티브 픽스처 하나를 추가하며, 이때 누적된 알파 순회 사본을 공용 헬퍼로 합칠지 범위를 먼저 판단한다.**

최신 바퀴(2026-08-29 17:38): **마을 우물 희박 알파 네거티브 검증**(`f94ebf22`) — 직전 계약의 `solid > pixels.Length / 6`이 정상 PNG만 PASS하던 빈틈을, 65,536픽셀 중 정확히 1/6만 불투명한 희박 픽스처 하나로 닫았다. 실제 우물은 PASS(투명 50,268·불투명 14,941/65,536)했고 `QA_NO_WELL_SPARSE_ALPHA`는 엄격한 경계값을 반드시 거부했다. 단독 SelfCheck는 신규 단언 PASS와 기존 영지 티어 기준선 15건을 재현했고, 전수 스윕도 신규 단언 PASS·현재 다른 작업 트리 포함 `6/216 FAIL`(프로세스 exit 0)로 기준선이 유지됐다. 화면 변경 전 검증 커밋 후 실제 256×256 PNG를 원본 해상도로 다시 보아 박공지붕·목재 기둥·도르래 손잡이·두레박·석조 원통이 분리되고 중심축·투명 여백·잘림 없음이 유지돼 작은 월드 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 눈확인은 배치 로그+실리소스 육안으로 대체했고 2D 검증이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 월드 프랍을 호출처·표시 크기부터 재스캔하고, 기존 생성본 한 종만 평가한다.**

최신 바퀴(2026-08-29 17:35): **마을 우물 ox-alpha 실소비 계약**(`9837f400`) — 품질 계약이 없던 월드 프랍을 호출처부터 재스캔해, 필드 마을 중앙 장식과 영묘 `QA_NO` 폴백이 함께 소비하고 `prop_scale.json`에서 1.60유닛으로 표시되는 기존 `village_well_0` 한 종을 골랐다. 자산은 다시 만들지 않고 실제 Resources PNG의 256×256 캔버스·투명 과반·불투명 1/6 초과를 계약으로 연결했다. 단독 신규 3단언은 모두 PASS(투명 50,268·불투명 14,941/65,536), SelfCheck 전체의 기존 영지 티어 기준선 15건은 동일했고 전수 스윕은 현재 다른 작업 트리 실패를 포함한 `6/216 FAIL`(프로세스 exit 0)이며 신규 우물 계약은 PASS다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 다시 보아 박공지붕·좌우 기둥·도르래/두레박·석조 원통이 즉시 읽히고 중심축·투명 여백·잘림 없음이 양호해 작은 월드 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 캡처는 배치 로그+실리소스 육안으로 대체했고 2D 검증이라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 우물 알파 임계값이 불투명 사각 배경 또는 지나치게 희박한 실루엣을 실제로 거부하는지 네거티브 픽스처 하나만 추가한다.**

최신 바퀴(2026-08-29 15:02): **마을 건초더미 희박 알파 네거티브 검증**(`89748bf7`) — 직전 계약의 `solid > pixels.Length / 10`이 정상 PNG만 PASS하던 빈틈을, 65,536픽셀 중 정확히 1/10만 불투명한 희박 픽스처로 채웠다. 정상 건초더미는 PASS(투명 48,348·불투명 16,277/65,536), `QA_NO_HAYSTACK_SPARSE_ALPHA`는 1/10 경계를 반드시 거부해 지나치게 희박한 실루엣 회귀의 실패 방향을 증명했다. 동시 작업이 먼저 코드를 HEAD에 반영해 이번 바퀴는 중복 수정 없이 `unity_meas` 동기화 후 단독·전수 로그로 해당 2단언 PASS를 재현했다. 단독은 기존 영지 티어 기준선 15건으로 exit 1, 전수는 현재 다른 작업 트리의 `EliteClassify`·`BossHp`·`EscapeForfeit` 추가 실패를 포함해 `6/216 FAIL`(exit 0)이며 본 변경의 두 건초더미 단언은 모두 PASS다. 런타임 화면·아트 변경이 없고 오너 Unity도 꺼져 있어 플레이 모드 눈확인은 배치 로그로 대체했다. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 월드 프랍을 호출처·표시 크기부터 재스캔하고 기존 생성본 한 종만 평가한다.**

최신 바퀴(2026-08-29 12:54): **마을 건초더미 ox-alpha 실소비 계약**(`27019053`) — 수레 다음의 품질 계약 미완료 월드 프랍을 호출처부터 재스캔해, 필드 마을 생활 흔적과 함정 `QA_NO` 폴백이 함께 소비하고 `prop_scale.json`에서 2.20유닛으로 표시되는 기존 `village_haystack_0` 한 종을 골랐다. 자산은 다시 만들지 않고 실제 Resources PNG의 256×256 캔버스·투명 과반·불투명 1/10 초과를 계약으로 연결했으며, 완전 불투명 사각 픽스처를 `QA_NO_HAYSTACK_ALPHA`가 거부하게 했다. 단독 신규 4단언은 모두 PASS(투명 48,348·불투명 16,277/65,536), SelfCheck 전체의 기존 영지 티어 기준선 15건은 동일했고, 전수 스윕도 기존 기준선 `4/216 FAIL`(프로세스 exit 0)이 유지됐다. 화면 변경 전 검증 커밋 후 실제 PNG를 중성 회색에 비파괴 합성해 원본 해상도로 다시 보아 원뿔형 3단 건초·결속 띠·꼭대기 말뚝이 즉시 읽히고 중심축·투명 여백·잘림 없음이 양호해 작은 월드 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 캡처는 배치 로그+실소스 육안으로 대체했고 2D 검증이라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 건초더미 계약의 임계값이 지나치게 희박한 실루엣도 실제로 거부하는지 진단하고, 필요하면 희박 알파 네거티브 픽스처 하나만 추가한다.**

최신 바퀴(2026-08-29 12:50): **마을 수레 불투명 배경 네거티브**(`6a733bee`) — 직전 바퀴의 256×256·투명 과반·불투명 1/8 초과 계약은 정상 파일만 확인해 임계값이 실제 회귀를 거부하는지 증명하지 못했다. 실제 판정을 `HasCartAlphaSilhouette` 한 경로로 분리하고 65,536픽셀 전부가 불투명한 사각 배경 픽스처를 주입해 `QA_NO_CART_ALPHA`가 반드시 거부하도록 고정했다. 단독 로그에서 실제 수레(투명 42,579·불투명 22,067)와 신규 네거티브 모두 PASS했고, 전수 스윕은 기존 기준선 `4/216 FAIL`(Estate 15건 포함, 프로세스 exit 0)이 그대로였다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 다시 보아 투명 배경, 두 바퀴·손잡이·짐칸·건초의 즉시 구분, 중심축과 잘림 없음이 유지돼 작은 월드 프랍 경쟁력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 캡처는 배치 로그+실리소스 육안으로 대체했고 2D 검증이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 아직 실소비 품질 계약이 없는 월드 프랍 후보를 호출처·표시 크기부터 다시 스캔하고, 기존 생성본이 역할에 맞는 한 종만 고른다.**

최신 바퀴(2026-08-29 12:50): **마을 수레 ox-alpha 실소비 계약**(`dc07fc6f`) — STATUS가 다음 아트 후보로 지목한 `village_cart_0`를 추적해 보니 이미 `719ead9e`에서 256×256 ox-alpha로 반입됐지만 바퀴 기록이 누락돼 있었다. 실제 소비는 필드 마을 배치와 경매장 `QA_NO` 폴백이며 `prop_scale.json` 1.30유닛 PPU 역산 경로를 탄다. 기존 SelfCheck는 이름 매핑만 보므로 옛 1740×1310 생성본이나 불투명 사각 배경이 돌아와도 통과해, 실제 Resources PNG의 256 캔버스·투명 과반·불투명 1/8 초과를 계약으로 연결했다. 단독 로그에서 신규 3단언 PASS(투명 42,579·불투명 22,067/65,536), 전수 스윕은 신규 계약 PASS와 기존 기준선 `4/216 FAIL`(Estate 15건 포함, 프로세스 exit 0)이 동일했다. 화면 변경 전 검증 커밋 후 실제 PNG를 원본 해상도로 다시 보아 두 바퀴·손잡이·짐칸·건초가 작은 프랍에서도 즉시 읽히고 투명 여백·중심축·잘림이 양호해 경쟁작 수준의 월드 장식 구분력 기준으로 합격했다. 오너 Unity가 꺼져 플레이 모드 캡처는 배치 로그+실리소스 육안으로 대체했고 2D 자산이라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 수레 계약의 임계값이 역할 회귀를 충분히 잡는지 진단하되, 새 화면·아트 없이 네거티브 픽스처 하나만 추가하거나 현 계약으로 충분하면 다음 실소비 미완료 후보를 다시 스캔한다.**

최신 바퀴(2026-08-29 12:44): **상태·보스 VFX 7종 실소비 프레임 비교판**(`21443f7b`) — `StatusVfxSelfCheck`가 런타임 `RequiredKeys` 순서의 7행×8프레임을 동일한 검은 배경에 출력하도록 했다. 균일 축소가 아니라 `FxPool.PlayStatus/Update`의 기준 64px, 0.65→1.15 ease-out 확대와 후반 50% 알파 소멸을 모사해 `projects/ashes-to-stars/results/status_vfx_runtime_frames.png`(704×616)를 배치에서 생성한다. 단독 **PASS exit 0**, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 비교판을 원본 해상도로 직접 보아 장판=주황 원형·독=녹색 운무·빙결=청색 결정·돌진=세로 쐐기·소환=보라 포털·부채꼴=붉은 섹터·차단=자홍 기둥/청색 X로 색과 실루엣이 겹치지 않고, 7종 모두 발화→성장→절정→소멸의 8단계가 읽히며 잘림·축 흔들림이 없음을 확인했다. 경쟁작 수준의 작은 전투 표식 구분력 기준으로 합격. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 비교판 육안으로 대체했고 2D 검증이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 실소비 미완료 후보 `village_cart` 한 종만 원본·호출처·작은 월드 크기에서 역할을 진단하고, 채택 가능한 기존 생성본이 없으면 약한 코드 대체 없이 보류한다.**

최신 바퀴(2026-08-29 12:38): **실소비 보스 차단 VFX 4×2 시트 정수 격자 정렬**(`3ab45238`) — `StatusVfxSheets.Frame`의 style 6이 재생하는 마지막 미완료 `boss_interrupt_sheet.png`(1774×887)를 기존 생성 그림 재생성 없이 직접 봤다. 작은 섬광→붉은 차단진 성장→광선 절정→파란 파편·잔광 소멸의 8프레임 역할을 채택했고, 공용 재패커로 셀별 독립 crop·Lanczos 축소·1024×512(256px 정수 셀)·8px 투명 안전 여백으로 반입했다. 2.06MB→702KB, 연속 2회 SHA-256 `99c5e731…b66e65f5` 불변. 7종 완료에 맞춰 `PackedStatusSheetCount`와 미완료 키에 의존하던 임시 계약을 제거하고 Python 재패커↔런타임 7종 `SetEquals`로 누락·여분 양쪽을 거부하게 단순화했다. `StatusVfxSelfCheck` 단독 **PASS exit 0**, 전수 스윗은 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(BossHp 1그룹·타 세션 Estate 3그룹, 프로세스 exit 0)이 동일했다. 커밋 후 최종 PNG를 원본 해상도로 다시 보아 중심축·셀별 여백·잘림 없음·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 상태·보스 VFX 7종을 동일한 검은 배경·실소비 크기로 한 장에 출력해 색·실루엣·프레임 구분을 세트 단위로 확인한다.**

최신 바퀴(2026-08-29 12:35): **상태 VFX 완료 수 선증가 드리프트 네거티브**(`57fb517f`) — 현재 Python `STATUS_SHEETS` 6종과 `PackedStatusSheetCount=6`의 정상 계약 및 미완료 런타임 키 1종을 목록에 먼저 넣는 기존 네거티브가 유효함을 재확인하고, 반대 순서인 **완료 수 상수만 먼저 7로 올린 상태**도 명시적으로 거부하게 했다. 계약 함수가 기대 수를 인자로 받아 실제 6종·선증가 7종·목록 확장 7종을 같은 판정 경로로 검증하므로 마지막 `boss_interrupt_sheet` 반입 전 양방향 드리프트를 막는다. 단독 배치와 커밋 후 재실행 모두 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(BossHp 1그룹·타 세션 Estate 3그룹, 프로세스 exit 0)이 동일했다. 런타임 화면·아트 변경이 없는 검증 전용 바퀴이며 오너 Unity가 꺼져 있어 플레이 모드 눈확인은 배치 SelfCheck 로그로 대체했고 2D 코드 계약이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 마지막 실소비 `boss_interrupt_sheet` 한 종만 역할·프레임 진행·1774×887 경계 접촉을 진단하고, 채택 가능하면 정수 격자로 반입하면서 부분집합+개수 계약을 런타임 7종 완전 동등 계약으로 단순화한다.**

최신 바퀴(2026-08-29 12:32): **실소비 보스 소환 포털 VFX 4×2 시트 정수 격자 정렬**(`dbb5ccc2`) — `StatusVfxSheets.Frame`의 style 4가 재생하는 `boss_summon_portal_sheet.png`(1774×887)는 셀 폭이 443/444px로 갈리고 하단 알파가 실제 시트 경계에 닿아 있었다. 기존 생성 그림을 다시 뽑지 않고 공용 `repack_job_vfx_sheet.py`의 상태 허용 목록에 이 한 종만 추가해 셀별 독립 crop·Lanczos 축소·1024×512(256px 정수 셀)·8px 투명 안전 여백으로 재패킹했다. 1.39MB→456KB, 연속 2회 SHA-256 `cc16b3da…d28c5bc` 불변, `StatusVfxSelfCheck` 공용 격자 계약을 6종으로 확장했다. 단독 배치 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(BossHp 1그룹·타 세션 Estate 3그룹, 프로세스 exit 0)이 동일했다. 커밋 후 최종 시트를 원본 해상도로 직접 보아 작은 발화점→포털 개방·확대→암석 동반 최대 소용돌이→폭발→세로 잔류 에너지 소멸의 진행, 공통 중심축, 셀별 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 SelfCheck 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 완료 수 `6`과 Python 목록 확장 드리프트 네거티브가 그대로 유효한지 진단하며, 마지막 `boss_interrupt_sheet` 반입 전에는 명시적 개수 가드를 유지한다.**

최신 바퀴(2026-08-29 12:28): **실소비 보스 부채꼴 경고 VFX 4×2 시트 정수 격자 정렬**(`bf9d666c`) — `StatusVfxSheets.Frame`의 style 5가 재생하는 `boss_cone_warning_sheet.png`(1774×887)는 셀 폭이 443/444px로 갈리고 하단 알파가 실제 시트 경계에 닿아 있었다. 기존 생성 그림을 다시 뽑지 않고 공용 `repack_job_vfx_sheet.py`의 상태 허용 목록에 이 한 종만 추가해 셀별 독립 crop·Lanczos 축소·1024×512(256px 정수 셀)·8px 투명 안전 여백으로 재패킹했다. 1.20MB→390KB, 연속 2회 SHA-256 `cf1b445a…17c11` 불변, `StatusVfxSelfCheck` 공용 격자 계약을 5종으로 확장했다. 단독 배치 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(BossHp 1그룹·타 세션 Estate 3그룹, 프로세스 exit 0)이 동일했다. 커밋 후 최종 시트를 원본 해상도로 직접 보아 작은 발화점→부채꼴 예고 확장→최대 발광→연기·불씨 소멸의 진행, 공통 중심축, 셀별 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 SelfCheck 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 완료 수 `5`와 Python 목록 확장 드리프트 네거티브가 그대로 유효한지 진단하며, 전체 7종 완료 전에는 명시적 개수 가드를 유지한다.**

최신 바퀴(2026-08-29 12:24): **상태 VFX 완료 수·Python 목록 확장 드리프트 네거티브**(`19051d61`) — `StatusVfxSelfCheck`의 정수 격자 완료 수 4를 `PackedStatusSheetCount`로 이름 붙이고, 런타임 7종 중 아직 Python `STATUS_SHEETS`에 없는 유효 키 하나를 목록에 추가한 상황을 네거티브로 재현했다. 이 목록은 런타임 부분집합이므로 기존 부분집합 검사만으로는 통과하지만 완료 수 가드가 거부해야 하며, 네거티브 픽스처가 정확히 5종인지도 강제한다. 단독 배치·커밋 후 재실행 모두 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(BossHp 1그룹·타 세션 Estate 3그룹, 프로세스 exit 0)이 동일했다. 런타임 화면·아트 변경이 없는 검증 전용 바퀴이며 오너 Unity가 꺼져 있어 플레이 모드 눈확인은 배치 SelfCheck 로그로 대체했고 2D 코드 계약이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 `StatusVfxSheets.RequiredKeys` 7종 중 아직 `STATUS_SHEETS`에 없는 실소비 시트 한 종만 역할·프레임 진행·1774×887 경계 접촉을 진단한다.**

최신 바퀴(2026-08-29 12:30): **실소비 보스 돌진 경고 VFX 4×2 시트 정수 격자 정렬**(`c8961997`) — `StatusVfxSheets.Frame`의 style 1이 재생하는 `boss_charge_warning_sheet.png`(1774×887)는 셀 폭이 443/444px로 갈리고 큰 프레임이 셀 경계에 닿아 있었다. 기존 생성 그림을 다시 뽑지 않고 공용 `repack_job_vfx_sheet.py`의 상태 허용 목록에 이 한 종만 추가해 셀별 독립 crop·Lanczos 축소·1024×512(256px 정수 셀)·8px 투명 안전 여백으로 재패킹했다. 1.35MB→438KB, 연속 2회 SHA-256 `c937968b…d8b1` 불변, `StatusVfxSelfCheck` 공용 격자 계약을 4종으로 확장했다. 단독 배치 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종 시트를 원본 해상도로 직접 보아 작은 발화점→전방 충전 경로 성장→최대 발광·폭발→연기·잔불 소멸의 진행, 중심축, 셀별 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 SelfCheck 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 정수 격자 완료 수 `4` 상수와 Python 목록 확장의 드리프트를 더 안전하게 네거티브 검증할 수 있는지 진단하되, 전체 7종 완료 전에는 명시적 개수 가드를 유지한다.**

최신 바퀴(2026-08-29 12:18): **상태 VFX 정수 격자 대상 목록 단일화**(`148c3bd4`) — `StatusVfxSelfCheck`에 Python `STATUS_SHEETS`와 같은 파일명 3종을 다시 적던 `PackedStatusSheets` 사본을 제거하고, 재패커에서 읽은 실제 목록 자체를 정수 격자 검사 원장으로 삼았다. 계약은 대상 3종·런타임 `RequiredKeys` 부분집합을 강제하며, 실제 목록에서 한 항목을 제거한 네거티브가 신규 보스 키 누락도 거부한다. 단독 배치 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(BossHp 1그룹·타 세션 Estate 3그룹, 프로세스 exit 0)이 동일했다. 런타임 화면·아트 자산을 바꾸지 않은 검증 전용 바퀴이며 오너 Unity가 꺼져 있어 플레이 모드 눈확인은 배치 SelfCheck 로그로 대체했고, 2D 코드 계약이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 실소비 `boss_charge_warning_sheet` 한 종만 역할·프레임 진행·1774×887 비정수 격자와 경계 접촉을 진단한다.**

최신 바퀴(2026-08-29 12:15): **실소비 보스 장판 예고 VFX 4×2 시트 정수 격자 정렬**(`c5a2bd1e`) — `StatusVfxSheets.Frame`의 style 0이 재생하는 `boss_aoe_warning_sheet.png`(1774×887)는 셀 폭이 443/444px로 갈리고 8프레임 모두 알파가 셀 경계에 닿아 있었다. 기존 생성 그림을 다시 뽑지 않고 공용 `repack_job_vfx_sheet.py`의 상태 허용 목록에 이 한 종만 추가해 셀별 독립 crop·Lanczos 축소·1024×512(256px 정수 셀)·8px 투명 안전 여백으로 재패킹했다. 1.46MB→468KB, 연속 2회 SHA-256 `d09c4d1c…1af3e` 불변, `StatusVfxSelfCheck` 공용 격자 계약에 포함했다. 단독 배치 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종 시트를 원본 해상도로 직접 보아 작은 예고 링→확장→최대 발광·폭발→잔불 소멸의 진행, 중심축, 셀별 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 SelfCheck 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 `PackedStatusSheets`와 재패커 `STATUS_SHEETS`의 파일명 목록 복제를 제거하거나, 최소한 새 보스 경고 키 누락 네거티브가 실제로 탐지되는 단일 계약으로 고정한다.**

최신 바퀴(2026-08-29 12:12): **상태 VFX 재패커 2종 정수 격자 공용 계약**(`580bf3f9`) — `StatusVfxSelfCheck`에 독·빙결별로 복제돼 있던 1024×512 단언을 제거하고, Python `STATUS_SHEETS`에서 실제 허용 파일 집합을 읽어 전부 같은 루프로 로드·검사하게 했다. 허용 집합은 `poison_status_sheet`·`freeze_status_sheet` 정확히 2종이어야 하며, 한 종만 남긴 네거티브 집합이 거부되는 계약도 추가해 새 허용 키의 검사 누락과 기존 키 소실을 함께 차단한다. 단독 배치와 커밋 후 재실행 모두 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 런타임 화면·아트 자산을 바꾸지 않은 검증 전용 바퀴이며 오너 Unity가 꺼져 있어 플레이 모드 눈확인은 배치 SelfCheck 로그로 대체했고, 2D 코드 계약이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 실소비 `boss_aoe_warning_sheet` 한 종만 역할·프레임 진행·1774×887 비정수 격자와 경계 접촉을 진단한다.**

최신 바퀴(2026-08-29 12:09): **실소비 빙결 상태 VFX 4×2 시트 정수 격자 정렬**(`1d99f420`) — `StatusVfxSheets.Frame`의 style 2가 실제 재생하는 `freeze_status_sheet.png`(1774×887)는 셀 폭이 443/444px로 갈리고 8프레임 모두 알파가 셀 경계에 닿아 있었다. 기존 생성 그림을 다시 뽑지 않고 공용 `repack_job_vfx_sheet.py`의 상태 허용 목록에 빙결 한 종만 추가해 셀별 독립 crop·Lanczos 축소·1024×512(256px 정수 셀)·8px 투명 안전 여백으로 재패킹했다. 1.77MB→572KB, 연속 2회 SHA-256 `3d1e7946…c2c4a2` 불변, `StatusVfxSelfCheck`에 빙결 정수 격자 계약을 추가했다. 단독 배치 exit 0·PASS, 전수 스윕은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종 시트를 원본 해상도로 직접 보아 냉기 섬광→결정 성장→설화 절정→잔류 냉기 소멸의 진행, 중심축, 셀별 최소 8px 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 `StatusVfxSelfCheck`의 상태 시트별 정수 격자 단언 복제를 공용 계약으로 정리하고 재패커 허용 2종을 모두 강제한다.**

최신 바퀴(2026-08-29 12:06): **상태 VFX 재패커·런타임 키 드리프 자동 탐지**(`1a45655c`) — Python `repack_job_vfx_sheet.py`의 `STATUS_SHEETS` 파일명을 읽어 런타임 `StatusVfxSheets.RequiredKeys` 집합에 모두 속하는지 `StatusVfxSelfCheck`가 강제하고, 등록되지 않은 가짜 키를 거부하는 네거티브 컨트롤을 추가했다. 재패커는 정수 격자 변환을 완료한 시트만 허용하므로 7종 전체 동등이 아닌 **허용 키 ⊆ 런타임 키** 계약으로 고정했다. 첫 단독 배치에서 C# 정규식 이스케이프 컴파일 오류를 탐지·교정한 뒤 단독 배치와 커밋 후 재실행 모두 exit 0·PASS. 전수 스윙은 신규 StatusVfx PASS와 기존 기준선 `4/216 FAIL`(BossHp/Estate 계열, 프로세스 exit 0)이 동일했다. 런타임 화면·아트를 바꾸지 않은 검증 전용 바퀴이고, 오너 Unity가 꺼져 있어 플레이 모드 눈확인은 배치 SelfCheck 로그로 대체했으며 2D 코드 계약이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 실소비 `freeze_status_sheet` 한 종만 역할·프레임 진행·원본 격자를 진단한다.**

최신 바퀴(2026-08-29 08:02): **실소비 독 상태 VFX 4×2 시트 정수 격자 정렬**(`e100951b`) — `StatusVfxSheets.Frame`의 style 1이 실제 재생하는 `poison_status_sheet.png`(1774×887, 셀 경계 443.5px)를 원본 해상도로 보고 독성 별빛 발화→독운 팽창→해골 표식 절정→연기·방울 소멸 흐름을 확인했다. 기존 생성 그림을 다시 뽑지 않고 공용 VFX 재패커에 상태 시트 허용 목록을 분리해 추가하고, 셀별 독립 crop·Lanczos 축소·1024×512(256px 정수 셀)·8px 투명 안전 여백으로 재패킹했다. 2회 실행 SHA-256 `6dcc774f…ff456` 불변, `StatusVfxSelfCheck`에 독 시트 정수 격자 계약을 추가했다. 단독 배치 exit 0·PASS, 전수 스윕은 이번 StatusVfx PASS와 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종 시트를 다시 직접 보아 중심축·셀별 여백·잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 상태 VFX 재패커 허용 키와 `StatusVfxSheets` 런타임 키의 드리프트를 자동 탐지하는 단일 계약을 추가한다.**

최신 바퀴(2026-08-29 07:57): **직업 VFX 재패커·런타임 키 드리프트 자동 탐지**(`288505d5`) — Python `repack_job_vfx_sheet.py`의 `SHEETS` 파일명 집합과 C# `JobVfxSheets.RequiredKeys`를 `JobVfxSelfCheck`가 직접 읽어 정확한 집합 일치를 강제했다. 키 하나만 둔 가짜 집합이 거부되는 네거티브 컨트롤도 함께 고정해 정상 상태만 확인하는 검사가 되지 않게 했다. 단독 배치와 커밋 후 재실행 모두 exit 0·PASS, 전수 스윕에서도 JobVfx PASS이며 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 15건의 `4/216 FAIL`(프로세스 exit 0)은 동일했다. 런타임 화면·아트 자산을 바꾸지 않은 검증 전용 바퀴이고 오너 Unity가 꺼져 있어 플레이 모드 눈확인은 배치 로그로 대체했으며, 2D 코드 계약이라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 런타임 Draw/Play 호출처가 있는 미완료 자산 한 세트만 진단한다.**

최신 바퀴(2026-08-29 07:53): **실소비 탱커 방어막 4×2 시트 정수 격자 정렬**(`f2d59d94`) — `JobVfxSheets.Frame`의 style 4가 실제 전투에서 소비하는 마지막 비정수 직업 시트 `tank_barrier_sheet.png`(1774×887, 셀 경계 443.5px)가 알파 기준 네 변에 닿아 있음을 원본 육안과 픽셀 실측으로 확인했다. 기존 생성 그림을 다시 뽑지 않고 공용 `repack_job_vfx_sheet.py`에 허용 키 하나만 추가해 셀별 독립 crop·Lanczos 축소·1024×512(256px 정수 셀)·8px 투명 안전 여백으로 재패킹했고, 여섯 번째 스크립트 사본은 만들지 않았다. 1.66MB→약 546KB, 연속 2회 SHA-256 `c84ea52d…cb336` 불변, `JobVfxSelfCheck`에 방어막 정수 격자 계약 추가. 단독 배치 exit 0·PASS, 전수 스윕은 이번 JobVfx PASS와 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 15건의 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종 시트를 원본 해상도로 직접 보아 작은 별빛 발화→육각 방벽 확장→충격 흡수 절정→수축·입자 소멸의 진행, 중심축, 셀별 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 검증 트랙으로 교대해 Python 허용 키 6종과 런타임 `JobVfxSheets.Keys` 6종의 드리프트를 자동 탐지하는 단일 계약만 추가한다.**

최신 바퀴(2026-08-29 07:50): **직업 VFX 재패커 5벌 허용 키 기반 공용 CLI 통합**(`a46d9cb1`) — 파일명만 다르던 62~71줄 스크립트 5개를 `art/repack_job_vfx_sheet.py` 하나로 합치고 `bard_aura/dps_slash/tank_slash/mage_fire/priest_heal` 외 키는 argparse 단계에서 거부하게 했다. 완료된 1024×512 파일은 재변환하지 않고 PNG 바이트·알파·셀별 8px 여백을 검증하며, 외부 원본은 1774×887만 허용해 타깃을 덮어쓴다. 다섯 키 연속 실행 전후 SHA-256 불변·잘못된 키 거부·py_compile PASS, `JobVfxSelfCheck` 단독 배치 exit 0·PASS. 전수 스윕은 이번 JobVfx PASS와 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 다섯 완성 시트를 원본 해상도로 각각 직접 보아 시작→확장/충돌→소멸의 진행, 중심축, 투명 안전 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: UI·아트 트랙으로 교대해 실소비 `tank_barrier_sheet` 한 종만 역할·프레임 진행·원본 격자를 진단하되, 변환이 필요하면 여섯 번째 사본 없이 공용 CLI 허용 키 확장으로 처리한다.**

최신 바퀴(2026-08-29 07:46): **실소비 사제 치유 4×2 시트 정수 격자 정렬**(`b9e6bb4e`) — `JobVfxSheets.Frame`이 style 3으로 실제 전투에서 소비하는 `priest_heal_sheet.png`의 1774×887 비정수 셀(443.5px)을 기존 생성 그림 재생성 없이 셀별 독립 crop·Lanczos 축소해 1024×512(256px 정수 셀)로 재패킹했다. 셀당 8px 투명 안전 여백·1774×887 원본 입력 제한·완료본 무변경 검증을 강제했고, 1.46MB→약 492KB, 연속 2회 실행 SHA-256 `fc76933c…024462` 일치를 확인했다. `JobVfxSelfCheck` 단독 배치 exit 0·PASS. 전수 스윕은 이번 JobVfx PASS와 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 15건의 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종본을 원본 해상도로 직접 보아 작은 치유 별→원형 치유장 확장→날개형 절정→빛기둥 수축→입자 소멸의 진행, 발원점·중심축, 셀 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 시트 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 아트 트랙을 한 바퀴 쉬고 검증 트랙에서 기존 직업 VFX 재패커 5벌을 허용 키 기반 공용 CLI로 통합한 뒤, 다음 아트 바퀴에 `tank_barrier_sheet`를 진단한다.**

최신 바퀴(2026-08-29 07:43): **실소비 마법사 화염 4×2 시트 정수 격자 정렬**(`5156895f`) — `JobVfxSheets.Frame`이 style 2로 실제 전투에서 소비하는 `mage_fire_sheet.png`의 1774×887 비정수 셀(443.5px)을 기존 생성 그림 재생성 없이 셀별 독립 crop·Lanczos 축소해 1024×512(256px 정수 셀)로 재패킹했다. 셀당 8px 투명 안전 여백·1774×887 원본 입력 제한·완료본 무변경 검증을 강제했고, 1.10MB→약 381KB, 연속 2회 실행 SHA-256 `d1a46996…db10` 일치를 확인했다. `JobVfxSelfCheck` 단독 배치 exit 0·PASS. 전수 스윕은 이번 JobVfx PASS와 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 15건의 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종본을 원본 해상도로 직접 보아 점화 섬광→화염탄 성장·비행→충돌 폭발→불씨·연기 소멸의 진행, 발원점·중심축, 셀 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 시트 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 같은 실소비 직업 VFX의 `priest_heal_sheet` 한 종만 역할·프레임 진행·경계 접촉을 진단한다.**

최신 바퀴(2026-08-29 07:41): **실소비 탱커 베기 4×2 시트 정수 격자 정렬**(`f475509b`) — `JobVfxSheets.Frame`이 style 0으로 실제 전투에서 소비하는 `tank_slash_sheet.png`의 1774×887 비정수 셀(443.5px)을 기존 생성 그림 재생성 없이 셀별 독립 crop·Lanczos 축소해 1024×512(256px 정수 셀)로 재패킹했다. 셀당 8px 투명 안전 여백·1774×887 원본 입력 제한·반복 실행 무변경 검증을 스크립트에 강제했고 1.33MB→약 437KB, 반복 SHA-256 `fe829850…7fa7` 일치를 확인했다. `JobVfxSelfCheck` 단독 배치 exit 0·PASS. 전수 스윕은 이번 JobVfx PASS와 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종본을 원본 해상도로 직접 보아 작은 황금 베기→청금색 방패형 참격 확장→교차 폭발→입자 꼬리 소멸의 진행, 발원점·중심축, 셀 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 시트 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 같은 실소비 직업 VFX의 `mage_fire_sheet` 한 종만 역할·프레임 진행·경계 접촉을 진단한다.**

최신 바퀴(2026-08-29 07:40): **실소비 물리 딜러 베기 4×2 시트 정수 격자 정렬**(`52c599d7`) — `JobVfxSheets.Frame`이 실제 전투에서 8프레임을 잘라 쓰는 `dps_slash_sheet.png`도 1774×887 원본이라 셀 경계가 443.5px였다. 기존 생성 그림을 다시 뽑지 않고 각 원본 셀을 독립 crop·Lanczos 축소해 1024×512(256px 정수 셀)로 재패킹하고, 셀마다 8px 투명 안전 여백과 결정론 계약을 강제했다. 1.08MB→약 357KB, 2회 인코딩 SHA-256 일치, `JobVfxSelfCheck` 단독 배치 exit 0·PASS. 전수 스윕은 이번 JobVfx PASS와 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 커밋 후 최종본을 원본 해상도로 직접 보아 작은 섬광→대각 베기→교차 폭발→잔광의 진행, 중심축, 셀 여백, 잘림·인접 셀 혼입 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 시트 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 같은 실소비 직업 VFX의 나머지 1774×887 시트 중 한 종만 역할·프레임 진행·경계 접촉을 진단한다.**

최신 바퀴(2026-08-29 07:33): **실소비 음유시인 오라 4×2 시트 정수 격자 정렬**(`a3de7939` + 육안 교정 `02b6ca10`) — `JobVfxSheets.Frame`이 실제 전투에서 8프레임을 잘라 쓰는 1774×887 원본은 셀 경계가 443.5px라 프레임 사이 픽셀 혼입 위험이 있었다. 기존 생성 그림을 다시 뽑지 않고 각 원본 셀을 독립 crop·Lanczos 축소해 1024×512(256px 정수 셀)로 재패킹했고, 1.6MB→약 587KB로 줄였다. 최초 자동검사·커밋 뒤 결과를 직접 보아 5번째 프레임이 왼쪽·아래 셀 경계에 닿는 것을 발견해 불합격 처리하고, 셀당 8px 투명 안전 여백 계약을 추가한 교정본을 새 커밋으로 반영했다. 최종본은 2회 SHA-256 일치, `JobVfxSelfCheck` 단독 배치 exit 0·PASS이며 전수 스윕은 이번 JobVfx PASS와 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(프로세스 exit 0)이 동일했다. 교정본을 원본 해상도로 다시 직접 보아 음표→마법진 확장→잔광 소멸, 중심축, 셀 여백, 잘림 없음을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 캡처는 배치 로그와 시트 육안으로 대체했고 2D 시트라 블렌더는 해당 없음. **다음: 같은 실소비 직업 VFX의 나머지 1774×887 시트 중 한 종만 역할·프레임 진행·경계 접촉을 진단한다.**

최신 바퀴(2026-08-29 07:31): **`FxPool` 8종 실소비 견본·공용 검증 계약 통합**(`0e0aa80d`) — 8자산마다 복제돼 있던 해상도·알파·가시 실루엣 검사를 이름·역할 데이터와 `AssertRuntimeSymbol` 헬퍼 하나로 정리하고, 배치 실행이 검은 배경 64px 4×2 견본(`results/fx_pool_runtime_symbols.png`, 순서 hit/slash/heal/fire | shield/taunt/summon/death)을 출력하게 했다. `FxPoolSelfCheck` 단독 배치 exit 0·PASS, 전수 스윕에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(프로세스 exit 0)은 동일했다. 규칙대로 코드 커밋 후 견본을 직접 보아 충격·초승달 베기·치유 고리·불꽃·보호 고리·동심파·룬진·사망 연기가 64px에서 서로 구분되고, 중심축·여백·잘림이 안정적이며 저채도 소환 룬도 검은 배경에서 윤곽이 남는 것을 확인했다. 오너 Unity가 꺼져 있어 플레이 모드 인게임 캡처는 배치 견본으로 대체했고 2D 심볼 검증이라 블렌더는 해당 없음. **다음: 검증 트랙을 닫았으므로 UI·아트 트랙으로 교대해, 런타임 Draw/Play 호출처가 있는 미완료 자산 한 세트만 새로 진단한다.**

최신 바퀴(2026-08-29 07:25): **실소비 사망 이펙트 `fx_death` 투명 512px 런타임 최적화**(`1bb0ac4a`) — `FxPool.Play(Kind.Death)`가 0.50초 동안 0.80→1.30배로 흩어지는 단품의 P23·P4 원본을 직접 비교했다. P23은 사망 캐릭터 전신이라 파티원·몽 공용 이펙트에 부적합해 반입하지 않고, P4 회백색 연기 구름을 선택해 배경 제거·중앙 정렬·16px 좌우 안전 여백·Lanczos 축소만 적용했다. 772×1238·1540KB→512×512·178KB, 투명 169,952px·가시 91,482px, bbox `(16,31)-(496,481)`이며 2회 SHA-256이 일치했다. `FxPoolSelfCheck` 단독 배치 exit 0·PASS, 전수 스윙에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(exit 0)은 동일했다. 커밋 후 최종 PNG를 직접 보아 연기 확산 실루엣·중심축·안전 여백·잘림 없음·투명 외곽을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 심볼이라 블렌더는 해당 없음. **다음: `FxPool` 8종 최적화가 완료됐으므로, 별도 검증 바퀴에서 8종을 같은 검은 배경·실소비 크기로 나란히 보여 색·실루엣 구분을 확인하고 `FxPoolSelfCheck` 복제 계약을 공용 헬퍼로 정리한다.**

최신 바퀴(2026-08-29 07:21): **실소비 소환 이펙트 `fx_summon` 투명 512px 런타임 최적화**(`0803a6bc`) — `FxPool.Play(Kind.Summon)`이 0.90초 동안 0.70→1.05배로 오래 남고 초당 40° 회전하는 단품의 P23·P4 원본을 직접 비교했다. P23은 소환사 전신이라 회전 이펙트에 부적합해 반입하지 않고, P4 보라색 이중 룬 마법진을 선택해 배경 제거·중앙 정렬·16px 안전 여백·Lanczos 축소만 적용했다. 1353KB→512×512·221KB, 투명 167,471px·불투명 67,726px이며 2회 SHA-256이 일치했다. `FxPoolSelfCheck` 단독 배치 exit 0·PASS, 전수 스윙에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(exit 0)은 동일했다. 커밋 후 최종 PNG를 직접 보아 룬 마법진 가독성·중심축·안전 여백·잘림 없음·투명 외곽을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 심볼이라 블렌더는 해당 없음. **다음: `FxPool` 마지막 미최적화 실소비 단품 `fx_death` 한 종만 P23·P4 역할 일치와 기존 7종 실루엣 중복을 진단한다.**

최신 바퀴(2026-08-29 07:16): **실소비 도발 이펙트 `fx_taunt` 투명 512px 런타임 최적화**(`05e17593`) — `FxPool.Play(Kind.Taunt)`가 0.45초 동안 0.30→1.80배로 멀리 퍼뜨리는 단품의 P23·P4 원본을 직접 비교했다. P4 황금 보호 고리는 방어막과 혼동돼 반입하지 않고, P23 동심 충격파를 선택해 배경 제거·중앙 정렬·16px 안전 여백·Lanczos 축소만 적용했다. 1720×1708·2.17MB→512×512·257KB, 투명 108,012px·가시 153,128px, bbox `(16,16)-(496,496)`이며 2회 SHA-256이 일치했다. `FxPoolSelfCheck` 단독 배치 exit 0·PASS, 전수 스윕에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(exit 0)은 동일했다. 커밋 후 최종 PNG를 직접 보아 도발/충격 확산 가독성·중앙축·안전 여백·잘림 없음·투명 외곽을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 심볼이라 블렌더는 해당 없음. **다음: `FxPool` 나머지 실소비 단품 중 `fx_summon` 한 종만 P23·P4 역할 일치와 배경·해상도를 진단한다.**

최신 바퀴(2026-08-29 07:12): **실소비 방어막 이펙트 `fx_shield` 투명 512px 런타임 최적화**(`b2e0c46c`) — `FxPool.Play(Kind.Shield)`가 0.60초 동안 1.10→0.90배로 감싸며 오무라드는 단품의 P23·P4 원본을 직접 비교했다. P23은 물방울 문양이라 보호막으로 읽히지 않아 반입하지 않고, 이미 생성된 P4 청록색 팔각 보호 고리를 선택해 배경 제거·중앙 정렬·16px 이상 안전 여백·Lanczos 축소만 적용했다. 1720×1708·2.89MB→512×512·151KB, 투명 174,590px·가시 86,764px이며 2회 SHA-256이 일치했다. `FxPoolSelfCheck` 단독 배치 exit 0·PASS, 전수 스윕에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(exit 0)은 동일했다. 커밋 후 최종 PNG를 직접 보아 보호 고리 가독성·중앙축·안전 여백·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 심볼이라 블렌더는 해당 없음. **다음: `FxPool` 나머지 실소비 단품 중 `fx_taunt` 한 종만 P23·P4 역할 일치와 배경·해상도를 진단한다.**

최신 바퀴(2026-08-29 07:07): **실소비 치유 이펙트 `fx_heal` 투명 512px 런타임 최적화**(`65538cd2`) — `FxPool.Play(Kind.Heal)`이 0.70초 동안 0.60→1.30배로 퍼뜨리는 단품의 P23·P4 원본을 직접 비교했다. P23은 회색 소용돌이라 치유로 읽히지 않아 반입하지 않고, 이미 생성된 P4 녹색 이중 치유 고리+십자 반짝임을 선택해 배경 제거·중앙 정렬·16px 이상 안전 여백·Lanczos 축소만 적용했다. 1149×1517·2.23MB→512×512·222KB, 투명 162,114px·가시 98,038px이며 2회 SHA-256이 일치했다. `FxPoolSelfCheck` 단독 배치 exit 0·PASS, 전수 스윗에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(exit 0)은 동일했다. 커밋 후 최종 PNG를 직접 보아 치유 역할 가독성·중앙축·안전 여백·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 심볼이라 블렌더는 해당 없음. **다음: `FxPool` 나머지 실소비 단품 중 `fx_shield` 한 종만 P23·P4 역할 일치와 배경·해상도를 진단한다.**

최신 바퀴(2026-08-29 07:18): **실소비 베기 이펙트 `fx_slash` 투명 512px 런타임 최적화**(`cf4773db`) — `FxPool.Play(Kind.Slash)`가 근접 기본 공격·정예 공격에서 실제 표시하는 P23 원본과 P4 대안을 먼저 직접 비교했다. 현재 P23 흰 초승달이 베기 역할과 기존 회화형 전투 톤에 더 맞아 신규 생성·원본 교체 없이 배경 정리·중앙 정렬·16px 이상 안전 여백·Lanczos 축소만 적용했다. 1665×1697·2.67MB→512×512·138KB, 투명 183,969px·가시 77,660px이며 2회 SHA-256 일치. `FxPoolSelfCheck` 단독 배치 exit 0·PASS, 전수 스윕에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(exit 0)은 동일했다. 커밋 후 최종 PNG를 원본과 64px 소비 모사로 직접 보아 흰 초승달 가독성·중앙축·안전 여백·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 소비 크기 모사로 대체했고 2D 심볼이라 블렌더는 해당 없음. **다음: `FxPool` 나머지 실소비 단품 중 `fx_heal` 한 종만 원본 의미·배경·해상도부터 진단한다.**

최신 바퀴(2026-08-29 06:59): **실소비 화염 이펙트 `fx_fire` 투명 512px 런타임 최적화**(`853ed016` + 육안 교정 `d6ffe242`) — `FxPool.Play(Kind.Fire)`가 실제 전투에서 표시하는 1794×1782·2.9MB 단품을 한 종만 처리했다. 최초에는 P23 동명 생성 원본을 배경 제거·16px 안전 여백·Lanczos 축소했고, 2회 SHA-256 일치·`FxPoolSelfCheck` PASS 뒤 규칙대로 먼저 커밋했으나 **커밋 후 직접 본 결과 회색 꽃무늬+회색 판으로 화염 역할이 읽히지 않아 불합격**했다. 신규 생성이나 약한 대체물을 쓰지 않고 이미 존재하던 P4 픽셀 불꽃 원본을 직접 확인해 교체했다. 최종본은 512×512·약 408KB, 투명 135,455px·불투명 123,897px, 2회 SHA-256 일치이며 단일 SelfCheck PASS. 전수 스윕에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·`EstateBuildingsSelfCheck` 3그룹의 `4/216 FAIL`(exit 0)은 동일했다. 최종 PNG를 직접 보아 주황–노랑–백색 픽셀 불꽃의 즉시 가독성·중앙축·안전 여백·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 심볼이라 블렌더는 해당 없음. **다음: `FxPool` 나머지 실소비 단품 중 `fx_slash` 한 종만 원본 의미·배경·해상도부터 진단한다.**

최신 바퀴(2026-08-29 06:50): **실소비 피격 이펙트 `fx_hit` 투명 512px 런타임 최적화**(`cf86a716`) — `FxPool.Play(Kind.Hit)`가 실제 전투에서 표시하는 P23 생성형 원본을 확인했다. 새 그림을 다시 생성하지 않고 기존 2048px 원본에 `knock_bg` 배경 제거·중앙 정렬·Lanczos 축소를 적용해 3.1MB→240KB로 줄였다. 2회 SHA-256 일치, 512×512·알파 0~255·가시 bbox·투명 140,612px·불투명 112,633px 계약 PASS. `FxPoolSelfCheck` 단독 배치 exit 0·PASS, 전수 스윕에서도 FxPool은 PASS했고 기존 `BossHpSelfCheck` 1그룹·`EstateBuildingsSelfCheck` 3그룹 때문에 `4/216 FAIL`(exit 0)은 동일했다. 커밋 후 512px PNG를 직접 보아 충격 실루엣·중심·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했고 2D 심볼이라 블렌더는 해당 없음. **다음: `FxPool` 나머지 실소비 단품 중 배경·해상도 계약을 위반하는 자산 한 종만 원본 재생성 없이 진단한다.**

최신 바퀴(2026-08-29 06:44): **실소비 재화·성장 아이콘 2종 ox-alpha 화풍 통일**(`8e911e07`) — 직전 포인터의 `gold/advancement_material`을 원본과 `ItemAtlas.KeyFor`·`ResultScreen`·`CharacterScreen` 소비 경로로 대조했다. 하단의 생성형 금화 더미·청색 수정 군집은 앞서 교체한 14칸보다 광원·세부 밀도와 외곽이 달라 48px 세트에서 튀었으므로, 신규 `gen_oxalpha_item_growth_icons.py`로 같은 216px 원형 배지·중심축·제한 웜톤에 맞췄다. `gold`는 결과 보상과 강화석의 이중 소비를 금화+강화 별로, 성장 재료는 결정+상승 갈매기로 색에만 의존하지 않게 구분했다. 생성기 팔레트·bbox·지정 2칸 밖 픽셀 불변·2회 SHA-256 일치와 PNG 알파 실로드 PASS, `ItemAtlasSelfCheck` 단독 배치 exit 0·PASS. 전수 스윕에서도 이번 ItemAtlas는 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹 때문에 `4/216 FAIL`(프로세스 exit 0)이 재현됐다. 커밋 후 아틀라스를 원본 해상도로 직접 확인해 두 의미 구분·공통 중심축·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했으며 2D UI라 블렌더는 해당 없음. **다음: `item_atlas` 실소비 폴리싱은 완료. UI·아트 큐를 다시 스캔하되 파일 존재가 아니라 런타임 Draw 호출처가 있는 미완료 자산 한 세트만 진단한다.**

최신 바퀴(2026-08-29 02:38): **목숨·귀환 아이템 4종 ox-alpha 화풍 통일**(`58eecca7`) — 직전 포인터의 `revival_tea/scroll_of_return/reborn_stone/special_job_token`을 원본과 `ItemAtlas.KeyFor` 소비 경로로 대조했다. 녹색 물약·파란 두루마리·거대 수정·금장 휘장의 생성형 회화 밀도가 서로 달라 48px에서 세트로 읽히지 않던 네 칸만 신규 `gen_oxalpha_item_life_icons.py`로 교체했다. 기존 장비 세트와 같은 216px 원형 배지·중심축·제한 웜톤을 쓰고, 십자 차통·돌아오는 화살표·붉은 재생 핵·별 토큰으로 의미를 나눠서 색에만 의존하지 않는다. 생성기 팔레트·bbox·지정 4칸 밖 픽셀 불변·2회 SHA-256 일치와 PNG 실로드 PASS, `ItemAtlasSelfCheck` 배치 exit 0·PASS. 전수 스윙에서도 이번 ItemAtlas는 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹 때문에 직전과 같은 `4/216 FAIL`(프로세스 exit 0)이 재현됐다. 커밋 후 아틀라스를 원본 해상도로 직접 확인해 네 의미 구분·공통 중심축·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했으며 2D UI라 블렌더는 해당 없음. **다음: 같은 `item_atlas`의 남은 실소비 재화·성장 계열(`gold/advancement_material`) 한 세트만 원본·호출 빈도·48px 가독성으로 진단한다.**

최신 바퀴(2026-08-29 02:34): **실소비 장비 아이콘 6종 ox-alpha 화풍 통일**(`01383b29`) — `ItemAtlas.DrawGear/DrawHud` 호출 그래프를 먼저 대조해 인벤토리·캐릭터·대장간·보상 화면에서 32~64px로 실제 소비되는 `sword/helmet/armor/gloves/boots/amulet`만 선택했다. 1254×1254 원본의 보석·금장·광원 세부는 축소 시 부위 실루엣이 뭉개져, 신규 `gen_oxalpha_item_equipment_icons.py`로 동일한 원형 배지·중심축·제한 웜톤의 굵은 픽토그램으로 교체했다. GUID·4×4 좌표·소비 경로와 나머지 10칸은 유지했다. 생성기 팔레트·bbox·지정 6칸 밖 픽셀 불변·2회 SHA-256 일치와 PNG 실로드 PASS, `ItemAtlasSelfCheck` 배치 exit 0·PASS. 전수 스윕에서도 이번 ItemAtlas는 PASS했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹 때문에 직전과 같은 `4/216 FAIL`(프로세스 exit 0)이 재현됐다. 커밋 후 원본과 48px 소비 크기 모사 시트를 실제 확인해 여섯 부위 구분·공통 중심축·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 소비 크기 모사로 대체했으며 2D UI라 블렌더는 해당 없음. **다음: 같은 `item_atlas`의 남은 실제 소비 아이템 중 목숨·귀환 계열(`revival_tea/scroll_of_return/reborn_stone/special_job_token`) 한 세트만 원본·호출 빈도·48px 가독성으로 진단한다.**

최신 바퀴(2026-08-29 02:28): **실소비 전투 상태 아이콘 3종 ox-alpha 화풍 통일**(`ef99c48f`) — 독립 `ui/icons`는 `CombatIconAtlas.Draw` 호출처가 0이라 더 손대지 않고, 실제 `W3Party` 파티 HUD가 `StatusIconAtlas.DrawRow`로 18px 소비하는 `shield/taunt/attack_up`만 선택했다. 원본은 1254×1254 생성형 회화 아틀라스라 작은 HUD에서 벌집 방패·광선 방패·검의 세부가 뭉개졌다. 신규 `gen_oxalpha_live_status_icons.py`로 아틀라스 규격·GUID·나머지 13칸·소비 경로를 유지하면서 같은 216px 원형 배지 안에 방패·도발 느낌표·공격 상승 화살표를 굵게 통일했다. 생성기 팔레트·bbox·지정 3칸 밖 픽셀 불변·2회 SHA-256 일치와 PNG 알파 실로드 PASS, `StatusVfxSelfCheck` 배치 exit 0·PASS. 전수 스윕은 이번 StatusVfx 포함 통과했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹 때문에 직전과 같은 `4/216 FAIL`(프로세스 exit 0)이 재현됐다. 커밋 후 원본과 18px 소비 크기 모사 이미지를 실제 확인해 세 의미 구분·공통 중심축·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 소비 크기 모사로 대체했으며 2D UI라 블렌더는 해당 없음. **다음: 소비처 0인 독립 스킬 아이콘 폴리싱은 중단하고, 실제 인벤토리·장비 화면이 소비하는 `item_atlas`의 구형 회화 자산 중 한 세트만 원본·소비 크기로 진단한다.**

최신 바퀴(2026-08-29 02:23): **실소비 위험 경고 아이콘 5종 ox-alpha 화풍 통일**(`34b32742`) — `CombatIconAtlas.Draw`가 아틀라스보다 우선 로드하는 독립 단품 중 `warn_circle/beam/summon/enrage/phase`를 직접 열어, 512×512 불투명 회화 배경과 작은 HUD에서 뭉개지는 세부 노이즈를 확인했다. 신규 `gen_oxalpha_ui_warning_icons.py`로 원본 512 규격·GUID·소비 경로를 유지하면서 투명 배경, 동일한 216px 원형 배지, 제한 웜톤 팔레트로 통일했다. 색뿐 아니라 원형 범위·대각 빔·소환 별·격노 불꽃·페이즈 순환 외곽으로 구분한다. 생성기 팔레트·bbox·5종 고유성 계약과 2회 SHA-256 일치 PASS, png 실로드 검사 PASS, `CombatIconAtlasSelfCheck` 배치 exit 0·PASS. 전수 스윕은 이번 항목을 포함해 통과했고 기존 타 작업 4그룹 때문에 직전과 같은 `4/216 FAIL`(프로세스 exit 0)이 재현됐다. 커밋 후 PNG를 원본 해상도로 실제 확인해 의미 구분·공통 중심축·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했으며, 2D UI라 블렌더는 해당 없음. **다음: 독립 `ui/icons` 중 실제 소비되는 스킬·상태 아이콘 한 세트를 소비 빈도와 원본 육안으로 진단한다.**

최신 바퀴(2026-08-29 02:18): **월드·전투 표식 8종 ox-alpha 화풍 통일**(`e4393da9`) — 아틀라스 y=700 행을 원본과 `UiAtlas.Pieces` 전체에 대조해 8종 모두 런타임 소비 키가 0임을 확인하고, 기능 배선은 늘리지 않은 채 기존 1448×1086·GUID·행 경계를 유지했다. 신규 `gen_oxalpha_ui_world_markers.py`로 길표식·빙결별·위험별·공허별·은하·성운·공격·방어를 동일한 88px 원형 배지, 전역 y=745 중심축, 제한 웜톤 팔레트로 통일했다. 생성기 계약(팔레트·전역 중심축·지정 영역 밖 픽셀 불변·결정론)과 2회 SHA-256 일치 PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 기존 `BossHpSelfCheck` 1그룹·타 세션 `EstateBuildingsSelfCheck` 3그룹 때문에 직전과 같은 4/216 FAIL(프로세스 exit 0)이 재현됐다. 커밋 후 실제 산출물을 원본 해상도로 확인해 8종 의미 구분·중심 정렬·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 PNG 육안으로 대체했다. **다음: UI 아틀라스 미소비 행 폴리싱은 멈추고, 실제 소비되는 독립 UI·아트 미완료 한 세트를 새로 진단한다.**

최신 바퀴(2026-08-29 02:15): **소비 건물 표식 4종 ox-alpha 화풍 통일**(`799f24a6`) — 아틀라스 y=570 행을 원본과 `UiAtlas.Pieces` 소비처로 대조해, 회화식 건물 7종 중 실제 런타임 키가 있는 대장간·경매장·영묘·수비대 4종만 선택했다. 신규 `gen_oxalpha_ui_building_icons.py`로 기존 1448×1086·GUID·Rect·소비 경로를 유지하면서 동일한 88px 원형 배지, 전역 y=632 중심축, 제한 웜톤 팔레트로 통일했다. 생성기 계약(팔레트·전역 중심축·지정 영역 밖 픽셀 불변·결정론)과 2회 SHA-256 일치 PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 기존 보스 벽 배율 1그룹·타 세션 영지 알파 3그룹 때문에 직전과 같은 4/216 FAIL(프로세스 exit 0)이 재현됐다. 커밋 후 산출물을 실제 확인해 망치·수비 깃발·모루·십자 영묘의 의미 구분, 중심 정렬, 잘림 없음과 인접 자산 불변을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 원본 육안으로 대체했다. **다음: 아틀라스 y=700의 월드·전투 표식은 실제 소비처를 먼저 대조하고, 소비되는 한 세트만 닫는다.**

최신 바퀴(2026-08-29 02:11): **재화·탐험 표식 8종 ox-alpha 화풍 통일**(`0ec60c84`) — 큐가 지목한 아틀라스 y=455 행을 원본으로 열어 동전 3단계·수정·약초·성운·두루마리·나침반의 회화 밀도와 외곽 폭이 제각각임을 확인했다. 신규 `gen_oxalpha_ui_resource_icons.py`로 기존 1448×1086·GUID를 유지하면서 동일한 88px 원형 배지, 전역 y=512 중심축, 제한 웜톤 팔레트로 통일했다. 생성기 계약(팔레트·전역 중심축·지정 영역 밖 픽셀 불변·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 기존 보스 벽 배율 1건·영지 아트 알파 그룹 3건 때문에 직전과 같은 4/216 FAIL(프로세스 exit 0)이 재현됐다. 커밋 후 원본 행을 다시 실제 확인해 8종 의미 구분·중심 정렬·잘림 없음·투명 배경을 확인했다. 런타임 `UiAtlas.Pieces` 직접 키는 0개라 소비 빈도 0임을 기록하며 배선은 늘리지 않았다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 원본 행 육안으로 대체했다. **다음: 아틀라스 y=570의 건물·월드 표식 행을 원본·실제 소비처로 진단해 한 세트만 닫는다.**

최신 바퀴(2026-08-29 02:10): **전투 표식 둘째 행 8종 ox-alpha 화풍 통일**(`0566c3ad`) — 큐가 지목한 아틀라스 y=330의 구형 회화식 표식 8종을 실제로 열어 보니 과녁·십자광·해골·가속·방어·재사용·정예·격노가 서로 다른 외곽 폭과 광원 밀도로 섞여 있었다. 신규 `gen_oxalpha_ui_combat_markers.py`로 기존 1448×1086·GUID를 유지하면서 동일한 96px 원형 배지, 전역 y=392 중심축, 제한 웜톤 팔레트로 통일했다. 생성기 계약(팔레트·전역 중심축·지정 영역 밖 픽셀 불변·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 기존 보스 벽 배율 1건·영지 아트 알파 그룹 3건 때문에 직전과 같은 4/216 FAIL(프로세스 exit 0)이 재현됐다. 커밋 후 원본과 50% 축소 견본을 실제 확인해 8종 의미 구분·중심 정렬·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 축소 견본으로 대체했다. **다음: 아틀라스 y=450의 재화·탐험 표식 다음 한 행을 원본·실제 소비처로 진단해 한 세트만 닫는다.**

최신 바퀴(2026-08-29 02:02): **상태·전투 표식 첫 행 8종 ox-alpha 화풍 통일**(`f536fc91`) — 큐가 지목한 `heart/heart_broken/poison/fire/ice/lightning/curse/bleed` 원본을 실제로 열어 보니 3D 보석·회화식 불꽃·눈·상처가 서로 다른 외곽과 광원 밀도로 섞여 있었다. 신규 `gen_oxalpha_ui_status_icons.py`로 기존 1448×1086·GUID·목숨 Rect 소비 경로를 유지하면서 제한 웜톤 팔레트와 같은 굵기의 픽토그램으로 교체했다. 생성기 계약(팔레트·전역 중심축·지정 영역 밖 픽셀 불변·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 기존 보스 벽 배율 1건·영지 아트 알파 그룹 3건 때문에 직전과 같은 4/216 FAIL(프로세스 exit 0)이 재현됐다. 커밋 후 원본 크기와 50% 축소 견본을 실제 확인해 8종 의미 구분·중심 정렬·잘림 없음·투명 배경을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 배치 로그와 실제 소비 크기 모사로 대체했다. **다음: 아틀라스 y=340 아래의 다음 구형 회화식 전투 표식 한 행을 원본·실제 소비처로 진단해 한 세트만 닫는다.**

최신 바퀴(2026-08-29 01:57): **역할 아이콘 4종 ox-alpha 화풍 통일**(`fde65181`) — 직전 큐가 지목한 아틀라스 `tank/damage/healer/buffer`를 원본으로 확인하니 방패·교차검·지팡이·오라가 서로 다른 회화 밀도와 외곽 실루엣이라 같은 역할 표식 세트로 읽히지 않았다. 신규 `gen_oxalpha_ui_role_icons.py`로 기존 Rect·1448×1086 규격·GUID·소비 경로를 유지하면서 네 조각을 동일한 103px 원형 배지, 아틀라스 전역 y=187 중심축, 제한 웜톤 팔레트로 통일했다. 역할은 방패·교차검·치유 십자 지팡이·4색 궤도 오라로 즉시 구분한다. 생성기 계약(팔레트·배지 bbox·중심축·지정 영역 밖 픽셀 불변·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 기존 보스 벽 배율 1건·영지 아트 알파 그룹 3건 때문에 직전과 같은 4/216 FAIL(프로세스 exit 0)이 재현됐다. 커밋 후 측정용 플레이어를 빌드해 1280×720 `go:Character` 화면을 실제 캡처했고, 탱 역할 방패가 파티 카드와 장비 카드의 작은 소비 크기에서도 선명하며 중심 정렬·잘림 없음·텍스트 비침범을 확인했다. **다음: 아틀라스의 다음 상시 노출 구형 회화 세트는 상태/전투 표식 첫 행(`heart`, `heart_broken`, `poison`, `fire`, `ice`, `lightning`, `curse`, `bleed`) — 원본과 실제 소비 빈도를 먼저 진단해 한 세트만 닫는다.**

최신 바퀴(2026-08-29 01:52): **하단 도크·화면 헤더 아이콘 5종 ox-alpha 화풍 통일**(`5fe92682`) — 큐가 지목한 상시 노출 아틀라스 아이콘 `territory/field/tower/worldmap/characters`를 원본으로 보니 회화 밀도·외곽 폭·바닥선이 서로 달랐다. 신규 `gen_oxalpha_ui_nav_icons.py`로 기존 Rect·1448×1086 규격·GUID·소비 경로를 유지하면서 다섯 조각을 동일한 103px 원형 배지, 아틀라스 전역 y=63 중심축, 제한 웜톤 팔레트로 통일했다. 생성기 계약(팔레트·배지 bbox·중심축·지정 영역 밖 픽셀 불변·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 기존 보스 벽 배율 1건·영지 아트 알파 그룹 3건 때문에 직전과 같은 4/216 FAIL(프로세스 exit 0)이 재현됐다. 커밋 후 측정용 플레이어를 빌드해 1280×720 `go:Field` 화면을 실제 캡처했고, 헤더 1개와 하단 도크 5개가 36px에서도 영지=성·필드=산과 해·탑=탑·월드맵=지구본·캐릭터=3인으로 구분되며 중심 정렬·잘림 없음·라벨 비침범을 확인했다. **다음: 아틀라스의 다음 상시 노출 구형 회화 세트는 역할 아이콘 4종(`tank/damage/healer/buffer`) — 원본·소비 빈도를 진단하되 기존 변경과 겹치지 않는다.**


최신 바퀴(2026-08-29 01:45): **등급 프레임 5종 ox-alpha 화풍 통일**(`e787f2b4`) — 큐가 지목한 UI 아틀라스의 `rarity_{common,uncommon,rare,heroic,legendary}`를 실제 원본으로 보니 등급마다 외곽 폭·실루엣·중심축이 달라 장비 아이콘이 등급 전환 때 흔들릴 여지가 있었다. 신규 `gen_oxalpha_ui_rarity_frames.py`로 기존 아틀라스 좌표·1448×1086 규격·GUID·소비 경로를 유지하면서 다섯 조각의 7px 외곽과 10px 안전 영역을 통일했다. 등급은 회색·녹색·청색·보라·금색과 모서리 표식 1→5개로 구분한다. 생성기 계약(아틀라스 규격·중앙 안전색·지정 영역 밖 픽셀 불변·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 공유 작업 트리의 기존 `BossHpSelfCheck` 1건·`EstateBuildingsSelfCheck` 15건 때문에 기존과 같은 4/216 FAIL(프로세스 exit 0)이 재현됐다. 커밋 후 전체 PNG를 원본 해상도로 직접 확인해 다섯 프레임 중심 정렬·등급 구분·잘림 없음·인접 조각 비침범을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 생략. **다음: UI 아틀라스의 상시 노출 아이콘 중 구형 회화 자산 1세트를 원본·소비 빈도로 진단하되 기존 변경과 겹치지 않는다.**



최신 바퀴(2026-08-29 01:37): **버튼 3상태 ox-alpha 화풍 통일**(`8c6d2aa5`) — 직전 큐가 지목한 `ui/chrome/button_{normal,hover,pressed}.png` 원본을 실제 해상도로 열어 보니 세 상태의 캔버스·외곽 실루엣이 제각각이고, 호버만 과도하게 발광하며 눌림은 검은 테두리가 뭉쳐 상태 전환 시 흔들릴 여지가 있었다. 신규 `gen_oxalpha_ui_chrome_buttons.py`로 각 원본 규격·RGBA와 GUID·소비 경로를 유지하면서 채택된 panel·HP·XP와 같은 웜톤 9-slice 구조로 통일했다. 보통=청회색, 호버=밝은 청회색+밝은 금선, 눌림=어두운 면+상단 음영으로 입력 상태를 구분했다. 생성기 계약(원본 3규격·제한 팔레트·좌우 대칭·16% 밖 가로 늘림 균일·상태별 중앙색·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 이번 UiAtlas 포함 통과했고 공유 작업 트리의 기존 `BossHpSelfCheck` 1건·`EstateBuildingsSelfCheck` 15건 때문에 종합 4/216 FAIL(프로세스 exit 0)이 재현됐다. PNG 3장을 실제 원본 크기로 확인해 공통 외곽 정렬·상태 차이·깨끗한 중앙 텍스트 영역을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 생략. **다음: 독립 `ui/chrome` 8종은 전부 ox-alpha 완료 — UI 아틀라스의 rarity 5종 또는 상시 노출 아이콘 중 구형 회화 자산 1세트를 원본·소비 빈도로 진단하되 기존 변경과 겹치지 않는다.**


> 인수인계서. 보드(`loop/board.py`)가 이 파일을 읽는다.

> 2026-08-23 빈 템플릿으로 갈리며 보드가 비었던 것을, 아카이브·WORKLOG 기준으로 복구.

최신 바퀴(2026-08-29 01:35): **경험치 프레임 ox-alpha 화풍 통일**(`84827973`) — 직전 큐가 지목한 `ui/chrome/xp_frame.png` 원본을 실제 1280×344로 열어 보니 구형 회화식 청동 질감과 네 모서리의 과대 정사각 장식이 16~22px 높이 경험치 바에서 뭉개지고, 최근 통일한 HP 크롬과 재질이 갈렸다. 신규 `gen_oxalpha_ui_chrome_xp_frame.py`로 원본 규격·RGBA와 GUID·소비 경로를 유지한 채 웜톤 6색 대칭 9-slice 프레임으로 교체하고 구판을 백업했다. 경험치 구분은 체력 정보와 경쟁하지 않는 얇은 청람색 인레이로 제한했다. 생성기 계약(크기·팔레트·좌우/상하 대칭·55px 밖 가로 늘림 균일·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. 전수 스윕은 공유 작업 트리의 미커밋 `EstateBuildingsSelfCheck` 알파 기준 때문에 기존과 같은 4/216 FAIL(이번 UiAtlas는 PASS). 커밋 후 PNG를 실제 원본 크기로 확인해 노이즈 제거·대칭·깨끗한 중앙 채움 영역을 확인했다. 오너 Unity가 꺼져 있어 인게임 캡처는 생략. **다음: 남은 구형 독립 크롬 중 버튼 3상태를 파일 원본과 인게임 노출 우선순위로 진단하되 기존 변경과 겹치지 않는다.**

최신 바퀴(2026-08-29 01:30): **일반 HP 프레임 ox-alpha 화풍 통일**(`3d542ab2`) — UI·아트 미완료 재탐색에서 `ui/chrome/hp_frame.png`가 구형 261KB 회화식 자산으로 남아 좌우 검은 잔상·잘린 동물 문양이 보이고, 채택된 panel·portrait·boss HP 크롬과 재질이 갈리는 것을 원본 육안으로 확인했다. 신규 `gen_oxalpha_ui_chrome_hp_frame.py`로 원본 1194×206·RGBA와 GUID·소비 경로를 유지한 채 웜톤 5색 대칭 9-slice 프레임으로 교체하고 구판을 백업했다. 일반 체력바는 보스판보다 얇은 황동 테두리와 작은 좌우 표식만 써 위계를 낮췄다. 생성기 계약(크기·팔레트·좌우/상하 대칭·37px 밖 가로 늘림 균일·결정론) PASS, `UiAtlasSelfCheck` 배치 exit 0·최종 PASS. PNG를 실제 원본 크기로 확인해 잔상 제거·대칭·깨끗한 중앙 채움 영역을 확인했다. 전수 스윕은 공유 작업 트리의 미커밋 `EstateBuildingsSelfCheck` 알파 기준과 기존 보스 벽 배율 때문에 4/216 FAIL(이번 UiAtlas는 PASS). 오너 Unity가 꺼져 있어 인게임 캡처는 생략. **다음: 남은 구형 크롬 중 상시 노출 `xp_frame`을 우선 육안 진단하되 기존 변경과 겹치지 않는다.**

최신 바퀴(2026-08-28 21:20): **보스 HP 프레임 ox-alpha 화풍 통일**(`df4be602`) — UI·아트 미완료 재탐색에서 `ui/chrome/boss_hp_frame.png`의 양끝 문양에 검은 뭉침·잘린 잔상이 남고 채택된 ox-alpha 패널과 재질이 갈리는 것을 원본 육안으로 확인했다. 신규 `gen_oxalpha_ui_chrome_boss_hp_frame.py`로 원본 크기 1194×206·RGBA와 GUID·독립 크롬 소비 경로를 유지한 채 웜톤 5색 대칭 9-slice 프레임으로 교체하고 구판을 백업했다. 생성기 계약(크기·팔레트·좌우/상하 대칭·37px 밖 가로 늘림 균일) PASS, `UiAtlasSelfCheck` 배치 exit 0·PASS. 커밋 후 PNG를 실제 확인해 잔상 제거·대칭·깨끗한 중앙 채움 영역을 확인했다. 전수 스윕은 타 작업의 미커밋 `EstateBuildingsSelfCheck` 알파 기준 때문에 4/216 FAIL(이번 UiAtlas는 PASS). 오너 Unity가 꺼져 있어 인게임 캡처는 생략. **다음: UI·아트 미완료 1건을 새로 발굴하되 기존 변경과 겹치지 않는다.**



최종 갱신: 2026-08-27 (보드) · 루프 STOP 유지 · last_test_report HEAD PASS · `0faaa969` · 코드=ox-alpha 출력 검증(개발) · 아트=ui/chrome/portrait_frame (panel `e8f282bf` 닫음) · UI=V4 오버레이 dim (탐험반경 `1425b4e4` 닫음). 직전: **필드 마을 우물 village_well_0 ox-alpha 256 반입 — 화풍 통일**(`742c301f`) — 오너 「게임 ui 시각화·이미지화·퀄리티 업」. 영지 8동·마을 집·헛간은 ox-alpha로 통일됐으나 우물(village_well_0)은 아직 나노바나나 1508×1852·3.2MB라 톤·해상도가 따로 놀았다. `art/gen_oxalpha_village_well.py` 신설 — 같은 웜톤 팔레트의 석조 우물(정면 원통 벽돌통+타원 림+물+좌우 목재 기둥+완만한 박공 널지붕+도르래 빔·크랭크 손잡이+매달린 두레박·밧줄)로 교체(3.2MB→약 3KB). **크기 무영향(실측, FieldDecor.cs:336, ppu=pxHeight/TargetUnits, village_well_0=1.60)** — 해상도 스왑이 스케일-세이프, .cs 변경 0. §6-A 준수(바닥 큰 원/고리/글로우 없음 — 우물통 자체가 실체인 정면 원통, 그림자는 밑 얇은 띠뿐). 검증: 생성 PNG 육안(석조 우물·영지/집/헛간 톤 일치, 투명 50268·불투명 14941) · game_asset_names 우물 관련 누락/낡음/meta 불일치 0(미사용 190·prop_scale 두 벌은 기존 노이즈) · .meta GUID 무변경 · 소비 경로 무변경. 배치·인게임 육안은 이 세션 유니티 권한 게이트로 미실행(오너 에디터 살아난 다음 바퀴 보완). 백업 art/bak_nanobanana_village_well_0.png. 아래는 직전 마을 헛간 바퀴 이력. · **필드 마을 헛간 village_barn_0 ox-alpha 256 반입 — 화풍 통일**(`7afaa832`) — 오너 「게임 ui 시각화·이미지화·퀄리티 업」. 영지 8동·마을 집은 ox-alpha로 통일됐으나 헛간(village_barn_0)은 아직 나노바나나 1838×1536·3.3MB라 톤·해상도가 따로 놀았다. `art/gen_oxalpha_village_barn.py` 신설 — 같은 웜톤 팔레트의 목조 헛간(석조 기초 띠+넓은 세로 널판 목벽+완만·넓은 박공 널지붕+중앙 대형 아치 쌍여닫이문 X자 가새+상단 박공 건초 다락문+호이스트 빔+좌우 창)으로 교체(3.3MB→2.8KB). 집보다 넓고 낮은 실루엣으로 헛간으로 읽힘. **크기 무영향(실측, FieldDecor.cs:336, ppu=pxHeight/TargetUnits, village_barn_0=4.00)** — 해상도 스왑이 스케일-세이프, .cs 변경 0. 검증: 생성 PNG 육안(넓은 헛간·영지/마을 집 톤 일치, 투명 43022·불투명 21331) · game_asset_names 누락/낡음/meta 불일치 0(미사용 190·prop_scale 두 벌은 기존 노이즈) · .meta GUID 무변경(49c35e3d…) · 소비 경로 무변경. 배치·인게임 육안은 이 세션 유니티 권한 게이트로 미실행(오너 에디터 살아난 다음 바퀴 보완). 백업 art/bak_nanobanana_village_barn_0.png. 아래는 직전 마을 집 바퀴 이력. · **필드 마을 집 village_house_0 ox-alpha 256 반입 — 화풍 통일**(`2f982e2c`) — 오너 「게임 ui 시각화·이미지화·퀄리티 업」. 영지 8동은 ox-alpha로 통일됐으나 필드 마을 프랍은 아직 나노바나나 2000px라 톤·해상도가 따로 놀았다. village_house_0을 같은 팔레트의 하프팀버 오두막(하단 석벽+상단 목조 뼈대+박공 널지붕+아치문+덧창+굴뚝)으로 교체(2.0MB→3KB). **핵심 발견: 프랍 크기는 ppu=pxHeight/TargetUnits로 목표 유닛에서 역산되므로 해상도 스왑이 스케일-세이프** — 이전 STATUS의 「ScaleFor 하드코딩 → 재튜닝 필요」 주의는 오류였다(그 심볼 없음). 검증: 생성 PNG 육안(오두막으로 명확·영지 톤 일치) · game_asset_names 누락/낡음/meta 불일치 0 · .meta GUID 무변경 · 소비 경로 무변경. 배치·인게임 육안은 이 세션 유니티 권한 게이트로 미실행(오너 에디터 살아난 다음 바퀴 보완). 백업 art/bak_nanobanana_village_house_0.png. 아래는 직전 부지 확장 바퀴 이력. · **§18-12 부지 확장 8×8→16×16(해금 20/50/80층) — 소비처0 해소**(`e516759c`) — 원장 재스캔(큐 2)의 남은 소비처0 후보 2건 중 하나를 닫음: 원장 §18-12 「격자 초기 8×8 → 부지 확장 최대 16×16, 해금 20/50/80층」이 코드에선 `EstateGrid.Size = const 8` 고정이라 소비처 0곳이었다. 확립된 소비처0 해소 패턴 준용: **BalanceConfig 3필드**(부지확장해금층1/2/3=20/50/80·§21-3 폴백) + **신규 EstateExpansion**(탑 최고 층 GameState.TowerFloor[단조]로 해금 단계 판정→격자 폭 8/11/13/16, 중간 11·13은 원장 미확정 명시, Gates가 0 이하·비단조 수치면 원장 기본 통째 폴백, QA_NO_ESTATE_EXPAND 네거티브) + **EstateGrid 소비처**(`Size`가 `EstateExpansion.CurrentSize`를 읽어 논리 격자 폭이 층에 따라 커짐 — 저장·인덱싱은 최대 폭 Stride(16) 고정 Idx 헬퍼로 재작성해 논리 폭이 커져도 저장 배치 불변, 옛 8×8 세이브는 16-stride로 이관, 허브 폴백은 해금된 논리 격자 내에서만, ResetForTest가 확장을 초기 8로 고정해 기존 영지 SelfCheck 8동을 보호) + **신규 EstateExpansionSelfCheck**(층별 8/11/13/16·실소비 InBounds 확장·ForceConfig 해금층·§21-3 폴백·QA_NO 네거티브·소스 계약)→GameSweep 등록. 외부 파일은 `EstateGrid.Size`를 읽기만 해 재스트라이드 blast radius가 EstateGrid.cs 내부로 국한됨(grep 확인). **컴파일 PASS(game_compile_check·네거티브 self-test 통과)**. 유니티 배치·MCP는 이 세션 권한 게이트로 막힘(Unity 바이너리 실행 승인 필요·MCP 미승인) → 소스 계약 grep 대체 검증. **배치 SelfCheck·전수 스윕(EstateExpansion + 기존 영지 8동 SelfCheck 회귀)·눈확인(격자 확대 렌더)은 오너 에디터 살아난 다음 바퀴 보완**(PROMPT ③). **다음: 큐 2 남은 소비처0 후보는 §18-13 디버프 중첩 최대 2개 별 1건(WorldStar 다중 별 겹침 모델 미존재라 클램프 대상 부재·중간 리스크) — 또는 큐 1(UI·아트 폴리싱).** 아래는 직전 동시 건설 슬롯 바퀴 이력. · **§18-12 동시 건설 슬롯(2개=본성 라인 1+그 외 1) 강제 — 소비처0 해소**(`97b8bd38`) — 원장 재스캔(큐 2)으로 발굴: 원장 §18-12가 ✅ 확정인데 코드는 반대로 칸마다 무제한 병렬 공사였다(EstateBuild 옛 주석 「칸마다 병렬 공사 OK」). 확립된 소비처0 해소 패턴(Reader+BalanceConfig 필드+단일 관문 소비+QA_NO 네거티브+SelfCheck 스윕 등록) 준용: `BalanceConfig.동시건설슬롯=2` 신설(0 이하 기본 폴백 §21-3) · 신규 `BuildSlots`(Cap()=슬롯 상한·OtherSlots()=Cap-1=본성 전용 슬롯 제외) · `EstateBuild.ActiveOtherBuilds()`(본성 뺀 공사 중 수)+`WhyCannotUpgrade`(비-본성)에 슬롯 관문 삽입(그 외 슬롯 차면 「동시 건설 슬롯이 찼다(§18-12)」로 착공 차단, 본성은 전용 슬롯이라 무제한) · 신규 `BuildSlotsSelfCheck`(수치 계약+강제+네거티브 병렬+소스 계약)→GameSweep 레지스트리 등록. 게임플레이 판정·W3Party 무접촉. **컴파일 PASS(game_compile_check)**. 유니티 배치·MCP는 이 세션 권한 게이트로 막힘 → 소스 계약 grep(필드 authored·BuildSlots 소비·EstateBuild 관문·레지스트리 등록) 대체 검증, 배치 SelfCheck·전수 스윕·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **다음: STATUS 큐 2(원장 재스캔) — 재스캔이 찾은 남은 소비처0 후보 2건: §18-12 부지 확장 16×16(EstateGrid.Size=8 하드코딩·배열 리사이즈+20/50/80층 해금 게이트라 중간 리스크)·§18-13 디버프 중첩 최대 2개 별(WorldStar 다중 별 겹침 모델 미존재라 클램프 대상이 없음·중간 리스크).** 아래는 직전 아트 바퀴 이력. · **영지 공용 공사판(scaffold) ox-alpha 256 코드합성 반입**(`9fbd34d3`) — INBOX 「게임 ui 퀄리티 업」(오너 2026-08-26). 영지 8동 _0/_1/_2가 전부 ox-alpha 256으로 통일된 뒤, 건물이 공사 중일 때 EstateYard.DrawScaffoldIfBusy가 72% 알파로 겹치는 공용 공사판만 옛 나노바나나(1864×2028·4.25MB)로 남아 튀었다. 신규 `art/gen_oxalpha_scaffold.py`로 같은 목재 웜톤 「비계 프레임」 합성(좌우 기둥+발판 2단+X 가새+자재 더미+상단 공사 깃발, 가운데 비워 밑 건물 비침·§6-A 준수) → `estate_scaffold_0.png` 반입(256×256·2232B, .meta/GUID·소비경로 무변경, 옛판 `bak_nanobanana_estate_scaffold_0.png` 보존). EstateBuildingsSelfCheck에 공사판 검증(256·프레임 오버레이 알파 투명>절반·불투명>0·HasDedicated·ScaffoldOf 소스 계약). **PIL 실측 256×256·투명 56840·불투명 8696·png/meta 69/69·keep_0 위 72% 합성 육안 확인(건물 비침+공사 깃발)**. 유니티 배치는 이 세션 권한 게이트로 막힘 → 소스 계약·PIL 대체 검증, 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **다음: 영지 아트는 8동 _0/_1/_2 + 공사판 전부 ox-alpha 완료 — 다음 바퀴는 STATUS 큐 2(원장 소비처 재스캔) 또는 큐 1의 마을 장식 프랍(village_house/tree/barn/well 등 11종이 아직 나노바나나 2000px, 단 FieldDecor.ScaleFor가 원본 크기 가정이라 256 전환 시 스케일 재튜닝 필요 — 주의).** 아래는 직전 auction 티어 바퀴 이력. · **영지 경매장(auction) 레벨 티어 _1/_2 ox-alpha 파생 반입 — 티어 8동째·마지막**(`de37398e`) — INBOX 「게임 ui 퀄리티 업」(오너 2026-08-26)·「티어 변형은 채택 확정 후 후속·한 바퀴 한 건물」(2026-08-25). 순서 mine→warehouse→barracks→smith→mausoleum→tower 다음 auction으로 **영지 8동 티어 세대 전부 ox-alpha 통일 완료(미교체 0동)**. 경매장 _0은 ox-alpha 채택(4397B)됐으나 _1/_2가 옛 나노바나나(4.4MB·~2000px)로 남아 PropOf(lvl 5-9→_1·10-13→_2) 레벨업에서 튀었다. `gen_oxalpha_estate_tiers.py auction`로 채택 _0에서 같은 256·같은 팔레트+지붕 장식만 파생 → `estate_auction_{1,2}.png` 반입(4.7KB·4.9KB, .meta/GUID·소비경로 무변경, 옛판 `bak_nanobanana_estate_auction_{1,2}.png` 보존). tierStems에 estate_auction 추가 + Auction TierName 계약 검증. **PIL 실측 256×256·불투명 단조 16886≤17208≤17495·png/meta 69/69 확인**. 유니티 배치는 이 세션 권한 게이트(Unity 바이너리가 허용 작업디렉터리 밖)로 막힘 → 소스 계약 대체 검증, 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **다음: 영지 티어 아트는 8동 전부 완료 — 다음 바퀴는 STATUS 큐 2(원장 소비처 재스캔) 또는 큐 1(UI 폴리싱).** 아래는 직전 tower 티어 바퀴 이력. · **영지 탑(tower) 레벨 티어 _1/_2 ox-alpha 파생 반입 — 티어 6동째**(`972792ba`) — INBOX 「티어 변형은 채택 확정 후 후속·한 바퀴 한 건물」(오너 2026-08-25). 순서 mine→warehouse→barracks→smith→mausoleum 다음 tower. 탑 _0은 ox-alpha 채택(3881B)됐으나 _1/_2가 옛 나노바나나(1640×2048·3.1MB·1600×2032·3.2MB)로 남아 PropOf(Arrow/Magic·lvl 5-9→_1·10-13→_2) 레벨업에서 튀었다. `gen_oxalpha_estate_tiers.py tower`로 채택 _0에서 같은 256·같은 팔레트+지붕 장식(첨탑·페넌트)만 파생 → `estate_tower_{1,2}.png` 반입(4.1KB·4.4KB, .meta/GUID·PropOf 소비경로 무변경, 옛판 `bak_nanobanana_estate_tower_{1,2}.png` 보존). **tierStems에 estate_tower 한 줄 추가** + Tower(Arrow) TierName 계약 검증. **PIL 실측 256×256·불투명 단조 9419≤9700≤9953 확인**. 유니티 배치는 이 세션 권한 게이트로 막힘 → 소스 계약 대체 검증, 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **다음: 남은 티어 1동(auction)만 닫으면 미교체 0동 = 영지 8동 티어 전부 ox-alpha 완료 — 한 바퀴 auction, 또는 STATUS 큐 2(원장 소비처 재스캔).** 아래는 직전 mausoleum 티어 바퀴 이력. · **영지 영묘(mausoleum) 레벨 티어 _1/_2 ox-alpha 파생 반입 — 티어 5동째**(`5599d437`) — INBOX 「티어 변형은 채택 확정 후 후속·한 바퀴 한 건물」(오너 2026-08-25). 순서 mine→warehouse→barracks→smith 다음 mausoleum. 영묘 _0은 ox-alpha 채택(3707B)됐으나 _1/_2가 옛 나노바나나(3.4MB·3.6MB·~2000px)로 남아 PropOf(lvl 5-9→_1·10-13→_2) 레벨업에서 딴 화풍·초대형으로 튀었다. `gen_oxalpha_estate_tiers.py mausoleum`로 채택 _0에서 같은 256·같은 팔레트+지붕 장식(첨탑·페넌트)만 파생 → `estate_mausoleum_{1,2}.png` 반입(4.0KB·4.3KB, .meta/GUID·PropOf 소비경로 무변경, 옛판 `bak_nanobanana_estate_mausoleum_{1,2}.png` 보존). **EstateBuildingsSelfCheck tierStems에 estate_mausoleum 한 줄 추가**(일반화 루프) + Mausoleum TierName 계약 검증. **PIL 실측 256×256·불투명 단조 11724≤12046≤12337 확인**. 유니티 배치는 이 세션 권한 게이트로 막힘 → 소스 계약(파일명·.meta 무변경·tierStems 일반화·TierName) 대체 검증, 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **같은 바퀴에 세 바퀴 미룬 승격 부채도 해소** — `EstateBuildingsSelfCheck`에 **전 8동 티어 세대 일괄 화풍 일관성 스캔** 신설(`3abb9a0c`, PROPOSALS 06:40→07:30 최상 승격): 각 건물 _0/_1/_2의 256×256 여부·파일크기 편차 3배 이내를 판정해 INFO(일관)/PEND(미교체)+미교체 N동 요약을 로그로 남긴다. 미교체는 `_fail`이 아니라 로컬 `_pending`으로만 세어 회귀 스위트를 오판시키지 않는다. 현 실측(PIL): tower·auction 2동만 나노바나나 티어 잔존(1640/2018px·~3MB) → 스캔이 이 2동을 PEND로 자동 가시화. **다음: 남은 티어 2동(tower·auction)만 닫으면 미교체 0동 = 영지 8동 티어 전부 ox-alpha 완료 — 한 바퀴 한 건물(tower→auction), 또는 STATUS 큐 2(원장 소비처 재스캔).** 아래는 직전 smith 티어 바퀴 이력. · **영지 대장간(smith) 레벨 티어 _1/_2 ox-alpha 파생 반입 — 티어 4동째**(`c4489be7`) — INBOX 「티어 변형은 채택 확정 후 후속·한 바퀴 한 건물」(오너 2026-08-25). 순서 mine→warehouse→barracks 다음 smith. 대장간 _0은 ox-alpha 채택(3571B)됐으나 _1/_2가 옛 나노바나나(4.5MB·4.4MB)로 남아 PropOf(lvl 5-9→_1·10-13→_2) 레벨업에서 딴 화풍·초대형으로 튀었다. `gen_oxalpha_estate_tiers.py smith`로 채택 _0에서 같은 256·같은 팔레트+지붕 장식(첨탑·페넌트)만 파생 → `estate_smith_{1,2}.png` 반입(3.9KB·4.1KB, .meta/GUID·PropOf 소비경로 무변경, 옛판 `bak_nanobanana_estate_smith_{1,2}.png` 보존). **EstateBuildingsSelfCheck tierStems에 estate_smith 한 줄 추가**(일반화 루프) + Smith TierName 계약 검증. **PIL 실측 256×256·불투명 단조 11992≤12326≤12621 확인**. 유니티 배치는 이 세션 권한 게이트로 막힘 → 소스 계약(파일명·.meta 무변경·tierStems 일반화·TierName) 대체 검증, 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **다음: 남은 티어 3동(mausoleum·tower·auction) 후속 — 한 바퀴 한 건물, 또는 STATUS 큐 2(원장 소비처 재스캔).** 아래는 직전 barracks 티어 바퀴 이력. · **영지 병영(barracks) 레벨 티어 _1/_2 ox-alpha 파생 반입 — 티어 3동째**(`db2ce8f8`) — INBOX 「티어 변형은 채택 확정 후 후속·한 바퀴 한 건물」(오너 2026-08-25). 직전 창고 티어를 닫았고 이번은 병영(순서: mine→warehouse→barracks). 병영 _0은 ox-alpha 채택(4233B)됐으나 _1/_2가 옛 나노바나나(3.8MB·4.3MB)로 남아 PropOf(lvl 5-9→_1·10-13→_2) 레벨업에서 딴 화풍·초대형으로 튀었다. `gen_oxalpha_estate_tiers.py barracks`로 채택 _0에서 같은 256·같은 팔레트+지붕 장식(첨탑·페넌트)만 파생 → `estate_barracks_{1,2}.png` 반입(4.5KB·4.8KB, .meta/GUID·PropOf 소비경로 무변경, 옛판 `bak_nanobanana_estate_barracks_{1,2}.png` 보존). **EstateBuildingsSelfCheck tierStems에 estate_barracks 한 줄 추가**(일반화 루프) + Barracks TierName 계약 검증. **PIL 실측 256×256·불투명 단조 15905≤16192≤16470 확인**. 유니티 배치는 이 세션 권한 게이트로 막힘 → 소스 계약(파일명·.meta 무변경·SelfCheck tierStems 일반화·TierName) 대체 검증, 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). ※커밋 제목의 「7동 중 4」는 오기(실제 티어 3동째). **다음: 남은 티어 4동(smith·mausoleum·tower·auction) 후속 — 한 바퀴 한 건물, 또는 STATUS 큐 2(원장 소비처 재스캔).** 아래는 직전 warehouse 티어 바퀴 이력. · **영지 창고(warehouse) 레벨 티어 _1/_2 ox-alpha 파생 반입 — 7동 중 2**(`315c0048`) — INBOX 「티어 변형은 채택 확정 후 후속·한 바퀴 한 건물」(오너 2026-08-25). 직전 바퀴가 mine 티어를 닫았고 이번은 warehouse. 창고 _0은 ox-alpha 채택(4931B)됐으나 _1/_2가 옛 나노바나나(4.2MB·4.4MB)로 남아 PropOf(lvl 5-9→_1·10-13→_2) 레벨업에서 딴 화풍·초대형으로 튀었다. `gen_oxalpha_estate_tiers.py warehouse`로 채택 _0에서 같은 256·같은 팔레트+지붕 장식(첨탑·페넌트)만 파생 → `estate_warehouse_{1,2}.png` 반입(5.2KB·5.5KB, .meta/GUID·PropOf 소비경로 무변경, 옛판 `bak_nanobanana_estate_warehouse_{1,2}.png` 보존). **EstateBuildingsSelfCheck tierStems에 estate_warehouse 한 줄 추가**(가드레일 「같은 로직 여러 곳」 — 일반화 루프라 문자열만) + Warehouse TierName 계약 검증. **PIL 실측 256×256·불투명 단조 19208≤19489≤19773 확인**. 유니티 배치·MCP는 이 세션 권한 게이트로 막힘(mine·IsElite 바퀴 재현) → 소스 계약(파일명·.meta 무변경·SelfCheck tierStems 일반화·TierName) 대체 검증, 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **다음: 나머지 5동 티어(barracks·smith·mausoleum·tower·auction) 후속 — 한 바퀴 한 건물, 또는 STATUS 큐 2(원장 소비처 재스캔).** 아래는 직전 mine 티어 바퀴 이력. · **영지 광산(mine) 레벨 티어 _1/_2 ox-alpha 파생 반입 — 7동 중 1**(`a5311993`) — INBOX 「티어 변형은 채택 확정 후 후속·한 바퀴 한 건물」(오너 2026-08-25). 직전 바퀴가 keep 티어를 닫았고 이번은 mine. 광산 _0은 ox-alpha 채택됐으나 _1/_2가 옛 나노바나나(3.9MB·4.1MB)로 남아 PropOf(lvl 5-9→_1·10-13→_2) 레벨업에서 딴 화풍·초대형으로 튀었다. `gen_oxalpha_estate_tiers.py mine`으로 채택 _0에서 같은 256·같은 팔레트+지붕 장식(첨탑·페넌트)만 파생 → `estate_mine_{1,2}.png` 반입(6.1KB·6.3KB, .meta/GUID·PropOf 소비경로 무변경, 옛판 `bak_nanobanana_estate_mine_{1,2}.png` 보존). **EstateBuildingsSelfCheck 티어 검증을 keep 하드코딩 → 교체완료 건물 목록(tierStems) 루프로 일반화**(가드레일 「같은 로직 여러 곳」 — 다음 건물은 문자열 한 줄 추가) + Mine TierName 계약 검증. **PIL 3-up 육안 _0→_1→_2 첨탑·깃발 점증·팔레트 일치, 불투명 단조 14644≤14966≤15168 확인**. 유니티 배치·MCP는 이 세션 권한 게이트로 막힘(IsElite 바퀴 재현) → 소스 계약(파일명·.meta 무변경·SelfCheck 소비 일반화) 대체 검증, 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **다음: 나머지 6동 티어(warehouse·barracks·smith·mausoleum·tower·auction) 후속 — 한 바퀴 한 건물, 또는 STATUS 큐 2(원장 소비처 재스캔).** 아래는 직전 keep 티어 바퀴 이력. · **영지 본성 레벨 티어 _1/_2 ox-alpha 파생 세트 — 화풍·크기 튐 수리**(`a5341c67`) — INBOX 「게임 ui 퀄리티 업」(오너 2026-08-26 21:55)+「티어 변형은 채택 확정 후 후속·한 바퀴 한 건물」. 영지 8동 _0은 ox-alpha 코드합성(256·~5KB)으로 채택됐으나 레벨 티어 _1/_2는 옛 나노바나나(keep 1989×1931·3.8MB)가 남아 PropOf(lvl 5-9→_1·10-13→_2)가 레벨업에서 딴 화풍·초대형으로 튀었다. **신규 `art/gen_oxalpha_estate_tiers.py`**(채택 _0 베이스에서 같은 256·같은 팔레트로 지붕 장식만 얹어 파생 — 톤 자동 일치, §6-A 준수: 바닥 원·고리·글로우 없음, 티어는 첨탑 높이·페넌트 수로만 읽힘) + `estate_keep_{1,2}.png` 반입(6KB, .meta/GUID·소비경로 무변경, 옛판 `bak_nanobanana_estate_keep_{1,2}.png` 보존) + EstateBuildingsSelfCheck 티어 14건 신설. **배치 PASS 87/0 · 전수 스윕 200/200 · Coverage 200=200 · PIL 3-up 육안 점증 확인**(유니티 MCP 인게임 눈확인은 다음 바퀴 보완). 아래는 직전 IsElite 바퀴 이력. · **정예 분류 술어 IsElite 추출 — kind 5(돌진형) 정예 오분류 수리**(`f800aa1c`) — 4바퀴 미룬 부채(PROPOSALS 03:00·03:30·04:00·04:30) 확정 처리. 원장 §10-2 정예 판정이 오래 `>= 3` 번호 관용구를 써 kind 5(돌진형=잡몹, 정예 3·4 **뒤에** 추가돼 번호가 3 이상)가 정예 HP(90 vs 26)·EliteScale·필드 정예 드랍·정예 사망 FX를 잘못 받았다(필드 약 25%가 과대 HP·크기·드랍 과다). **신규 `public static bool W3Party.IsElite(int kind)`**(3·4·6·7·8·9 true·0·1·2·5 false — 번호 비교가 아니라 명시 집합, 새 정예 종 추가 시 한 곳만 수정) + 9개 호출부(스폰 HP·스폰 스케일·드랍 훅·PushReward 문구·색·FX 블록·FX 사망 조건·크기·힐 클램프)가 술어 재사용(가드레일 「같은 로직 여러 곳」). **신규 EliteClassifySelfCheck**(술어 분류 12건 + 소스 계약: 관용구 0잔존·9치환 소비·정의 존재, GameSweep 레지스트리 등록). **유니티 배치·MCP 이 세션 권한 게이트로 막힘**(PROPOSALS 01:35·01:45 재현) → 소스 계약 grep로 9치환·관용구 0잔존·레지스트리 등록 대체 검증, 배치 SelfCheck·전수 스윕·돌진형 HP(90→26) 밸런스 재측정·눈확인은 오너 에디터 살아난 다음 바퀴 보완(PROMPT ③). **다음: STATUS 큐 1(UI·아트 상시 폴리싱)·2(원장 소비처 재스캔) 또는 회의 채택안.** 아래는 직전 처형자 바퀴 이력. · ~~정예 처형자(§10-2) 특수능력 구현 — 큐#3 4종 중 넷째(마지막) 종 · 큐#3 완결~~(`a3195672`) — 원장 §10-2(✅ 오너 결정 2026-08-13) 「딜 거울: 후열(힐·버퍼)로 돌진해 폭딜, 진로 차단·선처치가 정답」을 W3Party 실전투에 배선. **신규 `EliteExecutioner.cs`**(순수 배율 DamageMul — 명중원이 처형자면 폭딜·아니면 1, `QA_NO_ELITE_EXECUTIONER`면 폭딜 배율 1로 원복) + **BalanceConfig 2필드**(처형자폭딜배율 3.0·처형자돌진속도배율 0.95, 원장 「후열 돌진 폭딜」만 확정·수치 미확정 Tooltip 명시). **W3Party**: kind 9 신설 — 후열 저격(PickBackline)을 원거리형(kind 2)과 공유하되 탱 도발이 그 안에서 하드 락으로 진로를 끊음(딜 거울의 정답=진로 차단), 근접 명중 관문(TickMobs)이 `_execBurst`를 곱하고 이동 spd가 `_execRush`(정예 중 최속), RefreshExecutioners가 TickMobs 전 스냅(핫패스), 스폰 정예 10%를 셋째 바퀴 3/2/2/2/1에서 **2/2/2/1/2/1**(치유·소환·저주·처형·수호·군단)로 재분할(`? 8 :`·`? 6 : 7` 보존 → 형제 SelfCheck 회귀 방지, 총 비율 90~100 불변 → 측정 무흔들), MobKindBerserker 실루엣+case 9 핏빛 진홍 전용 색조(바닥 링·초승달 금지 §6-A), 보스 소환 몹(kind 1 고정) 무영향. **신규 EliteExecutionerSelfCheck**(스윕 등록). **컴파일 PASS(0건)**. **유니티 배치·MCP는 이 세션 권한 게이트로 막혀 배치 SelfCheck·전수 스윕·눈확인은 오너 에디터 살아난 다음 바퀴로 보완**(PROMPT ③). **다음: 큐#3(정예 거울 6종) 완결 — 다음 바퀴는 STATUS 큐 1·2번(UI·아트 상시 폴리싱 / 원장 소비처 감사) 또는 회의 채택안으로 이동.** 아래는 직전 저주술사 바퀴 이력. · ~~정예 저주술사(§10-2) 특수능력 구현 — 큐#3 4종 중 셋째 종~~(`2da37b37`) — 원장 §10-2(✅ 오너 결정 2026-08-13) 「디버퍼 거울: 오라 안 파티 회복량·이속 감소, 사거리 밖 이탈 후 처치」를 W3Party 실전투에 배선. **신규 `EliteCurse.cs`**(순수 배율 NearbyMul — 오라 안 대상=mul＜1·밖=1, `QA_NO_ELITE_CURSE`면 회복·이속 배율 전부 1) + **BalanceConfig 3필드**(저주술사오라반경 4.0·회복배율 0.7·이속배율 0.85, INBOX 제안 시작값 회복 −30%·이속 −15%, 수치 미확정 Tooltip 명시). **W3Party**: 회복 단일 관문 Heal이 회복 배율을 곱하고(정적 관문이라 캐시도 static `_curseHeal`), 파티 이동 단일 경로 TickParty step이 이속 배율을 곱한다(`_curseMove`). RefreshCursers가 TickParty 전 위치 스냅(핫패스), 스폰 정예 10%를 3/3/2/2에서 **3/2/2/2/1**(치유·소환·저주·수호·군단)로 재분할(`? 6 : 7` 보존 → 군단·수호 스폰 SelfCheck 회귀 방지, 총 비율 90~100 불변 → 측정 무흔들), MobKindShaman 실루엣+case 8 병색 독초록 전용 색조+개체 검보라 그을음(바닥 링 금지 §6-A), 보스 소환 몹(kind 1 고정) 무영향. **신규 EliteCurseSelfCheck**(스윕 등록). **컴파일 PASS(0건)**. **유니티 배치·MCP는 이 세션 권한 게이트로 막혀 배치 SelfCheck·전수 스윕·눈확인은 오너 에디터 살아난 다음 바퀴로 보완**(PROMPT ③). **다음: 큐#3 넷째(마지막) 종 처형자(딜 거울 — 후열 돌진 폭딜).** 아래는 직전 군단장 바퀴 이력. · ~~정예 군단장(§10-2) 특수능력 구현 — 큐#3 4종 중 둘째 종~~(`7d4d74f8`) — 원장 §10-2(✅ 오너 결정 2026-08-13) 「군단장=버퍼 거울: 주변 몹 공속·이속 증가, 방치하면 물량 압력 급상승, 처치 우선순위 2순위」를 W3Party 실전투에 배선. **신규 `EliteLegion.cs`**(순수 배율 NearbyMul — 군단장 자신=1·오라 안 잡몹=mul·밖=1, `QA_NO_ELITE_LEGION`이면 공속·이속 배율 전부 1) + **BalanceConfig 3필드**(군단장오라반경 4.0·주변공속배율 1.5·주변이속배율 1.3, 수치 미확정 Tooltip 명시). **W3Party**: 스폰 정예 10%를 수호자 바퀴의 4/3/3에서 **3/3/2/2**(치유·소환·수호자·군단장)로 재분할(총 비율 90~100 불변 → 측정 무흔들), 이속 버프=이동 spd 단일 경로 곱·공속 버프=근접/원거리 공격 쿨다운을 배율로 나눔, RefreshCommanders가 TickMobs 전 위치 스냅(핫패스), MobKindBard 실루엣+case 7 순금빛 전용 색조+개체 결집 광채(바닥 링 금지 §6-A), 보스전 미스폰 무영향. **신규 EliteLegionSelfCheck**(스윕 등록). 스폰 테이블 변경(`? 4 : 6`→`? 6 : 7`)에 맞춰 EliteGuardianSelfCheck 스폰 검증 문자열 보강. **컴파일 PASS(0건)**. **유니티 배치·MCP는 이 세션 권한 게이트로 막혀 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴로 보완**(PROMPT ③). **다음: 큐#3 셋째 종 저주술사(디버퍼 거울 — 플레이어 회복량·이속 감소).** 아래는 직전 수호자 바퀴 이력. · ~~정예 수호자(§10-2) 특수능력 구현 — 큐#3 4종 중 첫 종~~(`715b01e3`) — 원장 §10-2(✅ 오너 결정 2026-08-13) 「탱 거울: 주변 잡몹 피해 감소 오라·자신 고방어 → 오라 밖 유인·선처치가 정답」을 W3Party 실전투에 배선. **신규 `EliteGuardian.cs`**(순수 배율 함수 Multiplier — 수호자 자신=고방어·오라 안 잡몹=감소·밖=1, `QA_NO_ELITE_GUARDIAN`이면 전부 1) + **BalanceConfig 3필드**(수호자오라반경 3.5·주변피해배율 0.5·자체피해배율 0.4, 원장은 「감소 오라·고방어」만 확정·수치 미확정을 Tooltip에 명시). **W3Party**: 스폰 정예 10%를 3/4/6(치유·소환·수호자) 4/3/3로 분할(총 비율 불변 → 측정 단일 경로 무흔들), 소비처=단일 피해 관문 DamageMob이 배율 곱, RefreshGuardians가 파티 피해(TickParty) 전에 수호자 위치 스냅(핫패스 순수함수), MobKindGuardian 실루엣+강철빛 전용 색조+개체 광륜(바닥 링 금지 §6-A), 보스전엔 수호자 미스폰이라 무영향. **신규 EliteGuardianSelfCheck**(스윕 등록 — 배율 함수·설정 소비·네거티브·W3Party 소비 계약 검증). **컴파일 PASS(0건)**. **유니티 배치·MCP는 이 세션 권한 게이트로 막혀 배치 SelfCheck·눈확인은 오너 에디터 살아난 다음 바퀴로 보완**(PROMPT ③). **다음: 큐#3 둘째 종 군단장(버퍼 거울 — 주변 몹 공속·이속 증가).** 아래는 직전 아트 바퀴 이력. · ~~영지 경매장(auction) ox-alpha 반입~~(`7e5c3783`, INBOX 「영지가 밋밋함」 2026-08-25 밤·「게임 ui 좀더 시각화 퀄리티 업」 2026-08-26 21:55, 한 바퀴 한 건물) — `out_oxalpha_estate_auction.png`(256×256, 4397B)를 `Resources/props/estate_auction_0.png`로 반입, 기존 나노바나나 구판(1596×1569, 1.9MB)은 `art/bak_nanobanana_estate_auction_0.png` 보존. 파일명 동일 → .meta/GUID 무변경, 소비 경로 `EstateBuildings.PropOf`(Auction) 무변경 — 런타임 코드 변경 0. EstateBuildingsSelfCheck에 경매장 ox-alpha 검증 4건 신설(로드·파일·256·알파). 경매장은 천막·수레형이라 실체가 넓어 임계 1/8 여유. 검증: PIL 실측 256×256·불투명 26.03%(16713/65536)>임계 12.5%(8192). **유니티 배치·MCP 모두 이 세션 권한 게이트에 막혀 건너뜀**(PROMPT ③) — SelfCheck는 tower/mausoleum(57 PASS) 구조 동일 클론으로 대체 검증, 눈 확인은 오너 에디터 살아난 다음 바퀴에서 보완. **★ 영지 8종 A/B ox-alpha 반입 완료** — keep·mine·warehouse·barracks·smith·mausoleum·tower·auction 전부. 다음 아트 바퀴는 레벨 티어(`_1`/`_2`)·공사판 등 남은 영지 아트 품목 발굴, 또는 STATUS 큐 3번(정예 특수능력 4종)으로 이동.

> ⚠️ [2026-08-25 21:58 보드지킴이] 공유 인덱스의 `loop/board.py`·`loop/test_board.py`는 보드지킴이 커밋 `6b9699f4`(수함대 집계 수정+회귀검사) **이전** 스냅이다 — 이 상태로 맨몸 `git commit`하면 방금 닫힌 수정이 되돌려진다. board·test 두 파일은 커밋 전 작업 트리 기준으로 다시 `git add`해 해소할 것(board.py엔 인덱스에만 있는 cli_usage 변경분도 있으니 병합 확인).

루프 가동 중 — 실행기 체인(claude→grok→codex→opencode)에서 현재 opencode로 실랩 진행. 막힌 곳 해제: ①launchd 사용자 env `LOOP_AGENT=claude` 잔재가 loop/agent 파일 지정을 덮쓰던 것 제거 ②공급자 3종 만료(Claude 로그인·Grok/Codex 주간 소진)를 opencode 무료 실행기로 우회. 오너 위임 판정 완료(INBOX 2026-08-25 밤): ORDERS 3건 승인(P1부터 ②→③→① 순서) · §18-14 소환수 재소환 구현 승인 · G16/G17 출시 게이트 큐 등록 · G15 Steam Cloud/§6 오프라인 전투 정산/§14 동맹 쿨다운 보류. 오너가 터미널에서 `claude` 1회 로그인하면 체인이 자동으로 claude 우선 복귀. 사람 관문 더미 유지.





**정정 요약**: 직전 STATUS의 「AuctionHud·BagText 리크」는 오판으로 정정 — 유일 호출부가 QA 전용 게이트 뒤라 일반 플레이 리크 없음(`4dc069f4`). UI 폴리싱 캠페인 7화면 마감. 소비처0 재스캔 결과 §18-14 소환수 재소환은 신규 시스템 필요 → 오너 위임 승인 완료(INBOX 2026-08-25 밤). 원문 전문은 아카이브 참조.



## 관문 부채 (루프 밖 · 사람/대화 세션)



> 원장 §22 운영규칙 3: 소비처0 루프는 로드맵이 아님. **관문 재편(오너 2026-08-26)**: V2·W2 손맛 축은 **철회**(자동전투 중심 장르라 판정하지 않는다) · V3는 **보류**(구현 밀도가 판정 가능 수준이 된 뒤) · 사람 직접 테스트는 **개발 막바지 일괄**. 관문 FAIL을 근거로 작업을 만들지 말고, 관문 상태를 근거로 작업을 멈추지도 마라 — 지금 우선순위는 원장 ✅ 확정 기능의 구현 밀도다(INBOX 2026-08-26 20:40~20:55).



| 관문 | 상태 | 담당 | 재개 트리거 |

|---|---|---|---|

| — | ~2026-08-25 | **반복 재검증 통합 기록** — proposals-triage(최대 103차)·lane-doc(96차)·keeper-warn-chip(89차) 등 동일 지시 반복 배정 재검증 누적. 전 행 판정: 선반영 확인·변경 없음·중복 병합 대상 없음. 건너뛰기 처방(`a27b3d2b`)의 TASKS.json 적용은 미해결 — 별도 추적 | 세부 이력 아카이브 참조 · 이 커밋 |

| — | 2026-08-24 | **UI 폴리싱 — 하단 도크 활성 탭 라벨 회색 결함 수정 + §10-3 선반영 판정** — 플레이모드 순회 실측(영지·탑·필드·월드맵·파티)에서 유일 결함 확정: 현재 화면 탭 라벨이 `GUI.enabled = !here` 비활성 알파로 회색 처리돼 위 금강조선과 정반대로 「막힌 탭」처럼 읽힘(비허브 파티 화면은 전 라벨 밝아 경로 입증). 처방: DrawAtlasButton 뒤 GUI.enabled 복구(아이콘·라벨 선명)·클릭 금지는 !here 가드 보존·네거티브 `QA_NO_DOCK_ACTIVE_BRIGHT`. §10-3 계열 상성은 `a7f82e6a` 선반영(`FamilyAdv` 1.3/0.7·SelfCheck·DungeonScreen 소비처 실측)으로 재구현 없이 닫음 | 컴파일 PASS(에러 0) · 플레이모드 A/B 실측 `output/qa/ashes-to-stars/dock_active_shots/`(before_field 회색 → after_field 흰색 크롭 육안) · 네거티브 neg_field(옛 흐림 재현) · 콘솔 에러 0 · `777eacab` |

| 단계1 관문② (5h 지루함) | **더미 관문 PASS** 9/10 (5h+숙제감≤2) | 더미 | 시드 20260823 · `v4_dummy_sim.py` · 실측 표본 오면 §22 재측정 |

| V4 영구삭제 수용성 | **경계 잠금**(2026-08-27 회의) `docs/plans/V4_LOOP_BOUNDARY.md` — 루프=LifeSystem/Memorial 로그·30분 가드·reason_enum, 즉시계속=일시정지 오버레이, 70%/24h=사람. 더미 PASS는 표본 전 | 보드 배정 | 루프 훅 **닫음**(`4edba3af` GrowthStartUnix 왕복). 오버레이=UI 칸. 70%/24h=사람. V4 수용성 PASS 아님. 실측 표본 오면 §21-1 재측정 |

| ~~V2 손맛~~ · V3 | **V2 철회**(오너 2026-08-26) · V3 **보류** — 더미 3/5(t03 기믹 1종 · t04 단축 미달)는 갚을 빚이 아니다 | — | V2는 손맛 축 자체를 판정하지 않는다. V3는 구현 밀도가 오른 뒤 사람 테스트와 함께 §21-1로 측정. t03·t04를 맞추는 작업을 큐에 올리지 마라 |

| ~~W2 (회피 기회)~~ | **철회**(오너 2026-08-26 "지금 재와별 같은 게임에는 손맛이 필요없지") | — | 더미 FAIL(지배전략·숙련보상 미달, 시드 20260825)은 **철회된 관문의 기록**일 뿐이다. 위협밀도·대시 손맛 폴리싱·회피 인지 판정 작업은 큐에서 제외 |



## 다음 할 일 (원장 §22 — 위에서부터 하나만)
1. **UI·아트 상시 폴리싱** — 직전 바퀴가 검증 트랙이던 교대 규칙은 이번 바퀴가 아트 트랙이라 해소. **선반영 인수 소멸 확인**: 19:29 바퀴 캐릭터창 접힘한계 스크롤은 대화 세션이 `f5115e72`로 이미 마감(InfoFoldLimit 필드화·배치 PASS 실측) — 재작업 금지. INBOX 캐릭터창 「선택 안 됨」 `952662fa` 닫음(MouseDown). 「정보보기 불편」스크롤 `f5115e72` 재작업 금지. 파티 로스터 `c8941f6f` 닫음. 필드 로스터 `caa5b84c` 닫음. 장비 링 `6de8dd05` 닫음. 가방 셀 `bcc04e45` 닫음. 가방 필터 `43f316e9` 닫음. 타이틀 직업 `77b56474` 닫음. 영지 회수 `cdfa4fbf` 닫음. 영지 격자 `54f61377` 닫음. 공사완료 `0f65dfea` 닫음. V4 오버레이 `5b4ac392` 닫음. 영지 허브 `336a89f3` 닫음. 영지 방어 도크 `bbc56b67` 닫음. CompactAction `f7466c8f` 닫음. DrawTabs `2117b359` 닫음. Row `e586d62b` 닫음. 하단 도크 `9b0ad0bd` 닫음. DrawCard `cea5611d` 닫음. VfxTest `04849c23` 닫음. 귀환바 `9154a2fc` 닫음. 스크롤바 `171aceee` 닫음. 격자글씨 `67cc2ec4` 닫음. 이름표 `deaf43b4` 닫음. 대장간숫자 `13f82614` 닫음. 별이름 `cf0e0e57` 닫음. 탐험필드 `923597e8` 닫음. FillCell `e3e6ce7d` 닫음. 탐험반경 `1425b4e4` 닫음. 다음 UI=V4 오버레이 dim(1px→atlas. 오버레이 로직 재작업 금지). HuntGoldLine `6b8208ea` 닫음(라이브 FormatCurrency 캠페인 끝, Old* 폴백 유지). **영지 아트 ox-alpha 8종 A/B 반입 진행 중(한 바퀴 한 건물)**: keep 완료(`866b444d`) · mine 완료(`fac0507c`) · warehouse 완료(`4a34361a`) · barracks 완료(`d9ed1ffa`) · smith 완료(`19a98bf3`) · mausoleum 완료(`aff0e7f0`) · tower 완료(`b7c87c05`) · auction 완료(`7e5c3783`) — **8종 전부 완료**. 어색하면 즉시 되돌림(백업 `art/bak_nanobanana_estate_*.png`). **레벨 티어 ox-alpha 파생 진행 중(한 바퀴 한 건물)**: keep _1/_2 완료(`a5341c67` — `gen_oxalpha_estate_tiers.py`로 _0에서 파생, 옛 나노바나나 3.8MB→6KB) · mine _1/_2 완료(`a5311993`) · warehouse _1/_2 완료(`315c0048`) · barracks·smith·mausoleum 완료 · tower _1/_2 완료(`972792ba`) · auction _1/_2 완료(`de37398e`) — **8동 _1/_2 전부 완료**. **공사판(scaffold) 완료(`9fbd34d3` — ox-alpha 256 비계 프레임, 옛 4.25MB→2.2KB)**. **필드 마을 프랍 ox-alpha 반입 진행 중(한 바퀴 한 프랍)**: village_house_0 완료(`2f982e2c` — 하프팀버 오두막, 2.0MB→3KB) · village_barn_0 완료(`7afaa832` — 목조 헛간, 3.3MB→2.8KB) · village_well_0 완료(`742c301f` — 석조 우물, 3.2MB→3KB). **스케일 재튜닝 불필요(실측 반증, FieldDecor.cs:336)**: 프랍 인게임 크기는 `ppu=pxHeight/TargetUnits(prop_scale.json)`으로 목표 유닛에서 역산되므로 원본 해상도(2000→256)를 바꿔도 월드 크기 불변 — 이전 STATUS의 「FieldDecor.ScaleFor 하드코딩」 주의는 오류(그런 심볼 자체가 없음). 남은 후보: village_cart/lamp/fence/haystack/tree + village_house_1/2 + field_tree_0~3(아직 나노바나나 ~2000px). 트리류는 유기 형태라 ox-alpha 코드합성이 오두막만큼 잘 읽힐지 육안 확인 필수. 「다른 게임만큼」완료 금지. 기존 닫음 나열은 아카이브 참조.
2. **기획서 ✅ · 소비처 0곳** — 원장 `GAME_DESIGN_ASHES_TO_STARS.md`를 훑어 ✅인데 grep 소비처가 0인 칸 **하나만** 닫는다. **재스캔 완료(2026-08-27) — 남은 소비처0 후보 0건: §18-13 디버프 중첩은 `952ddb40`로 닫음. 큐#1 필드 생존 요약 드랍 `8e0ffb84` 닫음. 큐#5 G3 픽스처 `792f36ab` 닫음(50F/25F 전멸은 막대 유지). AttachWithAwake `d01e6807` 닫음. V4 훅 `4edba3af` 닫음. png 실로드 `d30a135c` 닫음. SrcNoComments `061c14ca` 닫음. zip 실로드 `ef9cd488` 닫음. run_selfcheck.sh `2056cf7d` 닫음. 일일퀘 `81cbe1cf` 닫음. commit_guard 2단 `06da7bdd` 닫음. GameFullCheck 4바퀴 `841fda59` 닫음. last_test_report `0faaa969` 닫음. 다음 코드는 ox-alpha 출력 검증(회의 073515 채택 #3). 50F/25F 전멸 수치 금지. §18-12 부지 확장 8×8→16×16(해금 20/50/80층)은 `e516759c`로 닫음(EstateGrid.Size 런타임화·Stride 16 고정·EstateExpansion 리더·SelfCheck). 저리스크 단일 앵커(§18-12 동시 건설 슬롯)는 `97b8bd38`로 닫음.** §18-11 잡몹 피해(MobDmg `fc2499d8`)·§18-11 잡몹 HP(MobHp `6f1e1226`)·§18-11 잡몹 이동속도(MobSpeed `8c89e69b`)·§18-8 PvE 회복시간(PveRecover `cff20f97`)·§4 PvP 회복시간(PvpRecover `903a1cf7`)·§18-11 플레이어 이동속도(MoveSpd `be1882cd`)·§18-2 소각목표(BurnTarget `470f416c`)·§18-1 티어1시간당골드(GhAnchor `12e3e0b4`)·§18-1 티어배율(TierMul `9de769f8`)·§10-9 투사체상한(ProjCap `89e7136d`)·§10-9 소환수상한(SummonCap `bfff2789`)·§10-9 잡몹상한(PerfCap `31836976`)·§4 사망상한(DeathCap `f45cd729`)·§4 부활초소지상한(ReviveCap `9b50c0d7`)·§3 전투 스타일 정예우선타겟·소모품자동사용(StyleScreen.ToggleLine `305bee56`)·§3 SkillDef.초필살기(SkillUltLine `24ef7e47` — 이번 바퀴 선반영 확인)·§18-13 별 인식(`1 + 층/10`)·§18-13 별 크기(`1 + 층×0.02`)·§18-11 대보스 개체 HP(2체 65·3체 45)·§10-2 정예 유형 1~2종(지도 Caption)·§10-7 탑 대보스 마릿수(60/30/10)·§18-10 레이드 벽(5층 ×1.5·10층 ×2.2)·§18-4 목숨 시세 상한(부활초 8·두루마리 4·환생석 300)·증표 시세 상한 400·§11 드랍 옵션 체력(`GearOpt.HpMul`→`EffectiveHpMul`)·경매 복원 등급·옵션·§13-3 창고 현재 칸 경로·§11 드랍 옵션 1~4·§10-8 정예 일반·보스 고급 장비·가방 60칸·무기 직업 계열·진입 면 선택·§18-9 RaceDef.전투당발동(`f9f195e8`)·§3 SkillDef.쿨다운(속성 SkillLine `92cc2feb`)·§18-9 RaceDef.이속배율(속성 SpeedLine `dbe02f57`)·§18-9 RaceDef.체력배율(속성 HealthLine `e4fd1d15`)·§18-9 RaceDef.방어배율(속성 DefenseLine `ead55a1d`)·§3 SkillDef.위력배율(속성 SkillLine ×P `50202ce5`)·§3 SkillDef.반경(SkillLine `e4557f35`)·§3 SkillDef.자원소모(SkillLine `564baaf8`)·§3 SkillDef.설명(SkillDescLine `017335fc`)·**§10-3 계열 상성**(`a7f82e6a`+`777eacab`+`927ce693`)·**§10-5 보스 스킬 수**(BossSkills 선반영)·**§18-14 소환수 재소환**(`996e98ef`)·**§18-12 동시 건설 슬롯 2개**(BuildSlots·EstateBuild 관문 `97b8bd38`)·**§18-12 부지 확장 8×8→16×16**(EstateExpansion·EstateGrid.Size 런타임화 `e516759c`)은 닫음. 시각 UI「다른 게임만큼」은 사람 관문이라 닫지 않는다(V2 손맛은 철회 · V4 70%는 막바지 일괄). `W3Party`·오너 Unity는 건드리지 않는다. **중복 리소스 재생성 금지.** 통과 기준: 소비처 ≥1 + SelfCheck + 네거티브. 증거 없는 완료 금지. **선반영 감지 시 재구현 금지·닫음 판정 후 다음 칸으로**(PROPOSALS 병합 항목 처방).
3. ~~**정예 특수능력 4종**~~ **✅ 완결(2026-08-27)** — 원장 §10-2(✅ 오너 결정 2026-08-13)의 거울 6종 중 특수능력 구현 대상 4종 **전부 완료**: 수호자(`715b01e3` — kind 6·EliteGuardian 오라·고방어)·군단장(`7d4d74f8` — kind 7·EliteLegion 공속·이속 버프)·저주술사(`2da37b37` — kind 8·EliteCurse 회복·이속 감소)·처형자(`a3195672` — kind 9·EliteExecutioner 후열 돌진 폭딜). 나머지 2종(치유=kind 3, 소환=kind 4)은 힐·소환 능력이 이전부터 구현돼 있음. 확립된 패턴: kind 추가 + 전용 Reader(수치 BalanceConfig 노출·원장 미확정 명시) + 단일 관문(DamageMob/Damage/Heal/TickMobs) 소비 + `QA_NO_ELITE_*` 네거티브 + SelfCheck 스윕 등록 + MobSpriteKind/FamilyTint 전용 색·실루엣. ~~**미해결(별도 처리): `_mKind[i] >= 3` 정예 관용구가 kind 5(돌진형)도 포함해 돌진형이 정예 HP·크기를 받는 선반영 결함**~~ **✅ 수리(2026-08-27 `f800aa1c`)** — 명시 술어 `W3Party.IsElite(kind)`(3·4·6·7·8·9 true·5 false) 추출, 9개 호출부(HP·스케일·드랍·FX·힐 클램프) 재사용, EliteClassifySelfCheck 신설·레지스트리 등록. 배치는 세션 권한 게이트로 소스 계약 grep 대체 검증. **배치 SelfCheck·전수 스윕·눈확인은 이 세션 유니티 권한 게이트로 4종 모두 미실행 — 오너 에디터 살아난 다음 바퀴에 EliteGuardian/Legion/Curse/Executioner SelfCheck + 전수 스윕 일괄 확인 필요.**

> 잔여 검증 후보: AttachWithAwake `d01e6807` 닫음. V4 훅 `4edba3af` 닫음. png 실로드 `d30a135c` 닫음. SrcNoComments `061c14ca` 닫음. zip 실로드 `ef9cd488` 닫음. run_selfcheck.sh `2056cf7d` 닫음. 일일퀘 `81cbe1cf` 닫음. commit_guard 2단 `06da7bdd` 닫음. GameFullCheck 4바퀴 `841fda59` 닫음. last_test_report `0faaa969` 닫음. 다음=ox-alpha 출력 검증(회의 073515 채택 #3).

## 다음 할 일 큐 (루프가 못 닫은 것)

| # | 항목 | 이유 |

|---|---|---|

| 1 | **필드 정예·전투 보정** | 선행 훅 **닫음**(`03035f8f` — `EliteDrop.FieldKills` 카운트+드랍 지급). 드랍 배율 **닫음**(`132e8b34`). 요약 드랍 **닫음**(`8e0ffb84`). 큐#1 완결 |

| 2 | **30층 성장 곡선** | 닫음. `BossHp` + 재측정 PASS |

| 3 | **영지 §5 드래그 UX** | 닫음. SelfCheck PASS · `6d9b4fae` |

| 4 | **환생·탐험·내구·수비명예·착용레벨** | **닫음.** 환생 `86485fd7` · 탐험 `8c0b4b4a` · 내구 `5213c8bc` · 수비명예 `cd21fa0c` · 착용레벨 `4d9bc2d2`(요구레벨 기본 0, 오너 수치표는 안 만듦) |
| 5 | **G3 권장 파티 픽스처 고도화 후 재판정** | 픽스처 **닫음**(`792f36ab` Equipment·Fusion CombatMuls). 50F/25F 권장 실측 전멸은 막대 유지 재판정(이 칸 실패 아님) |



## 최근 완료 내역 (History)



| 바퀴 | 일시 | 작업 내용 | 검증 결과 / 커밋 |
| — | 2026-08-26 (바퀴) | **영지 창고(warehouse) ox-alpha 반입 — 8종 중 세 번째 건물**(INBOX 「영지가 밋밋함」 2026-08-25 밤·한 바퀴 한 건물) — `out_oxalpha_estate_warehouse.png`(256×256)를 `Resources/props/estate_warehouse_0.png`로 반입, 나노바나나 구판(1522×1803, 2.6MB)은 `art/bak_nanobanana_estate_warehouse_0.png` 보존. 소비 경로 `EstateBuildings.PropOf` 무변경 — 런타임 코드 변경 0. EstateBuildingsSelfCheck에 창고 ox-alpha 검증 4건 신설(로드·파일·256·알파). 블렌더 꺼짐(3D 없음) · 이미지 생성 없음(기존 산출물 반입) | EstateBuildingsSelfCheck 배치 **PASS exit 0**(창고 4건 신규 포함, 44건 전체) · GameSweep 전수 **195/195 PASS exit 0** · SweepCoverage 195=195 · 커밋 가드 PASS · `4a34361a` |
| — | 2026-08-26 (바퀴) | **영지 본성(keep) ox-alpha 반입 — 8종 중 첫 건물**(INBOX 「영지가 밋밋함」 2026-08-25 밤·한 바퀴 한 건물) — `out_oxalpha_estate_keep.png`(256×256)를 `Resources/props/estate_keep_0.png`로 반입, 나노바나나 구판은 `art/bak_nanobanana_estate_keep_0.png` 보존. 소비 경로 `EstateBuildings.PropOf` 무변경. 부수: EstateBuildingsSelfCheck에 oxalpha 판정 4건(로드·파일·256·알파) 신설 + `ats.estate.b.*` 레벨 prefs DeleteKey 결정론 전제 — unity_meas 배치에서 레벨 5+ 잔상이 PropOf가 티어 `_1`을 반환해 기존 기대(_0)와 어긋나 FAIL 3건 재현(첫 실측 로그 보존) → 런타임 무변경·검증만 결정론화. Unity MCP 복구 실측 — 플레이모드 before/after 스크린샷 육안(검은 덩어리→명확한 성, 채택) · 폴백 경고 0 · 콘솔 에러 0. 블렌더 꺼짐(3D 없음) · 이미지 생성 없음(기존 산출물 반입) | EstateBuildingsSelfCheck 배치 **PASS exit 0**(신규 4건 포함) · GameSweep 전수 **195/195 PASS exit 0** · SweepCoverage 195=195 (`output/qa/ashes-to-stars/oxalpha_keep/` 로그 3건) · A/B 샷 `projects/ashes-to-stars/unity/Captures/oxalpha_keep_ab/{before_nanobanana_keep,after_oxalpha_keep}.png` 육안 · 커밋 가드 PASS · `866b444d` |
| — | 2026-08-26 20:05 | **루프 생존성 수리(INBOX STOP 「루프가 안 돈다」 19:25)** — 원인 체인 실측 확정: ①19:29 바퀴(캐릭터창 폴리싱)가 Unity MCP `Endpoint is unavailable`로 code=1 중간사망→STATUS 미갱신(`lap-20260826-192914` 로그 말미) ②미갱신 3회 「정상 종료(exit 0)」에 launchd KeepAlive(`SuccessfulExit=false`) 재실행 불가 ③재기동 감시자 loop_watch가 pgrep 의존+침묵 설계라 회복 실패 — **12:11~19:27 약 7시간 공백**(watch 로그 파일 부재 = log() 한 번도 미실행). macOS pgrep -f가 launchd 자식 일부 procargs를 못 읽는 신규 발견(PID 12651 loop.sh 살아있는데 rc=1 · speed_lane은 정상 매칭) → 감시 축을 lap 로그 mtime으로 교체(fresh ≤90분 통과·stale kickstart→bootstrap 폴백·매 실행 판정 근거 로그). **부수 복구: `com.ailab.loopwatch` 자체가 launchd 도메인에서 언로드돼 있었음(바퀴 진행 중 실측 — plist는 존재, print 「Could not find service」) → bootstrap 복구 후 새 로직 실서비스 첫 실행 PASS.** PROMPT.md엔 「유니티 MCP 불능 시 그 검증만 건너뛰고 배치 SelfCheck 대체 검증 후 커밋·STATUS 완주」 규칙 추가. 게임 런타임·W3Party 무접촉 · 이미지 생성·블렌더 해당 없음 | `bash -n` PASS · 샌드박스 격리 실측 4시나리오 전부 PASS(lap 부재→재기동 · stale 2h→재기동 · fresh→통과 · STOP→건너뜀 — `DRY_RUN`+`LOOP_WATCH_ROOT` 임시루트) · 실환경 스모크 rc=0·launchctl 호출 0·메인 PID 12651 무사 · loopwatch bootstrap OK→RunAtLoad 즉시실행 exit 0·`logs/loop_watch.log` 생성(20:09 정상 판정 흔적) · PROMPT.md 배포본 bootout 없이 cp 동기화(diff 일치) · 커밋 가드 temp-index 분리 PASS(2건+3건 일치) · `d2410dcf`+`2f351bf6` |
| — | 2026-08-26 (바퀴) | **검증 탐지 장치 패키지(구큐#1·회의 20260826-095813 채택 1)** — GameSweepSelfCheck의 하드코딩 65행을 `Entry[] Registry`(컴파일 타임 참조)로 전수화: 65→195종(단독실행 UiAtlasUv·스윕 본체 자신만 등록 밖 — 신규 SelfCheck는 등록 한 줄이 의무가 되고, 삭제·이름변경은 즉시 컴파일 에러). `SweepCoverageSelfCheck` 신설로 전수(반사 발견·GameFullCheck 같은 기준)↔등록 양방향 대조 — 첫 실측에서 탐지기가 UiAtlasUv 오등록을 스스로 포착해 장치를 즉시 실증. `QA_NO_SWEEP_COVERAGE` 네거티브(실제 등록부에서 BodyNavSelfCheck를 가려 정확히 지목하는지 재현). `One()`은 예외 시 FAIL 문구 유무와 무관하게 실패로 판정(GameFullCheck 기준 정렬 — 메시지에 키워드 없는 예외가 통과하던 구멍 제거). GameFullCheck은 Skip에 스윕 본체 추가(전수화로 인한 전체 중첩 2회 실행 방지) + catch 시 `InnerException.StackTrace` 병기(채택 2 일부 — BossBattleDps 스택 오독 재발 차단) | 스윕 배치 **195/195 PASS exit 0** · 네거티브 PASS exit 0(`output/qa/ashes-to-stars/sweep_full_registry_{first,rerun}.log`·`sweep_coverage_negative.log`) · **GameFullCheck 전수 195/195 PASS exit 0**(194+신설1−중첩1 · `full_check/full_check_20260826_1126.log`) · `loop/last_test_report.json`에 「SelfCheck 전수 195/195 통과」 흔적 확보 · 커밋 가드 temp-index 분리(공유 인덱스 타 세션 혼입 16건 차단 실측) · `57a8f6a7` |
| — | 2026-08-26 (바퀴) | **§18-14 소환수 재소환 — 소비처0 해소(오너 위임 승인)** — BalanceConfig에 재소환비용실버(50)·재소환쿨다운초(30) 신설, `Resummon` Reader가 읽고 캐릭터 속성 탭이 「소환수 재소환 50실버 · 쿨다운 30초(§18-14)」 줄로 소비. QA_NO_RESUMMON이면 옛 동작(줄 없음). W3Party 무접촉 — 소환수 개체·죽음 개념은 별도 심의 과제로 남김. 부수: 직전 랩이 커밋 전 중단해 남긴 미커밋 구현(Resummon 2종 신규+소비 3파일)을 원장 §18-14 수치 대조·전면 재실측 후 인수 커밋 — 재구현 없음(선반영 감지 처방 준수) | ResummonSelfCheck 배치 **PASS exit 0** · GameSweep **65/65 PASS exit 0**(`output/qa/ashes-to-stars/{resummon_selfcheck_rerun,game_sweep_resummon_rerun}.log`) · 오너 에디터 리프레시 후 콘솔 에러 0 · 플레이모드 A/B 실측 `resummon_shots/` 육안(포지티브 `qa_go:Character.png` 줄 있음 / 네거 `qa_negctrl_no_resummon.png` 줄 없음·나머지 동일) · 커밋 가드 temp-index 분리 · `996e98ef` |
| — | 2026-08-26 (바퀴) | **필드 정예 처치 훅(STATUS 큐#1 선행, 오너 위임 승인)** — W3Party.KillMob에 훅 1개(정예 종류 3·4·GameMode·`!DungeonRun.Active` 한정 — 던전 정예는 노드 Complete가 이미 지급, 이중 지급 차단·검증 보드 무영향). `EliteDrop.NoteFieldKill()`이 §10-1 「처치 시 추가 드랍」을 노드와 같은 보상(강화석+일반 장비 §10-8)으로 지급하고 `FieldKills` 카운트를 남긴다. QA_NO_ELITE_DROP이면 무지급·카운트 고정(기존 동작 유지). 게임플레이 판정 로직(AI·타겟팅·웨이브) 무변경. 부수: EliteDropSelfCheck가 GameSweep 등록 누락이던 것 발견 → 행 등록 | EliteDropSelfCheck 배치 **44/44 PASS exit 0**(훅 단언 11건 + W3Party 소스 계약 3건 신설) · GameSweep 전수 **64/64 PASS exit 0** · unity_meas 실측 로그 `output/qa/ashes-to-stars/{elite_field_hook_selfcheck,game_sweep_field_hook}.log` · 오너 에디터 리프레시 도메인 리로드 후 콘솔 에러 0 · 커밋 가드 temp-index 분리 · `03035f8f` |
| — | 2026-08-26 (바퀴) | **GameFullCheck 전수 정리 — 잔여 10종 원인 규명·계약 갱신** — 단독 배치 재현(SweepTen 러너)으로 10종 전부 FAIL 상세 확보 후 분류: Skill Cd16·Pow12·Cost1·Desc1·Rad1·Ult1 = SkillLine 합성 포맷(N초·×P·반경R·소모C) 닫는 괄호 기대 불일치 + CharacterScreen 소비 경로가 `JobInfo.*`→`RebirthSkill.*`(계승 위임 폴백)로 이동한 낡은 검증 계약 / BossBattleDps NRE = **에디터 비플레이 배치에서 AddComponent가 Awake를 안 부름**(W3Party._slots null→NextStyle:1126 · BossBattle.activeFloorAOEs null→Begin) — 기존 「W3Party.cs:809 종족 경로」 추정은 Debug.Log 스택 오독으로 폐기(단독 executeMethod 전체 스택 실측), BossAutoAttackSelfCheck와 같은 비활성 생성+수동 Awake 경계를 검증에 적용 / EstateYardCam1·Zoom2 = `Line()`의 §2-2 드래그 분기(`6d9b4fae`) 문구 선점이 새 정상 / Rebirth1 = 영묘 자막 소비처 `Rebirth.MausoleumSubtitle`→`RebirthSkill.MausoleumSubtitle` 이동 반영. **런타임·W3Party·BossBattle 무변경 — 검증 코드 10파일만** | SweepTen 10/10 PASS · GameFullCheck 전수 **194/194 PASS exit 0**(`output/qa/ashes-to-stars/full_check/full_check_20260826_*.log`) · 커밋 가드 temp-index 분리 · `c31479be` |
| — | 2026-08-26 09:25 | **ORDERS① 유령 과제 종결 정리** — 4항목 판정: CombatStyle 실소비(`W3Party.cs:1175·1939·2954` StyleFor 소비, `51c3e862`)·UseFixedStyle 측정 단일 경로(삼항 보존)·보스 쫄 소환(`BossBattle.cs:563→582→583`, `ecf4b4b4`+`bfff2789`)은 실코드 근거로 종결. `JobDef.이동기형태`는 ORDERS 오진 — 소비처 `JobInfo.MovementLine`(JobInfo.cs:280)→`CharacterScreen.cs:415` 존재해 제거 시 캐릭터창 줄 퇴보, 단일 출처 구조 그대로 종결. 부수: GameFullCheck 첫 전수 11건 FAIL 분류 — MemberStyleSelfCheck 검증 취약점 수정(prefs 영속 오염 `ats.style.*` 실측 → DeleteKey 전제 확립) 후 단독 배치 PASS exit 0 · Skill 6종 낡은 기대 포맷 확인(런타임 정상) · BossBattleDps NRE 다음 바퀴. 게임 런타임 무변경 | 단독 배치 실측 3회(MemberStyle FAIL→수정→PASS exit 0 · SkillCd FAIL 16건 재현 · BossDps NRE 재현) · 커밋 가드 혼입 16건 차단 후 temp-index 분리 커밋 · `bf979fc9` |
| — | 2026-08-26 08:35 | **ORDERS③ 측정 하네스 50층 확장 + G3 파티 실DPS 대조 게이트** — `TowerClimbCurveMeasure` TopFloor 30→50, 실판 시뮬(`W3Party.Step` 리플렉션 경로 — BossAutoAttackSelfCheck와 동일 경계)로 기대 레벨·전직 파티의 실측 DPS를 필요 DPS와 대조하는 G3 신설 + 약한 파티(Lv1 기본직) 네거티브. CSV/JSON 파일명 TopFloor 연동. 첫 실측 **FAIL 3건 공식 기록**(기준 낮추지 않음): G2 5h→Lv47 vs 필요50(6.04h) · G3 권장 파티 전멸(실측0 vs 필요542@25층/3076@50층 — 장비·합성·가호 미포함 베어 로스터). W3Party 런타임 무변경 | 배치 실행 컴파일 에러 0 · JSON/CSV 산출 육안 `output/qa/ashes-to-stars/curve/tower_climb_50.{json,csv}` · 실판 로그 실측(편성 5인·스킬4·시간경과 사망) · 네거티브 PASS · `f5e6b1c3` |
| — | 2026-08-26 08:00 | **ORDERS② 파티 멤버별 전투 스타일 실소비 배선**(직전 바퀴 — STATUS 갱신 누락분 보완) — TickParty·TickMobs·TickShots가 `StyleFor(m.Style)` 개별 적용, UseFixedStyle·QA_NO_MEMBER_STYLE 삼항으로 측정 단일 경로 보존, MemberStyleSelfCheck 20건 + GameSweep 등록 | GameSweep 재실측 ok:true 63/63(`loop/last_test_report.json` 07:45) · `51c3e862` |
| — | 2026-08-25 21:40 | **W2 회피 기회 더미** — 오너 지시로 `loop/w2_dummy_sim.py` 신설(§21-1c 방법론 재현: 전략 3종 피격 수·대시 회피기회율, 에셋 수치 MobSpeed 0.90/0.85/0.65·MoveSpd 4.2). 판정 **FAIL** — 직선/원형 계층·숙련 보상 밴드 미달, 동일 시드 해시 일치로 결정론 입증. 리포트 `output/qa/ashes-to-stars/w2_playtest_dummy/w2_report.json`. 기준 낮추지 않음 | 동일 시드 2회 실행 digest 일치 · 이 커밋 |
| — | 2026-08-25 21:35 | **보드 표준 정리** — History 285행·폐기 포맷 「최신 바퀴」 4섹션·서술형 정정 원문을 `docs/archive/legacy_loop_docs_20260826/`로 이관, 반복 재검증 행 14건 요약 통합, 관문 부채 핵심 5행 복구 검증. board.py 파서 계약 유지(126테스트 중 125 PASS — 1건은 워커 커밋 `70db7ebf` 샌드박스 테스트의 ROOT 누출로 별도 이슈) | 정리 전후 test_board 비교 · 이 커밋 |
| — | 2026-08-25 21:30 | **루프 인프라 — 커밋 직전 공용 인덱스 가드(회의 채택 #3·PROPOSALS 09:49 상 이행)** — 시작 시점에도 공유 인덱스에 타 세션 스테이징이 살아 있어(실측 5건) 맨몸 커밋 혼입 위험이 상시. `loop/commit_guard.sh`: `git diff --cached --name-only`(GIT_INDEX_FILE 임시 인덱스 존중)가 허용 경로 집합과 정확히 같은지 강제하고, 불일치 시 혼입·누락 경로를 지목해 1로 끝난다(커밋 중단). temp-index 커밋 후 공유 인덱스 잔해는 `git reset -q -- <내 경로>`로만 닦음(타 세션 분 보존). 게임 코드 무변경(W3Party 무접촉) · 이미지 생성·블렌더 해당 없음 | 샌드박스 12/12 PASS(`loop/test_commit_guard.sh`) · 실공유 인덱스 네거티브(혼입 6건 지목 차단) + temp-index 포지티브 PASS · test_board 재실측 126/126 · `70db7ebf` |

> 그 이전 완료 내역은 [`docs/archive/legacy_loop_docs_20260826/STATUS_history_20260826.md`](archive/legacy_loop_docs_20260826/STATUS_history_20260826.md)로 옮겼다 (매 바퀴 STATUS 전문을 읽으므로 이 파일은 짧게 유지한다).

## 오너 위임 판정 결산 (오너 확인용 · 2026-08-25)

| 항목 | 판정 | 현재 상태 |
|---|---|---|
| ORDERS ①유령과제 ②멤버별스타일 ③하네스50층+G3 | 전부 승인(P1 순서 ②→③→①) | **전부 완료** — ②`51c3e862` · ③`f5e6b1c3`(FAIL 3건 공식 기록) · ①`bf979fc9`(이동기형태는 ORDERS 오진으로 종결) |
| §18-14 소환수 재소환(0.5G/h·쿨30초) | 구현 승인(신규 시스템) | **완료** — `996e98ef`(속성 탭 소비·네거티브 실측) |
| 큐#1 필드 정예 훅(W3Party 최소 훅) | 루프 실행 승인(네거티브 필수) | **완료** — `03035f8f`(훅+EliteDropSelfCheck 44/44·GameSweep 64/64) |
| **W2 회피 기회** | **더미 구현 완료 → FAIL**(직선 도주 강함·원형 카이팅 밴드 초과, `loop/w2_dummy_sim.py` 시드 20260825 결정론) | 기준 유지 — 손맛 튜닝 후 재판정 |
| G16 로컬라이제이션·G17 접근성 | 출시 게이트 과제로 큐 등록 | 미착수 |
| G15 Steam Cloud / §6 오프라인 전투 정산 / §14 동맹 쿨다운 | 보류(외부 의존·설계 미확정·온라인 알파 이후) | 보류 유지 |
| 실행기 체인(스티키)·launchd env 잔재 제거 | 완료(`e48ea8d6`) | opencode 실랩 가동 중 |
| 반복 재검증 TASKS.json 처방 미적용 / 워커 테스트 REGRESSION(`70db7ebf` ROOT 누출) | 미해결 2건 — 추적 중 | 랩/다음 세션 처리 대상 |

## 막힌 것 · 보류
- ~~W2 FAIL~~ — **관문 철회**(오너 2026-08-26). 더미 FAIL 기록은 남기되 빚으로 취급하지 않는다.
- V2/V3/V4 — 더미 관문 소화 완료 · 데모·EA 전 §21-6 실측 재판정.
- G15 Steam Cloud·§6 오프라인 전투 정산·§14 동맹 쿨다운 — 보류(외부 의존/설계 미확정/온라인 알파 이후).
- 미해결 추적 1건: 재검증 건너뛰기 처방 TASKS.json 미적용. (워커 `70db7ebf` 테스트 ROOT 누출은 재실측 해소 — test_board 126 OK, 2026-08-25 21:45)
- 전체 이력: `docs/archive/legacy_loop_docs_20260826/STATUS_history_20260825.md`

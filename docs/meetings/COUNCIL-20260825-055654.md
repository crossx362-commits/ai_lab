# 정기 회의 20260825-055654 (4바퀴 시점)

> 심의 대상: `docs/feedback/PROPOSALS.md` 무표식 항목 + 회의 파트 2건(builder·tester).
> 판정 표식: ✅/⏸20260825-055654. STATUS 큐는 직접 수정하지 않음(권고 충돌은 사실만 기록).

## 참석 (planner/builder/tester 산출 요약 각 2줄)

- **planner** — 파트 부재. 소집 목록에 있던 `.council-part-20260825-055654-planner.md`가 저장소에 없음(실측: builder·tester 2건만 존재) — 계획 트랙 산입 없음.
- **planner** — 영향: 다음 바퀴 과제 후보군은 builder·tester 제안과 STATUS 큐 문언만 근거로 심의함.
- **builder**
  - 관찰: MobDef 형제 필드(속도·체력·피해)가 랩당 1칸씩 3랩 분할 소비(`8c89e69b`→`6f1e1226`→`fc2499d8`) · OnGUI 캡처 실패가 WORKLOG 검수 행마다 재발견 · 속성 탭 우선존 넘침 동일 유형 3회째.
  - 제안: 우선존 접기/스크롤 구조 수정(PROPOSALS:220)을 첫 UI 폴리싱 랩으로 확정 배정 + 형제 필드 동시 grep 절차(:215)·샷 경로 고정(:219)·열린 칸 자동 산출 스크립트(:125).
- **tester**
  - 관찰: `loop/last_test_report.json`이 14시간+ stale(`ok:false` 2건 — HuntBoonSelfCheck·AdvLookSelfCheck)인데 `loop/board.py:51`이 읽는 유일한 통합 합격판 · code=1(Max turns) 실패 랩의 중간 결론이 승계 안 됨(`lap-20260825-052704-4.log`) · 검증 샷 함정이 하루 새 4건 중복 제안(PROPOSALS:209·211·213·219).
  - 제안: 리포트 재생성+미확인 FAIL 2건 재실행 판정(상) · code=1 결론 승계 절차(중) · 검증 샷 공정 일원화(중). 회차 요청: FAIL 2건 재판정 우선.

## 채택 개선안 (번호·근거·다음 바퀴 업무 후보로서의 위치)

1. **검증 샷 공정 일원화(qa_shot.sh 경화 묶음)** — PROPOSALS:96·209·211·213·214·219 일괄 채택. 근거: 같은 manage_camera→OnGUI 검은 샷 문제가 하루 새 4건 재발견(tester 관찰3) + WORKLOG 검수 행마다 반복(builder 관찰2). 위치: 인프라·도구 경화 작업 — ScreenCapture 기본 경로·샷 후 해시 동일 시 재시도·ON/NEG 디렉터리 분리·execute_code ScreenCapture 폴백 내장을 한 번에 반영해 동종 제안 추가를 끊는다. UI 폴리싱 랩 어느 시점에도 선행 실행 가능.
2. **속성 탭 우선존 접기/스크롤 구조 수정** — PROPOSALS:220 채택. 근거: 동일 유형 3회 재발(이동속도 `90f65fd0` 단독 행 분리 → 잡몹 피해 줄로 2차 각성·직업 특성 행 패널 밖 밀림, builder 관찰3)·앞으로의 모든 소비처0 신규 줄이 같은 화면을 통과. 위치: STATUS 큐 1번 「지금 개발 직렬: UI 폴리싱 1건」 슬롯의 내용물 1순위(builder 회차 판정 요청 — 충돌 없음).
3. **소비처0 형제 필드 동시 grep 절차** — PROPOSALS:215 채택. 근거: MobDef 3랩 분할 실측(첫 랩에서 형제 필드를 같이 grep했으면 2랩 절감 가능, GhAnchor→티어배율 선례 동일 함정). 위치: 배정 템플릿 문구 보강 — 소비처0 트랙 전 바퀴에 적용되는 절차 개선.
4. **열린 칸 자동 산출 스크립트(`loop/find_open_spec_items.py`)** — PROPOSALS:125 채택. 근거: STATUS.md:43 「다음 폴리싱: GameScreen Body 한 곳 절단」이 `0fc2eb14` 닫음(05:36) 이후에도 갱신 없이 잔존 — 낡은 포인터 재판정 비용 실측(builder 제안4). 위치: 바퀴 시작 읽기 단계의 포인터 산출 자동화 보조.
5. **code=1(Max turns) 랩 결론 승계 절차** — tester 제안(중) 채택. PROPOSALS.md에 해당 줄이 없어 표식 없음(문서 기록만): 실패 랩 로그 말미의 중간 결론 1줄을 WORKLOG 또는 다음 배당 지시서에 첨부해 동일 탐색 반복을 끊는다.

## 보류 (사유 필수)

1. **PROPOSALS:207(SelfCheck 본문 GameScreen yMax=640·탑 도크 NavPlateTop-12)** — 사유: 선반영. 탑 도크 `16a57558`(STATUS.md:79)·월드맵 본문 yMax `fd0ba129`(STATUS.md:84)로 처방 실행 완료.
2. **PROPOSALS:210(영지 팔레트 좌·우 가장자리 이동)** — 사유: 처방 실행됨 — `37705577`(PaletteTiles 왼쪽 가장자리 EdgePad 8, STATUS.md:63). 잔여 마름모 겹침은 :218이 별도 항목으로 추적 중이라 중복 착수 방지 차원 보류.
3. **PROPOSALS:212(GameScreen Body 한 곳 절단)** — 사유: 선반영. `0fc2eb14`(05:36) `BodyNav.Fit`이 NavPlateTop-12로 본문 절단(STATUS.md:59) — 화면별 NavGap 복제 제거까지 완료.
4. **PROPOSALS:216(FieldScreen DrawChoice 클램프·본문 한 곳 절단)** — 사유: 선반영. 필드 경고 `9f553b44`(STATUS.md:67) + 절단 `0fc2eb14`(STATUS.md:59)로 양 처방 모두 실행.

## 다음 바퀴 권고 1건

**`loop/last_test_report.json` 재생성 + 미확인 FAIL 2건(HuntBoonSelfCheck 「가로 카드 아이콘·글씨가 금테 안에」·AdvLookSelfCheck 「수호기사 이 tank 를 본다」 — 조사(이/를) 파싱 오탐 가능성 포함) 재실행 판정.**(tester 회차 요청 채택)
unity_meas/GameSweep 최신 결과로 리포트를 재생성하고, `ok:false` 항목은 「재실행 PASS 로그」 없이 닫지 않는다. 근거: `loop/board.py:51`(TEST_REPORT_PATH)이 읽는 유일한 통합 리포트가 2026-08-24 15:31 stale인 채 그 뒤 20바퀴+ 폴리싱이 진행 — 진회귀·오탐 모두 루프 사각지대.

충돌 기록(큐 미수정): STATUS 큐 1번은 「지금 개발 직렬: UI 폴리싱 1건」을 지정(docs/STATUS.md:39) — 본 권고(QA 게이트 수선)는 개발 직렬 지정과 어긋나므로 순서 충돌 사실만 기록한다. 채택 #2(속성 탭 우선존)는 그 UI 폴리싱 슬롯의 내용물 후보로 유지.

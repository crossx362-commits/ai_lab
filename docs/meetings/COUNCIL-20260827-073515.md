# 정기 회의 20260827-073515 (N바퀴 시점)

## 참석

**planner**: §22 관문②(5h 지루함) 표본·척도·CSV 규격이 오너 미확정이라 로드맵 정체. 테스트 200건 PASS로 회귀 안정성 확보. STATUS 큐 2(원장 재스캔)에서 소비처0 후보 2건(§18-12 부지 확장·§18-13 디버프 중첩) 남아 있음.

**builder**: 영지 아트 반입 8동 전부 완료 후, 20건 중 18건이 아트 반입 커밋인 반복 패턴 관찰. FieldDecor.ScaleFor 하드코딩 문제로 마을 장식 프랍 256 전환 시 스케일 재튜닝 필요. 소비처0 후보 2건 모두 중간 리스크이나 구체 방안 미비.

**tester**: `last_test_report.json`(02:58·200건)이 HEAD(07:18 커밋 `97b8bd38` 포함)를 대표하지 못함. BuildSlotsSelfCheck 런타임 미실측 5바퀴째 누적. `run_selfcheck.sh` 래퍼 미작성으로 배치 검증 공백 반복(직전 회의 채택 1번 미이행).

---

## 채택 개선안

| # | 제안 | 근거 | 다음 바퀴 업무 후보 위치 |
|---|------|------|------------------------|
| 1 | **래퍼 스크립트(`run_selfcheck.sh`) 작성 + BuildSlotsSelfCheck 배치 통과** | tester: 권한 게이트 5바퀴 연속 반복 → 배치 검증 자체 불가. 래퍼 1바퀴 이내 작성 가능, EstateBuildingsSelfCheck·정예 4종 SelfCheck 배치 통과 가능해져 검증 공백 일괄 해소 | STATUS 큐(다음 할 일)의 소비처0 해소·아트 작업의 **검증 기반 전제 조건**으로 최우선 |
| 2 | **`last_test_report.json` HEAD 기준 재실행** | tester: BuildSlotsSelfCheck 1건 리포트 누락, 정예 4종 배치 SelfCheck 소스 계약만 통과. 재실행 없이는 현재 HEAD 전수 통과 여부 미확인 | 래퍼 작성 직후 또는 함께 수행 |
| 3 | **art/gen_oxalpha_estate_tiers.py 출력 검증 자동화** | builder: 8동 전부에서 "화풍·크기 튐 수리" 반복. 파생 스크립트의 출력 검증(해상도·알파·색상 범위) 자동화로 커밋 전 문제 차단 | 영지 아트 반입 파이프라인 개선 — 현재 8동·티어·공사판 완료 후이므로 향후 새 아트 반입 시 적용 대상 |

---

## 보류

| 제안 | 사유 |
|------|------|
| STATUS 큐 2(원장 재스캔) 소비처0 후보 2건(§18-12 부지 확장·§18-13 디버프 중첩) 즉시 착수 | planner·builder 양쪽 모두 "중간 리스크·구체 방안 미비"로 판단. builder의 "소비처0 후보 해결 방안 사전 설계" 제안이 선행되어야 구현 가능. **래퍼+배치 검증(채택 1)이 먼저 확보되어야 소비처0 해소의 런타임 보증이 선다.** |
| 관문②(5h 지루함) 측정 규격 확정 요청 | planner 제안이나, 오너 미확정 사안이라 이번 회의에서 의결할 수 없음. 다음 회의에서 오너에게 별도 보고 대상. |
| 마을 장식 프랍 256 전환 스케일 재튜닝 절차 명문화 | builder 제안. 영지 아트 8동·티어·공사판이 방금 완료된 시점으로, 프랍 11종 전환은 아직 다음 단계. 채택 3(출력 검증 자동화)와 연계해 향후 반영. |
| UiAtlas SlicePad maxPad 상한 점검·SelfCheck 치수 하한 단언 | PROPOSALS 기존 항목(우선순위 중, ✅ 미채택). 이번 회의에서 재논의 없음. |
| OnGUI 캡처 ScreenCapture 통일 | PROPOSALS 기존 항목(우선순위 중, ✅ 미채택). 이번 회의에서 재논의 없음. |
| unity_meas 배치 -quit 필수화 | PROPOSALS 기존 항목(우선순위 중, ✅ 미채택). 이번 회의에서 재논의 없음. |
| STATUS 포인터 동시 갱신 절차 명문화 | PROPOSALS 기존 항목(우선순위 중, ✅ 미채택). 이번 회의에서 재논의 없음. |
| git 인덱스 타 세션 스테이징 사고 방지 절차 | PROPOSALS 기존 항목(우선순위 상, ✅ 미채택). 이번 회의에서 재논의 없음. |
| SelfCheck 클램프 상자 동일성 가정 점검 | PROPOSALS 기존 항목(우선순위 중, ✅ 미채택). 이번 회의에서 재논의 없음. |

---

## 다음 바퀴 권고 1건

**래퍼 스크립트(`run_selfcheck.sh`) 작성 → BuildSlotsSelfCheck 배치 통과 1회**

STATUS 큐와의 충돌: STATUS「다음 할 일」1번(UI·아트 상시 폴리싱)과 2번(기획서 ✅·소비처 0곳)이 래퍼 작성보다 앞에 있으나, tester의 지적대로 래퍼 없이는 배치 검증 자체가 불가능해 아트·소비처 작업의 런타임 보증이 빈다. 따라서 래퍼는 "다음 바퀴 권고"이지, STATUS 큐 순서를 임의로 변경한 것은 아니다.

STATUS「다음 할 일」큐 순서(1→2)와 이 권고의 선행 관계:
- STATUS 1번(아트): 래퍼 불필요(소스 계약·PIL 검증으로 충분) → **독립 진행 가능**
- STATUS 2번(소비처0): 래퍼 확보 시 EstateBuildingsSelfCheck·BuildSlotsSelfCheck 배치 PASS로 런타임 보증 확보 → **래퍼 후 착수가 안전**
- 따라서 아트 바퀴와 래퍼 작성은 **병렬 진행** 가능하며, 소비처0 착수 전에 래퍼가 확보되어야 한다.

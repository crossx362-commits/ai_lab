# 정기 회의 20260827-030031 (n바퀴 시점)

## 참석

- **planner**: (파트 파일 미도착 — 이 세션에서 미작성)
- **builder**: (파트 파일 미도착 — 이 세션에서 미작성)
- **tester**: 테스트 200/200 전수 통과 확인. MCP 인게임 눈확인 미완료(에디터 연결 문제). ox-alpha 생성기 단일 소스 리스크 지적. ox-alpha 생성기 자체 테스트 추가(중)·MCP 인게임 눈확인 절차 보강(중)·잔여 7동 티어 파생 시 회귀 대비(하) 제안.

## 채택 개선안

| # | PROPOSALS 라인 | 요약 | 근거 | 다음 바퀴 업무 후보 |
|---|---|---|---|---|
| 1 | 298 (01:45) | `loop/run_selfcheck.sh` 래퍼 스크립트 작성 — loop 세션의 유니티 배치 허용 커맨드를 단일 경로로 고정해 세션별 권한 편차 제거 | STATUS 큐 1(티어 파생)의 잔여 7동 검증이 배치 권한 게이트에 막혀 매 바퀴 대체 검증으로 소진. tester도 ox-alpha 생성기 자체 테스트 필요성을 지적. 래퍼가 있어야 EstateBuildingsSelfCheck가 배치로 실제 통과 | **STATUS 큐 1 티어 파생의 검증 경로 확보** — 래퍼 작성 후 8동 전수 EstateBuildingsSelfCheck 배치 통과 1회 |
| 2 | 279 (09:20) | GameFullCheck 전수 4바퀴 의무 실측 등록 — 큐레이션 스위트(GameSweep)만 돌면 낡은 계약·신규 결함이 무기한 잠복 | 직전 바퀴에서 GameFullCheck 전수 첫 실행으로 11건 FAIL이 일괄 노출(Skill 6종·BossBattleDps·MemberStyle 등). 큐레이션만으로는 검증 사각지대 존재 입증 | **정기 바퀴 중4바퀴마다 GameFullCheck 전수1회** — 다음 바퀴부터 의무화 |
| 3 | 281 (09:40) | SweepTen류 러너 catch 시 `InnerException.StackTrace` 병기 + 스택 오독 의심 건 단독 executeMethod 재현 절차 | BossBattleDps NRE가 Debug.Log 내부 스택으로 오독돼 원인(NextStyle:1126)을 종족 경로로 잘못 지목하는 사고 재발 | **검증 러너의 예외 처리 갱신** — 다음 검증 코드 바퀴에서 SweepTen·GameFullCheck 러너에 스택 병기 반영 |
| 4 | 231 (00:54) | Unity MCP manage_camera 샷 실패 시 `execute_code` + `ScreenCapture.CaptureScreenshot`로 곧바로 전환 | manage_camera가 브리지 살아있어도 "No Unity Editor instances found"로 실패하는 실측 빈도 잦음. 같은 세션 execute_code ScreenCapture는 성공. 샷 폴백을 안 박으면 시각 검증이 카메라 도구 한 길에 묶임 | **PROMPT §③에 샷 폴백 규칙 명문화** — 다음 바퀴에서 반영 |

## 보류

| # | PROPOSALS 라인 | 요약 | 사유 |
|---|---|---|---|
| 1 | 18 (18:20) | MCP 브릿지 재연결 자동화(재시도 헬퍼 함수화) | 채택 1(래퍼 스크립트)이 먼저 확보돼야 브리지 권한 구조가 안정화됨. 래퍼 작성·실증 이후에 재시도 로직을 붙이는 게 순서 적절 |
| 2 | 287 (10:56) | 랩 종료 시 `SESSION_HANDOFF.md`에 미커밋 파일 강제 기록 | 현재 루프가 opencode 실행기로 안정 가동 중. SESSION_HANDOFF 표준화는 실행기 전환(예: claude 복귀) 시점에 함께 도입하는 게 비용 대비 효과 큼. 지금 당장은 not-started 상태 |

## 다음 바퀴 권고 1건

**STATUS 큐 1(필드 정예·전투 보정)의 잔여: §10-2 「정예 처치 시 다음 웨이브 드랍 배율」 연동·필드 요약 문구에 드랍 노출.**

충돌 없음 — STATUS「다음 할 일」1번(UI·아트 상시 폴리싱)과 별도 트랙. 채택 1(래퍼 스크립트)가 확보되면 EstateBuildingsSelfCheck 배치 검증도 함께 가능.

# 정기 회의 20260825-135006 (직전 회의 130130=34바퀴 기준 +4바퀴 · 38바퀴 시점)

> 입력: `docs/feedback/PROPOSALS.md` 전체 + 회의 파트 2건(`.council-part-20260825-135006-planner.md` 13:55 · `.council-part-20260825-135006-tester.md` 13:54) + `docs/STATUS.md` 「다음 할 일」
> 이례 기록: **builder 파트는 디스크에 미생성**(`.council-part-20260825-135006-builder.md` 부재 실측 — `.council-lock` 13:50, planner·tester만 접수). **연속 2회 부재**다. planner+tester 수렴 안건과 코드 작업을 수반하지 않는 절차 개선안만 채택했고, 게임 C#·SelfCheck 코드 작업 제안은 심의 연기로 표식 없이 유지했다.
> 판정 표식: 채택 ✅20260825-135006 — 등재 줄이 있는 항목만 PROPOSALS.md 해당 줄 끝에 표식(줄 추가 없음). 이번 회의에서 새로 ⏸ 표식을 붙인 등재 항목은 없다(보류는 파트 제안 2건뿐).

## 참석

**planner** — STATUS가 「닫음」이라 부르는 HuntBoon 도크(:44)·수호기사 전신(:40)이 `loop/last_test_report.json`(09:23 스냅, ok:false 2/58)에는 FAIL로 남아 있는데 큐 어디에도 정리 과제가 없다(문서-검증 불일치).
소비처 우물 바닥 도달(원장 운영규칙 3 전환 조건 충족 주장)·롤백 쌍 `0e4e4dcc`→`b68071e6`(GUI 상태 누수 미해결)·스윕 58항목 비대를 지적해 붉은 2행 닫기(상)·리포트 신선도 규칙(중)·관문② 전환 큐 규칙(중)·GUI 복원 재도전(하) 4건 제안.

**builder** — 부재(파트 파일 없음, 연속 2회).

**tester** — 09:23 리포트가 4시간+ 스테일인데 그 위로 랩 24~28회차(12:48~13:43)와 커밋 다수(`5734657a` 등)가 착수됐고 두 FAIL은 이미 닫혔을 개연성이 높은데 현재 HEAD 재실측 근거가 어디에도 없다.
랩 종료 게이트가 자기 SelfCheck만 봐 통합 회귀에 무책임(커버리지 드리프트: 랩 28 신규 EscapeHintHudSelfCheck가 58항목 목록에 없음)·채택된 건너뛰기 처방(`a27b3d2b`)이 큐에 미적용돼 proposals-triage 103차 등 무변경 랩 계속 누적 — 리포트 신선화 장치화(상) 등 3건 제안.

## 채택 개선안

1. **폴리싱 후보 선정 시 최신 정상 샷·관련 QA_NO 상태 대조 체크리스트** (PROPOSALS 229행, 중)
   - 근거: tester 제안 1이 본 항목을 선례로 직접 인용 — 「오래된 산출물을 현재 상태로 오인」 실패 유형은 스테일 리포트 문제(본 회의 planner·tester 공동 관찰)와 같은 뿌리. TowerHubCap 오판 실측 사건 기반, 절차 추가만으로 이행되고 코드 작업이 없어 builder 부재의 영향이 없다.
   - 위치: 다음 바퀴 권고(전수 재실측) 바퀴의 후보 선별·판정 단계 절차로 흡수 — 별도 큐 칸 불요.
   - 표식: 229행 ✅20260825-135006
2. **UI 결함 명칭은 커밋 전까지 가설로 두고, 네거티브 샷이 눈에 보이는 차이를 못 만들면 관찰 결함으로 재정의** (PROPOSALS 227행, 중)
   - 근거: 실측 사건이 문서화돼 있다(STATUS 59행 — 타이틀 QA 패널 바퀴에서 「종료 카드 가림」 가설이 A/B 샷에서 실제 겹침 없음으로 폐기). 정적 판정만으로 완료 기록을 내는 것을 막는 검증 판정 위생 절차로, 코드 작업 불필요.
   - 위치: 채택 1번과 같은 검증 신뢰 세트 — 권고 바퀴의 결함 명명 단계에 흡수.
   - 표식: 227행 ✅20260825-135006
3. **이미 채택된 건너뛰기 처방(`a27b3d2b`)을 TASKS.json 큐에 실제 적용해 무변경 반복 배정을 끊는다** (tester 제안 3 — PROPOSALS 80행)
   - 근거: 80행은 이미 ✅20260823-235725 채택 마커를 보유하는데 STATUS 관문 부채표(:19~21) 기준 proposals-triage 103차·lane-doc 96차·keeper-warn-chip 89차 재검증 랩이 계속 누적 — 「채택 마커는 심의 기록일 뿐 실행 장치가 아니다」가 103회 실측으로 입증(tester 관찰 3).
   - 위치: 다음 바퀴 업무 후보 — TASKS.json 완료 표시·제외는 루프 장치 편집을 수반하므로 권고 바퀴와 함께 처리할 때 회귀 확인을 동반할 것.
   - 표식: 생략 — 80행에 기존 채택 마커가 있어 이중 표식하지 않았다(줄 내용 변경 없음).

## 보류

1. **소비처 재스캔 연속 2회 0건 시 단계 1 관문② 실측 준비로 전환하는 큐 규칙 명기** (planner 중, 등재 줄 없음) — 사유: 직전 회의 보류 1(⏸20260825-101959)과 동일 안건 재상정. STATUS 머리 갱신 문구는 메인 루프 CAS 관리 대상이라 워커용 수기 분기 신설이 충돌하고, 관문② 일정 자체는 사람 관문 더미 유지(오너 2026-08-24, STATUS :15) 관할이라 에이전트가 선제 실행할 규칙이 아니다. builder 부재 해소 후 재심의.
2. **롤백된 `0e4e4dcc` GUI 상태 복원 재도전** (planner 하, 등재 줄 없음) — 사유: 게임 C# 코드 작업인데 builder 파트가 연속 2회 부재해 실행 가능성·회귀 영향 심의가 불충분하다. 제안 스스로 요구하는 QA_NO 네거티브+SelfCheck 선행 정의부터 빌더 검토 대상이다.
3. **미처분 계속(표식 없음)** — 등재 코드/UI 계열(95·174·175·206·228·230~232·234~236·241·244·245~248행)과 「없음」 관찰 행(224~226·237~239·242~243행)은 직전 회의 약정대로 「3파트 정상 접수 시」 심의 조건이 아직 오지 않았으므로 ⏸ 표식 없이 유지한다. 보류 확정이 아니라 심의 연기이며, 이번 회의도 builder가 빠져 해당 조건을 채우지 못했다.

## 다음 바퀴 권고 1건

**현재 HEAD에서 GameSweep 전수 재실측으로 `loop/last_test_report.json`을 재생성해 붉은 2행(HuntBoonSelfCheck·AdvLookSelfCheck)의 판정을 확정하고 스윕을 녹색으로 되돌린다 — 리포트 신선화를 랩 종료 절차에 박는 것을 동반한다.**
- 근거: planner·tester의 회차 판정 요청이 수렴한 유일 안건이다. 실측: 리포트는 09:23(ok:false 2/58)인데 이후 금일 랩 28회차(13:43)까지 진행됐고 STATUS 최종 갱신은 13:41(:6) — 리포트가 현재 HEAD를 대표하지 못한다. 상반 증거가 공존한다(STATUS :44 HuntBoon 닫음 `8a7e6b93`·`67664c3a`, :40 수호기사 전신 닫음 `b81ab754`, 커밋 `25e5b3ca` 메시지 「HEAD 격리 재실행 동일 실패 — 기존 결함·회귀 아님」 vs 현재 HEAD 재실측 기록 부재) — 어느 쪽이 참인지 측량 없이는 판정 불가. 목록 자체에 랩 28 신규 EscapeHintHudSelfCheck가 없어 커버리지 드리프트도 누적 중. 직전 회의(130130)가 동일 권고를 남긴 뒤 +4바퀴가 소진됐는데도 미이행이므로 재권고하며, SelfCheck 단언이 낡았으면 고치고 실결함이면 STATUS 큐에 회귀 항목 진입을 요구한다(tester 제안 2 흡수).
- **충돌 기록(큐 미편집)**: STATUS 「다음 할 일」은 위에서부터 하나만 규칙으로 1번 「지금 개발 직렬: UI 폴리싱 1건」을 지시하고 3번 소비처0 재스캔·4번 UI 폴리싱이 큐 축인데, 본 권고는 큐에 없는 검증 신뢰 복구 작업이라 충돌한다. 직전 회의와 동일한 충돌이며, 큐는 고치지 않고 사실만 기록했다. 채택 1·2번 절차는 별도 큐 칸 없이 권고 바퀴의 절차 단계로 흡수 가능하다.

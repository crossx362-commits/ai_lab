# 정기 회의 20260827-081437 (n바퀴 시점)

## 참석

- **planner**: (파일 미도착 — 회의 파트 `.council-part-*-planner.md` 미존재. 의장이 STATUS·PROPOSALS 기준으로 대리 판정.)
- **builder**: (파일 미도착 — 회의 파트 `.council-part-*-builder.md` 미존재. 의장이 STATUS·PROPOSALS 기준으로 대리 판정.)
- **tester**: last_test_report 200/200 전수 통과, 회귀 징후 0. 아트 반입 후 인게임 배치 미실행(권한 게이트) 8종 누적. png 실로드 검사 자동화·보간 접두 매칭·MCP 씬 검증 절차 3건을 다음 바퀴 권고.

## 채택 개선안

| # | 개선안 | 근거 | 다음 바퀴 업무 후보 |
|---|--------|------|----------------------|
| 1 | png 실로드 검사 자동화 — 새 아트 반입 랩 게이드 강화 | tester 제안(상), PROPOSALS `22:55` 항목과 동일 문제(새 PNG 반입 시 TextureImportRules 우회 → Resources.Load null 재발). 현재 트리류·잔여 프랍(5종+) 반입 진행 중으로 리스크 집중 구간. game_asset_names의 `$"..."` 보간 접두 추출 기능과 묶으면 미사용 오탐도 해소 | STATUS 큐#2(기획서 ✅ 소비처 0곳)와 연계 — 소비처0 재검증 신뢰도 확보 |
| 2 | git 인덱스 타 세션 스테이징 사고 방지 절차 | PROPOSALS `07:06`·`13:5x`·`16:25`·`09:49` 4건 관찰, `commit_guard.sh` 이미 구현(`70db7ebf`). 테스터 확인: 공유 인덱스 혼입 위험 상존 | 랩 종료 전 가드 재실행 + temp-index 재료 HEAD 재추출 2단 검증 의무화 |
| 3 | GameFullCheck 전수 의무화(4바퀴마다 1회) | PROPOSALS `09:20`·`09:56`·`09:46`·`09:43` 4건 관찰 — 큐레이션 스위트(GameSweep 63종)만 돌면 낡은 계약·신규 결함이 무기한 잠복. 오늘 전수 194/194 통과 확인으로 기반 확보 | 정기 바퀴 중 4바퀴마다 GameFullCheck 전수 1회 의무 실측 등록 |
| 4 | SelfCheck 소스 계약 검사 시 주석 스트립 후 검사 | PROPOSALS `05:00` 항목 — doc 주석에 옛 관용구 문자열이 남으면 소스 문자열 부재 단언 SelfCheck가 자기 커밋을 FALSE-FAIL시킴. `SrcNoComments(path)` 유틸 제안 | Editor 공용 유틸로 주석 스트립 헬퍼 추가 |

## 보류

| # | 개선안 | 사유 |
|---|--------|------|
| 1 | MCP 브릿지 재연결 자동화(재시도 헬퍼 함수화) | PROPOSALS `18:20` 항목, ⏸20260827-030031 표식 존재. 브릿지 불안정은 세션 한정 현상(핵심 ping류 정상)으로 코드 문제 아님 — 현재 세션에서 `execute_code` ScreenCapture 폴백이 정상 작동해 긴급도 하락 |
| 2 | 보간 접두 매칭 지원 — 소비처 인식 개선 | PROPOSALS `22:55` 항목, ⏸20260823-235725 표식 존재. png 실로드 검사(#1 채택)와 동일 검사기(game_asset_names)의 결함·처방이 달라 병합 불가. #1 우선 구현 후 후속으로 처리 |
| 3 | MCP 씬 검증 절차 문서화 — startScene 재지정 규칙 | PROPOSALS `22:55` 항목, ⏸20260823-235725 표식 존재. PlayFromTitle 씬 전환 문제이나 권한 게이트로 MCP 검증 자체가 빈번하지 않아 긴급도 하 |
| 4 | STATUS 포인터 동시 갱신 절차 명문화 | PROPOSALS `05:17` 항목. 닫음 셀 이중 판정 진입 문제 — #2(git 인덱스 안전)와 묶어 처리하면 충분. 단독 우선순위는 중이나 이번 바퀴에서 #2가 먼저 채택됨 |
| 5 | zip 실로드 검사 +4 placenta 파일 | tester #1과 동일 문제 영역. #1 채택으로 인프라가 먼저 갖춰지면 그 뒤에 반영 |
| 6 | zip/png unzip 검사 | #5 선행 인프라(#1 채택)가 필요 — #1 구현 후 후속 |

## 다음 바퀴 권고 1건

**png 실로드 검사 자동화 게이드 강화(#1 채택)** — STATUS 큐#2「기획서 ✅ · 소비처 0곳」와 충돌 없음(소비처0 재검증의 신뢰도를 오히려 높임). 다음 바퀴에서 game_asset_names의 `$"..."` 보간 접두 추출 기능을 구현해 새 PNG의 Resources.Load null 여부를 커밋 전 차단하는 것이 회귀 방어에 가장 효과적.

> 플래너/빌더 회의 파트 파일 미도착으로 의장이 STATUS·PROPOSALS 기준으로 대리 판정했음. 다음 회의에는 3자 동시 도착을 권장.

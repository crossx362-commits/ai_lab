# 정기 회의 20260825-101959 (현행 런 16바퀴 종료 시점)

> 입력: `docs/feedback/PROPOSALS.md` 전체 + 회의 파트 3건(`.council-part-20260825-101959-{planner,builder,tester}.md`) + `docs/STATUS.md` 「다음 할 일」
> 판정 표식: 채택 ✅20260825-101959 · 보류 ⏸20260825-101959 — 등재 줄이 있는 항목만 PROPOSALS.md 해당 줄 끝에 표식(줄 추가 없음). 파트 신설 제안 중 등재 줄이 없는 것은 본 문서에 기록만 남긴다.

## 참석

**planner** — 소비처0 고갈을 스스로 선언했는데(`docs/STATUS.md:43`) 큐에 관문 분기 항목이 0개다. 원장 운영규칙 3상 다음 할 일은 관문② 측정인데 큐는 재스캔·폴리싱 무한 반복을 전제로만 짜여 있고, 표본·척도 확정 요청도 어디에도 안건화돼 있지 않아 더미 판정이 무기한 연장된다.
TASKS.json은 `{"tasks": []}`로 비어 유일 과제 공급원이 폴백(재스캔→폴리싱) 하나뿐 — 「단계 1 관문② 측정 준비」 슬롯 신설(상)·폴백 전환 조건 명기(중)·원장 §22 「지금」 칸 갱신(하)을 제안.

**builder** — 채택 마커(✅20260823-235725)에도 실행 장치에 미반영돼 무변경 검증 랩이 누적 103회 소모 중이고, 최근 20커밋 중 수선 커밋 2건(`a0a14d48`·`5e82a569`)이 인덱스 오염 절차 부재의 재발 증거다.
unity_meas 배치의 -quit 부재로 유니티 종료 대기 최대 15분씩 누수되고 STATUS가 읽기 캡(50KB)을 초과해 소비처(board.py)·사람 모두 비용 증가 — 인덱스 절차 PROMPT §④ 명문화(상)·-quit 필수화(중)·STATUS 히스토리 롤링(하) 제안.

**tester** — 전수 스위프 적신호(ok:false 2/58: HuntBoon·AdvLook)가 최소 36시간째 무주공산인데 바퀴들은 누구도 소진하지 않고 자기 narrow SelfCheck PASS만 보고했다. check_all.py는 게임 SelfCheck 전수를 돌리지 않아 최종 보고의 「check_all exit 0」가 전수 상태를 가린다.
AdvLookSelfCheck는 오너 2026-08-18 「기본=전직 전」 지시로 뒤집힌 옛 계약(`guardian` 단언)이라 게임 버그가 아닌 영구 적신호 — 경보 피로로 진짜 회귀가 묻히는 상태. 스위프 재실행 게이트(상)·계약 갱신(중)·STATUS 머리 SWEEP 상태 명기(하)를 제안.

## 채택 개선안

1. **git 인덱스 오염 방지 절차의 배당 템플릿(PROMPT §④) 명문화** (builder 상)
   - 근거: 수선 커밋 2건 + 동일 계열 재발 금일 09:49(PROPOSALS 233행)까지 3회 — 「자동 장치 없으면 재발한다」 실증.
   - 위치: 다음 바퀴 업무 후보(루프 실행 장치 정비). 커밋 전 `git diff --cached --stat` 확인 + pathspec/temp-index commit-tree CAS를 배당 지시 한 줄에 박는다.
   - 표식: PROPOSALS.md 173행(07:06)·176행(13:5x)에 ✅20260825-101959 (양쪽 처방이 하나의 절차 계열).
2. **unity_meas 배치 `-quit` 필수화** (builder 중)
   - 근거: r56 실측 15분+ 정지 vs 옛 로그 즉시 종료 — 최소 변경으로 모든 UI 폴리싱·SelfCheck 랩 단위시간 직접 절감.
   - 위치: QA 공정 소요 절감 후보. executeMethod 배치 전반에 일괄 적용.
   - 표식: PROPOSALS.md 143행(05:15)에 ✅20260825-101959
3. **전수 스위프 재실행 게이트** (tester 상)
   - 근거: `loop/last_test_report.json` ok:false 36시간+ 지속에 lap 12~16 전부 미소진 — narrow PASS만으로는 전수 상태가 보이지 않음을 실증. 선례: png 실로드 커밋 차단(86행, 기존 ✅).
   - 위치: 다음 바퀴 업무 후보 — **적신호 소진 바퀴(권고 1건) 직후 순번**으로 loop.sh N바퀴마다 GameSweep 재실행, ok:false면 소진 과제 큐 선두.
   - 표식: 등재 줄 없음 — 줄 추가 금지 지시에 따라 본 문서 기록만.
4. **AdvLookSelfCheck 현행 계약 갱신** (tester 중)
   - 근거: `Runtime/UiPages.cs` 오너 지시(2026-08-18, 기본 폴더 반환) vs `AdvLookSelfCheck.cs:42-43` 낡은 단언 — 테스트가 낡은 영구 적신호. 30분 내외.
   - 위치: 권고 1건 세트의 구성 요소(게임 코드·W3Party 무접촉).
   - 표식: 등재 줄 없음 — 본 문서 기록만.
5. **「단계 1 관문② 측정 준비」 방향 채택** (planner 상)
   - 근거: 소비처0 고객 선언(STATUS:43)+더미 무기한 연장+빈 큐가 같은 지점에서 맞닿음 — 원장 운영규칙 3 분기 이행.
   - 위치: 다음 바퀴 업무 후보(§21-1 규격 CSV 초안 작성·오너 승인 요청 안건화까지만 에이전트 실행 가능). **충돌 기록**: STATUS 「다음 할 일」 1~3번은 UI 폴리싱·재스캔을 지시하며 관문 항목이 없다 — 사람 관문 일정은 오너 관문 부채표 관할(더미 진행, 2026-08-24 오너 결정)이라 큐 편집은 하지 않았다.

## 보류

1. **폴백 문구 전환 조건 명기** (planner 중) ⏸20260825-101959 — 사유: STATUS.md:8 편집은 메인 루프 CAS 갱신 대상 문구라 워커별 수기 규칙 신설 부담 + 채택 5번 이행 시 폴백 수렴 수요 자체가 완화. 이행 관측 후 재심의. (등재 줄 없음)
2. **원장 §22 「지금」 칸 갱신** (planner 하) ⏸20260825-101959 — 사유: 제안 스스로 「오너 확인 후」 조건. 원장 문서는 루프 권한 밖. (등재 줄 없음)
3. **agent_runner 무변경 재배정 차단 로직 코드 반영** (builder 회차 판정 요청) ⏸20260825-101959 — 사유: 원 항목(80행)은 이미 ✅20260823-235725 채택·이행 대기라 중복 판정 불요. TASKS.json 현재 비어(planner 관찰 3) 당장 재배정 피해 없음 — **큐 재유입 시점에 이행 최우선 재촉구**. 80행 기존 표식 유지.
4. **STATUS 히스토리 롤링** (builder 하) ⏸20260825-101959 — 사유: board.py가 매 바퀴 읽는 라이브 파일의 대량 행 이동이라 test_board 통과 등 소비처 호환 검증 선행 필요(AGENTS.md 「라이브 런타임 경로 이동 금지」 해석 여지). 50KB 캡 완화 대안(최근 행 압축 등)과 함께 재심의. (등재 줄 없음)
5. **STATUS 머리 SWEEP RED 1단어 명기 의무화** (tester 하) ⏸20260825-101959 — 사유: 채택 3번 게이트의 자동 보고로 대체 가능 + STATUS 머리 수기 편집 의무화는 CAS 경합 사례 다수(07:06·13:5x 계열)라 부담. (등재 줄 없음)

## 다음 바퀴 권고 1건

**전수 스위프 적신호 2건 소진 후 green 재확인 (1바퀴 세트)** — tester 회차 판정 요청 그대로.
① HuntBoonSelfCheck FitsInContent 실패의 실결함/테스트 오류 판정(UiAtlas SlicePad 계열 검사기 뿌리, 95행 참조) → ② AdvLookSelfCheck 현행 계약 갱신(채택 4번) → ③ unity_meas GameSweep 전수 재실행으로 `last_test_report.json` ok:true 확인까지 한 세트.

**충돌 기록(큐 미편집)**: STATUS 「다음 할 일」은 위에서부터 하나만 규칙으로 1번 「지금 개발 직렬: UI 폴리싱 1건」·3번 소비처0 재스캔을 지시하는데, 본 권고는 큐에 없는 검증 신뢰 복구 작업이다. 충돌 사실: 보드 「검증 결과」 칸의 실측원(last_test_report.json)이 36시간+ ok:false라 큐 이행 PASS 보고의 근거 자체가 깨져 있는 상태 — 이 때문에 소진을 선행 권고하되, 큐는 고치지 않았다.

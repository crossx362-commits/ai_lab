# 정기 회의 20260825-074704 (8바퀴 시점)

> 심의 대상: 전회(20260825-055654) 이후 신규 무표식 항목(PROPOSALS:221~:224) + 전회 미심의 잔여(:208·:217·:218) + 회의 파트(builder 1건).
> 바퀴 근거: 전회 4바퀴 이후 작업 랩 4건 실측(`b11cfdcf` UI → `ffb42f89` 코드 → `c332592a` UI → `bb4ae5e9` 코드).
> 판정 표식: ✅/⏸20260825-074704. STATUS 큐는 직접 수정하지 않음(충돌은 사실만 기록).

## 참석 (planner/builder/tester 산출 요약 각 2줄)

- **planner** — 파트 부재. 소집 목록에 있던 `.council-part-20260825-074704-planner.md`가 저장소에 없음(실측: builder 1건만 존재) — 계획 트랙 산입 없음.
- **builder** — 관찰: 같은 ScriptableObject 형제 필드가 바퀴당 1필드씩 분산 닫힘(MobDef 하루 3바퀴 `8c89e69b`→`6f1e1226`→`fc2499d8`, §10-2 연속 2바퀴 `ffb42f89`→`bb4ae5e9`) · 기록 전용 커밋이 최근 20건 중 9건(45%) · QA 네거티브 토글이 화면 탐색 시드까지 끊어 ON/NEG가 다른 탭을 찍음(:222, 미심의).
- **builder** — 제안: STATUS 3번 칸 「하나만 닫는다」 원칙에 동일 에셋 형제 필드 일괄 연결 허용 단서(상) · :222 토글/시드 분리 채택(중) · 기록 전용 커밋 축소(하). 회차 요청: 형제 필드 일괄 연결.
- **tester** — 파트 부재. `.council-part-20260825-074704-tester.md`가 저장소에 없음 — 검증 공정 관점은 builder 관찰과 PROPOSALS 당일 항목(:221~:223)으로만 심의함.

## 채택 개선안 (번호·근거·다음 바퀴 업무 후보로서의 위치)

1. **무인 캡처·네거티브 공정 경화 묶음** — PROPOSALS:221·:222 채택(✅). 근거: 배치모드 실행에서 ScreenCapture가 PNG를 안 만들고 GUI 실행은 정상(07:27 실측 · `edcea936` 자가학습 기록), QA_NO 단독 실행이 속성 탭 대신 장비 탭을 촬영해 ON/NEG 증거가 어긋남(07:34 실측 · builder 파트 인용) — 둘 다 당일 재현된 검증 공정 구멍. 위치: 도구·공정 작업으로 다음 바퀴(UI 폴리싱)와 병행 가능한 선처리 후보. 배치모드 금지 + 종료 후 PNG 존재·치수 검사 강제 + 표시 토글과 탐색 시드 분리(공용 `QA_GO_DETAIL_PAGE`)를 한 묶음으로.
2. **동일 에셋 형제 필드 일괄 연결 허용 단서**(builder 제안1·회차 요청 채택) — 근거: MobDef·§10-2 분산 실측(git log + GAME_WORKLOG:48-51), 통과 기준(소비처 ≥1 + SelfCheck + 네거티브)은 필드별 유지라 품질 저하 없음. PROPOSALS에 독립 줄이 없어 표식 없음(문서 기록만 — :215는 이미 ✅20260825-055654 표식돼 줄 추가 금지). 위치: 다음 소비처0 코드 바퀴부터 적용할 상시 규칙 — 즉시 실행 과제로 삼으면 STATUS.md:8 「직전=코드이므로 UI 폴리싱 1건」 지정과 순서 충돌이므로 규칙 채택으로 해소(충돌 사실 기록).
3. **탑 마지막목숨 빈 Info 행 미그리기** — PROPOSALS:217 채택(✅). 근거: `GameScreen.cs:514` Info()는 빈 문자열도 금테 칸을 그리며(`InfoWrap` :536은 IsNullOrEmpty skip — 동파일 대비 실측), `TowerScreen.cs:186`이 `LastLifeWarn.GearRest()`의 빈 문자열을 그대로 넘겨 장착 없음 시 빈 금테 한 줄 잔존(:217 본문 실측). 위치: STATUS 큐 「직전=코드이므로 UI 폴리싱 1건」 슬롯의 내용물 — 아래 권고 그 자체.

## 보류 (사유 필수)

1. **PROPOSALS:208(월드맵 헤더 부제·캡션 중복)** ⏸ — 사유: 처방 실행 완료. `b11cfdcf`(06:22) FieldCaption이 헤더 보유 줄을 비우고 QA_NO_EXPLORE_DUP 네거티브 + WorldExploreSelfCheck 추가 실측.
2. **PROPOSALS:218(영지 팔레트 남서 잔디 잔여 겹침 + 오너 Unity Play 중 스크립트 미실림)** ⏸ — 사유: STATUS.md:44가 해당 칸을 `37705577` 닫음으로 기록한 상태라 재착수 시 이중 판정. 잔여 겹침 주장은 새 증거 샷을 갖춘 별도 제안으로. stop→refresh→play 절차 조언은 채택 #1 실행 절차에서 참고.
3. **PROPOSALS:223(슬림 정보줄 한글 아랫획 잘림 → SelfCheck CalcHeight 단언)** ⏸ — 사유: 결함 수정+SelfCheck 선실행. `c332592a`(07:37) 28px 한글 획 칸 + CompactInfoSelfCheck PASS·네거티브(옛 20px) 비교 실측. 잔여 「대표 한글 렌더 크롭 비교」 강화는 별도 과제로 추적 가능하나 핵심 처방은 닫힘.
4. **builder 제안3(기록 전용 커밋 축소)** — 보류. 사유: 근거가 git log 가독성 통계(45%)뿐이고 히스토리 행 분리는 PROMPT §④ 선택 반영 절차와 결합돼 있어 지금 묶으면 문서 세션 경로를 흔들 위험이 이득보다 큼(하). PROPOSALS 독립 줄 없어 표식 없음(문서 기록만).

> 비고: :224(「없음」 자가학습 행)는 처방이 없는 중복 확인 기록이라 심의 대상에서 제외 — 무표식 유지. 전회 미표식 잔여(:94~:206 중 미도달분)는 신규 실측 변화가 없어 재판정하지 않음.

## 다음 바퀴 권고 1건

**UI 폴리싱 1건 — 탑 마지막목숨 경고의 빈 Info 행 미그리기(채택 #3 = PROPOSALS:217).**
`Info()`에 빈 문자열 skip(`InfoWrap` :536 선례) 또는 `TowerScreen.cs:186` 호출측 가드를 넣고, SelfCheck(빈 문자열 입력 시 패널 0개)와 네거티브(QA_NO면 옛 빈 금테 한 줄 재현)를 함께. 샷 증거는 채택 #1의 경화된 캡처 공정 사용.

충돌 기록(큐 미수정): STATUS 큐와 순서 충돌 없음 — STATUS.md:8·:40이 「직전=코드이므로 UI 폴리싱 1건」을 지정하고 본 권고가 정확히 그 슬롯의 내용물. 다만 STATUS.md:44 말미 포인터 「다음 폴리싱: GameScreen Body 한 곳 절단」은 `0fc2eb14`(05:36)로 이미 닫힌 낡은 포인터(전회 보류 #3 판정 유지) — 큐를 고치지 않고 본 권고가 그 자리를 대신한다는 사실만 기록.

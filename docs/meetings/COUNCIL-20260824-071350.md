# 정기 회의 20260824-071350 (8바퀴 시점)

> 의장 문서 · 참석 파트: `.council-part-20260824-071350-{planner,builder,tester}.md`
> 판정 표식: `docs/feedback/PROPOSALS.md` 해당 줄 끝 — ✅20260824-071350 채택 · ⏸20260824-071350 보류
> 전례 회의: `COUNCIL-20260823-235725.md` (채택 1번 agent_runner 건너뛰기를 이번 회의가 실행 확정으로 재심의)

## 참석

**planner**
- 소비처0 큐 바닥에 로드맵 에스컬레이션이 미반영됐다 관찰(원장 §22 운영규칙 3 — 관문② 루프 몫=CSV훅이 다음 할 일 어디에도 없음) · 채택(`✅20260823-235725`)된 건너뛰기 처방이 큐 적용으로 이어지지 않아 git log -20 전부가 무변경 문서 재검증 커밋(lane-doc 70차·keeper-warn-chip 67차·proposals-triage 74차) · 전수검사 FAIL 2건(HuntBoonSelfCheck·AdvLookSelfCheck)이 「기존 결함」 분류 후 큐 밖 방치(last_test_report.json 덮어쓰기로 유일 포인터 소실 위험).
- 제안: agent_runner 선반영 감지를 TASKS.json 큐에 실적용(상) · 다음 할 일을 원장 순서로 재정렬+관문② CSV훅 초안 삽입(중) · GameSweep 기존 결함 2건 큐 편입(중). 판정 요청 = 상안 실행 확정.

**builder**
- 채택 처방의 코드 구현이 부재하다 관찰(`loop/agent_runner.py`에 is-ancestor/선반영/건너뛰기 grep 0건 · TASKS.json에 반복 과제 3개 잔존 — 게임 코드 커밋은 최근 20건 중 1건뿐) · 병렬 세션 STATUS 동시 커밋 경합이 고아 커밋·언와인드 사고를 반복 낳음(d1b38ef3 CAS 거절 재시도 · c1aa355a 고아화 · `390a5868` 타 세션 스테이징 쓸려들기→`37239562` 언와인드) · PROPOSALS 정리 검사 마커 50회차 누적으로 전문 읽기 비용 증가(현행 항목은 14건).
- 제안: agent_runner 선반영 감지·건너뛰기 실코드 구현+큐 소비(상) · STATUS 행 1행 커밋 절차 공유 스크립트화(중) · 정리 마커 ARCHIVE 이전 절차(하). 판정 요청 = 제안 1 최우선.

**tester**
- 전수 리포트가 만료 red로 고정이다 관찰(`last_test_report.json` 04:13 ok:false 2/32 — 이후 코드 커밋 3건 통과(25e5b3ca·927ce693·`390a5868`)·스윕 32→33행 확장으로 FamilyAdvSelfCheck까지 감시망 밖) · red 2건(HuntBoon 금테·AdvLook 역할키)에게 소유자가 없다(「회귀 아님」 판정이 커밋 메시지에만 존재) · CharHudSelfCheck의 클램프 상자==그리기 상자 동일성 가정으로 「검사 통과·화면 결함」 재발(2026-08-15 유형 — 현재 red인 HuntBoon 금테도 같은 계열).
- 제안: C# 변경 바퀴 종료 조건에 전수 재실행+리포트 갱신 의무화(상) · red 항목 자동 큐화(중) · SelfCheck 단언 기준 1줄 규칙화(하 — 02:05 항목 명시 지지). 판정 요청 = 첫 코드 바퀴에서 GameSweep 전수 재실행+HEAD 기준 리포트 갱신.

## 채택 개선안

1. **agent_runner 선반영 감지·재배정 건너뛰기 실코드 구현 + TASKS.json 반복 과제(proposals-triage·keeper-warn-chip·lane-doc) 큐 소비** — PROPOSALS.md 21:34 병합 항목. 기존 ✅20260823-235725 유지(중복 표식 없음) — 본 회의는 **미이행 상태의 실행 확정**으로 재심의.
   - 근거: planner·builder 회차 판정 요청 2표 일치 + tester 공통 선행 인정(3/3). builder 실측 — 구현 grep 0건, 채택 후에도 65~74차 재배정 누적, git log -20 중 게임 코드 1건. 전 회의 채택(1번)이 바퀴 예산을 회수하지 못한 것이 오늘 관찰 전부의 근원.
   - 위치: 다음 바퀴 업무 후보 1순위 → 「다음 바퀴 권고」로 승격.

2. **GameSweep 전수 재실행 + last_test_report.json HEAD 기준 갱신(red 2건 유효성 재확정 포함, 33행 FamilyAdvSelfCheck 첫 전수 편입)** — tester 제안(상) 전단+판정 요청, planner 제안 3 동조. PROPOSALS.md 대응 줄 없음(줄 추가 금지로 표식 불가 — 본 문서에 기록).
   - 근거: 유일 전수 표면이 04:13 스냅샷(ok:false 2/32)인데 이후 커밋 3건 통과·스윕 33행 확장 — 현재 HEAD의 전수 통과 보증 전무. 비용 바퀴 1회분, 이후 폴리싱 바퀴 전체의 「진짜 회귀 vs 오래된 red」 구분 지반(tester).
   - 위치: 채택 1번 바퀴의 **완료 조건**으로 부여(별도 바퀴 소비하지 않고 종료 조건에 병합).

3. **공유 저장소 커밋 경합 방지 절차(커밋 전 `git diff --cached --stat` 확인 · pathspec 커밋 또는 temp-index commit-tree + update-ref CAS · HEAD 되감기(reset·amend) 금지·commit-tree 수선)** — PROPOSALS.md 07:06 항목 ✅20260824-071350.
   - 근거: builder 관찰 2의 동일 사고 계열 실측 3건(CAS 거절 재시도·고아화·덮어복구) + STATUS.md:47 오염 이력(타 세션 삭제 2건 쓸려들기·`37239562` 언와인드) — 문서 정리 세션 3커밋이 자가 복구에 소모된 실비용.
   - 위치: 채택 1번 구현 바퀴부터 모든 워커가 즉시 적용하는 작업 규격(공유 스크립트화는 채택 4번과 함께 후속).

4. **셀 닫음 판정 시 STATUS 머리·3번 칸 포인터 동시 갱신 절차 명문화(+히스토리 행 1행 커밋 공유 스크립트)** — PROPOSALS.md 05:17 항목 ✅20260824-071350.
   - 근거: builder 제안 2가 지지 — §10-3 이중 판정 실측(닫음 판정 후 포인터 잔존으로 재검증 진입) + 수기 CAS 매바퀴 재구현이 경합 사고(채택 3번 근거)의 구조적 온상이라는 builder 분석.
   - 위치: loop 도구 트랙 후보 — 채택 1번(예산 회수) 다음 순서.

5. **unity_meas 배치(-batchmode -executeMethod) -quit 플래그 필수화** — PROPOSALS.md 05:15 항목 ✅20260824-071350.
   - 근거: r56 실측 — 검사 완료·로그 기록 뒤 종료 단계 15분+ 정지 vs 옛 로그 「Exiting batchmode successfully」 즉시 종료. 채택 2번(전수 재실행 상시화)의 측정 인프라 신뢰성이 이 1줄에 매달림 — 바퀴 시간이 유니티 종료 대기로 소진되면 전수 재실행 의무화가 공짜가 아니게 된다.
   - 위치: 다음 unity_meas 배치를 실행하는 바퀴부터 적용하는 플래그 처방(단독 할당 불필요).

6. **SelfCheck 단언 기준 규칙화 — 단언 rect는 사용자 가시 영역 기준, 클램프 상자==그리기 상자 동일성 가정 금지** — PROPOSALS.md 02:05 항목(maxPad 상한 점검·치수 하한 단언) 및 07:06 항목(동일성 가정 점검 확장) ✅20260824-071350 각각.
   - 근거: tester 관찰 3 — CharHudSelfCheck이 stage 클램프 동일성 가정으로 장비 라벨 여백 침범을 못 잡은 자기 기록(lap 로그 :798, 2026-08-15 유형 재발) + tester 제안(하)이 02:05 항목을 원문 인용해 지지·확장. 현재 red인 HuntBoon 금테 검사도 같은 「프레임 내부 영역」 판정 계열.
   - 위치: 다음 UI 폴리싱 바퀴(STATUS 큐 #4 진행 중 트랙)부터 게임 C# 커밋 체크리스트 1줄로 부수 적용.

## 보류

1. **PROPOSALS.md 정리 검사 마커 ARCHIVE 이전**(builder 제안 3, 대응 줄 없음) — 사유: 「기존 내용 삭제 없음」 원칙이 실제 복구 절차로 작동 중임이 실측됨(triage 72차 — 타 세션 커밋 `390a5868` 스냅샷 잔해 편입으로 유실된 70·71차 마커를 바이트 기준 복구). 마커 비대 문제 자체는 인정하나 대량 이관은 그 복구 경로와 충돌한다. 소비처(board·agent_runner·각 세션 읽기) 영향 설계 후 재심의.
2. **다음 할 일 큐 재정렬 + GameSweep 결함 2건 큐 편입**(planner 제안 2·3, 대응 줄 없음) — 사유: STATUS 큐 직접 수정은 본 회의 지시상 의장 권한 밖. 필요성은 채택 2번(red 재확정·리포트 갱신)과 채택 1번(반복 과제 큐 소비)이 흡수하며, 결함 추적 표면화는 tester 제안 3번(아래)과 함께 loop 측 해법으로 이관.
3. **red 항목 자동 큐화**(tester 제안 중, 대응 줄 없음) — 사유: 방향은 찬성하나 순서 문제 — 채택 2번의 재확정 결과(2건이 회귀인지 오래된 red인지)를 먼저 받아야 큐화 라벨·만기 규칙이 정해진다. 채택 1번(agent_runner 구현) 바퀴에 병합 검토.
4. **C# 변경 바퀴 종료 조건 전수 재실행 «상시» 의무화 + keeper 리포트 신선도 칩**(tester 제안 상 후반부, 대응 줄 없음) — 사유: 1회성 재실행은 채택 2번으로 채택했으나, 상시 규칙화·기계 검사(kChip 확장)는 채택 1·4번이 만든 기반 위에서야 검증 가능. 후속 회의 안건으로 유지.

> 본 회의에서 보류 판정은 전부 PROPOSALS.md 대응 줄이 없는 신규 제안이라 ⏸ 표식 0건(기존 ⏸20260823-235725 4건 변동 없음). 심의 대상 외 미판정 항목(00:45·03:10 두 건 등)은 무표식 유지.

## 다음 바퀴 권고 1건

**agent_runner 선반영 감지·재배정 건너뛰기 구현 + TASKS.json 반복 과제(proposals-triage·keeper-warn-chip·lane-doc) 큐 소비**(채택 1번) — 완료 조건에 GameSweep 전수 재실행·last_test_report.json HEAD 기준 갱신(채택 2번)을 부과해 tester 판정 요청을 같은 바퀴에 흡수한다. 판정 요청 3/3 일치.

충돌 기록: STATUS.md 「다음 할 일」#1/#4는 「지금 개발 직렬: UI·아트 상시 폴리싱」(다음 화면=ConceptLine LabelClip·상세 패널 스탯 목록 마지막 줄 금테 접촉)을, #3은 「남은 알려진 오픈 셀 없음 — 다음 바퀴는 원장 재스캔」을 지정한다. 본 권고(loop 도구 코드)는 이 어느 쪽 큐 항목과도 일치하지 않는다. 아울러 planner는 큐 재정렬(중)을, tester는 전수 재실행 최우선을 요청해 권고 순서와 불일치가 3중으로 존재한다. 큐는 수정하지 않았으며 충돌 사실만 기록한다.

# 정기 회의 20260827-065728 (n바퀴 시점)

## 참석

- **planner**: (파트 파일 미도착 — 이 세션에서 미작성)
- **builder**: (파트 파일 미도착 — 이 세션에서 미작성)
- **tester**: last_test_report.json 시점(02:58)과 최신 커밋(06:37~06:50) 사이 검증 공백 지적 — 티어 아트 반입 5건 커밋 후 GameSweep 200건 재실행 미완. 정예 4종 배치 SelfCheck 5바퀴째 누적(소스 계약 통과≠실배치). 권한 게이트 반복으로 소스 계약 이외 추가 검증 경로 필요성 지적. `last_test_report.json` 재실행 최우선 권고.

## 채택 개선안

| # | PROPOSALS 라인 | 요약 | 근거 | 다음 바퀴 업무 후보 |
|---|---|---|---|---|
| 1 | 298 (01:45) | `loop/run_selfcheck.sh` 래퍼 스크립트 작성 — loop 세션의 유니티 배치 허용 커맨드를 단일 경로로 고정해 세션별 권한 편차 제거 | tester: 권한 게이트 4바퀴 연속 반복으로 배치 검증 공백 지속. 01:35·02:15 관찰에서 유니티 경로가 세션마다 다른 권한 프로파일로 막힘. 래퍼가 확보돼야 EstateBuildingsSelfCheck·정예 SelfCheck 배치 통과 가능 | **STATUS 큐 1 티어 파생 잔여 검증 경로 확보** — 래퍼 작성 후 8동 전수 EstateBuildingsSelfCheck 배치 통과 1회 |
| 2 | 279 (09:20) | GameFullCheck 전수 4바퀴 의무 실측 등록 — 큐레이션 스위트(GameSweep)만 돌면 낡은 계약·신규 결함이 무기한 잠복 | tester: 정예 4종 소스 계약 통과≠실배치 검증. 직전 바퀴 GameFullCheck 전수 첫 실행으로 11건 FAIL 일괄 노출(Skill 6종·BossBattleDps·MemberStyle 등). 큐레이션만으로는 검증 사각지대 존재 입증 | **정기 바퀴 중 4바퀴마다 GameFullCheck 전수 1회** — 다음 바퀴부터 의무화 |
| 3 | 281 (09:40) | SweepTen류 러너 catch 시 `InnerException.StackTrace` 병기 + 스택 오독 의심 건 단독 executeMethod 재현 절차 | tester: BossBattleDps NRE가 Debug.Log 내부 스택으로 오독돼 원인을 종족 경로로 잘못 지목하는 사고 재발 선례 확인 | **검증 러너의 예외 처리 갱신** — 다음 검증 코드 바퀴에서 SweepTen·GameFullCheck 러너에 스택 병기 반영 |
| 4 | 231 (00:54) | Unity MCP manage_camera 샷 실패 시 `execute_code` + `ScreenCapture.CaptureScreenshot`로 곧바로 전환 | tester: manage_camera 브리지 살아있어도 "No Unity Editor instances found"로 실패 빈도 잦음. 같은 세션 execute_code ScreenCapture는 성공 실측. 샷 폴백 미 확보 시 시각 검증이 카메라 도구 한 길에 묶임 | **PROMPT §③에 샷 폴백 규칙 명문화** — 다음 바퀴에서 반영 |

## 보류

| # | PROPOSALS 라인 | 요약 | 사유 |
|---|---|---|---|
| 1 | 18 (18:20) | MCP 브릿지 재연결 자동화(재시도 헬퍼 함수화) | 채택 1(래퍼 스크립트)이 먼저 확보돼야 브리지 권한 구조가 안정화됨. 래퍼 작성·실증 이후에 재시도 로직을 붙이는 게 순서 적절 |
| 2 | 287 (10:56) | 랩 종료 시 `SESSION_HANDOFF.md`에 미커밋 파일 강제 기록 | 현재 루프가 opencode 실행기로 안정 가동 중. SESSION_HANDOFF 표준화는 실행기 전환(예: claude 복귀) 시점에 함께 도입하는 게 비용 대비 효과 큼 |

## 다음 바퀴 권고 1건

**STATUS「다음 할 일」1번(UI·아트 상시 폴리싱) — 잔여 티어 2동(tower·auction) 닫기.**

충돌: tester 권고(`last_test_report.json` 재실행·정예 4종 SelfCheck)와 충돌 — tester는 검증 공백 해소를 최우선으로 보고, STATUS는 아트 진행을 최우선으로 본다. 채택 1(래퍼 스크립트)이 다음 바퀴에서 작성되면 양쪽을 동시에 해소할 수 있다(래퍼로 배치 검증 → tower 티어 파생+배치 SelfCheck). 래퍼 미작성 시 tester 권고优先.

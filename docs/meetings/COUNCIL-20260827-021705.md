# 정기 회의 20260827-021705 (정예 군단장 완료 시점)

## 참석

- **planner** (STATUS.md「다음 할 일」 기반): 정예 특수능력 4종 중 수호자·군단장 완료, 큐#3 셋째(저주술사) 착수 대기. 영지 아트 ox-alpha 8종 전부 완료. `_mKind[i] >= 3` 정예 관용구 오분류가 builder·tester 양쪽에서 시급 지적됨.
- **builder**: `_mKind[i] >= 3` 관용구가 kind 5(돌진형=잡몹)를 정예로 오분류 — 수호자·군단장 추가 후 괴리가 커짐. `IsElite(kind)` 명시 술어 리팩터(우선순위 상), `loop/run_selfcheck.sh` 래퍼 작성(상), GameFullCheck 4바퀴마다 의무(중)를 제안.
- **tester**: EliteKindsSelfCheck에 네거티브 단언 누락( kind 0·1·2·5의 HP·드랍 경로 미진입 검증 없음). 8바퀴 연속 인게임 검증 공백(유니티 배치·MCP 권한 게이트). `IsElite` 술어 추출 + 네거티브 SelfCheck 추가(우선순위 상), 래퍼 스크립트(중), BattleScreen 필드 EliteDrop.LastLine 노출(하)을 제안.

## 채택 개선안

| # | 개선안 | 근거 | 다음 바퀴 업무 후보 |
|---|---|---|---|
| 1 | **`IsElite(kind)` 명시 술어 리팩터 + 네거티브 SelfCheck** — `_mKind[i] >= 3` 관용구를 `IsElite(kind)` 순수함수(3·4·6·7 true, 5 false)로 교체하고, `EliteKindNegativeSelfCheck`를 신설해 kind 0·1·2·5에서 HP·드랍·FX 경로가 진입하지 않는지 단언 | builder(상)·tester(상) 양쪽 동의, PROPOSALS line 302·303 관찰. 정예 4종 구현 완료 시점에서 가장 시급한 기술 부채 | **큐#3 셋째(저주술사) 착수 전 필수 선행** — STATUS「다음 할 일」3번과 직접 연결 |
| 2 | **`loop/run_selfcheck.sh` 래퍼 스크립트 작성** — Unity.app 절대경로를 작업 디렉터리 내 래퍼로 감싸 loop 세션 허용 커맨드에 등록. 작성 즉시 8종 EstateBuildingsSelfCheck 배치 통과로 A/B 8종 증거 기반 확정 | builder(상)·tester(중) 제안, PROPOSALS line 297·298·301. 8바퀴 아트 반입의 배치 검증 공백 해소 | 다음 코드 바퀴에서 작성 권장 |
| 3 | **GameFullCheck 4바퀴마다 의무 실측** — 큐레이션(GameSweep 63종)만 돌면 낡은 계약·신규 결함이 무기한 잠복. 4바퀴마다 전수 1회 의무화 | builder(중) 제안, PROPOSALS line 279(이전 회의 채택 기록 재확인). 검증 커버리지 확보 | 다음 정기 바퀴에서 의무 등록 |

## 보류

| # | 개선안 | 사유 |
|---|---|---|
| 1 | **BattleScreen 필드 survived 분기 `EliteDrop.LastLine` 노출** | tester(하) 제안. 정예 드랍 확대 시 UX 사각지대이지만, 큐#1 잔여(필드 정예·전투 보정)와 묶어 후속 바퀴에서 처리하는 것이 순서에 맞음 |
| 2 | **EliteAura 공용 베이스(스냅 배열+반경 조회+오라 판정 순수함수) 추출** | builder(중) 제안. 4종(수호자·군단장·저주술사·처형자) 모두 완료된 뒤 회고 리팩터로 처리하는 것이 적절 |

## 다음 바퀴 권고 1건

**`IsElite(kind)` 술어 추출 + 네거티브 SelfCheck** — 저주술사(큐#3 셋째) 착수 전 필수 선행. STATUS「다음 할 일」3번의 정예 특수능력 구현 흐름과 충돌 없음(오히려 선행 요구).

> planner 파트 파일(`.council-part-*-planner.md`) 미존재로, STATUS.md「다음 할 일」목록을 planner 요약으로 대체했음.

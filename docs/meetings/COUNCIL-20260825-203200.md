# 정기 회의 20260825-203200 (현행 런 1바퀴 시점)

> 시점 근거: 최종 랩 로그 `logs/2026-08-25/lap-20260825-151350-1.log`(런 15:12 시작 · lap 1 · Claude OAuth 만료로 code=1 종료) 이후 공급자 3종 전부 소진(STATUS.md:7)으로 신규 랩 없음 — 20:31 오너 재개 지시 대기 중 개최.
> 의장 주의: 오너 지시상 PROPOSALS.md는 줄 추가 금지·기존 항목 줄 끝 표식만 허용이므로, 이번 회의 신규 제안(tester 발의 검증 신호 화해 규칙 등)은 본 문서에만 채택 기록하고 표식은 심의된 기존 항목 4건 줄 끝에만 덧붙였다. 그 외 미판정 항목은 이번 의제 밖이라 표식 없이 보존. 커밋은 하지 않음.

## 참석 (planner/builder/tester 산출 요약 각 2줄)

- **planner**: ① 큐가 빈 상태(`loop/TASKS.json` tasks:[])인데 원장 운영규칙 3(`GAME_DESIGN_ASHES_TO_STARS.md:1868~1869`)이 지정한 다음 단계(관문② 측정)가 큐에 없고 STATUS:7 ORDERS 3건만 오너 대기다. ② `loop/last_test_report.json`(09:23) FAIL 2건(HuntBoonSelfCheck·AdvLookSelfCheck)이 열린 채 STATUS는 해당 트랙을 닫음으로 적어 재개 루프가 미해결 회귀 위에서 출발할 위험과, ✅ 채택 처방의 실행 추적 부재(`a27b3d2b` 병합 항목 89~103차 재배정 누적 후 흔적 없이 소멸)를 지적했다.
- **builder**: ① 폴리싱 코드 커밋마다 「문서:」 커밋이 1:1 동반돼 직전 20커밋에서 커밋 수가 배증했고(git log 실측) CAS 스냅샷 경합·포인터 만기 창도 매번 배가된다. ② 사용량 소진 중단이 반복되고(인수인계 5건/20커밋) OnGUI 캡처 한계를 랩마다 재학습 중이라며, player-facing 판정 절차 고정(PROPOSALS 19:15 항목)을 상 우선으로, 공용 PlayerCopy 통합 방침 판정(18:50)·OnGUI 캡처 ScreenCapture 통일(02:05) 채택을 요청했다.
- **tester**: ① 검증 신호(`last_test_report.json` ok:false · 09:23 고정 · 작업 트리 미커밋 65행)와 STATUS 「닫음」 판정(STATUS.md:59·63)이 모순된 채 FAIL 2건이 소유자 불명으로 방치됐다. ② 스테이징 혼입 사고가 `423d97f3`까지 동일 사고 2회째(선례 `390a5868`)이고 check_all.py가 영구 exit 1로 사실상 사망했다 — 인덱스 가드(PROPOSALS:250) 조기 채택과 ok:false 화해 규칙 신설을 요청했다.

## 채택 개선안 (번호·근거·다음 바퀴 업무 후보로서의 위치)

1. **GameSweep 열린 FAIL 2건 선소화 + ok:false 화해 규칙**(tester 제안 1·planner 제안 1·양측 회차 판정 요청 수렴 — 신규 제안이라 본 문서에만 기록) — 근거: 세 참석자 모두 `last_test_report.json`(2026-08-25 09:23, ok:false 2/58)과 STATUS 닫음 표기의 모순을 독립 실측했고 보드가 이 파일을 상태 입력으로 읽는다(`loop/board.py:51`). 위치: 다음 바퀴 최우선 업무 후보 — 재실측 후 회귀면 「수리」, 스테일이면 「판정 갱신」으로 분류·종결하고 SelfCheck 영향 커밋 후에는 전수 스윕을 재생성한다.
2. **player-facing 판정 절차 — grep 카운트 단독 판정 금지·소비처 추적 필수화** (`docs/feedback/PROPOSALS.md:25` [19:15, 상] → ✅20260825-203200) — 근거: builder(상)·planner 동시 지지 + grep-only 오판 1건이 STATUS.md:9~15 전면 정정으로 번진 실측(게이트 뒤 QA 오버레이를 리크로 오판). 위치: 별도 작업 칸이 아니라 잔여 §-leak 검증·원장 재스캔 바퀴에 즉시 적용하는 공정 규칙.
3. **자동 루프 커밋 직전 인덱스 가드** (`docs/feedback/PROPOSALS.md:250` [09:49, 상] → ✅20260825-203200) — 근거: tester(상) — 동일 스테이징 혼입 사고 2회(`390a5868`·`423d97f3`) 실측, `git diff --cached --name-only` 허용 경로 일치 강제는 구현 수 줄 이내. 위치: 루프 재개 직후 소화 가능한 짧은 과제 후보(공용 가드 1건).
4. **공용 PlayerCopy 통합 방침 채택** (`docs/feedback/PROPOSALS.md:19` [18:50, 중] → ✅20260825-203200) — 근거: planner 제안 3·builder 제안 2가 모두 「방침 판정」을 요청했고 UI 폴리싱 캠페인 마감으로(AuctionHud 방어적 로컬 복제 추가 실츬, STATUS.md:16~17) 신규 복제 증가를 끊을 타이밍이다. 위치: 즉시 리팩터링 착수가 아니라 화면 코드에 손대는 바퀴에서 신규 로컬 복제 금지·공용 정적 유틸(`Runtime/PlayerCopy.cs`류, EnvNo 접두사 매개변수) 수렴을 강제하는 상시 방침 — 전면 통합 작업 자체는 권고 1건 이행 뒤 후순위 후보.
5. **OnGUI 게임 화면 캡처 ScreenCapture 단일 경로 확정** (`docs/feedback/PROPOSALS.md:113` [02:05, 중] → ✅20260825-203200) — 근거: builder(하) — WORKLOG 검수 줄에서 이미 사실상 표준 수행, 유사 관찰 다수 동조(동 파일 00:54·02:42·05:25 항목), 문서화 채택 비용 거의 없음. 위치: 절차 문서 반영 1건 수준의 저비용 과제 후보.

## 보류 (사유 필수)

1. **관문② 착수 패키지 작성·큐 등록**(planner 제안 2) — 사유: 원장이 표본·척도·CSV 규격의 오너 확정을 측정 선행 조건으로 명시(`GAME_DESIGN_ASHES_TO_STARS.md:1881~1883`)해 루프 단독 착수 불가 + STATUS 큐 편집은 의장 권한 밖(오너 지시). 권고 1건(FAIL 소화) 이행 후 다음 업무 후보로 후순위 유지.
2. **check_all.py WARN 백로그 청소 또는 exit 코드/WARN 신호 분리**(tester 제안 3) — 사유: 하네스 읽기 전용 원칙(AGENTS.md) 유지가 전제라 랩 보고 양식 분리만 가능한데, WARN 원인 목록화가 선행되지 않아 분리 기준이 미확정이다. 경보 피로 해소 취지는 채택하되 원인 정리 후 재심의.
3. **✅ 채택 표식에 「구현 커밋 해시 또는 미구현 사유」 병기 관행**(planner 제안 4) — 사유: 표식 형식 변경이 proposals-triage 등 ✅ grep 소비처와 상호작용해 영향 확인이 필요하고, 채택 항목의 다음 회의 재심의 자동 등록 규칙까지 묶인 설계 결정이라 다음 정기 회의 심의로 회부.

미처분: 이번 3파트 의제에 오르지 않은 기존 미판정 항목(예: 18:20 브릿지 재연결 자동화(하) 등)은 표식 없이 그대로 보존하며 다음 정기 회의 심의 대상으로 남긴다.

## 다음 바퀴 권고 1건 (STATUS 큐와 충돌하면 충돌 사실만 기록 — 큐를 직접 고치지 마라)

**권고**: 루프 재개(Claude 로그인 복구) 후 첫 바퀴를 신규 작업 금지 1바퀴로 삼아 unity_meas GameSweep 전수 재실행으로 새 `last_test_report.json`을 확보하고, 열린 FAIL 2건 — ① HuntBoonSelfCheck 가로 카드 아이콘·글씨 금테 이탈 ② AdvLookSelfCheck 수호기사 tank 역할 폴더 낙하 — 을 「재현→수리」 또는 「스테일 보고서 판정 갱신」으로 분류·종결한다. 완료 기준은 ok:true 전수 스냅샷이며, 확보 전까지 폴리싱·재스캔 바퀴의 PASS 판정은 참고치로만 취급한다(planner 제안 1·tester 회차 판정 요청 존중).

**충돌 기록**: STATUS.md 「다음 할 일」 1번(영지 §6 아트/UI 폴리싱 직렬)과 3번(「남은 알려진 오픈 셀 없음 — 다음 바퀴는 원장 재스캔으로 새 칸 발굴」, STATUS.md:62)은 본 권고(FAIL 2건 재실측 선소화)와 다음 바퀴 순서가 충돌한다. 큐 문구는 수정하지 않고 충돌 사실만 여기에 기록한다.

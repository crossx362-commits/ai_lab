# 정기 회의 20260823-235725 (4바퀴 시점)

> 의장 문서 · 참석 파트: `.council-part-20260823-235725-{planner,builder,tester}.md`
> 판정 표식: `docs/feedback/PROPOSALS.md` 해당 줄 끝 — ✅20260823-235725 채택 · ⏸20260823-235725 보류

## 참석

**planner**
- 무변경 재검증 랩(keeper-warn-chip 19차·lane-doc 19차·proposals-triage 24차)이 커밋 예산을 잠식하고, 전수 SelfCheck 리포트가 `2026-08-19`(129/130 · BossBattleDpsSelfCheck FAIL)로 4일째 부실이라 관찰. 큐 구조 자체는 원장 운영규칙 3과 정합하나 원장 §22에 닫힌 항목(EstateBuild §2-3 `25559505`)이 잔존.
- 제안: agent_runner 선반영 감지 건너뛰기 착수(상) · 전수 SelfCheck 재실행+리포트 갱신 필수화(상) · 낡은 포인터 정리 절차를 원장 §22까지 확장(중). 판정 요청 = agent_runner 건너뛰기 채택·구현.

**builder**
- 최근 커밋 20건 중 18건이 무변경 재검증·마커 보정 문서 전용 커밋이고, 낡은 「다음」 포인터(초필살기 `24ef7e47`·ConceptLine fold `77cee37a` 선반영 잔존)가 바퀴를 통째로 소비했다 관찰. STATUS 닫음 인라인 누적 + PROPOSALS 마커 11개로 관리 비용도 함께 증가.
- 제안: png 실로드 검사 추가(상) · agent_runner 건너뛰기 처방 조속 채택(중) · STATUS 닫음 인벤토리 아카이브 분리(하). 판정 요청 = agent_runner 선반영 감지 구현·적용.

**tester**
- 전수 SelfCheck가 129/130(2026-08-19) 이후 미재실행 + 알려진 FAIL(BossBattleDpsSelfCheck)이 환경/코드 미판정 방치, OFF 네거티브가 원 결함을 재현 못 한 채 닫음(lap 로그 :248·:309), 커밋 인용 증거가 전부 git 제외 경로(results/·output/*)에 있음을 관찰.
- 제안: last_test_report.json 신선도 게이트(상) · 네거티브 재현 의무화+QA 캡처 해상도 고정(중) · 증거 무결성 기록(중)+png 실로드 검사 지지. 판정 요청 = 전수 SelfCheck 재실행·갱신을 회차 최우선으로.

## 채택 개선안 (번호·근거·다음 바퀴 업무 후보로서의 위치)

1. **agent_runner 선반영 감지·재배정 건너뛰기·큐 소비** — PROPOSALS.md 21:34 항목(23:56 lane-doc 건 병합, 동일 처방)
   - 근거: 최근 커밋 20건 중 18건이 무변경 랩(builder 실측)·lane-doc 20바퀴 연속 재배정 실측. planner·builder 회차 판정 요청 2표 일치, `a27b3d2b` 병합 처방과 동일.
   - 위치: 다음 바퀴 업무 후보 1순위 → 「다음 바퀴 권고」로 승격.
2. **last_test_report.json 신선도 게이트 + 전수 SelfCheck 재실행·갱신(BossBattleDpsSelfCheck FAIL 재판정 포함)** — tester 제안1·판정요청 + planner 제안2 동조. PROPOSALS.md 대응 줄 없음(줄 추가 금지로 표식 불가 — 본 문서에 기록).
   - 근거: 전수 리포트 2026-08-19 이후 미갱신, 그 사이 수십 랩의 개별 SelfCheck만 PASS라 현재 HEAD의 전수 통과 보증이 전무. 비용 1바퀴 이하, 이후 모든 닫음 판정의 지반(tester).
   - 위치: 권고 항목 다음 바퀴 후보. 부실 리포트 경고 칩(board.html kChip 재용, planner 제안2 후반부)은 그 다음 순서.
3. **game_asset_names PNG 실로드(null) 커밋 전 차단 검사** — PROPOSALS.md 22:55 항목(상)
   - 근거: fx_dash_trail 3장이 TextureImportRules 우회(Multiple+PPU100)로 `Resources.Load<Sprite>` null 실측 — §6-A 「스킬 이펙트 로딩 상시 실패」 동일 결함의 자동 장치 없는 재발(builder 상 · tester 명시 지지).
   - 위치: 1·2순위로 회수된 바퀴 예산으로 착수할 코드 트랙 후보.
4. **낡은 포인터 정리 절차 + 원장 §22 확장** — PROPOSALS.md 23:45 항목(중) + planner 제안3 확장안
   - 근거: 타 세션 선반영 2건(`24ef7e47`·`77cee37a`)을 큐가 「다음 칸」으로 남겨 직전 바퀴가 검증 전용으로 소비된 실측. 원장 §22에도 닫힌 항목(EstateBuild §2-3, WORKLOG `25559505`) 잔존(planner 관찰2).
   - 위치: 바퀴 시작 읽기 단계 절차라 단독 할당 불필요 — 채택 1번과 병행 적용(도구 공유: `git merge-base --is-ancestor`).
5. **네거티브(OFF) 재현 의무화 + QA 캡처 해상도 고정(좁은 폭 프리셋 포함)** — tester 제안2. PROPOSALS.md 대응 줄 없음(표식 불가 — 본 문서에 기록).
   - 근거: OFF가 우측 잘림을 미재현한 채 닫음 처리(STATUS.md:42 스스로 인정) — ON/OFF 쌍이 그리기 경로 전환만 증명. 다음 결함(로스터 카드 이름·하트 겹침, STATUS.md:26)이 같은 공정을 탈 예정이라 재발 차단 가치.
   - 위치: 다음 UI 폴리싱 바퀴부터 적용하는 공정 규격(결함 닫음 절차 ①OFF 재현 샷 → ②ON 해소 샷).

## 보류 (사유 필수)

1. **보간 접두 매칭 지원**(PROPOSALS.md 22:55 항목, 중) — 사유: 같은 game_asset_names 검사기를 두른 오탐 정리로, 본결함 차단은 채택 3번이 담당. 검사기 이중 수정 방지를 위해 3번 안정화 후 재심의.
2. **board.py 더임 판정 표시**(PROPOSALS.md 22:4x 항목, 하) — 사유: 소비처인 V4 수용성이 사람 관문 대기(STATUS.md 「관문 부채」 표 · 데모·EA 전 재개 트리거)라 당분간 조회 수요 없음.
3. **PROFILE 키트 이동**(PROPOSALS.md 22:4x 항목, 하) — 사유: 더임 리허설 자체가 사람 관문 대기 상태라 비개발자 편집 수요가 발생하지 않음.
4. **MCP W2 씬 검증 절차 문서화**(PROPOSALS.md 22:55 항목, 하) — 사유: 문서 전용 산출은 현재 무변경 문서 커밋 급증(builder 관찰1)과 반대 방향이고 실측 빈도 2회의 하 순위. 필요성은 인정, 예산 회수 후 재심의.
5. **STATUS 닫음 인벤토리 분리**(builder 제안3, PROPOSALS.md 대응 줄 없음) — 사유: STATUS.md는 보드(board.py)가 읽는 인수인계서라 대규모 이관은 소비처 리스크. 하 순위 유지.

## 다음 바퀴 권고 1건

**agent_runner 선반영 감지·재배정 건너뛰기 구현·적용**(채택 1번). planner·builder 판정 요청 2/3 — 무변경 랩 하나를 끊어야 채택 2·3번(전수 SelfCheck·png 실로드 검사)에 투입할 바퀴 예산이 회수된다.

충돌 기록: STATUS.md 「다음 할 일」#3/#4 교대 규칙(「직전 트랙이 코드면 UI·아트 상시 폴리싱」, 직전=루프도구 코드 — STATUS.md:22)상 다음 바퀴는 UI 폴리싱이 예정돼 있어 본 권고(루프 도구 코드)와 충돌한다. 아울러 tester는 전수 SelfCheck 재실행(채택 2번)을 회차 최우선으로 요청해 권고 순서와 불일치. 큐는 수정하지 않았으며 충돌 사실만 기록한다.

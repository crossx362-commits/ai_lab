# 정기 회의 20260826-095813 (현행 런 4바퀴 시점)

> 시점 근거: 당일 최근 랩 로그 `logs/2026-08-26/lap-20260826-093842-4.log`(필드 정예 훅 바퀴, 커밋 `03035f8f`+`35cb89a9`) · `loop/last_test_report.json`(09:51, 64/64 PASS) 이후 개최.
> 의장 주의: 오너 지시상 PROPOSALS.md는 줄 추가 금지·기존 항목 줄 끝 표식만 허용이므로, 이번 회의 신규 제안(tester 발의 정예 훅 런타임 경로 보강 등)은 본 문서에만 기록하고 표식은 심의된 기존 항목 6건 줄 끝에만 덧붙였다. 그 외 미판정 항목은 이번 의제 밖이라 표식 없이 보존. 커밋은 하지 않음.

## 참석 (planner/builder/tester 산출 요약 각 2줄)

- **planner**: ① 오픈 셀 바닥 임박을 지적 — STATUS.md:53 「남은 알려진 오픈 셀 없음·원장 재스캔 발굴」이 원장 운영규칙 3(`GAME_DESIGN_ASHES_TO_STARS.md:1866~1869` 「소비처0 바닥나면 다음 칸 찾기가 아니라 단계 1 관문 측정」)과 어긋나고, 남은 칸이 §18-14·큐#1 잔여뿐이라 전환 시점이 이번 런 안에 온다. ② G3 권장 파티 픽스처(큐#5)가 관문 측정의 선결조건인데 후순위 방치 중이라며, §18-14를 1바퀴로 닫은 뒤 「큐#1 잔여 → G3 픽스처 고도화」 순 배정으로 이번 런 안에 관문 측정 가능 상태(픽스처 정의 일치 + 64/64 GREEN 유지)까지 만들 것을 회차 판정 요청.
- **builder**: ① 검증 계약 부패 구조를 실측 — GameSweep(63종)만 매 바퀴 돌고 전수(194종)는 `c31479be` 때 처음 돌아 잔여 10종 FAIL이 전부 「런타임 정상 + 검증 계약 낡음」이었고, EliteDropSelfCheck 등록 누락(8/18부터 스윕 밖 잠복)에서 같은 패턴 재확인. ② GameFullCheck 전수 4바퀴마다 의무화 + SelfCheck↔GameSweep 등록 대조 검사 + 소비 경로 이동 시 계약 체크리스트를 한 패키지로 채택하고, 배치 예외 스택 기록·AttachWithAwake 공용화는 비용이 작으니 다음 검증 코드 바퀴에 함께 반영할 것을 회차 판정 요청.
- **tester**: ① `last_test_report.json` 64/64 PASS가 큐레이션만 커버한다는 점을 근거 제시 — EliteDropSelfCheck 8/18부터 스윕 밖 잠복(PROPOSALS:284)·전수 첫 실측에서 11건 일괄 발화(STATUS.md:80)로 「정의된 검증 vs 등록된 검증」 대응 강제 부재를 입증. ② unity/와 unity_meas/의 productName 공유로 prefs 오염이 배치 SelfCheck를 비결정적으로 깨는 실측(PROPOSALS:280)과 정예 훅 `_mKind[i]>=3` 소스 계약 미실측을 제기 — 회차 요청은 등록 대조 검사 신설+전수 의무화 이행을 다음 회의 때 last_test_report 흔적으로 재확인하는 것.

## 채택 개선안 (번호·근거·다음 바퀴 업무 후보로서의 위치)

1. **검증 탐지 장치 패키지 — GameFullCheck 전수 4바퀴마다 의무화 + Editor *SelfCheck↔GameSweep 등록 대조 검사 + 소비 경로 이동 시 검증 계약 체크리스트** (`docs/feedback/PROPOSALS.md:279` [09:20, 상] · :284 [09:56, 중] · :283 [09:46, 중] → ✅20260826-095813) — 근거: builder·tester의 회차 판정 요청이 동일 패키지로 수렴(참석 3명 중 2명 최우선), 전수 첫 실측 11건 일괄 발화 + 8/18부터의 등록 누락 잠복 실측. 위치: 다음 바퀴 업무 후보 — 대조 검사 신설 1건 + PROMPT 절차 문구 수준의 인프라 과제.
2. **배치 예외 스택 기록(`InnerException.StackTrace` 병기) + `AttachWithAwake<T>` Editor 공용 유틸 추출** (`docs/feedback/PROPOSALS.md:281` [09:40, 상] · :282 [09:43, 중] → ✅20260826-095813) — 근거: builder 제안 2 — BossBattleDps NRE 스택 오독(`W3Party.cs:809` 오독 → `NextStyle:1126` 정정 실측) 재발 차단, 구현 비용 작음. 위치: 다음 검증 코드 바퀴에서 채택 1번과 함께 반영하는 짧은 과제 후보.
3. **G3 픽스처를 코드 정의와 일치시킨 뒤 재판정(Equipment 장착 경로+Fusion CombatMuls 심기)** (`docs/feedback/PROPOSALS.md:277` [08:40, 중] → ✅20260826-095813) — 근거: builder 제안 3 지지 + planner 제안 2(관문 측정 선결조건 승격) 동조 — 베어 로스터 전멸(실측0 vs 필요3076)은 밸런스 결함이 아니라 정의 어긋남. 위치: STATUS 큐#5 그 자체와 일치 — 관문② 측정 착수의 전제 작업으로서 후순위 취급하지 않음.
4. **unity_meas productName 측정 전용 분리 + 영속 상태 의존 SelfCheck의 전제 확립(DeleteKey)** (`docs/feedback/PROPOSALS.md:280` [09:22, 중] → ✅20260826-095813) — 근거: tester 제안 2 — prefs 오염(`ats.style.*`)이 MemberStyleSelfCheck를 깬 실측 + DeleteKey 전제 확립 선례(`bf979fc9` 바퀴) 존재. 위치: ProjectSettings 1행 변경 + 검증 전제 보강 수준의 저비용 과제 후보(test 단독 후원임을 명기).
5. **BattleScreen 필드 survived 분기의 `EliteDrop.LastLine` 소비(필드 요약 문구에 elite 드랍 노출)** (`docs/feedback/PROPOSALS.md:285` [09:57, 하] → ✅20260826-095813) — 근거: planner 제안 3(큐#1 잔여 소비 시점과 내용 일치 확인)+tester 제안 4 — STATUS 큐#1 남은 항목(§10-2 웨이브 배율 연동·필드 요약 문구 노출)과 정확히 같은 결함. 위치: 큐#1 소비 바퀴에서 웨이브 배율 연동과 한 번에 처리 가능한 후보.
6. **보드 「개선안 수함대」 집계 수정 선반영 확인(board.py proposals_info 다중 행 표식 판정 + test_board.py ProposalsInfoTests)** (`docs/feedback/PROPOSALS.md:9` [2026-08-25 21:35, 하] → ✅20260826-095813) — 근거: 처방이 보드지킴이 커밋 `6b9699f4`(수함대 집계 수정+회귀검사)로 이미 반영(STATUS.md:13 실측) — 미처리 수치 오분류 원인 해소 확인. 위치: 추가 업무 없음 — 선반영 닫음 표식.

## 보류 (사유 필수)

1. **STATUS 큐에 「소비처0 바닥 → 단계 1 관문 측정 전환」 조건 명시**(planner 제안 1·상) — 사유: STATUS 「다음 할 일」 편집은 이번 회의 의장 권한 밖(오너 지시 — 큐 직접 수정 금지). 원장 운영규칙 3(`GAME_DESIGN_ASHES_TO_STARS.md:1866~1869`)이 이미 상위 근거라 다음 랩 워커가 큐 갱신 시 참조하면 충분 — 내용은 본 문서에 보존.
2. **큐#1 잔여의 소비 시점(§18-14 완료 직후 다음 바퀴) 큐 표 명기**(planner 제안 3·하) — 사유: 동일하게 STATUS 큐 편집 불가. 실질 중복도 확인 — 해당 잔여는 큐#1 행(STATUS.md:63)과 채택 5번(PROPOSALS:285)에 이미 기재돼 있음.
3. **정예 훅 런타임 경로 보강 — EliteDropSelfCheck 소스 계약에 `_mKind[i]>=3` 정예 종류 조건 단언 추가 + 다음 필드 플레이모드 실측 바퀴에서 FieldKills 증가를 통과 기준에 포함**(tester 제안 3·중, 신규 제안) — 사유: PROPOSALS.md 줄 추가 금지(오너 지시)로 수함대 등록 불가해 본 문서에만 기록. 착수 시점은 큐#1 필드 실측 바퀴와 묶이는 것이 적절해 해당 바퀴 배정 시 재심의로 회부.

미처분: 이번 3파트 의제에 오르지 않은 기존 미판정 항목(예: PROPOSALS:278 가드 GUARD=1 해석 준칙(하)·:14 MCP 브릿지 재연결 유틸(하) 등)은 표식 없이 그대로 보존하며 다음 정기 회의 심의 대상으로 남긴다.

## 다음 바퀴 권고 1건 (STATUS 큐와 충돌하면 충돌 사실만 기록 — 큐를 직접 고치지 마라)

**권고**: 채택 1번(검증 탐지 장치 패키지)을 다음 바퀴 업무로 실행한다 — ① 「Editor *SelfCheck.Run 전수 vs GameSweep 등록 목록」 대조 검사를 신설해 등록 누락을 컴파일 단계에서 포착 ② GameFullCheck 전수 1회 의무 실측으로 `last_test_report`에 전수 실행 흔적 확보(builder·tester 회차 판정 요청 공통, tester는 다음 회의에서 흔적으로 이행 재확인 예고). 완료 기준: 대조 검사 GREEN + 전수 194종 스냅샷 1회 확보.

**충돌 기록**: STATUS.md 「다음 할 일」 1위는 §18-14 소환수 재소환(오너 위임 승인 완료, STATUS.md:52)이라 본 권고(검증 장치 선깔기)와 다음 바퀴 순서가 충돌한다. builder는 신규 시스템(§18-14)이 붙는 동안 검증 부채가 잠복하기 전 탐지 장치를 먼저 깔라고 했고, planner는 §18-14를 1바퀴로 먼저 닫은 뒤 관문 준비로 가라고 요청했다 — 두 요청이 상반된다. 큐 문구는 수정하지 않고 충돌 사실만 여기에 기록하며, 순서 판단은 랩 배정 규칙(오너 위임)에 맡긴다.

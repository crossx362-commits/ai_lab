# 정기 회의 20260825-144115 (32바퀴 시점)

> 시점 근거: tester가 최종 랩 로그로 `logs/2026-08-25/lap-20260825-143244-32.log`(lap 32)까지 실측 · STATUS 최종 갱신 14:40(`bc7eb983`) 이후 개최.
> 의장 주의: builder 파트 파일(`.council-part-20260825-144115-builder.md`)이 생성되지 않아 planner·tester 2건만으로 심의. 오너 지시상 PROPOSALS.md 줄 추가 금지이므로 이번 회의의 신규 제안(tester 발의 3건 포함)은 본 문서에만 기록하고 기존 항목 줄 끝에만 표식을 덧붙였다. 그 외 기존 미판정 항목은 이번 의제 밖이라 표식 없이 보존.

## 참석 (planner/builder/tester 산출 요약 각 2줄)

- **planner**: ① 소비처0 바닥 구간에서 루프 기본값(원장 재스캔·UI 폴리싱)이 원장 `GAME_DESIGN_ASHES_TO_STARS.md:1866~1869` 운영규칙 3(「다음 할 일은 단계 1 관문② 측정」)과 어긋나며 STATUS 큐 어디에도 관문② 측정 준비 칸이 없다. ② `loop/last_test_report.json`이 2026-08-25 09:23 `ok:false`(2개 실패/58)인 채 약 5시간 고정이고 열린 FAIL 2건(HuntBoonSelfCheck·AdvLookSelfCheck)이 큐 어디에도 배정되지 않았으며, 원장 §22 「지금 당장 할 일」 포인터(EstateBuild §2-3)도 `25559505` 닫음으로 만기됐다.
- **builder**: 파트 파일 부재(glob 실측 — planner·tester만 존재). 산출 요약 불가, 부재 사실만 기록.
- **tester**: ① 전수 검증 보고서가 5시간 넘게 낡은 채 `ok:false`이고 그 사이 lap 11~32가 각자 단일 SelfCheck만 재실측했으며, 바퀴 종료 조건에 전수 게이트가 없어(`loop/PROMPT.md` grep 0건) 실패 보고서 옆을 계속 통과했다. ② HuntBoon 금테 FAIL(:6)·AdvLook 수호기사 tank 낙하 FAIL(:62) 2건이 STATUS 「닫음」 기록(STATUS.md:40·44)과 모순되는 무배정 회귀이며, `ok:true` 보고서 확보까지 이후 폴리싱 바퀴들의 PASS 판정 신뢰도가 보장되지 않는다고 판정을 요청했다.

## 채택 개선안 (번호·근거·다음 바퀴 업무 후보로서의 위치)

1. **GameSweep 열린 FAIL 2건 선소화 + 바퀴 통과 기준에 `last_test_report.json` 재생성(전수 0 FAIL·ok:true) 추가** — 근거: tester obs 1~3·planner obs 2 동일 실측(타임스탬프 09:23 고정), 보드가 이 파일을 상태 입력으로 읽음(`loop/board.py:51`·`:3250`). 두 참석자 판정 요청이 한결같이 FAIL 선행으로 수렴. 위치: 다음 바퀴 최우선 업무 후보(HuntBoon 카드 여백·AdvLook dedicated 매핑 코드 수정 + 종료 시 전수 스윕 재실행).
2. **바퀴 시작 읽기 단계(PROMPT §②)에 `last_test_report.json` 신선도·합격 게이트 추가** — 근거: tester 제안 1(`ok:false`면 FAIL 항목을 그 바퀴 최우선 배정, 12시간 경과 시 GameSweep/GameFullCheck 재실행 선행 요구). PROPOSALS 상 우선 자동 검사망 선례(png 실로드 차단 검사, `docs/feedback/PROPOSALS.md:86`)와 동일 계열. 위치: 루프 규칙 편집 업무 후보(코드 FAIL 소화와 별행 가능한 문서 1건).
3. **열린 칸 자동 산출 스크립트 `loop/find_open_spec_items.py`** (`docs/feedback/PROPOSALS.md:125` → ✅20260825-144115 표식) — 근거: planner 제안 3 「채택 시급」, STATUS 3번 칸 포인터 만기 반복 실측(planner obs 3: 원장 §22 `1884`행도 EstateBuild 닫음 `25559505`을 못 받아 만기). 위치: 포인터 수동 갱신·재검증 비용을 대체하는 도구 1건, 다음 바퀴 업무 후보.
4. **셀 닫음 판정 시 STATUS 머리·3번 칸 포인터 같은 커밋 동시 갱신 절차 명문화** (`docs/feedback/PROPOSALS.md:144` → ✅20260825-144115 표식) — 근거: planner 제안 3 후반부(원장 §22에도 확장 적용), §10-3 이중 판정 사례(05:17 항목 원문)와 원장 §22 포인터 만기가 같은 병임을 입증. 위치: 운영 절차 명문화로 채택안 3과 병행 적용.
5. **레이아웃 FAIL 수정 시 ContentRect maxPad 상한 점검 + SelfCheck 치수 하한 단언 준용** (`docs/feedback/PROPOSALS.md:95` → ✅20260825-144115 표식) — 근거: planner 제안 2가 HuntBoon 금테 이탈 수정의 「기존 처방 준용」으로 해당 항목 원문을 그대로 채택. 위치: 채택안 1의 실행 방법론(회귀 재발 방지 단언 동시 반영).

## 보류 (사유 필수)

1. **관문② 측정 준비물 완성(척도 초안·CSV 규격·unity_meas 하네스)의 큐 최상위 편성**(planner 제안 1) — 사유: 취지는 채택하나 이번 권고 1건을 FAIL 소화로 고정했고, 사람 관문 부분은 오너 2026-08-24 지시(더미 진행)와 사람 승인 단계가 걸려 루프 단독 판정 불가. FAIL 소화·ok:true 확보 직후의 다음 업무 후보로 후순위 유지.
2. **원장 §22 「지금 당장 할 일」 갱신의 문서 세션 수동 위임**(planner 제안 4) — 사유: 채택안 3(자동 산출 스크립트) 채택으로 수동 위임의 필요성이 줄었고, 자동화 착수 결과를 본 다음 회기에 재판정하는 것이 이중 작업을 피한다.
3. **SelfCheck FAIL 메시지 조사·띄어쓰기 교정 템플릿 통일**(tester 제안 3) — 사유: 로그 판독 편의 개선이지만 진행 중인 FAIL 2건 소화가 선행 과제이고, 메시지 포맷 변경은 자동 파싱 소비처(board.py 리포트 표시 등) 영향 확인이 필요해 별도 칸으로.
4. **STATUS 큐 재편성(소비처0 바닥 선언·재스캔 금지 등 planner 판정 요청의 큐 고정)** — 사유: 의장 권한 밖(오너 지시: 큐를 직접 고치지 마라). 충돌 사실만 아래 권고란에 기록.

## 다음 바퀴 권고 1건 (STATUS 큐와 충돌하면 충돌 사실만 기록 — 큐를 직접 고치지 마라)

**권고**: 바퀴 시작 읽기 단계에서 `loop/last_test_report.json`(2026-08-25 09:23, ok:false 2/58)을 확인해 열린 FAIL 2건 — ① HuntBoonSelfCheck 가로 카드 아이콘·글씨 금테 이탈 ② AdvLookSelfCheck 수호기사 tank 역할 폴더 낙하 — 을 최우선 소화하고, 바퀴 종료 시 unity_meas GameSweep 재실행으로 `ok:true` 전수 보고서를 확보한다. HuntBoon 수정 시 채택안 5의 처방(maxPad 점검+치수 하한 단언)을 준용한다.

**충돌 기록**: STATUS.md 「소비처0 다음」(8행)과 「다음 할 일」 3번(43행)은 「원장 재스캔·새 칸 발굴, 없으면 UI 폴리싱」을 지시해 본 권고(FAIL 2건 선소화)와 다음 바퀴 순서가 충돌한다. 큐 문구는 수정하지 않고 사실만 기록하며, FAIL 소화 후에는 관문② 측정 준비물이 재스캔·폴리싱에 앞서는 후보임도 함께 기록해 둔다(planner 판정 요청의 2단 순서 존중).

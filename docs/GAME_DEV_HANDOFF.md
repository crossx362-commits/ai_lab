# 재와 별 — 개발 인수인계 (세션 이어가기용)

> **이 문서의 목적**: 대화 세션이 길어져 새 세션을 열 때, 이 파일 하나만 읽으면
> 하던 작업을 그대로 이어갈 수 있게 한다. 새 세션 첫 메시지에 이렇게 쓰면 된다:
>
> ```
> docs/GAME_DEV_HANDOFF.md 읽고 개발 루프 이어서 진행해
> ```
>
> 갱신 규칙: **작업을 하나 끝내고 커밋할 때마다 이 파일의 §3·§4를 함께 갱신한다.**
> 갱신 안 된 인수인계 문서는 없느니만 못하다.
>
> 새 세션이 이 파일을 **자동으로 찾도록** 영속 메모리에도 색인을 걸어 뒀다
> (`~/.claude/projects/D--ai-lab/memory/project_ashes_to_stars_game.md`).
> 오너가 아무 말 안 해도 세션 시작 시 이 문서가 떠오르게 하는 장치다.

---

## 1. 무엇을 만들고 있나

**재와 별 (Ashes to Stars)** — 스팀 PC용 2D 쿼터뷰 픽셀아트 게임.
뱀서라이크 전투 + 키우기 + 탑 100층 + 레이드 + 영지 + 비동기 PvP(침략).

핵심 정체성은 **"죽으면 캐릭터가 진짜 사라진다"** — 3회 사망 시 장비까지 영구 삭제,
환생석으로만 복구. 가챠 없음. 일일 퀘스트·숙제 없음.

| 문서 | 내용 |
|---|---|
| `docs/GAME_DESIGN_ASHES_TO_STARS.md` | **기획서 본문** — 23개 절. 모든 확정 사항의 출처 |
| `docs/GAME_ART_RESOURCES.md` | 아트 리소스 목록·물량 산정·최소화 방침 |
| `docs/GAME_DEV_HANDOFF.md` | 이 파일 |

---

## 2. 어디에 무엇이 있나

```
projects/ashes-to-stars/
  unity/                     유니티 프로젝트 (**Unity 6000.3.14f1** — ProjectVersion.txt가 정답)
    Assets/_Game/
      Data/                  ScriptableObject — 기획서 §18 수치가 여기 들어 있다
      Prefabs/               캐릭터 11 · 몬스터 26 · 투사체
      Art/FX · Art/Ground/   이펙트·바닥 텍스처
      Scenes/                Sandbox · GameStructure_Overview ← 에디터에서 열어볼 씬
      Scripts/Runtime|Editor
    Assets/Resources/sprites/          런타임이 실제로 읽는 유일한 곳
      tank|dps|healer|buffer/          오너 픽셀아트 (직업 4 × 8프레임 = 32장)
      mob_*.png · boss_*.png 등        블렌더 플레이스홀더
    Assets/Scripts/          W1~W3 검증용 (StressTest·W2Arena·W3Party·SpriteBank)
  blender/                   에셋 생성 파이프라인 (헤드리스)
    source_sheets/           오너가 준 시트 원본
    split_sheets.py          시트 → 프레임 단위 스프라이트
    gen_*.py                 캐릭터·바닥·이펙트·프랍 생성기
    out_*/                   생성 결과
  build_w1_perf/ build_w2_control/ build_w3_party/  스탠드얼론 빌드 (git 제외)
  results/                    측정 CSV·스크린샷·로그 (git 제외)

projects/ai-team/skills/마루_게임개발/tools/
  game_build_verify.py       빌드→실행→렌더 검증
  game_regression.py         W1~W3 통합 회귀
  game_balance_sim.py        경제·성장 수치 검산
  game_kiting_sim.py         잡몹 속도비 시뮬레이션

output/qa/ashes-to-stars/    검증 리포트 (git 추적)
```

### 실행 명령 (검증된 것)

```bash
# 유니티 빌드 (배치모드)
# ⚠️ 버전을 문서에서 베끼지 말고 ProjectVersion.txt를 읽어라 — 한때 문서가 6000.0.36f1로
#    낡아 있었고, 그 버전으로 배치를 돌리면 프로젝트 전체 재임포트가 터진다.
# ⚠️ 에디터가 열려 있으면 배치는 "another Unity instance" 로 즉시 죽는다(exit 21).
#    죽이기 전에 `wmic process where "name='Unity.exe'" get ProcessId,CommandLine` 로
#    **오너가 Hub로 열어둔 에디터(-useHub)인지** 확인할 것. 그건 죽이면 안 된다.
"C:\Program Files\Unity\Hub\Editor\6000.3.14f1\Editor\Unity.exe" -batchmode -quit \
  -projectPath "D:\ai_lab\projects\ashes-to-stars\unity" \
  -executeMethod W1Runner.Build -logFile "...\results\build.log"

# 블렌더 (반드시 --factory-startup — 설치된 애드온이 에러를 뿜는다)
"C:\Program Files\Blender Foundation\Blender 4.1\blender.exe" --background --factory-startup \
  --python "D:\ai_lab\projects\ashes-to-stars\blender\gen_props.py"
```

---

## 3. 지금까지 한 것 (완료)

| # | 작업 | 결과 |
|---|---|---|
| 1 | 기획서 v0.6 | 23개 절 · 악용 감사 3회(A20/B8/C8) 전건 대응 |
| 2 | W1 성능 검증 | **통과** — 500체 729fps (목표의 12배). DOTS 불필요 |
| 3 | W2 조작감 | 대시 무적 흡수 횟수로 계량화. 잡몹 속도 0.64→0.90 조정 후 포위 시간 90.8% |
| 4 | W3 파티 대조 실험 | 1인 불가·도발 효과 **검증**. 딜특화·힐러없음은 상한에 걸려 미검증 → 웨이브 점증으로 재측정 |
| 5 | 유니티 구조 | ScriptableObject·프리팹·Sandbox·게임구조_전체 씬 |
| 6 | 블렌더 파이프라인 | 캐릭터 64 · 바닥 4 · 이펙트 14 · 프랍 26 |
| 7 | 폴더·파일명 정리 | 뜻이 보이는 한글 이름으로, 참조 10개 파일 갱신 후 빌드 검증 |
| 8 | 오너 픽셀아트 반영 | 시트 3장 → 32프레임 분할 → 프리팹 연결 |
| 9 | W3 소모전 재측정 문서화 | 기획서 §21-1h 신설. E(힐러없음) **검증**, B(딜특화) **반증** — 소모될 자원이 없어 트레이드오프가 성립 안 함. W3 관문 종료 |
| 10 | 이름 전면 영어화 (오너 지시) | 자산 83개를 .meta와 짝으로 rename + blender·빌드·결과 폴더 + 참조 갱신. 한글 0건·meta 고아 0건 |
| 11 | 오너 픽셀아트 **실제** 적용 | 파일이 Resources 밖이라 런타임이 못 읽던 것 + **파티원 5명 중 4명이 몹 스프라이트로 그려지던 것** 수리 |
| 12 | 플레이 씬 흐름 (오너 지시) | 타이틀→영지(허브)→필드·탑·월드맵·캐릭터→전투→결과→종료. 하단바 5칸(§16). 씬 생성·빌드 검증은 대기 |
| 13 | 기적(사제) 0회 원인 수리 | 조건값이 아니라 **파동(≥2)이 기적(≥3)의 창을 막는 구조**였다. 신앙도 §3대로 회복량 누적으로 교정 |
| 14 | 스프라이트 분할 정확도 수리 | 노이즈(50px 이상 고아 픽셀)·배경 잔재(flood fill 시작점 누락)·행 밀림(격자선 경계 crop)·높이 정규화(프레임별 bbox 바뀜) 4건 해결 |
| 15 | 스프라이트 프레임 정렬 재작업 | 공통 캔버스 + 가로 중앙(본체 기준)·세로 바닥 정렬. 캐릭터는 중단 자세만 정규화(눕는 death는 미포함) |
| 16 | 몬스터 로스터 95종 + 프롬프트 | 잡몹 20·정예 30·보스 45종 명단. 애니메이션 상태·프레임 길이·생성 프롬프트 작성 |
| 17 | 이동기(dash) + 몬스터 스프라이트 통합 | 대시 애니메이션 추가. 몬스터 스프라이트 + 배경 잔재 제거로 런타임 로드 통합 |
| 18 | Play 씬 생성 플레이모드 가드 | playModeStartScene 고정 + 씬 생성 플레이모드 guard. Play 시 항상 타이틀부터 시작 강제 |

---

## 4. 다음에 할 것 (우선순위 순)

1. **⏳ 플레이 씬 생성·검증 (미완, 최우선)** — 코드는 다 있으나 **한 번도 컴파일·실행된 적이 없다.**
   에디터에서 `재와별 ▸ 플레이 씬 생성` 을 실행하면 씬 8개가 만들어지고 빌드 세팅에 등록된다.
   배치로 하려면 `-executeMethod PlayableScenesBuilder.BuildGame` (씬 생성 + 스탠드얼론 빌드).
   확인할 것: ①컴파일 오류 ②Play 시 타이틀→각 화면 이동 ③ESC·종료 버튼 ④픽셀아트가 실제로 보이는가
2. **W3 재측정 → §21-1i** — 속도 상수(0.85/0.65)와 기적 수정이 들어갔는데 **아직 안 돌렸다**.
   §21-1h 표는 옛 상수 기준이다. 재측정해 결론(특히 B 반증)이 유지되는지 확인.
   기적이 또 0회면 CSV 새 열 `faith_peak`으로 "신앙이 안 찼다 / 조건이 안 걸렸다"를 가른다
3. **[오너 결정 대기] 소모전 축 설계** — B(딜특화) 반증으로 §3의 트레이드오프가 무너졌다.
   후보 ⓐ힐 자원화 ⓑ누적 피로 ⓒ버퍼 오라 시간 비례 (§21-1h 말미)
4. **방향별 스프라이트 도착 시 처리** — 오너가 "나중에 준다"고 함.
   규격은 `docs/GAME_ART_RESOURCES.md` §0-A의 「방향별 스프라이트 도착 시 처리」 표
5. **수직 슬라이스 진입** — 기획서 §22 로드맵 1단계. 지금 씬 흐름은 **껍데기**이므로
   전투(W1~W3 구현)를 Battle 화면에 합류시키는 것이 첫 작업이다

---

## 5. 이 프로젝트에서 실제로 겪은 함정 (반복하지 말 것)

| 함정 | 무엇이 일어났나 | 어떻게 막나 |
|---|---|---|
| **빈 화면 700fps** | 텍스처 Read/Write 누락으로 아틀라스가 조용히 실패, 스프라이트를 하나도 안 그린 채 성능만 측정 | FPS와 **렌더 검증**(스크린샷의 유닛 픽셀 수가 몹 수에 비례하는지)을 짝지어 판정 |
| **낡은 빌드로 측정** | `W1.exe` 런처는 코드가 바뀌어도 타임스탬프가 안 변한다. 불가능한 수치(4초에 피격 10,508회)를 읽었다 | `*_Data/Managed/Assembly-CSharp.dll` 시각을 확인 |
| **유니티 크래시 오진** | 씬 생성이 두 번 죽어 TextMesh 탓으로 보고 멀쩡한 코드를 고쳤다. 진짜 원인은 **다른 Unity 인스턴스가 프로젝트를 잡고 있던 것** | 배치 실행 전 `taskkill /IM Unity.exe /F` + `Temp/UnityLockfile` 제거. 크래시는 스택을 먼저 읽을 것 |
| **runInBackground=false** | 창이 포커스를 잃자 Update가 멈춰 자동 봇 검증이 아예 안 돌았다 | 검증 빌드는 항상 `runInBackground = true` |
| **측정 상한에 걸린 결과** | W3 다섯 구성 중 넷이 45초를 완주해 생존 시간으로 구분 불가 | 상한에 닿은 결과는 통과가 아니라 **측정 실패**. 전멸까지 돌리고 압력을 점증 |
| **밝기로 배경 제거** | 시트 배경(밝기 13)보다 캐릭터 외곽선(0~4)이 더 어두워서, 임계값으로 지우니 캐릭터가 부서졌다 | 배경은 "어두운 색"이 아니라 **특정 색**이다. 색 비교 + 가장자리 flood fill |
| **대화 속 이미지** | 오너가 붙여넣은 이미지는 디스크에서 못 꺼낸다 | `Downloads` 등에서 파일을 찾거나, 저장 폴더를 안내할 것 |
| **Resources 밖 자산** | 픽셀아트를 프리팹에 연결하고 "적용 완료"라고 기록했는데 화면엔 안 나왔다. `Resources.Load`는 **`Assets/Resources/` 밖을 못 본다**. 게다가 W1~W3 빌드는 프리팹을 안 쓰고 SpriteBank를 쓴다 | 런타임이 실제로 읽는 경로에 넣었는지 확인. SpriteBank는 캐릭터가 하나도 안 잡히면 **LogError로 알린다** — 조용히 플레이스홀더로 굴러가지 않게 |
| **아군이 몹 스프라이트** | W3 파티 슬롯에 `{Player, Mob0, Mob1, Summon, Mob2}`를 꽂아 **파티원 5명 중 4명이 몬스터로 그려지고 있었다.** 수치 검증은 전부 통과했다 — 스프라이트가 뭐든 시뮬레이션은 돌기 때문 | 렌더 검증은 "픽셀이 있는가"만 보지 "**맞는 그림인가**"는 안 본다. 스크린샷을 사람 눈으로 한 번은 볼 것 |
| **에디터 락 ≠ 죽여도 되는 프로세스** | 배치 빌드가 exit 21로 죽어 `taskkill`을 하려 했는데, 그 Unity.exe는 **오너가 Hub로 열어둔 에디터**였다(자식 AssetImportWorker 2개까지) | 죽이기 전에 `wmic process where "name='Unity.exe'" get ProcessId,CommandLine`. `-useHub`가 붙어 있으면 사람이 연 것이다 |
| **rename 루프가 meta를 깼다** | `.png.meta`를 확장자 분리로 처리하다 `.meta`로 만들어 32개 GUID가 전부 고아가 될 뻔했다 | 자산 rename 뒤엔 **반드시** png 수와 png.meta 수를 세어 대조. 고아 meta 0건을 확인하기 전엔 끝난 게 아니다 |

---

## 6. 결정을 어디서 찾나 — "예전에 뭘 정했더라?"

세션이 바뀌면 대화 기록은 사라진다. **결정은 전부 파일에 있고, 종류별로 사는 곳이 다르다.**

| 알고 싶은 것 | 찾을 곳 |
|---|---|
| 게임 규칙이 어떻게 정해졌나 | `GAME_DESIGN_ASHES_TO_STARS.md` — **✅ 표시가 오너 확정**, 💡는 제안, ⚠️는 주의, 📌는 폐기된 안의 기록 |
| 수치가 왜 그 값인가 | 같은 문서 **§18 수치 밸런싱 기준표** — 앵커(T1 시간당 1골드·티어 ×1.6·스케일링 0.65)에서 유도된다 |
| 이 규칙이 왜 생겼나 | 같은 문서 **§19 악용 대응** + 각 절의 📌 — 감사에서 나온 구멍과 대응이 근거와 함께 있다 |
| 아트 규격·물량 | `GAME_ART_RESOURCES.md` — §0-A가 최신 방침(픽셀아트), **§0-B(최소화)가 물량의 최종 권위**, §1~6은 그 이전 산정 |
| 몬스터가 몇 종이고 뭐가 나오나 | `GAME_MONSTERS_ASHES_TO_STARS.md` — 잡몹 20·정예 30·보스 45종 명단, 계열 배율, 티어별 웨이브 편성표 |
| 스프라이트를 어떻게 만드나 | `GAME_MONSTER_ANIM_SPRITE_PROMPTS.md` — 애니메이션 상태·프레임 길이(밸런스 수치와 직결), 생성 프롬프트. **§0을 먼저 읽을 것** — 종수와 작화 단위를 혼동하면 물량이 4배가 된다 |
| 검증 결과 수치 | 기획서 **§21 프로토타입** + `output/qa/ashes-to-stars/` 리포트 |
| 왜 이렇게 구현했나 | **커밋 메시지** — `git log --oneline --grep=game` 로 훑는다. 사고와 교훈을 본문에 적어 뒀다 |
| 코드가 왜 이런가 | 소스 파일 상단 주석 — 각 스크립트가 "왜 이 방식인가"를 적고 있다 |

- ⚠️ **문서와 코드가 다르면 문서가 맞다**고 가정하지 마라. 실제로 `W3Party.cs`의 잡몹 속도가
  기획서 §18-11과 어긋난 적이 있다(정합성 감사로 발견). 둘이 다르면 **기획서를 기준으로 코드를 고친다**
- ⚠️ 이 문서가 최신이 아닐 수 있다. **§3·§4가 마지막 커밋과 맞는지** `git log -3` 으로 먼저 대조하라

## 7. 작업 방식 (오너 지시)

- **작업 하나 끝낼 때마다 커밋**하고, 다음 작업을 정한 뒤 실행한다. 이걸 반복한다
- 병렬 가능한 일은 **에이전트를 만들어 동시에** 돌린다
  (단 유니티는 프로젝트 락 때문에 **한 번에 하나만** — 에이전트에 유니티 실행 금지를 명시할 것)
- **항상 기획서를 참고**한다. 수치·규칙의 출처는 언제나 `GAME_DESIGN_ASHES_TO_STARS.md`
- 커밋·푸시는 master 직행. 푸시가 403이면
  `git config --local credential.https://github.com.username crossx362-commits` 후 `gh auth setup-git`

# Local agent/tool state
.agent/
# 루프 런타임 신호 — 빈 HOLD가 추적되면 재개할 때마다 30분을 잔다
loop/HOLD
loop/STOP
loop/STOP_LANE
loop/agent
loop/board_checks.json
loop/board_decisions.json
loop/owner_commands.json
loop/loop.pid
loop/grok_usage.cache.json
loop/claude_usage.cache.json
loop/codex_usage.cache.json
loop/provider.state
loop/runtime_state.json
loop/runtime_state.json.lock
loop/recovery_context.log
loop/.control-lock/
loop/logs/
.claude/
.mcp.json
agent-reach/
.logs/
logs/
projects/.logs/
projects/.daemon_state.json
projects/output/
.telegram_sent_cache.json

# Runtime logs, process state, and temporary files
*.log
*.pid
*.bak
*.tmp
*.old
*.exe
*.csv
.temp/
projects/ai-team/scripts/.manual_stop*

# Generated reports and runtime snapshots
reports/status/harness_latest.json
reports/harness/*.json
# 성장루프 학습 런타임 로그(append-only 산출물)
reports/learning/reward/
reports/learning/punishment/
reports/uploads/
projects/ai-team/skills/*/tools/last_run.json

# Secrets and credentials
.env
*.env
.env.key
.env.backup*
.env_backup_secure/
.env.encrypted
.env.encrypted.backup
client_secret.json
client_secret.json.template
gmail_client_secret.json
*.pickle
youtube_token.pickle

# Generated media and preview artifacts
output/*
# 단, 에이전트 레지스트리 메타(SSOT)는 캐시가 아니라 버전관리 대상 — 기계 간 전파 필요
!output/cache/
output/cache/*
!output/cache/agent_registry.json
# 데몬이 상시 갱신하는 스냅샷 리포트 — 추적하면 트리가 늘 더러워져 git pull 배포가 막힘
reports/research/*_latest.md
*.mp3
*.mp4
*.wav
web_preview.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Node/Vercel/build output
node_modules/
.vercel/
projects/ai-team/node_modules/
projects/ai-team/out/

# Backups, archives, and old wrappers
.backups/
.archive/
.superpowers/
commit_push.bat
*.bat
fix_agent_paths.py

# Understand-Anything 지식그래프 산출물 (도구 생성물)
.understand-anything/

# Unity 프로젝트 (자체 .git 보유 — ai_lab 저장소에 섞지 않는다)
My project/

# git 내부 lock/temp 잔재가 중단된 작업 뒤 여기로 밀려나는 경우가 있다(2026-08-04)
_to_delete/
# 루트에 떨어진 일회 캡처 — 정본은 output/qa/ashes-to-stars/
qa_vfx_live/

# git worktrees (로컬 전용 — 자동 커밋 에이전트가 쓸어담지 않도록)
.worktrees/
.superpowers/

# Game build output (Ashes to Stars)
# 빌드 산출물 — 이름을 하나씩 적으면 새 빌드(build_game)가 조용히 커밋된다(실제로 154MB가 들어갔다)
projects/ashes-to-stars/build_*/
projects/ashes-to-stars/build_w1_perf/
projects/ashes-to-stars/build_w2_control/
projects/ashes-to-stars/build_w3_party/
projects/ashes-to-stars/results/

# 유니티 에디터가 Play 중에 저장하는 QA 캡처. **소스 자산이 아니다** —
# Assets/ 안에 있으면 유니티가 빌드에 임포트하고, 매번 새 파일이 생겨 커밋이 지저분해진다.
# 시각 QA의 정본은 `output/qa/ashes-to-stars/shots/`(qa_shot.sh)다.
projects/ashes-to-stars/unity/Assets/Screenshots/
projects/ashes-to-stars/unity/Assets/Screenshots.meta
loop/board_keeper.json
loop/.keeper_count
loop/COUNCIL_NOW
loop/.keeper_last_fix

# 작업 트리 소음 정리 (2026-08-26) — 보드 「저장 안 된 변경」이 131개까지 불어난 원인.
# 전부 산출물·로컬 설정이라 정본이 따로 있다. 지운 게 아니라 추적만 안 한다.
.DS_Store
.cursor/
# 유니티가 뱉는 캡처·산출물. 시각 QA 정본은 output/qa/ashes-to-stars/
projects/ashes-to-stars/unity/Captures/
projects/ashes-to-stars/unity/output/
projects/ashes-to-stars/unity/mono_crash.*.json
# 잘못된 cwd로 만들어진 중복 QA 산출물 — 정본은 저장소 루트의 output/qa/ashes-to-stars/
projects/ashes-to-stars/output/

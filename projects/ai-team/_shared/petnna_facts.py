"""펫과나 프로젝트의 확정 사실 — 구현·리뷰 프롬프트가 공유하는 단일 소스.

왜 필요한가(2026-08-06 실측): 같은 사실을 두 프롬프트가 각자 다르게 알고 있었다.

  · 예원 리뷰어 프롬프트는 앱을 "펫나"라고 소개했다 → 워드마크를 **실제 이름인
    '펫과나'로 정확히 구현한** 브랜치를 "브랜드 정체성 훼손"이라며 반려했다.
    멀쩡한 구현이 영구 보류로 갔다.
  · 수리 구현 프롬프트에는 "기존 디자인 시스템을 따르라"만 있고 **실제 팔레트가
    없었다** → 클로드가 violet/purple을 골랐고, 예원이 "브랜드 컬러 불일치"로
    반려했다. 이 경로로 3건이 죽었다.

즉 반려 사유가 "구현이 나빠서"가 아니라 **양쪽이 사실을 몰라서**였다. 지어내지
못하게 못 박고, 한 곳만 고치면 양쪽이 같이 갱신되게 한다.

사실이 바뀌면(리브랜딩·팔레트 교체) 여기만 고친다.
"""

# index.html의 <title>·og:title, tailwind.config의 theme.extend.colors.brand가 근거.
PETNNA_FACTS = (
    "[프로젝트 사실 — 이 값들은 확정이다. 다르게 기억하고 있어도 여기 적힌 것이 맞다]\n"
    "- 앱 이름/워드마크: **펫과나** ('펫나'가 아니다. index.html <title>과 og:title이 근거)\n"
    "- 브랜드 팔레트: Tailwind 커스텀 `brand-*` (brand-500=#cc785c, 테라코타/코랄 계열).\n"
    "  보조 강조색은 amber·rose·emerald. violet/purple/indigo는 이 앱의 색이 아니다 —\n"
    "  새 배지·버튼·카드에 강조색이 필요하면 brand-* 또는 amber/rose/emerald에서 고른다.\n"
    "- 색을 지적하거나 고를 땐 실제 클래스명(brand-500 등)을 근거로 대라.\n\n"
)

# 제안을 만드는 프롬프트(회의·미오·나무)가 공유하는 규칙 — 2026-08-08.
#
# 왜 필요한가(실측): 보류 53건 전수 조사에서 5건이 **이미 구현된 것을 다시 구현하라는
# 제안**이었다. 그중 3건은 제안이 요구한 회귀 테스트까지 이미 존재했다
# (예: "structurally_blocked에 엔진 자기수정 판별 추가 + 회귀 테스트 동반" →
#  touches_agent_ops()와 test_backlog_agent_ops_routing.py가 이미 있었다).
# 이런 제안은 자동 루프에 들어가면 **반드시** 3회 실패하고 보류로 간다 — 시도 3번과
# 클로드 호출 3번이 확정적으로 낭비되며, 사람이 전수 조사를 해야만 발견된다.
#
# 이미 "이미 정해진 방향을 뒤집지 마라"(제목 대조) 규칙은 있었지만, 그건 **결정**의
# 중복을 막을 뿐 **구현**의 중복은 못 막았다. 실재 확인은 별개 단계다.
VERIFY_BEFORE_PROPOSING = (
    "[제안 전 필수 확인 — 이미 있는 것을 다시 만들라고 하지 마라]\n"
    "무언가를 '구현하라/추가하라'고 제안하기 전에, 그것이 이미 있는지 Grep으로 직접 확인하라.\n"
    "- 함수·모듈을 요구할 거면 그 이름이 이미 정의돼 있는지 (`def <이름>` / `function <이름>`)\n"
    "- UI·스타일을 요구할 거면 해당 셀렉터·클래스가 css/index.html에 이미 있는지\n"
    "- 회귀 테스트를 요구할 거면 같은 이름의 테스트 파일이 tests/ 아래 이미 있는지\n"
    "이미 있으면 제안하지 말고, 대신 **무엇이 부족한지**만 구체적으로 적어라"
    "('있긴 한데 목표 미달'은 유효한 제안이다 — 단 측정값이나 코드 근거를 함께 대라).\n"
    "확인하지 않은 채 '없을 것 같다'로 제안하지 마라 — 그건 추측이다.\n\n"
)

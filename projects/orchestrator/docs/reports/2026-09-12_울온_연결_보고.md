# 울온 연결 보고 (2026-09-12)

오너 명령(2026-09-11 22:54) 「울온 개발해」의 전제 작업. 울온(`projects/ulon`)을 오케스트레이터의
target으로 붙이고, **모델 비용 0**으로 게이트가 끝까지 도는 것을 확인했다.

## 결론
- `ulon`(Unity): **PASS** — task 368. sparse worktree → 공유 Library → 컴파일 → SliceSelfCheck 93초 통과 →
  게이트 부산물 6파일 되돌림 → 커밋은 프로브 `OrchLinkProbe.cs` + `.meta` 2파일.
- `ulon_props`(Blender): PASS — task 356(어제).
- 다음 단계: `./orch plan "…" --target ulon` (유료, 오너 명령이 승인).

## 막혔던 것 셋과 수리
| 증상 | 원인 | 수리 |
|---|---|---|
| SliceSelfCheck 18초 만에 exit 1 (task 360) | worktree엔 미추적 `server/.venv`가 없어 `python3`로 폴백 → psycopg2 부재 | target `link_paths: ["server/.venv"]` — 본 체크아웃에서 링크, worktree info/exclude 등록 |
| venv 링크 뒤에도 exit 1 (task 361) | Postgres 16이 재부팅으로 죽어 있었음(launchd 아님). 시도 3회를 전부 태움 | ① `LC_ALL=en_US.UTF-8 pg_ctl start`로 기동(LC_ALL 없으면 multithreaded 즉사) ② target `preflight`(pg_isready) — 실패면 AI 호출 전 BLOCKED/UNKNOWN |
| 게이트가 `Bootstrap.unity`·지형 6~8파일을 재저장 → AI 변경으로 집계·커밋될 판 | SliceSelfCheck는 빌더를 돌려 씬을 저장한다 | 게이트 전 `git status` 스냅샷, 게이트 뒤 새 변경만 되돌림. AI가 만든 자산의 `.meta`는 남김(첫 판엔 지워서 고침) |

## 함께 고친 것 — 계획 안 코드 전달(Codex 감사 P1)
계획 7의 T4가 T1·T2 코드를 못 봐 반려됐던 원인. 이제 run-plan이 통합 브랜치 `orch/plan-NNNN`을
HEAD에서 열고 Task PASS 커밋마다 전진시킨다. 다음 Task worktree는 그 위에서 출발한다.
NC `plan_inherit`: T2가 T1의 파일을 보고 다른 행동을 해야만 초록 — 전달이 끊기면 빨간불.

## 증거
- 스모크 로그: `/tmp/smoke_ulon3.log`, 게이트 로그 `logs/task0368-a1.gate-sliceselfcheck.unity.log`
- NC: `plan_inherit` PASS, `preflight` PASS(전제 실패 시 「전제 미충족」·시도 0회). 전체 46종 결과는 아래 갱신.
- 스크린샷: `2026-09-12_board.png`

---

## 추가 (2026-09-12 11:00) — 보드 「지금 · 다음」 개편, 자동 기동 등록

오너 지시 「보드에 진행되는 일 자세히 나오게, 누가 뭐하고 다음엔 뭐하고 시각적으로」.

- **단계 막대**: 작업마다 실제 실행 순서를 점으로 그린다 — 준비 → 구현 → 변경 집계 → 컴파일 → 테스트 →
  SliceSelfCheck → 리뷰 → 커밋. 지난 단계는 초록, 현재 단계는 파란 점이 뛴다. 각 점 아래에 담당(사람 이름·Unity·git).
  Blender target은 준비 → 구현 → 변경 집계 → Blender 검증 → 리뷰 → 커밋으로 다르게 그린다.
- **한 줄 요약**: 「지금 Unity가 SliceSelfCheck (39초 전 시작) → 다음 리뷰 · Claude」.
- **시도 이력**: 1회/2회 누가 무엇으로 반려됐는지 한 줄씩(색으로 PASS/FAILED).
- **이어서 할 일**: 계획의 BACKLOG를 순서대로, 선행 미완이면 「선행 대기: T1」로 흐리게. 접수된 명령도 함께.
- **최근 흐름**: 끝난 단계 12건 — 시각·PASS/FAIL·누가·무엇을·몇 초.
- **정직성**: 떠 있는 프로세스가 없으면 단계를 「기록으로 짐작」이라고 적는다. 실행 중인 척하지 않는다.

자동 기동(오너 허락): Postgres 16 → `brew services start postgresql@16`(LC_ALL 포함된 공식 plist),
Ollama → launchd `com.ailab.ollama`(ollama serve, KeepAlive). 재부팅해도 게이트 전제와 로컬 모델이 산다.

동시 세션 사고 하나: 내 정리 루프와 Codex의 새 작업이 겹쳐 작업 #406이 `refs/heads/orch` 디렉터리
경합으로 시도 0회에 즉사했다(git ref lock). worktree 생성에 1회 재시도를 넣었다.

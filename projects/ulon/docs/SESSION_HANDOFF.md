# SESSION HANDOFF — Ulon 자율 개발 루프

## 자리
- 작업 트리 **`/Users/junholee/ai_lab-loop`**, 브랜치 `loop-claude`, 푸시는 `git push origin HEAD:master`.
- `/Users/junholee/ai_lab`는 **남의 트리**(검수·Grok·Codex `autodev`) — 건드리지 않는다.
- 보고는 검수 세션 `local_7b28e464-020e-4be4-905a-270a96c891d7`. 안건 판정은 대장
  `local_07be93d8-1ac7-44a4-8d67-c82f238176a1`. 오너 직행은 셋뿐 — 다운로드·되돌릴 수 없는 삭제·돈.

## 도구 (모두 `projects/ulon`에서)
```bash
SELFCHECK_LOG=$PWD/unity/Logs/selfcheck_dev.log ./tools/slice_selfcheck.sh   # 편집 뒤 두 판 EXIT=0
./tools/qa_shots.sh            # builds/qa/*.png — 찍고 **눈으로 본다**
```
- 유니티는 프로젝트당 한 인스턴스(`pgrep -f "Unity.*-batchmode"` 먼저).
- `-nographics`로는 카메라 렌더가 죽는다(SIGSEGV) — 렌더가 필요하면 빼라.
- 셀프체크는 매 판 `Bootstrap.unity`를 **커밋 상태로 되돌리고** 시작한다(백업 한 벌).
- **씬은 커밋된 산출물이다** — 배치 코드만 고치면 화면은 안 바뀐다. `Ensure*` 멱등 패스로 수렴시켜라.
  재드레싱 사슬은 닫혔다(`Dress Village` 두 번 + 셀프체크 EXIT=0).
- 유니티가 오염시킨 자산을 되돌릴 땐 **`git stash` 먼저, 자산 경로만** 지정
  — `git checkout -- Assets`는 내 편집도 지운다(실제로 한 번 잃었다).

## 커밋 꼬리
… `17704870`(뼈대 기준 사람 몸) → `6844c534`(뼈대 예외 로그) → `d8e8641f`(사냥터 원장) →
**`2071e56f`(랩 B — 킷 배율 2.1 + 해가 셋이던 마을)**

## 닫힌 것
보스 그립 NC · mono_crash · 은행 지붕 · 배우 전수 스윕(③) · 장비를 자리로 찾기(①) · 랩 A 그림↔캡슐 ·
민가 지붕 판때기 · 재드레싱 사슬 · 껍데기 몹 · 랩 ② 뼈 고르기 · 사냥터 간격(방위각) ·
**랩 B(킷 배율 2.1)** — 조건 넷 전부 충족(아래).

### 랩 B 결과 (`2071e56f`, 푸시 완료)
- 원장 하나: `Shared/WorldScale.cs`의 `Kit = 2.1`(유도 주석: 문 개구부 0.74m, 필요 1.8×0.85=1.53m →
  1.53/0.74 = 2.07 → 2.1). 빌더 좌표(`Module()`)·공유 상수·게이트가 전부 이 값을 탄다. 제자리 확대 아님.
- 조건 ① 굽기 전 백업(`builds/scene_backup/Bootstrap.beforeKitScale.unity`) · 셀프체크 EXIT=0 두 판 ·
  **QA 전량 통과**(`qa_shots.sh` EXIT=0). ② 옛 씬 5304개/442종 → 새 씬 4848개/442종, **사라진 이름 0**,
  감소는 울타리 −139·Visual −228·산포 −89로 전부 배율로 설명된다. ③ 두 번 구운 판끼리 의미 차이 0.
  ④ 배율을 뺀 집은 문/사람 게이트가 빨간불(양방향 NC).
- **굽는 김에 나온 진짜 결함(랩 B의 값)**: `SetupLighting()`이 `FindAnyObjectByType<Light>()`로
  **아무 등불이나** 집어 해 설정을 덮어써, 화덕 점광·테스트 공간 점광이 각각 **또 하나의 태양**
  (Directional 1.18)이 돼 있었다. 마을이 해 셋(합 3.54)을 받아 광장 화면의 **24~39%가 순백(255)**.
  포화 픽셀 위에서는 효과를 얹어도 픽셀 차가 0 → VFX 실화면 게이트가 마을에서만 하한 미달
  (0.07~0.18% vs 0.25%)이었다. **자를 무르게 하지 않고** 바닥을 고쳤다: 해는 이름으로 지목(`FindSun`),
  이미 해가 된 등불은 점광으로 되돌리고, 방향광이 둘이면 `SingleSun()` 게이트가 운다.
  결과 — 포화 **0.2%**, 마을 VFX **0.38~0.48%**(하한 0.25), 지하 1.87~2.43%, NC 양쪽 빨간불 유지.
- 도로 알베도도 유도로 바꿨다(감마 공간: 조도 1.48 → 목표 화면값 0.80 → 알베도 0.54, `KenneyStoneRoadMat`).
- VFX 실화면이 실패하면 **그 판의 두 프레임을 `builds/qa/vfx_fail_*.png`로 남기고** 실패한다
  (수치만으로는 왜 안 읽혔는지 안 보인다 — 이번 원인도 이 두 장이 갈랐다).

### 배치 충돌 (검수 지시 2026-09-09, `0ccec70d`~`19749e06` 푸시 완료)
던전 소품 자(`PropOverlapFrac` = 얇은 쪽 두께의 절반)를 **마을 소품↔건물**로 넓혔다
(`SliceSelfCheck.VillageSpacing.cs`, 양방향 NC). 새 자를 만들지 않고 `Penetration`/`Thickness`를 재사용한다.
첫 판 19쌍 → 지금 **0쌍**(최대 36%). 수리는 굽는 쪽(`VisualSliceBuilder.PropClear.cs`)이 한다:
줄(울타리·산울타리)은 걸린 조각을 빼고 — 집이 그 자리를 대신 막는다 — 나머지는 수평으로 밀어낸다.
셀프체크 수리 사슬에도 같은 패스를 걸었다(`Ensure*`가 집을 다시 지으면 커밋된 덤불이 되살아난다).
곁가지 둘: 부지 집이 **상호작용 기둥이 선 부지 한가운데**를 덮고 있었다(집을 서쪽으로),
꺼진 건물은 렌더러가 꺼져 **바운드를 못 잰다**(정리 중 잠깐 켰다 되돌린다).

## 다음 (순서)
1. **은행원 칸 밝기**(검수가 랩 B 뒤로 미뤄 둔 것) — 「밝은 방위 고르기」 +
   그 어두움이 **실제 플레이 화면에서도 그런지** 한 줄 확인. 해가 하나로 준 지금 다시 볼 것
   (`50_villagers.png` 첫 칸·`26_behind_bank.png`).
2. **절대 미터를 비율로** — `SliceSelfCheck.BossTraits.cs`의 `WeaponGripDistMax = 0.25f`·
   `WeaponTipDistMin = 0.50f`는 **몸 높이 대비**, 왕관 축 `0.15f`는 **머리 폭 대비**로.
   발-지표 `0.10`·장비 관통 `0.05`는 절대값 유지하되 **매 판 실측 여유를 로그로**.
   같은 랩에서 **물림 자 셋이 보스에만 걸린 것**을 일반 배우로 넓힐지 판단(검수 경증).
3. MegaKit(모루·화덕/절구통)은 오너가 `_ThirdParty/Quaternius/`에 파일을 넣어야 진행(다운로드는 오너 직행).

## 손대지 말 것
`OfflineWorld*`, `NetAvatar.cs`, 루트 `docs/SESSION_HANDOFF.md`(검수 것),
`.worktrees/herdr-*`·`autodev-*`, `OfflineWorld.Player`.

## 랩 규칙
읽기 → 하나 만들기 → **내 눈으로 검증**(셀프체크 두 판 EXIT=0 + QA PNG 실제로 보기) →
커밋·푸시(`git add -A` 금지, 경로 명시, add+commit 한 호흡) → 검수 보고 → 이 파일 갱신.
보고 끝에 **「구조 한 줄」**(잔재·비대 목록 + 원칙 한 줄).

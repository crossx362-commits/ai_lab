# 원장 — 반복해서 걸린 함정 (검수 채택 문장)

같은 실수를 이름 붙여 남긴다. **여기 있는 것은 판정이 끝난 문장뿐**이고, 새 문장은 검수가 랩을 닫을 때
한 줄씩 채택해 넣는다. 사례는 문장 밑에 한 줄로만 붙인다 — **같은 뜻의 문장을 두 번 적지 마라.**
원장이 불어나면 아무도 안 읽고, 안 읽히는 원장은 없는 것과 같다.

(2026-09-09 신설: 그전까지 이 문장들은 검수 세션의 `docs/SESSION_HANDOFF.md`에만 있었다 —
인수인계 파일은 다 끝나면 **비우는** 파일이라 문장이 같이 사라질 참이었다. 개발 세션이 「원장 파일이
어디냐」고 물어 드러난 구멍이다.)

## 게이트와 자(尺)

- **통과한 게이트가 무엇을 쟀는지 물어보지 않으면, 초록은 증거가 아니라 습관이다.**
  (2026-09-09 광장 담: `GetComponent<Renderer>()`로 걸렀더니 **한 조각도 못 세고 초록**이었다 —
  킷 담은 껍데기에 렌더러가 없고 자식이 그린다. 「0이면 실패」가 없으면 못 센 자가 통과한 자로 보인다.)
- **자가 있는데 안 불리면 없는 것과 같다.** / **호출부 없는 자는 자가 아니다.**
- **같은 질문이면 자를 늘리지 말고 절을 늘린다 — 마당은 이름이 아니라 자리다.**
  (2026-09-09: 던전 입구 마당을 재던 `RegionIntrusion`에 광장을 한 자리 더 물렸다. 새 자를 만들었으면
  같은 질문을 두 곳에서 답하게 됐을 것이다. 반경은 자리마다 따로 유도한다 — 야외 2.0m를 광장에 옮기지 않았다.)
  (2026-09-09 사냥터: 몹 자리 원장이 **에디터에만** 있어 도포가 몹 자리를 모른 채 얼룩을 깔았다 —
  「몹은 잔디에, 흙은 그 옆에」의 뿌리. `Shared/HuntRoster.cs`로 옮겨 놓는 쪽·깔는 쪽·재는 쪽이 같이 읽는다.
  **원장이 한쪽에만 있으면 다른 쪽은 추측으로 그린다.**)
  (이어서: 첫 반경 4.75m는 「걸린 것과 결백한 것 사이」로 유도했지만 **내가 고른 속**이었다.
  화면이 마당으로 읽는 것은 **돌 깔린 넓이 8.4m**였다. 자리의 경계는 유도 구간이 아니라
  **화면이 쓰는 경계**로 잡는다.)
- **자가 맞추는 자를 만들면 언제나 통과다.** — 게이트가 시켜서 넣은 보정 코드는 게이트와 한 몸이다.
- **빌더가 쓴 값을 그대로 읽는 자는 늘 초록이다** — NC를 매 판 같이 돌려야 자로 남는다.
- **자가 제 대상을 기준에 넣으면 그 자는 절대 만족되지 않는다.**
  (2026-09-09 은행원: 방 바운드에 그 방의 사람이 들어 있었다. 사람을 벽에서 떼면 방이 같이 줄어,
  굽는 쪽은 옮겼는데 자는 계속 「붙어 있다」고 했다.)
- **계측이 세계를 바꾸면 그 판의 초록도 빨강도 못 믿는다.**
- **재는 자가 제가 만든 구멍을 세계로 착각한다.**
- **간헐적으로 우는 자는 자가 아니다.** / **캐시가 세계보다 오래 살면 자가 거짓말을 한다.**
- **자는 재기만 하고 수리는 굽는 쪽에 둔다** — 게이트가 잰 상자를 굽는 쪽이 그대로 받아 쓴다.
- **자를 넓히지 말고 묻는 것을 좁혀라 — 넓힌 자는 멀쩡한 것도 죽인다.**
  (2026-09-09 샷: 앞물건 각도를 25°·35°로 넓혔더니 멀쩡한 방위까지 죽어 차양이 화면 절반을 덮었다.
  「실루엣에 겹치는가 +10°」로 좁히자 풀렸다.)
  (같은 랩: 렌즈 앞을 **거리**로 재면 안 된다 — 화면을 얼마나 먹는지는 **각크기**다. 물러나기는 프레임만
  넓히고 사이에 선 것을 못 치운다. 방위를 45°→270°로 **돌아서** 풀었다.)
- **두 자가 다투면 화면이 이긴다 — 진 자는 지우지 말고 셈으로 강등하라.**
  (2026-09-09 배너: 광선 자가 문틀 사각형 가장자리에 격자를 깔았는데 그 가장자리는 화면에서 **기둥 뒤**였다.
  「25% 가림」 빨간불이 났고 그 판의 샷에는 가려진 것이 없었다. **바운드 상자의 가장자리는 화면에서
  남의 뒤일 수 있다.** 게이트는 화면 자로 갈아 끼우고 광선 자는 셈으로 남겼다.)
  (2026-09-09 배너: **자가 좋아졌는데 화면이 나빠지면 화면이 이긴다.** 등불을 진입로로 밀자 겹침 지표는
  풀렸지만 등불이 기둥에 파묻혀 **꼭대기에 얹힌 컵**이 됐다 — 되돌렸다.)
- **평지는 결함을 숨긴다 — 지형을 올리면 묵은 오정의가 화면 위로 올라온다.**
  (2026-09-09: 길이 문 뒤에 깔린 것은 오래된 결함이었지만 평지에서는 문 뒤가 프레임에 안 잡혀
  아무도 못 봤다. 언덕이 그것을 들어 올려 드러냈다.)
- **띠는 자리를 정할 뿐 대상을 정하지 않는다 — 띠 안에서 「무엇인지」를 가려서 세라.**
  (2026-09-09 문구멍 어둠: 띠가 두 기둥 **중심** 사이라 기둥 안쪽 절반이 들어왔고, 재질을 바꿔도
  50%→41%에서 멈췄다 — **안 움직인 몫이 곧 돌기둥**이었다. 포털 실루엣만으로도 모자라
  기둥이 판 앞에 서는 자리에서 최대 밝기 1.000이 찍혔다: **앞을 무엇이 가리면 그 픽셀은 문이 아니다.**)
- **어둠은 값이 아니라 성질이다 — 빛을 받는 재질은 아무리 검어도 구멍이 안 된다.**
  (2026-09-09: 검수는 「알베도를 낮춰라」라고 판정했지만 옛 알베도는 이미 **0.03**(255 눈금 8)이었다.
  밝기를 만든 것은 알베도가 아니라 **조도** — 눈을 진입로로 옮기며 문짝이 등불 둘을 정면으로 받았다.
  값을 더 깎는 대신 **빛을 안 받게**(Unlit) 고쳐 발밑 지표 대비 5%가 됐다.
  **목표를 재서 잡으라는 조건이 판정의 전제를 무너뜨렸다.**)
- **자가 문 것이 곧 원인은 아니다 — 어긋난 자리는 마지막에 만진 손이 만든다.**
  (2026-09-09 문 뒤 언덕: 「떠 있음」 자가 덤불 −0.19m를 물었지만 범인은 언덕이 아니라 **빌드 뒤 도는
  `ClearPropsFrom*`**였다 — 비탈에서 소품을 옆으로 밀면 지표가 달라진다. **민 놈만** 다시 앉혔다:
  전체 스냅을 한 번 더 돌리면 **일부러 물가에 내려놓은 낚시터를 둑 위로 끌어올린다.**)
- **부호는 이름이 아니라 좌표가 정하게 한다.**
  (2026-09-09 입구: `inward`/`fwd` 같은 이름을 세 곳이 제각기 해석해 네 번 물렸다. `EntranceFront(pos,yaw)`
  — **마을 쪽이 앞** — 하나를 문짝·QA 눈·자가 함께 읽게 하자 부호 다툼이 끝났다.
  그 함수는 좌표가 어느 입구인지 원장에서 **못 찾으면 던진다** — 지어낸 방위로 잰 값은 세계가 아니다.)
  (2026-09-09 이어서: **정의를 고치면 그 정의를 읽는 자도 같이 옮겨라.** 문짝·QA 눈은 새 정의로 옮겼는데
  **도포 규칙만 옛 `-Heading(yaw)`로 남아** 9.5m 길을 세 곳 다 문 뒤에 깔고 있었다. 자까지 옛 정의라
  고치자마자 「문 앞 도포 0.00」 빨간불이 났다 — 자도 원장으로 옮겨야 끝난다.)
- **눈을 옮기면 자보다 NC가 먼저 낡는다.**
  (2026-09-09: 눈을 진입로로 옮기자 NC 둘이 무력해졌고 **둘 다 NC 쪽이 원인**이었다 — 구멍 NC가
  물건을 「판 자리」에 세워 아치 뒤에 두고 있었고(구멍은 문틀 사이지 판 자리가 아니다), 구멍이 통째로
  보이자 한 뼘 앞 등불이 13%밖에 못 덮었다. **자를 고칠 때 NC도 같이 다시 잰다.**)
- **화면에 한 픽셀도 안 나오는 것은 그림 결함이 아니다 — 다만 「어느 눈에서」 안 나오는지를 같이 적는다.**
  (2026-09-09 던전 3: 입구 12m 안 남의 렌더러 열(집터 일곱 포함)이 **QA 눈에서 전부 0.0%**였다.
  고치지 않고 셈만 남겼다 — 다만 QA 눈 하나로 「없다」고 하면 플레이 눈에서 되살아난다.)
- **NC 자리는 재서 고른다 — 어떤 자리에서는 같은 망가뜨림이 결함을 안 만든다.**
  (2026-09-09 포털: 던전 3을 90° 돌리면 4%/47%로 무는데, 던전 1은 같은 90°에 **58%/1%로 통과**한다
  — 카메라가 비스듬해 안쪽으로 뻗은 판도 화면을 막는다. **자리를 안 고르면 NC가 자를 봐준다.**)
  (2026-09-09 문 뒤 언덕: D1은 뒤가 **원래 산비탈**이라 「막히나」로는 언덕이 있으나 없으나 막힌다.
  **있음/없음으로 못 가르는 자리는 차이로 잰다** — 「언덕이 막히는 지점을 앞당긴 거리 ≥3m」.)
- **물건이 사라지면 그 물건을 쓰던 NC는 조용히 죽는다 — NC는 대상을 못 찾으면 건너뛰지 말고 실패해야 한다.**
  (2026-09-09: 배너를 걷은 뒤로 문구멍 가림 NC와 배너 실루엣 NC가 매 판 **로그 한 줄만 남기고 건너뛰어졌고
  게이트는 초록불**이었다. 상시 있는 등불로 바꾸고 「못 찾으면 실패」로 고쳤다.)
- **NC는 가리는 쪽에 유리해야 자의 힘을 잰다 — 약한 NC의 통과는 자의 무력함이 아니라 나의 게으름이다.**
  (2026-09-09 배너: NC 셋이 세 번 다 **NC 쪽이 틀렸다**. 문 반대편으로 밀면 화면은 안 가려지고,
  원점을 구멍에 맞추면 천이 구멍 **위로** 뜨고(원점은 장대 밑), 카메라를 향해 돌리면 얇은 판이 **선**이 된다.
  고쳐 세운 NC는 구멍 앞에 세우고 **두 회전 중 넓은 쪽**을 골라 24%로 걸렸다.)
- **자가 안 움직이는 자리는 자가 무딘 것이 아니라 그 화면의 원인이 다른 것이다 — 판별 테스트로 가르고 판정에서 빼라.**
  (2026-09-09 띠 대비: 마스크를 사실상 평평하게 만들자 `03`은 39.6→27.0으로 떨어졌는데 `16`은 74.4→75.4로
  꿈쩍도 안 했다. 색을 찍어 보니 `16`의 어두운 쪽은 R−B 14.9(회색빛) — 그 화면의 어둠은 얼룩이 아니라
  **기복의 그늘**이었다. 안 갈랐으면 `16` 수치를 근거로 마스크를 더 만졌을 것이다.)
- **무력한 자는 초록불로 남기지 않는다 — 판정에서 빼고, 왜 못 재는지를 적는다.**
  (2026-09-09 입구 「샘」 자: 판을 절반으로 줄여도 0% — 바운드 광선이 문 뒤 옆벽·기둥에 먼저 맞아
  판이 작아져도 뒤가 여전히 막혀 있었다. NC를 못 넘은 자를 게이트에 두면 **없는 안전을 판다.**)
  (2026-09-09 배너: **NC를 못 세우는 자는 게이트가 아니라 로그다 — 상시 초록불도 빈 통과다.**
  배너를 기둥 정면에 세워도 기둥은 98% 보인다(1.3k px 대 3.9k px) — 어떤 하한도 NC를 못 넘는다.)
- **바운드로 재는 자는 뒤에 선 것을 못 가른다** — 「뚫렸나」는 광선이 아니라 화면에서 묻는다.
- **화면 밖 도구(파이썬)는 절대값을 말할 수 없다 — 전후 비교로만 쓴다.**
  (2026-09-09 `qa_sky.py`: 띠 안의 배경 잔디까지 세므로 25%가 「샌다」는 뜻이 아니다. 파이썬은
  문구멍이 어디인지 모른다. 한계를 머리말에 적고 **같은 샷의 전후 전용**으로 못 박았다.)
- **합쳐서 재면 한 장만 성한 것도 통과한다 — 화면은 평균이 아니라 최악의 한 장으로 읽힌다.**
  (2026-09-09 배너: 두 장을 합쳐 「기둥겹침 38%」였는데 화면에서는 왼쪽만 걸리고 **오른쪽은 떠 있었다.**
  같은 랩에서 던전 3은 **입구와 무관한 4m 밖 배너**까지 세어 지표가 0%였고, 등불 가림을 따로 빼서
  「등불보임 **−43%**」가 나왔다 — **음수가 나오면 자를 의심한다.**)
- **점으로 성기게 재는 자는 화면에서 선으로 보이는 것을 통과시킨다.**
  (2026-09-09 던전 입구: 문구멍 49점 광선이 **0%**를 냈는데, 화면에는 얇은 배너가 붉은 선으로
  포털 위를 세로로 지나간다. 얇은 것은 표본 사이로 빠진다 — 실루엣을 묻는 자는 점이 아니라 픽셀로 잰다.)
- **한 색만 세는 자는 다른 색으로 새는 것을 못 본다.**
  (2026-09-09: 「문 너머로 바깥이 보이나」를 **파랑**으로 물어 0.10%가 나왔지만, 실제로 새던 것은
  아치 왼쪽으로 보이는 **초록 들판**이었다. 묻는 것이 「바깥」이면 세는 것도 바깥 전부여야 한다.)
- **같은 파일에서 세 번 물린 부호는 실수가 아니라 이름이다 — 방향은 주석 말고 이름 붙인 함수로 고정한다.**
  (2026-09-09 입구: `fwd`가 안쪽인 것을 문구멍 물리기·둔덕·배너에서 **세 번** 물었다.
  「기둥 앞면에 건다」는 주석은 세 번 다 그대로 있었다.)
- **꺼진 렌더러를 세는 자는 화면이 아니라 씬을 본다 — 자는 「무엇을」 셌는지 이름을 적어야 한다.**
  (2026-09-09 투구 걷기: 화면엔 맨머리와 왕관뿐인데 실루엣 자가 **꺼진 투구**를 재서 빨간불을 냈다.
  이름이 로그에 없어 엉뚱하게 왕관을 줄이는 코드를 썼다가 되돌렸다. **자가 이름을 안 적으면
  빨간불은 수리를 엉뚱한 곳으로 보낸다.** 같은 랩에서 렌더러 바운즈로 머리 높이를 쟀더니
  스킨드 바운드가 부풀어 통과시켰다 — **스킨드 바운즈는 화면의 크기가 아니다.**)
- **자가 안 보는 자리에 물건을 놓으면, 게이트는 초록불로 화면을 덮는다.**
  (2026-09-09 던전 입구: 문 뒤에 두른 바위가 카메라와 문 사이에 쌓여 근접 샷을 통째로 덮었는데
  셀프체크는 **두 판 다 EXIT=0**이었다. 자는 그 입구가 가려졌는지 묻지 않는다.
  **새 물건을 놓는 랩은 「그것이 무엇을 가리는가」를 묻는 자가 없으면 화면이 유일한 자다.**)
- **지면을 첫 히트나 가장 낮은 히트로 정하면, 둘 다 지면이 아니다.**
  (같은 랩: 첫 히트는 **문틀**을 맞아 「중심이 3.92m 솟음」, 가장 낮은 히트는 **지하 방 바닥**을 맞아
  「4m 고리가 5.4m차」. 위아래 극단은 자의 구멍이지 세계가 아니다 — 지면은 **지면인 것을 골라서** 잰다.)
- **고르는 자는 틀려도 빨간불이 안 난다.**
- **소스를 읽는 자는 파일 이름이 아니라 함수로 찾는다.**

## 이름과 예외

- **이름 원장이 샜을 때의 수리는 단어를 더 적는 것이 아니라, 이름 말고 다른 것으로 재는 자를 하나 더 세우는 것.**
- **예외는 자에 뚫은 구멍이다 — 무엇을 봐주는지 적었으면, 실제로 그것만 봐주는지도 세어 봐라.**
  (2026-09-09 던전: 「벽걸이는 봐준다」가 실제로 봐주던 것은 **기둥**이었고, 등불 넷이 기둥 안에 박혀 있었다.)
- **자가 찍는 수를 읽어라 — 「통과」만 보고 지나가면 세 줄만 보는 자를 몇 랩이고 데리고 다닌다.**
  (2026-09-09: 배선 217줄을 다른 파일로 옮긴 뒤에도 중복 배선을 세는 자는 **본체만 읽어**
  「서로 다른 게이트 **3개**, 중복 없음」이라고 매일 찍고 있었다. 빨간불도 빈 통과도 아닌 중간이라
  아무도 안 봤다. 읽는 곳을 고치니 **3개 → 220개**. **빨간불만 보는 눈은 죽은 자를 못 본다.**)
- **제외 경로는 조용히 넓어진다** — 매 판 제외 이름과 수를 찍고, 조항이 0개면 빨간불.
  **단 「0건이면 죽은 것」은 판정을 무르게 하는 조항에만 해당한다.** 재는 쪽과 찍는 쪽이 갈릴 때
  둘을 맞추려고 둔 **대칭 분기는 0건이어도 남긴다.** **다만 그것은 규칙이 한쪽에만 있을 때의 처방이다** —
  규칙이 **원본으로 들어가 양쪽이 같은 함수를 부르게 되면 그 분기는 두 번째 사본이므로 지운다.**
  두 벌이 살면 한쪽만 고쳐져 화면과 숫자가 다시 갈린다.
  (2026-09-09: 「대상 **뒤**에 선 것은 가릴 수 없다」를 QA 쪽에 0건짜리로 두었다가,
  다음 랩에서 그 규칙이 런타임 `Hide`에 들어가자 지웠다 — 지우는 것이 맞다.)
- **못 찾으면 자를 의심하기 전에, 세는 도구가 그것을 세고 있는지부터 본다.**
  (2026-09-09: `OutdoorCensus`의 건물 판정이 「사람이 들어 있으면 건너뛴다」보다 뒤에 있어
  **NPC를 품은 은행·잡화점·치유사가 한 번도 안 세어졌다.** 순서만 바꾸니 목록에 나왔다.
  적어 놓지 않은 제외는 코드 **순서**로도 생긴다.)

## 네거티브 컨트롤

- **이유가 다른 빨간불은 NC가 아니다** (컴파일 에러 ≠ NC).
- **무작위로 뽑은 희생양은 NC가 아니다 — 판마다 다른 것을 겨누면 초록도 빨간불도 우연이다.**
- **NC가 「되돌렸는데도 빨간불」이라고 하면 자를 의심하기 전에 세계를 봐라.**
- **NC가 병을 세게 재현하지 못하면 그렇다고 적고, 근거를 다른 축에서 세운다.**
  (2026-09-09 HUD 고정: 계정·시계를 둘 다 끈 NC가 **1장**밖에 안 갈렸다 — 두 판을 같은 기계에서
  연달아 돌리면 실시간 흐름이 우연히 비슷해지기 때문이다. 대신 **부하 시험**(`qa_shots`를 같이 돌려
  기계를 바쁘게 만든 판 대 조용한 판)에서 0장이 나온 것이 시계 고정이 일한다는 증거가 됐다.
  **약한 NC를 세다고 쓰는 것보다, 약하다고 쓰고 다른 증거를 대는 것이 낫다.**)
- **NC를 걸었으면 로그에서 NC 줄을 세어라 — 「안 걸린 NC」는 「반만 끈 NC」보다 나쁘다.**
  (2026-09-09: NC 스위치를 `qa_shots`에 걸어 비교했는데, `qa_shots`는 그 패스를 **아예 안 돈다.**
  스위치는 켜져 있고 아무 데도 닿지 않았다 — 그런 판의 「차이 없음」은 증거가 아니라 침묵이다.)
- **비교하는 두 판은 같은 절차를 지나야 한다.**
  (2026-09-09: 「전」을 커밋된 씬으로, 「후」를 셀프체크를 돌린 씬으로 찍어 24장이 달랐다.
  얼굴과 무관한 **프레이밍 이동**이었다 — 빌드 패스가 씬을 조금씩 조정한다.
  「전」도 같은 패스를 지나게 하자 0장이 됐다.)
  (2026-09-09 분할 ②: 보이는 차이 37장의 범인은 분할이 아니라 **사이에 낀 셀프체크 두 판**이었다 —
  같은 씬으로 샷만 두 번 찍으면 0장, 분할을 되돌려 찍어도 0장. **전·후 사이에 세계를 다시 세우는 것을
  끼우지 마라.**)
  **단 이 규칙은 「화면이 안 바뀌어야 하는 랩」의 것이다** — 굽는 랩(지형 도포 등)은 굽지 않으면 화면이
  안 바뀌므로 셀프체크가 절차의 일부다. 그런 랩은 픽셀 주장을 하지 말고 **눈으로 판정한다.**
  (2026-09-09 돌길: 개발 세션이 이 구분을 먼저 물어 왔다 — **규칙을 받을 때 적용 범위를 되묻는 것이 옳다.**)
- **반만 끈 NC는 약한 빨간불이라 더 위험하다 — 빨간불이 떴다고 NC가 맞는 것이 아니다.**
  (2026-09-09 씨앗: 첫 NC가 씬 파티클만 끄고 **샷 직전에 나는 VFX 셋은 안 꺼서** 4장만 빨갛게 떴다.
  전부 끄니 5장이었다. 빨간불의 **크기와 얼굴**까지 봐야 NC가 정말 그 결함을 재현한 것이다.)

## 값과 원장

- **자식으로 붙인 물건은 부모를 헐 때 같이 사라진다 — 재건되는 것에 매달지 마라.**
  (2026-09-09 배너: 문틀을 다시 짓는 패스가 기둥을 헐자 배너까지 없어져 자가 「배너 0개」로 울었다.
  부모는 재건 안 되는 루트에 두고 **자리만 유도**한다.)

- **한 값은 한 곳에서만 정해진다 — 여럿이면 화면은 그중 하나만 보여 준다.**
  (2026-09-09 광산: 지표와 배치가 `WorldSplat.MinePit` 한 좌표를 읽는다. 둘로 들었으면 다음 랩에서 흙만 옆으로 밀렸다.)
- **재질을 바꿔도 화면이 안 바뀌면, 재질이 아니라 그 재질이 얹힌 자리를 의심하라.**
  (2026-09-09 광장: 돌포장 무늬를 만들어 킷 판때기 104칸에 칠했는데 **한 픽셀도 안 바뀌었다** —
  Kenney 모델은 색상 아틀라스라 UV가 한 점에 몰려 있어 어떤 텍스처를 씌워도 단색이다.
  바닥을 지형 도포로 옮기자 무늬가 살았다.)
- **무늬·재질 생성기를 지역끼리 공유하면 지역 구분이 사라진다.**
  (2026-09-09 숲: 바닥이 마을 광장과 같은 무늬였던 원인은 잔풀 잡음 생성기를 그대로 쓴 것이었다.
  물건을 더 놓는 수리가 아니라 **생성기를 가르는** 수리였다.)
- **자가 우는데 화면이 옳으면, 무르게 할 것은 한도가 아니라 표본이다** — 그 자가 원래 무엇을
  묻고 있었는지부터 다시 읽어라. **두 자가 부딪히면 한쪽을 깎기 전에 둘의 표본이 겹치는지부터 본다.**
  (2026-09-09 잔디 커튼: 「중턱에 풀이 섞였나」를 묻는 자가 표본을 **높이로만** 골라 80° 암벽까지
  「중턱」으로 세고 거기에도 풀을 요구했다 — 그것이 커튼의 근원이었다. 하한 0.35를 낮추는 대신
  벽을 표본에서 뺐다. 뺀 수를 매 판 찍고 **한 자리도 안 빼면 죽은 예외로 실패**시킨다.)
  (2026-09-09 보스 머리: 첫 판이 머리대를 **위 25%**로 잡아 **왕관만 셌다** — 「머리대에 머리가 없다」는
  세계가 아니라 **자의 표본**이었다. 45%로 넓히자 겹침 100%가 바로 나왔다.)
- **자를 바꾸면 세계가 바뀐 것처럼 보인다 — 규칙을 의심하기 전에, 그 규칙이 무엇으로 쟀는지부터 본다.**
  **재는 자의 길이는 원장에 적는다. 자가 두 곳에 살면 판정도 둘이 된다.**
  (2026-09-09 산허리: 「55° 위는 벽」이 **둥근 흙산의 45° 비탈까지** 맨바위로 만들었다. 규칙이 아니라
  자가 틀렸다 — 잔주름이 심한 지형이라 0.587m 자가 8m 자보다 **평균 15° 높게** 읽는다(8m로 30~40°인 곳이
  0.587m로는 46°). **긴 자는 반대쪽으로 샌다**: 중앙차분이 8m 밖 절벽을 끌어와 벽 밑 평평한 단을
  벽으로 잡았다(60°↑의 8.8%에 풀 1.00). 그래서 짧은 쪽(3m)과 긴 쪽(8m)의 **작은 값**을 쓴다 → 1.1%.)
- **겹을 늘릴 땐 원장·굽는 쪽·셰이더 셋을 같이 고친다 — 하나를 빼먹으면 자는 초록인데
  그 겹이 없는 세계가 된다.** 안 그려지는 겹은 값으로 안 잡힌다 — **그 겹을 빨강으로 칠해 봐라.**
  (2026-09-09: `WorldSplat.LayerCount`는 10인데 셰이더가 9겹만 더하고 있었다. **CliffDark가 아예 안
  그려졌다.** 「한 겹 2%」 자는 내내 초록이었다. 빨강 테스트를 하기 전엔 아무리 어둡게 해도 화면이 안 변했다.)
- **섞어 놓은 것이 밋밋하면, 섞기를 의심하기 전에 재료를 하나씩 칠해 봐라.**
  (2026-09-09: 밝은 겹만 칠하면 결 9.71, 어두운 겹만 칠하면 4.71 — 섞는 과정이 아니라 **겹 자체**가
  밋밋했다. 색 범위가 0.42 대 0.20으로 **딱 절반**이었고 측정된 결 비율과 같았다.
  **톤은 그대로 두고 폭만 넓히는 것**이 수리였다 — 평균을 건드리면 갈라 놓은 톤이 무너진다.)
- **기본값은 결정이 아니다 — 아무도 정한 적 없는 값이 규칙처럼 보인다.**
  (2026-09-09 인물 샷: 시설 사람 넷이 전부 정북(0°)을 보고 있었다. 북을 보라는 규칙이 있어서가 아니라
  **얼굴 방향을 정하는 자리가 세계에 아예 없었다.** 해가 150°에서 오니 전원 역광이었고, 눈을 어디로
  옮겨도 얼굴은 그늘이었다 — 카메라 문제로 보이던 것이 **세계의 빈칸**이었다.
  채운 값도 세계 자료에서 유도했다: `offset`이 이미 「시설에서 손님 쪽」이므로 그것이 곧 얼굴 방향.)
  (2026-09-09 흔들리는 바닥: 우리 코드에 `Random.`도 `InitState`도 **0건**인데 판마다 그림이 달랐다.
  씬의 `ParticleSystem` 11곳이 `useAutoRandomSeed` **기본값 그대로**였다 — 유니티 기본이 「판마다 새 씨앗」이다.
  **아무도 무작위를 켠 적이 없는데 무작위였다.**)
- **시뮬레이션 대상과 씨앗 대상은 다르다 — 그물을 친 시점 뒤에 태어나는 것이 있다.**
  (2026-09-09: 씬의 파티클 11곳에 씨앗을 박아도 `24`·`25`는 채널차 130~170으로 남았다.
  그 VFX 셋은 **샷 직전에 태어나는 것**이라 그물 밖이었다. 씨앗은 계층 경로 FNV로 뽑는다 —
  `string.GetHashCode`는 실행마다 흔들려 **「고정했다」는 말만 남는다.**)
- **분할은 목적이 아니다 — 큰 파일을 줄이는 일은 위험이 커지는 자리에서 멈춘다.**
  (2026-09-09: 에디터 쪽 자 파일들을 여섯 랩에 걸쳐 갈라 최대 파일이 6,737 → 1,472줄이 됐고,
  그 1,472줄은 **런타임 전투 판정 본체**였다. 여기서부터는 「크니까 가른다」가 아니라
  **「고치려는 사람이 생겼을 때 가른다」**이다. 화면에 값을 못 주는 위험은 지지 않는다.)
- **여러 구간을 떼어 낼 때는 뒤에서부터 자른다 — 앞을 먼저 자르면 뒤 구간의 좌표가 밀린다.**
  (2026-09-09: 흩어진 주제를 모으다 함수 한가운데가 잘려 컴파일이 깨졌다(CS1524).
  되돌리고 **전체를 내림차순으로** 자르게 고쳤다.)
- **순서에 뜻이 있는 파일과 없는 파일은 가르는 법이 다르다 — 먼저 세어 어느 쪽인지부터 정한다.**
  (2026-09-09: `SliceSelfCheck.cs`는 1,840줄짜리 `Run` 하나라 **순서를 지킨 채 덩이별로** 뗐고,
  `Stealth.cs`는 지역 변수가 안 넘나드는 **독립 `Assert` 열 개**라 주제로 갈랐다.
  같은 「분할」이라도 판정 방법이 다르다 — 앞은 실행 순서 대조, 뒤는 문장 다중집합 대조.)
- **고르는 쪽은 탈락만 적지 말고 통과도 적는다 — 후보가 하나뿐이면 비교는 애초에 안 돈다.**
  (2026-09-09: 은행원은 통과 방위가 하나뿐이라 볕 비교가 한 번도 돌지 않았는데,
  로그에 탈락만 남아서 **비교가 안 돈다는 사실 자체가 안 보였다.**)
- **지시서에 남은 옛 수치는 없는 일을 시킨다 — 상태를 적을 땐 언제 잰 값인지 같이 적는다.**
  (2026-09-09: 검수가 「`VisualSliceBuilder.cs` 6,737줄 분할」을 1순위로 지시했는데, 그 파일은
  **전날 이미 partial 11개로 갈려 175줄**이었다. 개발 쪽이 **착수 전에 세어** 헛일을 막았다.
  「먼저 세라」는 지시는 지시받는 쪽만이 아니라 **지시하는 쪽에도 걸린다.**)
  (2026-09-09 **두 번째 재발**: 검수가 인수인계에 `VisualSliceBuilder.cs` 6,737줄을 또 적었다.
  실제로는 이미 partial 16장으로 갈려 **175줄**이었고, 분할 폭을 묻는 안건 하나가 통째로 헛것이었다.
  **같은 파일에서 두 번 틀렸으면 그 수치는 지시서에 적지 말고 셀 명령을 적는다.**)
- **「픽셀 0」을 조건으로 걸기 전에 바닥부터 재라 — 같은 코드·같은 씬으로 두 번 찍어도 다른 것이 있다.**
  (2026-09-09: 62장 중 **15장이 매 판 다르다**(불·VFX·애니메이션, 최대 22,011px).
  그러니 판정은 「0인가」가 아니라 **「바닥과 같은 얼굴인가」**다 — 다른 장이 늘거나 차이의 모양이
  달라지면 그때가 회귀다.)
  **그리고 자가 세는 것이 사람이 보는 것과 같은지 먼저 확인하라 — 채널차 1은 「다름」이 아니다.**
  (2026-09-09: 「바닥 50장」의 대부분이 **눈에 안 보이는 양자화 잡음**이었다(최대 채널차 1~3).
  임계를 걸자 던전·보스 샷은 차이 영역이 아예 없었다. **자를 고치니 바닥이 0장이 됐다** —
  흔들리는 세계가 아니라 흔들린다고 말하는 자였다.)
  **바닥은 매 판 다시 잰다 — 어제의 바닥은 오늘의 기준이 아니다.**
  (2026-09-09: 분할 전후가 62장 중 48장 달라 반려처럼 보였는데, 같은 코드로 두 판 다시 찍으니
  **그날의 바닥이 50장**이었다. 어제 값(15장)을 그냥 썼으면 멀쩡한 분할을 반려했을 것이다.
  다만 **바닥이 커진 것 자체는 따로 볼 신호다** — 무엇이 판마다 달라지고 있는지는 별개 질문이다.)
  (2026-09-09 분할: **로그 다중집합도 같은 코드로 흔들린다** — 분할판이 29줄 달랐는데 같은 코드 두 판의
  바닥이 29~35줄이었다(씬 fileID 순서에 따라 이름 나열과 표본이 흔들린다). **「같은가」는 바닥을 먼저
  재고 묻는다** — 안 쟀으면 없는 회귀를 쫓았을 것이다.)
- **줄을 옮기는 랩에서는 실행 순서를 문장 단위로 대조한다 — 그 대조가 픽셀보다 먼저 말한다.**
  (2026-09-09 게이트 분할: 넉 줄을 덩이 끝에 붙였더니 **27문장을 건너뛰어 먼저 돌았고**,
  「스케일·드레싱이 끝난 뒤 발을 붙인다」는 뜻이 깨져 한 샷이 104,155픽셀 달라졌다.
  제자리로 돌리고 **왜 여기여야 하는지**를 그 자리에 적었다 —
  **순서에 뜻이 있는 코드는 주제로 재배열하면 동작이 바뀐다.**)
- **재기 전에 적은 수는 수가 아니다.**
  (2026-09-09: 새 자의 상한 주석에 44.6%/18.5%를 짐작으로 써 넣었는데 실측은 10.1%/2.6% — 한 자릿수였다.
  짐작한 수가 주석에 남으면 다음 사람은 그것을 **측정값으로 읽는다.**)
- **자를 세울 땐 그 자가 못 보는 것도 자 옆에 적는다.**
  (2026-09-09 섞임 자: 이 자는 격자가 아니라 **격자가 앉을 면적**을 잰다. 같은 날카로움에서 무늬를
  눕히면 값은 그대로인데 화면엔 격자가 뜬다 — **그 판은 이 자가 못 잡는다.** 실제로 한 번 그랬다.)
- **새 자의 하한은 앞 판을 실제로 재서 잡고, 그 자가 잡았어야 할 판이 정말 못 지나가는지 확인한다.**
  (2026-09-09 두 샷 벌어짐: 하한을 상수로 두지 않고 **고르는 루프가 실제로 도는 눈금**
  — 방위 격자 한 칸 45° — 에서 가져왔다. **자의 하한은 고르는 쪽이 움직이는 단위에서 나온다.**)
  **재는 자가 세계를 바꾸면 안 된다.**
  (2026-09-09 결 자: 하한 0.055는 **반려판 0.051 · 수리판 0.061 · 밋밋한 겹으로 갈아 끼운 판 0.041**을
  다 재서 잡았다 — 반려된 판이 이 자를 실제로 못 지나간다. 임포트 설정을 바꾸지 않고 디스크의 PNG를 읽는다.)
- **이유가 사라진 값은 상수가 아니라 부채다 — 그 이유를 없앤 랩에서 같이 걷는다.**
  (2026-09-09: 포화 방지 0.14~0.86과 급경사 그늘 가산은 둘 다 「겹쳐야 세로줄이 끊긴다」에서 나온 값인데,
  그 줄은 랩 ⑧에서 없어졌다. 자도 마찬가지다 — 「절벽이 한 겹인가」는 이유가 사라진 자였고,
  **그 자가 초록인데 화면이 한 톤이었던 것**이 증거였다.)
- **자가 보는 것은 파일이 아니라 화면이어야 한다 — 파일 이름으로 여는 자는 분할 한 번에 다 죽는다.**
  (2026-09-09 HUD 분할: `Client/SliceHud.cs`를 **파일 이름으로 열어** 배선을 찾던 자 다섯이
  한꺼번에 빨간불이 됐다. 배선은 그대로였다. 원본을 이어 붙여 주는 한 자리를 만들고 다섯이 그걸 쓴다 —
  **느슨해진 것이 없다는 확인은 NC로**(배선 한 줄을 바꾸면 rc=1). 못 보는 것도 적었다:
  이름이 `SliceHud`로 안 시작하는 파일로 또 쪼개면 그물 밖이다.)
- **전후 비교의 자가 서려면 상태가 고정돼야 한다 — 누적되는 세계에서 찍은 두 장은 비교가 아니다.**
  (2026-09-09 HUD 샷: 분할 전후 다섯 장이 「보이는 차이」로 떴는데 열어 보니 **사냥터 대 마을**이었다
  (HP 46/51 대 50/50). HUD 요소는 같았다. **눈으로는 회귀 없음, 자로는 증명 못 함** —
  그 상태를 그대로 적는 것이 「통과」라고 쓰는 것보다 낫다.)
- **자를 물려받을 땐 유도 근거도 같이 물려받는다.**
- **상태 전환·보류에는 사유를 코드가 강제로 남긴다.**
- **무엇과 비교했는지가 곧 무엇을 쟀는지다 — 기준선이 다른 세계의 값이면 대조군을 직접 만들어라.**
  (2026-09-09 지형 셰이더: 어제 기준선과 그냥 비교했으면 월드 조망 0.8→1.4ms **1.7배**를 셰이더 탓으로
  적을 뻔했다. 그 사이 씬 내용이 달라졌을 뿐이다. 재질을 빼고 같은 절차로 다시 재니 실제 비용은 **+0.1ms**.
  「계측기가 결과를 만든다」의 짝 — 계측기가 옳아도 **비교 대상**이 딴 세계면 결론은 지어낸 것이다.)
- **생성물을 저장소에 넣을 땐 「누가 언제 다시 굽는가」를 같이 적는다.**
  (2026-09-09 지형 컨트롤 텍스처 8.4MB — 지금은 결정적이라 재커밋이 안 되지만, 도포가 바뀔 때마다
  저장소가 그만큼 무거워진다. 굽는 주체를 안 적으면 다음 사람은 이게 손으로 만든 것인지 모른다.)

## 화면과 판정

- **화면에서 지적받은 것을 월드 수치로 재면 초록이 나온다** — 증상이 있는 축에서 재고, 재는 눈과 찍는 눈은 원장 하나로 묶어라.
  (2026-09-09 부두: 「낚시터 높이 — 수면 대비 0.1m」는 내내 초록이었다. 그 자는 부두가 **물가에 있는지**만
  쟀고, 그 물가가 **뭍과 이어졌는지**는 아무도 안 쟀다. 화면이 틀렸는데 게이트가 초록이면
  게이트가 다른 것을 재고 있는 것이다.)
  (2026-09-09 인물 방위 계측기: 고르는 쪽은 껍데기 안까지 당겨 보는데 자는 **원래 거리에서만** 재서,
  화면에서 멀쩡한 상인이 「전 방위 막힘」으로 나왔다. **고르는 쪽과 다른 자리를 재면 그 숫자는 세계가 아니다.**)
- **고르는 루프 안에서는 한 값만 바꿀 수 없다 — 하한을 올리면 선택이 통째로 바뀐다.**
  (2026-09-09 `41` 프레임: 여유를 1.06배로 더 줬더니 당김 하한이 올라가 **방위 고르기가 다른 조합을 골라**
  왕관이 도로 프레임 밖으로 나갔다. 거리는 독립된 손잡이가 아니다.)
- **같은 대상을 찍는 두 샷이 같은 그림이면 한 장은 낭비다 — 프레이밍 자는 「들어왔나」만 묻고 「다른가」는 안 묻는다.**
  (2026-09-09: `41`을 왕관까지 물리자 프레임이 넓어져 **`17`과 거의 같은 화면**이 됐다.
  표식을 넣는 자를 세울 때 **샷끼리의 차이를 지키는 자**도 같이 세워야 한다.)
- **그것을 그것으로 읽게 하는 표식이 프레임 밖에 있으면, 그 샷은 그것의 샷이 아니다.**
  (2026-09-09 투구 걷기: 45° 근접에선 걷은 판이 이겼는데(눈 하나→둘+왕관) 정면 초근접에선 **전이** 이겼다.
  맨머리는 왕관이 화면 밖으로 나가 **그냥 사람 얼굴**이 됐기 때문이다. 같은 변경이 각도마다 갈리면
  고칠 것은 대상이 아니라 **프레임**이다.)
- **없어 보이는 것이 정말 없는지부터 세어라 — 대개는 없는 게 아니라 큰 것에 묻혀 있다.**
  (2026-09-09 광산: 갱구 기둥·광맥은 있었고 2.1배 바위 28개가 삼키고 있었다. 수리는 붓기가 아니라 덜어내기였다.)
  (2026-09-09 밭: 검수가 화면을 보고 「깃발·상자·기둥이 겹쳐 선 소품 무더기」라고 이름 붙였는데,
  세어 보니 **삼켜진 던전 2 입구**였다 — 밭 뙈기가 입구 위를 덮고 있었다. **검수가 붙인 이름도 추정이다.**
  (2026-09-09 던전 1 입구: 「문 너머로 하늘이 보인다」는 보고를 검수가 화면을 안 열고 받아 **막이 랩**을
  지시했다. 자(`qa_sky.py`)를 먼저 만드니 문 구멍 파랑 **0.10%** — 이미 막혀 있었고, 보이던 하늘은 문
  **옆과 위**였다. **셈이 눈을 고쳤고, 검수는 없는 일을 시켰다** — 보고가 붙인 이름도 화면으로 확인하고 받는다.)
  고치기 전에 세는 도구(`OutdoorCensus`)를 먼저 만든 것이 정체를 밝혔다.)
  (2026-09-09 `60`: 개발 세션이 제 눈으로 의심한 둘이 **재보니 둘 다 아니었다** — 언덕은 오히려 모래가
  적었고(안 0.04 vs 밖 0.14) 크림빛은 **마른풀**, 포털이 문틀 밖으로 나온 비율은 **0.0%**였다.
  **화면의 인상은 가설이지 발견이 아니다.** 그 자의 첫 판은 문틀 **실루엣**과 비교해 정면에서도 59.7%가
  나왔는데 아치 가운데가 원래 비어 있어서였다 — **「무엇의 밖인가」는 윤곽으로 정의한다.**)
- **섞으라는 지시를 개체마다 굴리면 눈에서 평균이 난다 — 갈림은 덩이 단위여야 보인다.**
  (2026-09-09 나무: 그루마다 색을 굴렸더니 네 색이 화면에서 한 톤으로 평균 났다. 군락(4.2m 칸)으로 묶자 갈렸다.)
- **한 겹이던 것을 두 겹으로 나누면, 그 한 겹만 읽던 다른 자들이 전부 거짓 빨간불이 된다.**
  (2026-09-09 잔디: 풀을 짙은 풀·마른 풀로 나누자 평지 풀 하한·산 중턱 자가 울었다. 값은 그대로 두고
  **계열 합**을 읽게 고쳤다 — 하한을 낮추는 것이 아니다.)
  (2026-09-09 호수 사면: 자 셋이 서로 반대로 당겼다 — 안식각 상한을 걸면 「물가 모래 최소 0.6」이 울고,
  그것을 풀면 「모래띠 폭」이 운다. 답은 값이 아니라 **자가 묻는 말**이었다: 물가에서 물어야 하는 것은
  **「모래인가」가 아니라 「잔디가 아닌가」**다. 급한 물가는 자갈·바위가 맞다.
  **자 셋이 서로 반대로 당기면, 셋 중 하나가 틀린 것을 묻고 있다.**)
- **규칙과 규칙 사이에 아무도 안 맡은 구간이 있으면 거기서 얼룩이 난다 — 경계는 겹치게 잡는다.**
  (2026-09-09 호수 사면: 벽 판정은 **50°부터**, 모래는 안식각 **34°까지** — 그 사이 30~50°가 무주공산이라
  물가 모래가 41° 사면을 통째로 덮었다(도포 모래 79%·벽 판정 0.00). 산은 같은 자리에서 결이 산다(0.20).)
  (수리: **걷어낸 몫을 그대로 다른 것으로 채우면 경계가 저절로 겹친다** — 25°에서 줄어 34°에서 0이 되는
  모래, 그 줄어든 만큼이 자갈(너덜). 사이 구간은 둘이 나눠 가지므로 빈 자리가 안 생긴다.
  모래 79% → 모래 10%·자갈 69%. 이때 **내가 지난 랩에 세운 자도 같은 병이었다** — 모래 한 겹만 읽어
  0.0m라고 울었다. **새 규칙을 세우면 내 옛 자부터 다시 읽어라.**)
- **자리를 순서대로 고르는 자에 상한을 걸면, 상한이 곧 편향이다 — 못 채운 자리는 늘 훑는 순서의 끝이다.**
  (2026-09-09 절벽: 격자 순서로 훑는 자에 「34개」 상한을 걸었더니 동북 사면만 채우고 **가장 크게 보이는
  앞쪽 절벽을 비웠다.** 무작위가 아니라 **상한**이 필요한 자리를 비운 경우.)
- **새 모델을 쓰기 전에, 그 모델을 칠하는 패스가 있는지부터 본다.**
  (2026-09-09: Nature 킷 바위의 dirt/grass 서브메시가 무텍스처라 재질 자가 228개로 울었다.
  재질 목록을 늘리는 것이 먼저지, 자를 무르게 하는 것이 아니다.)
- **의미 있는 배치는 우연히 생기지 않는다 — 무작위 산포는 하필 필요한 자리를 비워 둔다.**
  (2026-09-09 사냥터: 엄폐물은 결정적 자리에 놓아야 했다. 그리고 무리 **앞**에 놓으니 몹이 바위 뒤로
  숨었다 — **엄폐물은 무리를 가리는 것이 아니라 무리가 돌아 들어갈 자리다.**)
- **관찰이 맞아도 수리 방향은 틀릴 수 있다.** / **프레이밍으로 못 고치는 것이 있다.**
- **미결은 코드 옆에 남긴다.**
- **샷은 게임이 아니다 — 샷이 못 찍는 것과 세계가 틀린 것을 가르고, 세계를 샷에 맞추지 마라.**
  **유도가 그럴듯한 자일수록 무엇을 기준으로 삼았는지 되물어라: 화면인가, 세계인가.**
  (2026-09-09 은행원: 「근접 샷 하한 1.9m」에서 2.05m를 유도해 사람을 벽에서 밀었다. 유도는 그럴듯했지만
  기준이 **샷**이었다. 다시 재니 몸은 껍데기와 안 겹쳤다 — 세계는 옳고 그 각도로 못 찍을 뿐이었다.
  자를 되돌리고 **못 찍는 이유를 매 판 로그로** 남겼다. 광장 4.75m와 같은 병.)
- **어두운 화면이 늘 세계의 잘못은 아니다 — 해가 어디 있는지부터 보고, 조명 대신 눈을 옮겨라.**
  (2026-09-09 능선: 눈자리가 **그늘 면을 정면으로** 보고 있어 검은 실루엣이었다. 조명을 만지면 스물한 샷이
  같이 바뀌므로 눈을 볕 쪽으로 옮겼다. 위 문장의 뒷면이다 — **세계가 옳으면 샷을 고치는 것이 맞다.**)
- **잘 세운 자는 화면보다 먼저 말한다 — 그 판은 찍기 전에 걷힌다.**
  (2026-09-09: 등고선 좌표 하나로 벽 무늬를 뽑자 낙하선/등고선 비율이 1.63→**1.22**로 떨어졌다.
  「등고선 쪽으로 더 변한다 = 세로줄」이고, 화면도 같았다. 자가 먼저 말했으므로 시도는 그 자리에서 걷혔다.)
- **촘촘한 주기를 축에 정렬시키면 격자로 읽힌다 — 눕히는 것은 큰 덩이에만, 잔 주기는 눕히지 마라.**
  (2026-09-09: 벽 평면의 높이 축 주파수를 4배로 올리자 비스듬한 면에 **바둑판**이 떴다.
  검수는 「두 평면이 겹치는 자리」로 단정했으나 **틀렸다** — 겹치는 폭을 좁혀도(지수 4→16)
  화면이 그대로였다. 원인은 **잔 주기 7m에도 눕히기를 먹여 벽에서 1.7m 간격이 된 것**이었다.
  **화면이 안 변하는 수정은 원인이 아니다** — 그 판이 아니었으면 엉뚱한 분기를 지을 뻔했다.)
- **상식 모순은 물건을 옮기기 전에 「사람이라면 어떻게 했을까」로 푼다.**
  (2026-09-09 던전 2: 입구가 농경지 중심에서 14.3m, 둘레 24m 지표의 **59%가 갈아엎은 흙**이었다.
  입구를 옮기면 워프·도달 게이트를, 지역을 옮기면 그 지역 소품 전부를 건드린다. 대신 **문 앞은
  갈지 않는다**는 규칙 하나로 풀었다 — 7m 안은 지역 도포 0, 12m에서 원래대로.)
- **커밋 메시지에 백틱을 쓰지 마라 — 셸이 그 구절을 실행하고 문장이 사라진다.**
  (2026-09-09 `820192da`: 한 구절이 통째로 없어졌다. force-push 금지라 되돌릴 수 없어 인수인계에 남겼다.)
- **소품으로 깐 길은 깔개다 — 길은 땅에 칠한다.**
  (2026-09-09 입구: 킷 `ground_pathTile`(2.10m)을 1.6m 간격으로 넷 깔아 0.50m씩 겹친 6.9×2.1m **한 판**이
  됐다. 이 킷의 땅 타일은 **제 흙바닥을 통째로 들고 다녀서** 「길」이 아니라 「깔개」로 읽힌다.
  땅에 칠할 때 「땅」과 「깔개」를 가르는 셋: **가장자리를 노이즈로 흔들고 · 끝에서 가늘어지며 사라지고 ·
  세기를 다 안 올려 밑이 비치게.** 광장에서 이미 나온 답이었다 — **처방은 다시 발명하지 않는다.**)
- **좁은 이야기는 넓은 이야기 **뒤에** 칠한다 — 세기로 겨루게 하면 문 앞이 지역에 진다.**
  (2026-09-09: 문 앞 길을 세기 비교로 끼웠더니 던전 2가 **0.00** — 그 앞을 농경지 0.95와 마을 길 1.00이
  지나가 0.88짜리가 매번 졌다. 규칙 순서로 풀 일을 값으로 풀려 하면 값만 올라간다.)
- **덩이는 크기만 맞으면 되는 것이 아니다 — 지형이 가진 방향을 안 따르면 무늬가 아니라 얼룩이 된다.**
  (2026-09-09 절벽 덩이: 주기를 두 자리에서 같이 유도해 크기는 맞췄는데, 화면에서는 **곰팡이·위장 무늬**로
  읽혔다. 등방성 잡음이라 골·능선·층 어느 것과도 관계없이 떠 있었기 때문이다.
  암반의 어두운 부분은 **등고선 방향으로 누운 층**이지 둥근 반점이 아니다.)
  (2026-09-09 띠 대비: **포화된 값에서는 건너가는 폭을 넓혀도 대비가 안 준다 — 대비는 양 끝이 정한다.**
  잡음이 문턱에서 멀어 이미 양 끝에 몰려 있으면 폭 0.20→0.24는 중간톤을 문턱 언저리에서만 늘리고
  덩이 배분만 무너뜨린다. 클램프를 0.05~0.95 → 0.18~0.82로 **안으로 당겨** 대비 39.6→34.4.)
- **물러설 수 없으면 원근을 없애라 — 판정 대상이 안 찍히는 샷은 판정이 아니다.**
  (2026-09-09 보스 머리: 「3.4m·55°라 원근이 부풀린 것 아니냐」를 가르려 10m 물러섰더니 **벽 속**이었다.
  물러서는 대신 **같은 자리에서 직교 투영 한 장**을 찍었고, 얼굴이 같았다 — 원근 탓이 아니었다.
  샷을 못 옮기는 방에서도 **재는 방식은 바꿀 수 있다.**)
- **되돌리는 것도 수리다** — 잘못 세운 자는 남겨 두면 계속 세계를 민다.
- **결과를 두 번 손봐도 안 끊기면, 그건 결과가 아니라 성질이다 — 그때는 원인을 바꾼다.**
  (2026-09-09 절벽 세로줄: 무늬를 두 겹으로 끊고(랩 ⑥) 바위를 104개까지 얹어도(랩 ⑦) 화면의 줄은
  그대로였다. 원인은 **평면 투영**이었고, 삼면 투영으로 바꾸자 한 랩에 사라졌다.
  보류해 두었던 반대 문장 「투영을 못 바꾸면 결과를 끊어라」는 **기각한다** — 화면에서 두 번 실패했다.)

## 조사와 보고

- **겹쳐 볼 때 색만 보지 마라 — 톤이 갈렸는지와 결이 남았는지는 다른 질문이다.**
  (2026-09-09 마스크 투영: 보고는 「`16` 앞 절벽 위쪽에 덩이가 앉아 **톤이 갈렸다**」였고 그건 맞았다.
  같은 자리를 확대해 보니 랩 ⑧이 얻어 낸 **암석 결이 통째로 사라져** 매끈한 플라스틱 면이 돼 있었다.
  고친 축에서 좋아진 것을 확인하는 눈은, 같은 화면의 다른 축이 나빠진 것을 잘 못 본다.)
- **「전과 같다」는 눈으로 겹쳐 본 뒤에 쓴다 — 겹쳐 보지 않고 쓴 「변화 없음」은 보고가 아니라 기대다.**
  (2026-09-09 잔디 커튼: 보고는 「`03_hunt_mobs`의 완만한 산은 전과 같이 초록이 올라간다」였는데,
  전후를 겹쳐 보니 **산허리의 초록이 통째로 걷혀 베이지 한 장**이 됐다. 회귀 목록에 이름을 올려 놓고
  화면을 안 본 것이 아니라, **의도한 곳만 보고 나머지는 안 바뀌었을 것이라 믿은** 것이다.)
- **셈이 전제를 무너뜨리면 승인받은 방법도 하지 않는다 — 안 하는 것이 결과다.**
  (2026-09-09 보스 머리 ②: 「투구가 비뚤게 씌워졌을 것」이라는 전제로 정렬 랩을 승인받았는데,
  재 보니 **회전 0°·좌우 중심차 0.00m**였다. 옮길 것이 없어 정렬 판을 찍지 않았다.
  승인은 방법에 대한 것이지 **그 방법을 반드시 쓰라는 명령이 아니다.**)
- **화면 변경이 0인 랩에서는 씬을 되돌리고 소스만 커밋한다.**
  (2026-09-09: 셀프체크가 매 판 `Bootstrap.unity`의 fileID 순서를 뒤바꿔 **134k줄 diff**를 만든다.
  같은 저장소를 다른 사람이 쓰고 있으면 그 diff가 곧 충돌이다.)
- **모순이 없으면 랩을 열지 않는다 — 안 여는 것도 결과다. 대신 센 것을 코드에 남겨라.**
  (2026-09-09: 지시받은 자를 **먼저** 만들었더니 고칠 것이 없어 랩이 그대로 닫혔다.
  **자를 먼저 만드는 순서는 수리를 아끼는 것이 아니라 안 해도 되는 수리를 없앤다.**)
  (2026-09-09 물: 강 중심선 125표본이 전부 수면 아래였고, 시작은 호수 안·끝은 바다로 이어졌다.
  「산자락이 마르다」는 표현의 한계지 배치 문제가 아니므로 **경사 물 메시를 만들지 않았다.**
  셈을 `OutdoorCensus.Water.cs`에 남겨 다음 사람이 같은 의심을 하면 3분에 다시 센다.
  자의 한계도 옆에 적었다 — **지표 높이만 읽으므로 물 메시가 어디까지 그려지는지는 모른다. 그건 눈.**)
  (2026-09-09 배너: 자리 81칸과 매달기 한 판을 다 재고 **걷었다**. **걷는 것도 결과다 — 다만 왜 못 붙였는지를
  계측기 머리말에 남겨야 결과가 된다**(`banner-red`는 장대+가로대+천이 한 몸이고 원점이 장대 밑이라
  기둥 표면에 대면 천이 옆으로 뻗는다). 다시 시도하려는 사람이 먼저 읽는다.)
- **조사 결과는 확인/미확인을 나눠 적는다** — 「있을 것 같다」를 표에 섞으면 다음 사람이 그걸 근거로
  오너 승인을 받는다. 근거가 세상에 없으면 **없다고 말하는 것**이 지시를 지키는 것이다.
- **판정이 엇갈리면 고르지 말고 묻는다. 판정을 바꾸는 쪽은 무엇을 무효로 하는지 적는다.**
  (2026-09-09: 같은 패스를 두 번 넣고 두 번 걷었다. 개발 쪽은 옛 메시지를 최신으로 읽고 골라 실행했고,
  검수 쪽은 판정을 뒤집으면서 **「앞 판정은 무효」라고 쓰지 않았다.** 양쪽 다 고칠 것이 있다.)
- **잔재와 증거는 다르다 — 치우는 규칙을 세울 땐 먼저 무엇이 증거인지부터 예외로 적는다.**
  (2026-09-09: 「이 판이 다시 안 찍은 PNG를 지운다」를 넣으면서 `*_before.png` 예외를 빠뜨려
  **옛 전판 18장을 지웠다**. `builds/`는 git 밖이라 되돌릴 수 없다. 치우는 규칙은 지우는 규칙이고,
  지우는 규칙에는 되돌릴 길이 없다 — 판단은 **지우기 전에** 끝나 있어야 한다.
  기준을 이름 목록이 아니라 **판의 시각**으로 잡은 것은 옳다: 목록으로 지우면 목록을 고칠 때마다
  같은 잔재가 다시 난다.)
- **버린 시도의 부산물은 그 랩 안에서 치운다** — 다음 사람이 「이건 왜 있지」를 묻게 두지 않는다.
  **다만 사유는 남긴다 — 안 남기면 다음 사람이 같은 곳을 다시 판다.**
  (2026-09-09 절벽 세로줄, 걷은 시도 넷: ①킷 바위 60→104개 — 줄은 그대로 ②지형을 6m마다 양자화해
  수평 턱 — 줄은 그대로고 **능선만 첨탑**이 됐다 ③바위를 옆으로 눕히기 — 암반 턱이 아니라 **판때기 선반**
  ④크기 2.2~4.0배 — 「붙여 놓은 흰 상자」. 결론: **저폴리 계단 상자로는 60m 암벽의 일부가 될 수 없다.**
  키우면 이물, 줄이면 안 보인다 — 남은 수단은 투영을 바꾸는 것뿐이었다.)
- **「안 어울린다」는 눈의 말이지만, 걷을 때는 숫자로 걷는다 — 얼마나 어긋나는지를 재 두면
  다음 사람이 다시 시도할지 말지를 안다. 두 번 실패한 시도는 사유가 두 줄이어야 한다.**
  (2026-09-09 절벽 바위 재시도: 바위 표면 (168,159,144) vs 바로 옆 벽 (101,96,83) — **1.66배**.
  맞추려면 알베도 0.62배인데 야외 색조 하한 0.30에 걸리고, **차이의 절반은 톤이 아니라 평평한 면이
  해를 받는 각**이라 톤으로는 못 지운다. 「어떤 개수·크기·톤으로도 안 된다」가 이렇게 닫혔다.)
- **응답 성공 ≠ 지시 전달 성공**, **커밋했다 ≠ 배포됐다**, **저장됐다 ≠ 반영됐다**,
  **초록이 나왔다 ≠ 내가 의도한 대로 돌았다.**
  (2026-09-09 HUD 고정: 계정을 지운다는 코드가 **엉뚱한 폴더**를 지우고 있었는데 화면은 조용했다 —
  같이 넣은 시계 고정이 이미 일하고 있었기 때문이다. **지우는 코드는 지운 것을 세어 확인한다.**)
  (2026-09-09: 보수와 자를 **씬 저장 뒤**에 배선해 「자는 초록인데 화면은 그대로」인 판을 세 번 냈다.
  **QA가 보는 것은 저장된 씬이다** — 고치는 것도 재는 것도 저장 앞에서 돌아야 한다.)
  (같은 함정 두 번째: 재질·지형은 **에셋**이라 `SaveAssets()` 없이는 디스크에 옛 값이 남는다.
  **자는 메모리를 보고 화면은 디스크를 본다 — 생성물이 에셋이면 저장까지가 수리다.**)

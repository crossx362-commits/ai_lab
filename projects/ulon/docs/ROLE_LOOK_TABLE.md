# 역할 ↔ 외형 대조표 (2026-09-07, 검수 지시)

> **이 문서는 표다. 여기서 아무것도 고치지 않았다.** 우선순위는 검수가 표를 보고 정한다.
>
> 왜 만드는가: 은행이 풍차, 훈련사가 마법사, 야생하트가 덤불 — 전부 「기능은 있는데 그 기능으로
> 안 보이는」 같은 종류인데, 지금까지 **하나씩 우연히** 발견돼 왔다. 무엇이 무엇처럼 보여야 하는지
> 적어 두지 않으면 계속 그렇게 발견된다.
>
> **근거**(「무엇처럼 보여야 하나」 열)는 기획서에서만 끌어왔다 — 내 취향으로 채우지 않았다.
> - §8.1 「멀리서도 무엇인지 즉시 읽히는 실루엣. 장비와 직업 정체성이 눈에 먼저 들어와야 한다.」
> - §8.1 「큰 머리·명확한 손발·큰 장비 실루엣으로 **역할을 읽게** 한다.」
> - §8.2 「벽 타일 더미를 집으로 치지 않는다. **문·창·지붕이 붙어 멀리서 집으로 읽혀야** 한다.」
>
> **근거 데이터**는 손으로 적지 않았다. `Editor/RoleLookAudit.cs`(감사 도구, 게이트 아님)가 씬을 훑어
> 「오브젝트 | 역할 | 표시명 | 보이는 메시 | 크기」를 찍는다. 아래 수치는 그 로그
> (`unity/Logs/role_look.log`, 41행)를 그대로 옮긴 것이다. 다시 뽑으려면:
>
> ```bash
> Unity -batchmode -nographics -quit -projectPath unity -executeMethod Ulon.Editor.RoleLookAudit.Run -logFile unity/Logs/role_look.log
> ```
>
> 크기는 (가로, 높이, 세로) m. **사람·몹의 크기는 신뢰하지 마라** — 스킨드 메시 바운드가 부푼다
> (기존 교훈). 시설·소품 크기는 실제 값이다.

## 1. 마을 시설 — 「그 기능으로 보이는가」

| 역할 | 지금 무엇으로 보이나 | 무엇처럼 보여야 하나(근거) | 판정 |
| --- | --- | --- | --- |
| **[P1] 은행 `Banker`** | `windmill.fbx` **날개 한 장**(바운드 0.47×3.11×3.11, 렌더러 1개) — 몸통도 안도 없다. 반투명 처리는 제대로 걸렸지만 **원래 얇은 판이라 비쳐도 여전히 아무것도 아닌 것으로 보인다**(검수 2026-09-07, `26_behind_bank.png`) | **들어갈 수 있는 은행 건물**. §8.2 문·창·지붕이 붙어 멀리서 집으로 | **불합격** |
| **[P1] 던전 입구 문틀** ×3 | 무텍스처 **검은 직육면체 3개**(기둥 2 + 상인방) + 검은 문구멍 판. 대낮 야외에 검은 비석 셋으로 서서 마을 경관까지 망친다(`09_d2_entrance.png`) | 등록 CC0 던전 조각(아치·기둥·벽)으로 만든 문. §8.2 「프리미티브 색칠 큐브」 금지 | **불합격**(소품 랩에서 반려한 것과 **같은 결함이 입구에만 남아 있었다**) |
| 상점 `Vendor`(잡화) | `stall.fbx` 좌판, 1.0×**0.4**×0.7 — 높이 40cm | 물건이 쌓인 좌판 + **상인**. §8.1 역할이 읽히는 실루엣 | **어긋남** |
| 대장간 `Forge` | `stall.fbx` — **상점과 같은 메시** | 모루·화로·망치가 읽히는 자리. §8.1 | **어긋남**(상점과 구분 불가) |
| 목공소 `Carpenter` | `stall-bench.fbx`, 0.9×**0.2**×0.3 — 높이 20cm | 작업대·톱·목재. §8.1 | **어긋남** |
| 화덕 `Campfire` | **`lantern.fbx`(등불)** | 불·솥이 있는 화덕. §8.1 | **어긋남**(등불이 화덕 노릇) |
| 절구 `Mortar` | `stall-stool.fbx`(걸상), 0.3×0.2×0.3 | 절구·약병이 읽히는 연금 자리. §8.1 | **어긋남** |
| 훈련소 `Trainer` | `Mage.fbx` — **마법사 차림 사람** | 훈련사. §8.1 직업 정체성이 눈에 먼저 | **어긋남**(모든 기술을 마법사가 가르친다) |
| 치유소 `Healer` | `fountain-round.fbx`(분수), 2.0×**0.3**×2.0 | 치유사(사람) 또는 신전. 표시명이 「치유사」인데 사람이 없다 | **어긋남** |
| 마구간 `Stable` | `stall.fbx`+`poles.fbx` — 표시명은 「마구간지기」 | 마구간(축사)과 **지기(사람)**. §8.1 | **어긋남**(사람 없음) |
| 집터 `HousePlotStation` | `poles.fbx`, 0.1×1.0×1.0 | 「여기에 집을 지을 수 있다」가 읽히는 표식 | 판단 보류(표식으로는 읽힐 수 있음 — 화면 판정 필요) |
| 낚시터 `FishingSpot` | **`watermill.fbx`(물레방아)**, 0.9×1.8×1.8 | 물가·낚시 자리. §8.1 | **어긋남** |

## 2. 자원 — 「무엇을 캐는지 읽히는가」

| 역할 | 지금 무엇으로 보이나 | 무엇처럼 보여야 하나(근거) | 판정 |
| --- | --- | --- | --- |
| 철광맥 `MineVein1~3`·`IronVein` | `rock-large.fbx`, 1.7~2.3m | 광맥(광석이 박힌 바위). §8.1 | 맞음(바위로 읽힘, 광석 티는 약함) |
| 필드 철광 `FieldOre` | `rock_largeA.fbx`, 0.8×0.3×1.0 — 납작 | 위와 같음 | 판단 보류(30cm라 돌멩이로 읽힐 수 있음) |
| 나무 `OakTree`·`FieldOak` | `tree-high.fbx`, 1.0×3.0×1.0 | 벨 수 있는 나무 | **맞음** |
| 아마(천) `FieldFlax` | `plant_bushLarge.fbx` 덤불 1개, 0.4×0.2×0.3 | 밭·작물로 읽히는 것. §8.1 | **어긋남**(20cm 덤불 하나가 「아마밭」) |
| 수지 `ResinBush` | `plant_bushLarge.fbx`, 0.4×0.2×0.3 | 수액 나는 덤불 | 맞음(덤불이 덤불) |

## 3. 사람 — 「누구인지 읽히는가」

| 역할 | 지금 무엇으로 보이나 | 무엇처럼 보여야 하나(근거) | 판정 |
| --- | --- | --- | --- |
| 플레이어 | `Knight.fbx` | 플레이어. 장비가 실루엣으로 읽혀야 | 맞음 |
| 동료 `Companion` | `Knight.fbx`(플레이어와 같은 모델) | 내 편임이 구분돼야. §8.1 | **어긋남**(플레이어와 같은 모습) |
| 훈련사 | `Mage.fbx` | 위 1번 표와 같은 행 | **어긋남** |
| 상인·은행원·마구간지기 | **없음**(전부 소품이 역할을 대신한다) | 말을 거는 상대. §18.19 마을 서비스 | **어긋남**(마을에 서비스 NPC 사람이 없다) |

## 4. 몹·보스 — 「종류가 갈리는가」

사람형 모델 7종을 19체가 돌려쓴다. 지금은 **잡몹 착색 + 보스 대비 게이트**로 갈리고 있다.

| 모델 | 쓰는 몹 | 판정 |
| --- | --- | --- |
| `Knight.fbx` | 플레이어·야만인·던전야만인·강철폭군(보스)·기사×2 | 겹침 6체 — 보스와 잡몹이 **같은 모델**(대비 게이트로만 갈림) |
| `Mage.fbx` | 도적·던전도적·헥사크(보스)·**훈련사** | 겹침 4체 + 역할 혼선(도적이 마법사 차림) |
| `Rogue.fbx` | 자객×2·섀도우캡틴(보스) | 겹침 3체 |
| `Skeleton_Warrior.fbx` | 스켈레톤·던전스켈레톤·본워든(보스) | 겹침 3체 |
| `Skeleton_Mage`·`Skeleton_Minion`·`Skeleton_Rogue` | 주술사·졸병·해골도적 각 1 | 맞음 |

- **도적이 `Mage.fbx`**인 것은 이름-외형 어긋남이다(§8.1 직업 정체성).
- 보스 3체는 잡몹과 같은 모델을 쓰되 왕관·큰 무기·크기 1.3~1.5배로 갈린다(§10.2, 기존 게이트).

## 5. 조련 생물 — 자산 자체가 없다

| 역할 | 지금 무엇으로 보이나 | 무엇처럼 보여야 하나 | 판정 |
| --- | --- | --- | --- |
| 야생하트 `TameCritter` | `plant_bushLarge.fbx` 덤불 | 짐승 | **자산 없음**(저장소에 동물 메시 0개, 오너 승인 대기) |
| 멧돼지 `TameBoar` | `plant_bush.fbx` 덤불 | 짐승 | **자산 없음**(위와 같음) |

## 6. 곁가지 — 표 밖이지만 같은 종류

- 던전 입구 문틀은 **§1의 P1 행으로 올렸다**(검수 2026-09-07 판정: 톤 문제가 아니라 §8.2가 막는
  프리미티브 그 자체). 재료: `DungeonWall`(0.16/0.27 회색) 기둥·상인방 + `DungeonPortal`(0.03/0.08
  거의 검정) 문구멍, 마을 서·동·남서 세 곳.

## 7. 이 표가 답하지 못하는 것

- 「보인다/안 보인다」는 잰 값이지만 **「그것처럼 읽힌다」는 사람 눈**이다. 판정 열의 「어긋남」은
  기능명과 메시가 다른 것을 가리킬 뿐, 화면에서 얼마나 나쁜지는 샷으로 봐야 한다.
- 사람·몹의 크기 수치는 스킨드 메시 바운드라 부풀어 있다 — 크기 비교에 쓰지 마라.
- 지금 씬에 **없는** 역할(여관·상인 NPC 등)은 표에 행이 없다. 없는 것을 못 세는 한계다.

# 울온 Blender 소품 (오케스트레이터 target `ulon_props`)

CC0 팩에 없는 물건(절구통·모루·화덕…)을 Blender 배치(bpy)로 만든다. `build.py`의 `build()`가 산출물,
`tests/test_*.py`가 자, `orch_check.py`는 검증 장치(수정 금지). 오케스트레이터가 팩토리 빈 씬에서
돌려 실제 메시·테스트 통과를 재야 완료다. 내보내기(FBX → `unity/Assets/_ThirdParty/Orch/`)는 다음 단계.

# DIRECTIVES.md — 통합 핵심 지침

이 문서는 저장소 전체의 공통 원칙만 담는다. 세부 사고 이력과 오래된 운영 절차는 매 작업의 기본 컨텍스트로 싣지 않는다.

## 1. 방향
1. **실제 결과 우선.** 문서 수, 회의 수, 커밋 수보다 동작하는 기능과 검증 결과를 우선한다.
2. **비용 최소화.** AI가 필요 없는 검색·상태 확인·로그 필터·테스트는 로컬 코드로 처리한다.
3. **좁게 읽고 좁게 고친다.** 요청과 직접 관련된 파일만 읽고 최소 변경한다.
4. **검증 후 완료.** AI의 설명이 아니라 실행 결과가 완료를 결정한다.
5. **추측보다 확인.** 현재 작업에 필요한 사실만 코드·로그·실행 결과로 확인한다.
6. **오너를 병목으로 만들지 않는다.** 안전한 범위는 자율 수행하고, 되돌리기 어렵거나 외부 영향이 큰 작업만 확인한다.
7. 사람에게 보내는 보고는 한국어로 간결하게 한다.

## 2. AutoDev v2 특별 규칙
재와별 자율개발은 아래가 최우선이다.
- `projects/autodev-v2/CORE_RULES.md`
- `projects/autodev-v2/KNOWLEDGE.md`

AutoDev v2 모델 흐름:
`Grok → Grok 재시도 → Codex 최대 1회 → STOP`

- Claude 사용 안 함.
- 여러 모델 동시 토론 금지.
- Director는 큐가 비었을 때만 작업 묶음을 만든다.
- 다음 작업 선택은 로컬 Task Queue가 처리한다.
- 작업마다 새 모델 세션을 쓴다.
- 프로젝트 전체, 과거 대화 전체, 전체 로그를 자동 첨부하지 않는다.
- `ORDERS.md`, `GAME_WORKLOG.md`, 회의록은 v2의 진행상태 원천이 아니다.
- 같은 오류/같은 상태를 반복하면 중단한다.

## 3. 일반 작업 절차
1. 현재 목표와 완료 조건 확인
2. 관련 파일만 찾기
3. 최소 변경
4. 가장 싼 검증부터 실행
5. 실패하면 새 정보가 있는 경우에만 재시도
6. 통과하면 종료

작업이 끝났다는 이유로 주변 코드 전체를 재검토하거나 리팩터링하지 않는다. 별도 결함이 보여도 현재 목표와 무관하면 메모만 남기고 확장하지 않는다.

## 4. 안전
- 루트 `.env` 등 시크릿 파일을 커밋하지 않는다.
- API 키·토큰·쿠키·개인키를 출력하거나 외부 전송하지 않는다.
- force-push 금지.
- 병합 실패나 충돌 상태를 방치하지 않는다.
- 사용자의 기존 미커밋 변경을 임의로 되돌리지 않는다.
- 오너가 열어둔 Unity 등 사용자 프로세스를 강제 종료하지 않는다.
- 외부 게시·배포·구매·계정 변경은 명시적 승인 없이 하지 않는다.
- `_shared/`는 공용 영향 범위가 크므로 변경 시 관련 회귀 테스트를 수행한다.

## 5. Git
- 필요한 파일만 add한다.
- 커밋 전 diff 범위를 확인한다.
- 동시 작업 가능성이 있으면 staging을 오래 방치하지 않는다.
- AutoDev v2 Worker는 commit/push를 하지 않는다.

## 6. 검증
- 테스트가 실제로 실행됐는지 확인한다.
- SKIP/판정불능을 PASS로 바꾸지 않는다.
- 컴파일 검증으로 충분하면 전체 빌드를 하지 않는다.
- 전체 빌드/플레이는 런타임·화면·milestone 검증이 필요한 경우에만 한다.

## 7. 문서와 지식
- 동적 진행상태를 여러 문서에 복제하지 않는다.
- AutoDev v2 진행상태는 `output/autodev_v2/ashes-to-stars/state.json`이 단일 진실원천이다.
- 안정적인 사실만 `projects/autodev-v2/KNOWLEDGE.md`에 둔다.
- 사고 이력과 오래된 세부 절차는 Git history/HANDBOOK에 보존하되 자동 로드하지 않는다.

## 8. 금지할 반복 패턴
- 문제 하나에 여러 AI 동시 호출
- 6인 회의 뒤 또 역할별 감사
- 일정 주기로 전체 프로젝트 재검토
- 작업마다 STATUS/WORKLOG 갱신
- '더 좋아질 수 있음'을 이유로 완료 후 계속 수정
- 코드 변화 없이 같은 프롬프트 재전송
- 로그 전체를 LLM에 전달

핵심은 **AI가 계속 생각하게 하는 것이 아니라, 프로그램이 계속 진행하고 생각이 필요한 순간에만 AI를 호출하는 것**이다.

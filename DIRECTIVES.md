# DIRECTIVES.md — 모든 작업의 통합 지침

> **모든 세션(사람 보조·헤드리스 에이전트·자동 루프)은 작업 전 이 문서를 따른다.**
> 오너 지시 2026-08-04: 흩어진 명령·지침·사고 교훈을 하나로 종합한 단일 지침.
> 권위: A등급(HANDBOOK §1-2). 이 문서는 **행동 원칙의 요약**이고, 개별 사고의
> 세부·근거는 [CLAUDE.md](CLAUDE.md) 「하네스 가드레일」 원장이 원천이다 —
> 충돌하면 가드레일 원장의 구체 기록이 이긴다. 새 원칙이 생기면 여기 갱신하고,
> 새 사고가 나면 가드레일에 1줄 추가한다(§5).
>
> **강제 지점(어떻게 "작업 시작 시 반영"되는가)**: ①`CLAUDE.md`·`AGENTS.md` 헤더가
> 이 문서를 필수 지정(대화형·헤드리스 모두 자동 로드) ②헤드리스 세션은
> `_shared/cc.py`가 시스템 프롬프트로 핵심 원칙 준수를 강제 ③수리 엔진은 자체
> [규칙] 절 + 워크트리 `CLAUDE.md`로 커버 ④Cowork 세션은 프로젝트 메모리
> (`directives-first`)가 시작 시 상기.

---

## 1. 오너 원칙 — 방향을 정하는 것

1. **비용 최소화.** LLM은 로컬(Ollama) 우선 → 구독 클로드(Max, `claude -p`) → 구독 GPT(Plus) → Gemini → API 크레딧 순. 클라우드는 haiku 고정, 고품질 필요 시에만 상향.
2. **승인 없는 게이트 도입 금지.** "분석해봐"는 분석 지시지 차단 로직 도입 승인이 아니다. 오너가 완화한 설정을 코드 하한으로 덮지 마라. 충돌이 보이면 먼저 물어라.
3. **맥이 유일 운영 기계.** 기계 간 합의가 필요한 설정은 `_shared/fleet_machine_policy.json`(평문·git 추적)이 단일 소스 — 기계별 파생 키로 암호화된 값으로 합의를 공유하려 하지 마라.
4. **오너를 병목으로 만들지 마라.** 자율 실행이 기본: 확인 없이 바로 실행하고, 위험한 변경도 트레이드오프 한 번 설명 후 짧은 승인이면 충분. 검토·판단을 자동화할 수 있으면 예원에게 위임한다.
5. **경보는 못 고친 것만.** 스스로 고칠 수 있는 문제는 조용히 고쳐라. 텔레그램은 사람의 판단·행동이 필요한 것만.
6. **문제는 계속 찾아 고치고 지식화하라.** 기능 하나를 고치면 그 주변 코드 경로 전체를 재검토한다 — 결함은 연쇄로 드러난다. 발견→수정→회귀 테스트→가드레일 기록까지가 한 사이클.
7. **추측·예측 금지(오너 지시 2026-08-04).** 항상 확인된 사실(코드 직접 열람·실측·실행 결과)이나 검색으로 확인한 정보에만 근거해 작업한다. 모르면 웹서치로 확인부터 하고, 확인하지 못한 것은 "추정·추가 확인 필요"로 명시한다. 회의·LLM의 결론도 가설이다(§2-3).
8. **항상 한국어로(오너 지시 2026-08-04).** 사람에게 보내는 모든 응답·보고·텔레그램·커밋 메시지·회의록은 한국어로 작성한다(코드·식별자·로그 원문은 예외).
9. **모든 텔레그램 메시지는 예원 검수를 거친다(오너 지시 2026-08-04).** 개별 스크립트(하네스 등)가 사람에게 직접 페이지하지 않는다 — 예원의 remediate-then-alert 파이프라인(`harness_monitor.auto_remediate` → 남은 것만 검수 후 발송)을 거쳐야 한다. 새 텔레그램 발신 지점을 만들 때는 이 경로를 우회하지 마라(§1-5와 연결 — "못 고친 것만"을 판단하는 주체가 예원이어야 한다).

## 2. 작업 절차 — 매 작업의 순서

1. **읽기 최소화.** 요청과 직접 관련된 파일만. 5개 이상 읽기 전 승인. 전체 스캔·광범위 리팩터링은 명시 요청 시에만.
2. **타겟 패치.** 전체 재작성 금지, 인접 코드 "개선" 금지, 기존 패턴 유지. 출력은 간결한 diff.
3. **회의·LLM의 결론은 가설이다.** 구현 전 지목된 코드를 직접 열어 확인하라 — 6인 전원이 오독한 전례가 있다. 오진이면 사유를 적어 종결한다.
4. **`git add`+`commit`은 한 호흡.** 이 저장소는 데몬들이 동시에 커밋한다 — 스테이징을 방치하면 남의 커밋에 쓸려 들어간다.
5. **커밋 전 diff 크기 대조.** 예상보다 크면(줄바꿈 CRLF 변환·충돌 마커·문서 중복) 원인부터. 여러 줄 일괄 치환 전 줄바꿈 형식 확인.
6. **JS를 고쳤으면 `index.html`의 `?v=` 캐시버전도 올려라.** 안 올리면 재방문 사용자에게만 안 보인다 — 자동 검증이 전부 통과하는 함정.
7. **완료 선언은 검증 후에만.** 테스트 실행·실측 스크린샷·실제 응답 확인이 근거다. "커밋했다 ≠ 배포됐다" — 반영 안 됐다는 신고는 `curl <배포URL>`로 배포본부터 대조.

## 3. 안전 규칙 — 어기면 사고

1. **시크릿**: 전부 루트 `.env` 하나. 평문 커밋 금지, 프로젝트별 `.env` 금지, 하드코딩 금지. `.env.encrypted`는 기계 전용 로컬 파일 — git에 올리지 마라. 헤드리스 세션엔 `cc.scrub_secrets`로 env를 스크럽해 상속시켜라.
2. **헤드리스 CLI 호출**: 여러 줄 프롬프트는 argv가 아니라 stdin(`input=`)으로, `encoding="utf-8"` 명시. "응답 성공 ≠ 지시 전달 성공", "Popen 성공 ≠ 스크립트 정상 실행" — 산출물로 검증하라.
3. **자동 git 작업 실패 시 원상복구.** 병합 실패는 즉시 `merge --abort` — mid-merge 방치가 충돌 마커를 배포에 실었다. master 직접 수정 금지(수리), force-push 금지.
4. **같은 로직을 두 곳에 만들지 마라.** 판정·안전장치·상수는 `_shared/`에 공용 함수 하나로 만들고 재사용한다. "재발"의 대부분은 같은 로직의 사본이 한쪽만 고쳐져서다.
5. **소비자 없는 배정 금지.** 백로그 owner는 그 에이전트가 실제로 백로그를 읽을 때만(`AUTO_OWNERS`). 새 상태값·새 이름 목록을 만들면 소비자·레지스트리 대조부터.
6. **빈 응답 ≠ 부재.** 권한 부족은 빈 목록을 돌려준다 — 대상별 직접 조회로 404를 확인해야 "없음"이다. `disabled`·"정상"처럼 보이는 상태가 "판정 불능"일 수 있다.
7. **원격 동기화는 교체가 아니라 병합.** 원격 행으로 로컬을 통째 대입(`local[k]={...row}`)하면 로컬 전용 필드가 유실된다. null로 덮지 마라.
8. **`_shared/` 수정은 전 에이전트 영향** — 회귀 테스트 전체를 돌려라. 데몬 뮤텍스 락 제거 금지.
9. **재시작은 stop의 '실제 소멸'을 확인한 후 start.** `kill → sleep(1) → start`는 락 경합 전멸 코스 — 소멸을 폴링으로 확인하고, 안 죽으면 SIGKILL 승격. 고정 `sleep(N)`은 확인이 아니다(2026-08-04 사고).
10. **프로그램끼리 쓰는 로컬 서버는 포트 고정 금지.** 포트 0(커널 위임)으로 바인딩 후 실제 포트를 읽어라. 사람이 URL을 입력하는 화면(fleet_view 등)만 고정 포트(2026-08-04 사고).
11. **상시 데몬은 `start_new_session=True`로 띄워라.** 세션을 분리하지 않으면 띄운 프로세스의 그룹에 묶여, 그쪽이 죽을 때(launchd 잡 teardown 등) 연좌로 같이 죽는다. `ps -o pid,pgid`에서 PGID가 자기 PID가 아니면 남의 수명에 묶인 것이다(2026-08-04 사고: 코드 갱신마다 펫나 6종 전멸).
12. **'경보 유예'와 '부활 유예'를 같은 상수로 묶지 마라.** 경보를 미루는 비용은 '사람을 덜 부름'이고 부활을 미루는 비용은 '죽은 채 방치'다 — 값이 다르니 상수도 달라야 한다.
13. **병합 판정은 diff가 아니라 조상 관계로.** 병합했는데 브랜치를 안 지운 상태를 '빈 diff = 변경 없음'으로 읽으면 배포까지 끝난 작업을 반려한다. `merge-base --is-ancestor`로 볼 것(2026-08-04 사고).

## 4. 검증 체크리스트 — 인프라를 만졌으면

- [ ] `python3 projects/ai-team/harness/check_all.py` — exit 0
- [ ] `projects/ai-team/tests/test_*.py` 관련 파일 실행 — 전부 OK
- [ ] schedules.json에 잡을 추가했으면 `launchctl list | grep com.ailab.sched.<id>`로 실제 등록 확인(파일에 적었다고 도는 게 아니다, sched_sync 최대 15분)
- [ ] 새 정시 스크립트는 자기 자신이 `load_env()`를 부르는지 확인(독립 프로세스는 아무도 대신 안 불러준다)
- [ ] UI를 만졌으면 1440px에서 빈 여백·중복 카드 자가 점검 + 모바일 하단바 도달성("렌더된다 ≠ 도달할 수 있다")
- [ ] E2E 앵커는 스타일 클래스가 아니라 고유 ID

## 5. 재발 방지 — 사고가 나면

1. 근본 원인을 코드로 강제한다(문서 경고만으로는 안 막힌다 — "문서에 적었다 ≠ 코드가 막는다").
2. 회귀 테스트를 만든다. 가능하면 **고의로 망가뜨린 상태를 주입해 실제로 탐지되는지**(네거티브 컨트롤) 확인.
3. [CLAUDE.md](CLAUDE.md) 가드레일에 1줄 추가: 날짜·증상·근본원인·수리·테스트 경로.
4. 같은 계열 결함이 다른 곳에도 있는지 훑는다(§1-6).
5. 큰 수정 뒤에는 감사를 다시 돌려라 — 어제 고친 것이 오늘 문제의 원인일 수 있다.

## 6. 진단 순서 — 대표 증상별 1순위 용의자

| 증상 | 먼저 볼 것 |
|---|---|
| 에이전트 전체 침묵 | ① API 크레딧 ② 구독 토큰 만료(`claude -p "ok"` 실측 — `auth status`는 거짓말) ③ Ollama 생존 |
| 정기 보고 안 옴 | 정시 잡 실행자 생존 → `launchctl list` 등록 여부 → 그 시간 기계가 켜져 있었나 |
| 저장했는데 반영 안 됨 | 저장 코드보다 그 뒤에 도는 비동기 동기화·복원 루틴 |
| 고쳤는데 안 고쳐짐 | 저장소가 아니라 배포(`curl`로 배포본 대조) → 브라우저 캐시 |
| 수리가 "처리할 것 없음" 반복 | ① 백로그 `대기` 잔량 ② 유령 PR대기(브랜치 실재) ③ owner 불일치 |
| 하네스 WARN | 데이터를 지우기 전에 규칙이 틀렸는지부터 의심 |
| 재시작 직후 데몬 전멸 | ① `ps -o pid,pgid`로 PGID≠PID면 띄운 쪽 수명에 묶인 것(`start_new_session` 부재) ② 로그에 `데몬 시작` 뒤 `Lock acquired` 부재 = 락 경합 즉사(stop 미확인 재시작) |
